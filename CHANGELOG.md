# 변경 이력

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고,
버전은 [유의적 버전](https://semver.org/lang/ko/)을 따른다.

배포 대상 프로그램이라 첫 코드 커밋부터 이력을 남긴다.

## [Unreleased]

### 그룹 축의 문턱이 바로 옆 문턱과 갈려 있었습니다 (2026-08-17)

흩어진 누수를 잡는 그룹 축(`3da78a1`)을 채택할지 판정하려고 실주입 구간을
제품 탐지기에 태웠더니 **`#65`·`#66` 을 둘 다 놓쳤습니다.** 축이 안 도는 것은
아니었습니다 — `python` 그룹 합계는 주입 중 1,239 → 4,868MB 로 제대로 자랐고,
15분 창 안 증가량이 최대 1,376MB 로 **문턱 1,536MB 에 160MB 모자랐습니다.**

문턱이 그 값이 된 경위가 원인이었습니다.

```
PID별  rss_mb  min_delta = 1,303.6MB   ← min_delta_ram_ratio 0.02 × 63.65GB
그룹   rss_mb  min_delta = 1,536.0MB   ← 코드에 하드코딩 (= 512 × 3)
```

주석은 "PID별 512MB 의 3배"라는 산수로 1536 을 잡았는데, **512 는 이 PC 에서
실제로 쓰이는 값이 아닙니다.** `min_delta_ram_ratio` 가 PID별을 1,303.6MB 로
덮어쓰고 있어 실제 배수는 3.0 이 아니라 **1.17** 이었습니다. 그러면 "개별로는
문턱 미달인 것을 합쳐 잡는다"는 이 축의 존재 이유가 성립하지 않습니다.

규칙 위반이 둘 겹쳐 있었습니다. 문턱이 코드 상수라 튜닝에 코드 수정이 필요했고
(설계 규칙 3), 절대값이라 4GB PC 에서는 RAM 의 37% 라 영원히 안 걸립니다
(설계 규칙 2). 바로 옆 PID별 룰은 이미 RAM 비율이었습니다.

**문턱을 절대값으로 따로 두지 않고 PID별 문턱의 배수로 표현했습니다.**
그룹에도 `min_delta_ram_ratio` 를 따로 주는 안은 기각했습니다 — 두 값이 각자
하드웨어를 해석하면 다시 갈릴 수 있고, 이번 사고가 정확히 그 형태였습니다.
같은 이유로 `monotonic_ratio` 도 PID별 것을 물려받게 해 config 에서 뺐습니다.

```yaml
  group:
    enabled: true
    min_delta_multiple: 0.8     # 이 PC 에서 1,042.8MB
```

**0.8 의 근거** (08-16 정상 9시간 + 주입 3건, 결과 보기 전에 적은 기준으로 판정):

```
정상 구간 최대 그룹 증가량   chrome  +433MB   (13:02~16:02)
주입 구간 python 그룹        +1,351 ~ +1,589MB
                             ~~~~~~~~ 사이가 3.1배 비어 있다
```

1 미만인 것이 의도입니다. 프로세스가 하나뿐이면 `members < 2` 억제가 그룹
신고를 막으므로 PID별 축과 중복 신고가 나지 않고, 이 축이 더할 것은 "흩어져
있어 각자는 작은" 영역뿐입니다.

고친 뒤 같은 구간을 다시 태운 결과입니다.

```
#65  +23.7분  python 10개 합계 +1,090MB
#66  +20.6분  python 10개 합계 +1,045MB
#67  +16.6분  python 23개 합계 +1,057MB
정상 9시간(37,226 관측)  신규 발화 0건
```

**이 버그가 있는 동안 테스트 10개가 전부 통과했습니다.** 문턱을 상수에서 읽어
`GROUP_RULES[0].min_delta == 1536.0` 으로 단언했기 때문에, 그 상수가 옆 문턱과
갈려 있다는 사실을 아무것도 보지 않았습니다. 무력화 항목 3개를 새로 넣었고
(`procleak_group_multiple`·`procleak_group_wiring`·`procleak_group_axis`)
전부 [잡힘] 입니다.

> **배선 무력화는 두 번 만에 맞았습니다.** 처음에는 `min_delta_multiple` 을
> 0.8 → 99.0 으로 바꿨는데 565개가 전부 통과했습니다. 그건 배선이 멀쩡해서
> 탐지기 값도 같이 99.0 이 된 것이라 **정상**입니다. 재야 할 것은 `build()` 가
> config 를 안 읽고 코드 기본값으로 도는 상황이었습니다.

> **측정 하나를 버렸습니다.** A/B 를 `mutation_sweep` 과 병렬로 돌렸다가
> 단조성이 0.0 으로 무력화된 소스를 읽었습니다. 수치는 그럴듯했고 오염됐다는
> 사실은 출력 한 줄(`monotonic = 0.0`)에만 있었습니다. 하네스에 상수 오염
> 검사를 넣고 단독으로 다시 쟀습니다.

### exe 가 8일 만에 다시 검증됐습니다 (2026-08-17)

마지막 빌드가 08-09 였고 그 사이 창(PySide6)·`procleak` 그룹 축·config 스키마가
바뀌었습니다. **「모든 단계가 배포 가능」이 설계 원칙인데 그게 8일간 미지수였습니다.**
특히 오늘 `defaults.yaml` 에 절을 새로 넣었는데, 그 파일은 PyInstaller `datas` 로
번들되는 리소스라 소스에서 멀쩡해도 exe 에서만 조용히 깨지는 자리입니다.

```
상주 argus.exe      빌드 39초 · 198MB · 1,436 파일   (08-09: 195MB · 1,429)
창   argus-ui.exe   빌드 49초 · 299MB · 1,675 파일
```

**빌드 성공은 절반입니다** — 08-03 에 exit 0 으로 성공한 뒤 실행에서
`ModuleNotFoundError` 로 죽은 적이 있습니다. 그래서 격리 디렉터리
(`ARGUS_DATA_DIR`)에서 실제로 돌려 **값**을 봤습니다.

```
metrics_raw 40행 = 40초 × 1Hz 정확 · drop 0 · user_version 18
PDH   ctx_switches_ps 30,391~180,417 · disk_resp_ms 0~0.233
NVML  clock_sm 1710MHz · temp 69도 · power 126~128W · RTX 3080 인식
창    실시간 12표본(기대 7 이상) · 프로그램 77종 · 상태 "정상"
```

`capabilities.json` 의 `available: true` 나 행 수만 보지 않은 이유는, pywin32 가
빠지면 **예외 없이 psutil 폴백으로 내려가** PDH 유래 컬럼이 전부 NULL 이 되는데
그때도 행 수는 정상이기 때문입니다.

**오늘 넣은 `process_leak.group` 절이 번들에서 읽힙니다.** exe 가 그것을 포함한
`settings.yaml` 을 생성했습니다 — `_MEIPASS` 경유 리소스 경로가 살아 있다는 뜻입니다.
거기서 탐지기까지의 배선은 플랫폼 무관한 파이썬이고 무력화로 따로 검증했습니다.

> **창의 첫 실행이 `[FAIL]` 이었는데 제품 버그가 아니었습니다.** 수집이 3분 전에
> 끝난 격리 DB 를 가리켜 실시간 표본이 0이었고, 창은 그걸 "수집이 멈췄습니다"로
> 표시하며 시끄럽게 실패했습니다 — 오히려 제대로 동작한 것입니다. 창은 상주가 도는
> DB 옆에 붙는 것이 정상 조건입니다. **재는 조건이 제품 사용과 달라 결론이 기울
> 뻔한 것이 오늘만 네 번째입니다.**

**남은 것 하나.** `ARGUS_UI_SCREEN=1` 을 줬을 때 로그가 `창 위치: 모니터 1
(LG ULTRAGEAR)` 로 찍힙니다. 인덱스가 0부터인지 1부터인지 이 출력만으로는 알 수
없어 **2번 모니터에 떴는지 판정할 수 없습니다.** 「GUI 는 2번 모니터에만 띄워
테스트한다」가 프로젝트 규칙이라 이 표기는 고칠 값이 있습니다.

### 이길 상대가 사라졌습니다 (2026-08-17)

Phase 5·7(다차원 이상탐지·시퀀스 모델)은 하나의 선행 조건에 막혀 있었습니다 —
*"룰이 못 잡는 시나리오가 실재하는가"*. 08-16 에 그 증거를 찾았다고 봤고, 그것이
분산 누수 주입 `#65`·`#66` 두 건의 미탐이었습니다.

**그 둘은 위 문턱 버그 때문이었습니다.** 고치자 셋 다 16~24분에 잡힙니다.
미탐 라벨 표본이 2건에서 **0건**이 됐습니다.

주입 없이 리플레이로 다시 물었습니다(`--spread 8 --quadratic`, 창 고정).

```
                      per_program=off (배포 기본값)   per_program=on (이 PC)
우상향  5분           [발화] 4.5분  「메모리 이상 증가」   [발화] 4.5분
우상향 30분           [발화] 10.5분 procleak:python.exe   [발화] 10.5분
우상향 60분           [발화] 23.6분 procleak:python.exe   [발화] 23.6분
톱니    5분           [발화] 4.5분  「메모리 이상 증가」   [발화] 4.5분
톱니   60분           [미탐] -1.83%p                       [발화] 36.5분
```

60분 우상향 램프의 탐지 시각 +23.6분이 실주입 `#65` 의 +23.7분과 맞았습니다.
리플레이가 실주입을 예측한다는 교차 검증이 덤으로 나왔습니다.

남은 후보는 톱니 60분 하나였는데, **그것도 사각지대가 아니었습니다.** 되돌림
비율을 4점 훑었습니다 — 그러려고 `--saw-drop`·`--saw-period` 를 CLI 로 뺐습니다.
그전에는 코드 상수라 한 점밖에 못 쟀고, **한 점으로 사각지대를 선언하는 것이
08-16 의 실수였습니다.**

```
                  per_program=off (배포 기본값)      per_program=on (이 PC)
되돌림 10%        [발화] procleak 그룹축 +23.6분      [발화] procleak +23.6분
되돌림 20%        [미탐]                              [발화] rules +36.5분
되돌림 30%        [미탐]                              [발화] rules +36.5분
되돌림 50%        [미탐]                              [발화] rules +36.5분
```

미탐이 **연속적**입니다. `procleak` 이 손을 떼는 지점이 10~20% 사이에 있고 그
위로는 일관됩니다 — 우연이 아니라 `monotonic_ratio = 0.9` 가 설계대로 거르는
것입니다. 그리고 `per_program=on` 이면 시스템 룰이 전부 잡습니다. 이 현상을
이상으로 판정할 능력은 제품에 이미 있고, `off` 에서 못 잡는 것은 전역 베이스라인이
뭉개져서입니다. **새 모델이 필요한 간극이 아니라 `per_program` 기본값 문제입니다.**

되돌림 20~30% 를 잡고 싶다면 `monotonic_ratio` 를 낮추는 것이 모델을 들이는 것보다
압도적으로 쌉니다(상수 하나). 다만 그 조건은 등락하며 오르는 정상 프로세스를
거르려고 있는 것이라 낮추면 오탐이 늡니다. **Phase 5·7 을 여는 근거로 쓰려면 이
대안이 먼저 실패해야 합니다.**

**결론: 이 시나리오 계열에 이길 상대가 없습니다.** 60분짜리 주입으로 라벨을 더
모으는 일을 없앴습니다 — 재는 대상이 없어진 상태에서 표본만 늘리는 것이 가장 비싼
실수입니다. Phase 5·7 은 계속 보류하고, 근거가 필요하면 다른 축에서 찾습니다.

### 착수 조건이 재려는 것을 세고 있지 않았습니다 (2026-08-16)

`severity` 등급 역전(발열이 `info` 라 안 나가는데 사용자는 "알려줄 만했다"고
답하는 것)을 손대는 조건이 **사람 답 20건**이었습니다. 그런데 라벨이 늘어난
방향을 보니 조건이 다른 것을 세고 있었습니다.

```
08-14  라벨  7건 = CPU·경합 4(전부 normal) + 발열 3(전부 real)
08-16  라벨 11건 = CPU·경합 8(전부 normal) + 발열 3(전부 real)
                   ~~~~~~~~~~~~~~~~ 늘어난 4건이 전부 여기
```

등급 역전의 증거는 **미탐에만** 있고 이 PC 에서 그 축은 발열뿐인데, 그 표본은
3건에서 하나도 안 늘었습니다. 전체가 20건이 되어도 판단의 근거는 그대로 3건입니다.
20건은 "7건은 아직 적다"에서 나온 어림수였지 재려는 대상을 세는 수가 아니었습니다.

조건을 **발열 축 사람 라벨 6건**으로 바꾸고 `tools/readiness.py` 가 판정하게
했습니다. 6인 이유는 답이 등급과 무관하다면 한 방향으로 몰릴 확률이 3건에서는
12.5% 로 우연히도 나오지만 6건이면 1.6% 이기 때문입니다.

```
[대기] severity 등급 역전
      x THERMAL 축 사람 라벨 6건 이상
        └ 현재 3건 (real 3) · 전체 라벨은 11건 — 3건 부족
```

축은 판정 안에서 다시 정하지 않고 `label.ask_unnotified_bottlenecks` 를 읽습니다.
같은 개념이 두 곳에 있으면 설정을 고쳐도 이 판정만 옛 축을 셉니다.

**답 없이 창을 지난 발열 미탐 56건은 일부러 쓰지 않습니다.** 8~19일 전 미탐은
흔적이 없어 답이 짐작이 되고, 지금 발열 라벨 3건이 완벽히 일관돼 신뢰도가 높은데
거기에 흐린 답을 섞으면 그 일관성이 뭘 뜻하는지 알 수 없게 됩니다.

### 창이 닫히는 데 4초 걸리던 것 (2026-08-16)

창을 닫으면 **4초**가 걸렸습니다. 원인은 폴러 두 개가 "다음 차례"를 기다린 것입니다.

```
_HealthPoller (맨 윗줄 상태)   3.01초   ← 5초 주기라 최악 5초
RealtimePoller (실시간 차트)   0.98초   ← 1초 주기
나머지 페이지 6개              0.00초
```

두 폴러가 `msleep` 으로 **통째로 잠들어** 종료 신호를 못 봤습니다. 신호를 세워도
잠에서 깨야만 그걸 확인하니, 걸린 시간이 곧 남은 대기 시간이었습니다.

다른 페이지들이 0.00초인 이유는 **이미 깨울 수 있는 방식**을 쓰기 때문입니다. 두
폴러를 같은 방식으로 맞췄습니다.

> **닫기 4.07초 → 0.20초** (실제 창으로 측정)

여는 속도는 원래 문제가 없었습니다(1.78초 — 모듈 적재 0.58 · 창 구성 0.61 ·
첫 데이터 0.37). 그대로입니다.

### 창 기본 크기를 1600x900 으로 (2026-08-16)

기본값이 1280x720 이었습니다. **이미 쓰던 PC 에서는 마지막에 닫은 크기가 우선**
이라(`%APPDATA%\Argus\window.json`) 기본값을 바꿔도 화면은 그대로입니다 — 창
크기를 손으로 조절하시면 그 값이 계속 이깁니다.

### 목록에서 "어느 것이 나를 방해했나"가 보입니다 (2026-08-16)

사건 목록에 **알림** 열이 생겼습니다. 발송된 알림에 `●` 가 붙습니다.

**등급과 알림 발송은 다릅니다.** `경고` 인데 안 나간 것이 있고(억제되거나 상위
사건에 물린 경우), `정보` 라서 안 나간 것도 있습니다. 그전에는 목록에 등급만 있어
어느 것이 실제로 풍선을 띄웠는지 알려면 사건을 하나씩 열어 봐야 했습니다.

함께 고친 것 둘입니다.

- **답 표시(`?`)가 답 대기와 어긋나 있었습니다.** 목록은 `notified` 만 보고 "안
  물어봄"이라 적는데 답하기 버튼은 그 사건으로 데려갔습니다 — 전날 미탐을 답 대기에
  넣으면서 생긴 어긋남이라, 이제 목록이 답 대기 판정을 그대로 받아 씁니다.
- **긴 안내 문구가 목록을 밀어내고 있었습니다.** 줄바꿈이 막힌 라벨이 문장 전체
  폭을 요구해, 창이 좁으면 목록에 `시각` 열만 남았습니다. 시각에서 초를 빼고
  목록에 최소 폭을 줘서 다섯 열이 항상 보입니다.

### 알림이 안 나간 사건도 물어봅니다 (2026-08-15)

지금까지 "답할 알림"은 **발송된 알림만** 셌습니다. 근거는 "아무도 성가시게 하지
않은 사건에는 줄일 것이 없다"였고 그 자체는 맞습니다. **그런데 등급이 낮아 알림이
안 나간 것이 사실은 나갔어야 했던 경우**는 그 설계로 영영 드러나지 않습니다.

실제로 그랬습니다. 답해 주신 7건 중 발열 3건은 전부 알림이 **안** 나간 사건이었고
(목록에서 직접 골라 답하셨습니다), 3건 모두 "알려줄 만했다"였습니다. 서로 다른
날이라 우연이 아닙니다.

이제 답 대기에 **실측 근거가 있는 종류의 미탐**이 함께 올라옵니다. 지금은 발열
하나뿐이고, 다른 종류는 근거가 생기면 추가합니다 — 최근 14일 미탐만 68건이라
통째로 내밀면 아무도 답하지 않습니다.

**미탐에는 더 짧은 기간(7일)을 씁니다.** 발송된 알림은 그때 풍선이라도 떠서 기억에
걸리지만, 안 나간 사건은 아무 흔적이 없어 오래되면 짐작이 됩니다.

묻는 문장도 갈라집니다. 답의 뜻(정상/비정상)은 같고 방향만 반대입니다.

> 발송된 알림 — 이 알림이 쓸모 있었나요?
> 안 나간 사건 — 이 사건을 알려줬어야 했나요? (알림은 나가지 않았습니다)

답 대기 기간도 설정으로 옮겼습니다(`label.window_days`). 그전에는 코드에 박혀
있어 바꾸려면 수정이 필요했습니다.

### 끝난 주입 배치를 "남음 17분"이라고 말하던 것 (2026-08-15)

`tools/inject_progress.py` 가 **13일 전에 끝난 배치**를 두고 개별 회차는 전부
`완료 100%` 라 적으면서 전체는 `85% · 남음 약 17분 43초` 라고 발표했습니다.
열린 라벨은 0건이었습니다. 겹쳐서 강제 중단한 두 회차(채점 제외로 닫은 것)를
"덜 끝난 회차"로 세고 있었습니다 — 그 회차는 덜 돈 것이 아니라 **더 돌 계획이
없는 것**입니다.

진행을 재는 도구가 스스로 틀린 진행을 말하면 그 도구로 판정한 모든 회차가
의심스러워집니다. 셋을 고쳤습니다.

- 끝난 배치에는 `배치 종료 — 마지막 회차 08-02 16:14` 로 적습니다
- 끝난 회차는 계획을 다 쓴 것으로 셉니다(개별 줄과 전체가 어긋나지 않습니다)
- 헤더에 **배치 시작 날짜**가 들어갑니다 — 그전에는 유일한 시각이 "지금"이라
  13일 전 배치를 보면서 지금 도는 것으로 읽혔습니다

### 발열 사건의 표가 Argus 자신을 범인으로 지목하던 것 (2026-08-15)

GPU 발열 사건의 상세 화면이 **"원인 후보"** 라는 제목으로 표를 그리고, 그 1위가
`pythonw` — **Argus 자신**이었습니다. 실측 `#59` 는 GPU 90°C 사건인데 1위가
`pythonw` 22%, 실제로 GPU 를 태운 롤 클라이언트는 2위였습니다.

발열·GPU 는 프로세스별 사용량을 얻을 수 없어 **CPU 상위로 대신 분해**합니다.
리포트 본문은 그 사실을 정직하게 적고 있었습니다 — *"이 병목의 원인 프로세스는
특정할 수 없습니다 / 참고 — CPU 사용 상위"*. **그런데 바로 아래 표의 제목이
그것을 뒤집었습니다.** 둘이 붙어 있으면 표가 이깁니다: 순위가 매겨진 표를
사람은 답으로 읽습니다.

이제 귀인이 성립하지 않는 사건에서는 표 제목이 이렇게 바뀝니다.

> 참고 — CPU 사용 상위 (이 사건의 원인은 특정할 수 없습니다. 아래는 범인이 아닙니다)

표 자체는 계속 보여 줍니다. 그 구간에 무엇이 돌고 있었는지는 여전히 쓸모가 있고,
지우면 "왜 아무것도 없지"가 됩니다. **CPU·디스크·메모리 병목처럼 귀인이 성립하는
사건은 그대로 "원인 후보"입니다.**

### 밀린 사건의 자동 판정이 하루 동안 멈춰 있던 것 (2026-08-15)

08-15 에 자동 라벨이 관측자(Argus 자신)를 이름 대신 실측으로 가리도록 고치면서
`judge()` 에 인자를 하나 더 넣었는데, **이미 쌓인 사건을 판정하는 도구
(`tools/autolabel_backfill.py`)만 그 인자를 안 넘겨 실행 즉시 죽고 있었습니다.**
그래서 새 판정은 앞으로 닫히는 사건에만 적용되고, 밀려 있던 것들은 옛 판정 그대로
남아 있었습니다. 이제 도구가 돌아가고, 답 대기 20 → 19건이 됐습니다.

**도구에 테스트가 한 줄도 없어서 전체 531개가 그대로 통과했습니다.** 근본 원인은
인자 하나가 아니라 **미리보기와 실제 저장이 서로 다른 코드 경로**였다는 것입니다 —
판정에 필요한 조회가 늘 때마다 미리보기 쪽만 조용히 뒤처집니다. 둘을 한 함수로
합치고, 도구를 실제로 실행하는 회귀 테스트를 붙였습니다(무력화 2/2 잡힘).

### 알림이 정상인지 기계가 먼저 판정한다 (2026-08-14)

라벨이 안 모이는 이유가 하나 더 있었습니다. 08-14 오전에 화면을 고쳐 7건이 붙었지만,
남은 21건 앞에서 막힌 지점은 화면이 아니라 **판단 기준**이었습니다 — "이게 정상인지
아닌지를 무엇을 보고 정하냐". 그 기준을 사람 머릿속에 두는 한 라벨은 사건마다 다시
어려워집니다.

**기준은 셋이고, 셋째는 "모른다"입니다.**

| | 조건 | 판정 | 근거 |
|---|---|---|---|
| ① | 하드웨어가 실제로 성능을 깎았다 (열 스로틀) | **비정상** | 어떤 앱을 돌렸든 내가 시켜서 그런 것이 아니고 조치가 있다. 실측 라벨 3/3 |
| ② | 원인이 내가 띄운 앱이다 (포어그라운드 이력) | **정상** | 그 앱을 쓰려고 PC 를 켠 것이다. 실측 라벨 4/4 |
| ③ | 그 외 | **판정하지 않는다** | 메모리 압박·백그라운드 원인은 실측 라벨이 **한 건도 없다** |

셋째를 비워 두는 것이 가장 중요한 부분입니다. 근거 없이 채우면 다음 사람이 그 값을
데이터로 읽습니다. 실제로 밀린 알림 49건 중 **18건이 판정 없이 남았고**, 그 18건에도
"무엇이 왜 안 걸렸나"가 근거로 저장됩니다 — 기준을 넓힐 때 그것을 셉니다.

**사람 답(`user_label`)과 같은 칸을 쓰지 않습니다**(`auto_label`). 라벨의 유일한 쓰임이
"알림을 줄일지"를 정하는 것인데, 기계가 매긴 값을 섞으면 **기계가 매긴 것으로 기계를
고치게 됩니다.** 칸이 둘이면 언제든 사람 답만 다시 세어 볼 수 있습니다. 그래서 화면도
둘을 갈라 보입니다 — 목록은 `·정상`처럼 점을 붙이고, 오탐 비율 타일은 `사람 7건 ·
자동 31건 기준`이라고 구성을 그대로 적습니다.

판정하지 않는 자리 셋을 더 둡니다. **안 나간 알림**(사건이 알림의 세 배라 열면 타일이
기계 답으로 덮입니다), **결함 주입 구간**(사람에게 안 묻는 것과 같은 이유), 그리고
**Argus 자신과 개발 도구**(관측자가 병목이 된 상황을 자동 라벨이 가리면 안 됩니다).

기존 사건은 `python tools/autolabel_backfill.py` 로 채웁니다(기본은 미리보기,
`--apply` 로 저장). 문턱은 전부 `config` 의 `autolabel` 절에 있습니다. 실측 결과
**밀린 답이 21건에서 5건으로** 줄었고, 남은 5건은 기계가 판정을 거부한 것들이라
사람만 답할 수 있습니다.

**무력화 측정이 처음에 3/5 였습니다** — 그리고 못 잡은 둘이 하필 오염을 막는 장치였습니다
(사람 답 보호 · 미발송 차단). 원인은 두 테스트가 `program_info` 를 안 채워 둔 것입니다.
그러면 판정이 어차피 "직접 띄운 적이 있는지 모른다"로 끝나 `auto_label` 이 None 이 되고,
**보호 장치를 뜯어내나 안 뜯어내나 결과가 같습니다.** "사람 답을 안 덮었다"를 확인한 것이
아니라 **애초에 덮을 답이 없는 상태**를 확인하고 있었던 것입니다.

고친 방법은 픽스처가 포어그라운드 이력을 먼저 넣고, 두 테스트가 **"막지 않았으면 `normal`
이 나왔을 조건"임을 먼저 단언**하게 한 것입니다. 그 한 줄이 없으면 같은 테스트가 언제든
다시 조용해집니다. 재측정 5/5.

### 정리 최적화 하나가 웜 내보내기를 실패로 만들던 것 (2026-08-14)

08-14 02:19 에 웜 내보내기 자식 프로세스가 죽고 `[ERROR] 치명적 오류` 가 남았습니다.
원인은 커넥션을 닫으면서 도는 `PRAGMA optimize` 의 `database is locked` 였습니다 —
이 명령은 ANALYZE 를 돌릴 수 있어 쓰기 트랜잭션을 잡는데, 상주 본체가 백필 중이면
`busy_timeout` 10초를 넘깁니다(같은 날 25초짜리 쓰기가 실제로 관측됐습니다).

**그 회차는 내보낼 날짜조차 없어 실제로는 아무 일도 하지 않은 회차였습니다.** 데이터에
아무 영향이 없는 정리 작업이 프로세스의 성패를 뒤집었고, 부모는 그것을 "웜 내보내기
자식이 실패했다"로 남겼습니다. 성공한 내보내기에 같은 일이 나면 **멀쩡한 결과가 실패로
보고됩니다.** 이제 이 실패는 삼키고 다음 닫기에서 다시 시도합니다.

**삼키는 것은 `OperationalError` 뿐이고, 삼켜도 흔적은 남깁니다**(설계 규칙 4) —
잦아지면 그때는 락 경합 자체가 봐야 할 문제이기 때문입니다.

웜 스토어는 08-04~08-13 이 빠짐없이 있어 **손실은 없었습니다.** 자식은 매 회차 새
프로세스로 뜨므로 상주 재시작 없이 다음 회차부터 고쳐진 코드로 돕니다.

### 알림에 답하는 자리를 눈에 띄게 (2026-08-14)

알림이 맞았는지 알려 주는 경로는 08-09 에 만들었는데, 닷새 뒤에도 **라벨이 0건**이었습니다
(사건 173건 · 알림 50건 · 라벨 0건). 경로가 없어서가 아니라 **창을 열어도 답할 것이 있다는
말이 어디에도 없었기** 때문입니다 — 라벨 자리까지 탭·선택·스크롤 세 단계가 걸렸고,
08-13 에 풍선을 무음으로 바꾼 뒤로는 알림이 뜬 줄도 모르고 지나갑니다.

- **맨 윗줄에 "알림 N건 답하기".** 어느 탭을 보든 늘 있는 자리이고, **"정상"일 때도
  보입니다** — 답할 알림은 대개 이미 끝난 사건이라, 진행 중일 때만 뜨는 `사건 보기`
  옆자리로는 영영 닿지 않습니다. 누르면 가장 최근에 답 안 준 알림이 펴집니다.
- **사건 목록에 "답" 열.** 상세를 열기 전에는 어느 것이 남았는지 알 수 없어 하나씩
  눌러 봐야 했습니다. **알림이 안 나간 사건은 빈칸**입니다 — 답을 안 준 것과 물은 적이
  없는 것은 다르고, 173건 전부에 물음표를 세우면 밀린 것이 그 안에 묻힙니다.
- **"피드백 없음" 을 "알림 N건이 맞았는지 알려주세요" 로.** 앞의 문구는 결과처럼 읽혀
  할 일이 있다는 것을 전하지 못했고, 그 문구가 닷새 동안 화면에 있던 전부였습니다.
- **답하기 버튼을 상세 맨 위로.** 리포트와 원인 후보 표 아래에 있어 창이 작으면 시야
  밖으로 밀렸습니다. 설명을 읽고 답하는 순서가 자연스러워 보였지만, **읽히지 않는 자리에
  있는 버튼은 순서가 없습니다.**
- **선택지를 상자로.** `정상` · `비정상` 두 상자이고 서로를 끕니다. 누름 버튼이던 때는
  눌러도 버튼이 그대로여서 "내가 이 사건에 답을 했던가"가 화면에 남지 않았습니다 —
  답한 것과 안 한 것이 같아 보이면 같은 사건을 다시 열어 다시 누르게 됩니다. 켠 것을
  다시 누르면 꺼지고, 그것이 곧 취소입니다.
- **묻는 말을 "이 알림이 쓸모 있었나요?" 로.** 앞의 "이 판단이 맞았나요?"는 사실을
  묻는 말로 읽힙니다 — "롤이 CPU 26% 를 썼나"는 매번 참이라 그렇게 모은 답은 전부
  "비정상"이 되고 아무것도 걸러내지 못합니다. **정상은 "안 알려도 됐다", 비정상은
  "알려줄 만했다"** 입니다(각 상자에 설명이 붙어 있습니다). 애매하면 답하지 않고
  넘기는 것이 맞습니다 — 짐작으로 붙인 답은 없는 것만 못합니다.
- **힌트 문구를 사실대로.** "정상으로 표시한 구간은 정상 데이터로 편입됩니다"라고
  단언하고 있었는데, 이 답을 읽는 곳은 아직 화면의 "오탐 비율" 타일뿐입니다.
  하지 않는 일을 적으면 자기 답이 이미 쓰이고 있다고 믿게 됩니다.
- **`모르겠음` 을 더했습니다.** 아무것도 안 누르고 지나가면 답 대기에 그대로 남아,
  "답하기"를 누를 때마다 같은 사건으로 되돌아옵니다 — 애매한 것이 가장 최근이면 거기서
  막힙니다. `모르겠음` 은 다시 묻지 않고, **오탐 비율의 분모에도 들어가지 않습니다**
  (판단이 아니라 판단할 수 없었다는 표시라, 세면 답을 모을수록 문제가 작아 보입니다).
  이 수가 쌓이면 그것대로 발견입니다 — 무슨 일이었는지 모를 알림은 문턱이 아니라
  **설명이 부족한** 알림이라 고칠 곳이 다릅니다.
- **결함 주입 구간의 알림은 묻지 않습니다.** 밀린 28건 중 5건이 08-02 의 `handle_leak`
  배치 구간이었습니다 — `메모리 압박 — python 24%`(주입기 자신)까지 "이 알림이 쓸모
  있었나"로 물어보고 있었습니다. 일부러 만든 부하에 대한 답은 실사용 문턱의 근거가
  아닙니다. 목록에는 `주입` 으로, 상세에는 "결함 주입 중 — 답하지 않아도 됩니다"로
  나옵니다(**빼 놓고 이유를 안 적으면 사라진 것을 버그로 읽습니다**). 28건 → 23건.

**묻는 것은 최근 14일에 실제로 발송된 알림뿐입니다.** 사건 전체를 물으면 아무도 답하지
않고, 그보다 오래된 것은 "그때 실제로 느렸나"를 기억하지 못해 짐작으로 답하게 됩니다 —
짐작으로 붙인 라벨은 문턱을 고칠 근거가 되지 못하므로 없는 것만 못합니다. 답이 밀리지
않았거나 수집이 멈춘 상태에서는 묻지 않습니다(그때 화면이 시켜야 할 일은 "상주를
확인하라" 하나뿐입니다).

이 기계에서 실제로 28건이 밀려 있었습니다. 탐지·판정 코드는 한 줄도 바뀌지 않았고
(`detection`·`decide`·`explain` diff 0), 상주 재시작 없이 **창만 다시 열면** 됩니다.
무력화 측정 6/6, 전체 테스트 487개 통과.

### 일일 생산성 리포트 (2026-08-13)

하루를 요약해 남깁니다 — 쓴 시간, 분류별(게임·개발·브라우징·소통·미디어), 상위 5개,
시간대별(새벽·오전·오후·저녁). 창의 **보기 → 일일 리포트** 에 있습니다.

**사용시간 화면과 세는 것이 다릅니다.** 저기는 프로그램이 *켜져 있던* 시간이고 여기는
창이 *앞에 놓여 있던* 시간입니다. 게임을 켜 두고 자리를 비운 두 시간은 저기서는 세지고
여기서는 빠집니다. 두 화면의 같은 프로그램이 다른 숫자를 보이는 것이 정상입니다.

**포어그라운드 시간은 `process_metrics` 원본에서 셉니다.** 설계 초안은 이미 접혀 있는
`process_5m` 을 쓰려 했지만, 그 테이블은 CPU 상위 40 ∪ RSS 상위 40 만 남긴 것이라
포어그라운드 여부가 선정에 들어가지 않습니다. 실측에서 chrome·롤·디스코드는 손실 0%
인데 `windowsterminal` 은 11.6%, 게임 런처는 90.1% 를 잃었습니다 — **가벼운 앱만 조용히
깎이는데**, 하필 이 리포트가 답하려는 질문에서 제일 나쁜 방향입니다.

**원본이 잘려 나간 날은 요약을 만들지 않습니다.** `daily_report` 는 영구 보존인데 원본은
하루면 지워지므로, 밀린 과거를 그대로 접으면 "그날 0.8시간 썼다"가 굳고 나중에 고칠
방법이 없습니다. 원본이 관측 시간의 절반도 못 덮으면 건너뛰고 "기록 없음"으로 둡니다
(실측에서 잘린 날 6.6~17.9% vs 온전한 날 79.5% 로 뚜렷이 갈렸습니다).

보존 규칙도 함께 고쳤습니다 — `process_metrics` 를 읽는 롤업이 둘이 됐으므로
(`process_5m`·`daily_report`) **둘 다 접은 뒤에야** 지웁니다. 하나만 보면 나머지 하나는
영영 접지 못합니다.

분류·시간대는 `usage.categories`·`usage.slots` 로 바꿉니다. 기본 목록은 개발 PC 의
것이라 배포 기본값의 근거가 아니고, 없는 이름은 "기타"로 갑니다 — 분류가 비어 있어도
총 시간·상위 5개·시간대는 그대로 나옵니다.

곁들여 창의 종료 경로도 고쳤습니다. 탭을 추가하면 폴러를 멈추는 목록이 조용히
뒤처져 `QThread` 가 살아 있는 채로 파괴되고, 그러면 **테스트가 전부 통과한 뒤 프로세스만
죽습니다**(0xC0000409). 실패한 테스트가 하나도 없이 종료 코드만 비0 이라 원인이 보이지
않고, mutation sweep 은 "무력화 전부터 테스트가 실패한다"며 기준선에서 멈춥니다.
이제 `stop_all()` 이 등록된 탭 목록에서 돌고, 새 탭이 빠지면 테스트가 잡습니다.

### 풍선 알림을 무음으로 (2026-08-13)

알림이 뜰 때마다 Windows 기본 알림음("띵동")이 났습니다. 상주 프로그램의 알림은
사용자가 부른 것이 아니라 **끼어드는 것**이라, 소리까지 나면 오탐 한 번의 비용이
훨씬 커집니다(탐지 규칙 1 — 오탐 3번이면 사용자는 알림을 끕니다).

`Shell_NotifyIcon` 의 `dwInfoFlags` 에 `NIIF_NOSOUND` 를 OR 합니다. 등급 아이콘
(NIIF_INFO/WARNING/ERROR)은 그대로 남습니다 — 덮어쓰면 등급 구분이 사라집니다.

`general.notify_sound: false` 가 기본이고, 소리를 원하면 `true` 로 켭니다.
**Windows 전역 알림음은 건드리지 않습니다** — 다른 앱의 소리는 Windows 설정 소관입니다.

### 사용시간에 "내가 쓴 프로그램만" (2026-08-12)

사용시간 표의 상위가 전부 배경 서비스였습니다 — svchost 238h · conhost 209h ·
runtimebroker 200h. 정의대로 동작한 결과지만("켜져 있던 시간") 알고 싶은 것은
"내가 무엇을 얼마나 했나"입니다.

**포어그라운드 이력으로 가릅니다.** 창을 띄워 앞에 놓인 적이 있으면 사람이 쓰는
프로그램입니다. 경로나 회사명으로는 안 갈립니다 — `crashpad_handler`(사용자 폴더)·
`node`·`saclient`(Program Files)가 전부 통과하고, Microsoft 를 빼면 탐색기·
작업관리자까지 사라집니다.

거기에 **개발·운영 도구와 Windows 셸 호스트**를 `usage.exclude`(YAML)로 더 뺍니다.
사람마다 경계가 다르므로 코드가 아니라 설정입니다 — 개발자에게 터미널은 도구지
콘텐츠가 아니고, 누군가에게는 그 반대입니다. `explorer`·`taskmgr` 은 빼지 않았고,
Argus 자신은 뺐습니다(관측자가 관측 대상 목록에 오르면 안 됩니다).

```
200종  →  74종(포어그라운드)  →  61종(제외 적용)

chrome 213h · medal 199h · op.gg 148h · steamwebhelper 121h · discord 39h
League of Legends · FC ONLINE · Football Manager · Delta Force · Civilization VI
```

체크박스는 기본 켜짐이고 끄면 전부 나옵니다 — 그 값이 틀린 것이 아니라 다른
질문의 답이기 때문입니다.

### 상주 메모리 535MB → 208MB (2026-08-12)

예산(RSS 300MB)을 넘겨 스로틀이 걸리던 문제의 실제 원인은 **`import pyarrow` 하나**
였습니다. 파이썬은 모듈을 프로세스가 죽을 때까지 놓지 않으므로, 하루 한 번 도는 웜
내보내기가 함수 안에서 임포트해도 그 366MB 는 상주 수명 내내 남습니다.

```
import duckdb          private + 10.1MB   (읽기 — 상주에 남긴다)
Parquet 조회 전체        private   36.4MB
import pyarrow         private +366.4MB   (쓰기 — 자식 프로세스로 뗐다)
```

**읽기와 쓰기를 갈랐습니다.** 지문·발열 드리프트가 웜을 읽어야 하는데 그 경로는
pyarrow 를 쓰지 않습니다. 내보내기(쓰기)만 `argus --export-warm` 자식 프로세스로
빼면, 자식이 죽을 때 366MB 도 함께 사라집니다.

같이 고친 것:

- **내보내기를 스트리밍으로.** 하루치를 통째로 올려 pyarrow 테이블을 한 벌 더
  만들던 것을 `pq.ParquetWriter` 로 5만 행씩 흘려 씁니다. 읽기는 별도 읽기전용
  커넥션이라 **내보내는 동안 `db._lock` 을 붙들지 않습니다** — WAL 을 택한 이유가
  "읽기와 쓰기가 서로를 막지 않는다"인데 전역 락이 그걸 무효화하고 있었습니다.
- **`null` 타입 파티션 버그.** 옛 코드는 그날 전부 NULL 인 컬럼을 `null` 타입으로
  저장해, 같은 컬럼이 날마다 다른 타입인 파일이 생길 수 있었습니다. 이제
  `PRAGMA table_info` 로 스키마를 명시합니다.

실측(재시작 후): private **535MB → 208MB**, 스레드 46~47 → 41.

### 프로그램이 무엇인지 알려줍니다 · 창 크기 기억 (2026-08-12)

**"svchost 238시간"은 정보가 아닙니다.** 사용시간·프로세스 표에서 상위를 차지하는 것
대부분이 사용자가 설치한 적 없는 이름들이라, 그 표를 봐도 무엇이 도는지 알 수
없었습니다. 이름 옆에 사람 말 설명을 붙였습니다.

```
svchost                  Host Process for Windows Services     238.1h
chrome                   Google Chrome                         213.3h
conhost                  콘솔 창 호스트                          208.8h
runtimebroker            Runtime Broker                        200.3h
```

출처는 exe 의 버전 리소스(탐색기 속성 창의 "파일 설명")입니다. `ctypes` 로 `version.dll`
을 부르므로 새 의존성이 없고, 한글 Windows 에서는 한글 설명이 나옵니다. **언어를 파일에서
읽습니다** — 흔한 구현처럼 미국 영어를 박아 두면 한글 Windows 의 시스템 파일에서 전부
빈손이 되는데, 예외가 아니라 빈 설명으로 나와 본인 PC 에서는 보이지 않습니다(PDH
카운터명과 같은 로케일 함정).

경로는 `process_events.exe` 에 이미 있어(이름 427종 중 405종) 새 수집기가 필요 없었고,
**경로 자체는 저장하지 않습니다**(민감 정보 — 설계 규칙 5).

실측: 405종 3.6초 · 개당 8.8ms · 성공률 71%. 한 회차 40개씩 나눠 읽습니다. 이 PC 에서
242종이 채워졌습니다.

**그리고 창 크기를 닫을 때 기억합니다.** 기본값을 그대로 쓰면 열 때마다 같은 조정을
반복하게 됩니다. 못 쓸 크기(최소화 중 닫힘)와 최대화 상태는 저장하지 않습니다.

### 창을 HD 로, 답을 맨 위로 (2026-08-12)

창을 1280×720 으로 열려 했는데 **실제로는 1280×1255 로 떴습니다.** `QStackedWidget` 은
담긴 페이지 전부의 최소 높이 중 최댓값을 자기 하한으로 삼는데, 스크롤 영역이 하나도
없어 가장 빽빽한 자기 상태 페이지(1179px)가 창 전체의 하한을 정하고 있었습니다.
나머지 여섯 페이지까지 그 크기를 끌고 다녔습니다. 크기 요청이 무시될 뿐 **예외도
경고도 없어** `--seconds` 스모크는 그 상태에서도 전부 정상이었습니다.

```
before  1280x1255   자기 상태 1179 · 타임라인 824 · 실시간 663
after   1280x720    자기 상태  687 · 타임라인 632 · 실시간 663
```

페이지를 스크롤 영역에 담고, 자주 안 보는 것(저장소 표 · 시스템 사건 · 탐지기 채점 ·
결함 주입 이력)을 접었습니다. `setMaximumHeight(0)` 이 아니라 `setVisible(False)` 로
접습니다 — 눌러 접으면 위젯이 살아 있어 최소 높이에 계속 잡히고, 그러면 접는 목적이
사라집니다.

**그리고 맨 위에 "지금 괜찮은가" 한 줄을 놓았습니다.** 사용자가 창을 여는 이유가
그것인데, 답(`incidents.title`, 예: "디스크 병목 — chrome 68%")은 사건 탭을 열어야만
보였고 나머지 화면은 수치와 차트뿐이라 판단을 사용자가 직접 해야 했습니다.

- **수집이 멈췄는지를 사건보다 먼저 봅니다.** 멈추면 사건도 안 생기므로 "정상"과
  "죽음"이 똑같이 조용해 보입니다. 그 상태로 초록불을 켜면 사용자는 모니터가 죽은
  것을 모른 채 안심합니다. 스로틀(주기 ×10)은 정상 동작이라 60초까지는 멈춤으로
  보지 않습니다 — 그때마다 빨간불이 뜨면 그것이 오탐입니다.
- 진행 중 사건의 병목에 해당하는 타일 하나만 강조합니다. **창에 임계값을 두지
  않았습니다** — 하드웨어마다 틀리고(디스크 응답 50ms 는 HDD 사용자에겐 오탐),
  탐지기와 별개의 두 번째 판정이 생기면 맨 윗줄과 갈립니다.
- 탭을 보기/진단으로 나누고 사용 빈도순으로 놓았습니다. 이 제품의 산출물인 사건이
  일곱 중 다섯 번째에 있었습니다.

### 프로그램별 누적 실행 시간 (2026-08-12)

"어떤 프로그램을 얼마나 켜 뒀나"를 창의 **사용시간** 페이지에서 봅니다. 별도 프로그램이
아니라 Argus 안에 넣었습니다 — 새 수집기가 필요 없고(`procsource` 가 356개를 33.3ms 에
가져옵니다), 상주가 둘이 되면 설계 규칙 1 위반입니다.

**기존 테이블로는 답이 안 나왔습니다.** 실측(관측 383시간):

```
              process_5m 버킷 합   process_events 수명 합
chrome              237.8h                1,073.0h
discord             238.8h                  125.4h
steam               238.6h                   17.9h
```

버킷 합은 **포화합니다** — 5분에 한 번만 보여도 5분을 다 세므로 오래 켜 두는 것은 전부
관측 전체로 붙습니다(존재 여부이지 시간이 아닙니다). 수명 합은 **이중계산합니다** —
chrome 프로세스 4,443개의 수명 합이라 관측 시간을 넘습니다.

맞는 계산은 **이름 단위 구간 합집합 + 관측 세션 클램프**입니다. `program_usage_daily`
(migration 014)에 하루 단위로 접고, 도입 즉시 과거치가 백필됩니다(이 PC: 16일 · 2,865행).

- **단위는 실행 경로가 아니라 이름입니다.** 경로로 나누면 자동 업데이트마다 쪼개집니다
  (Discord 는 16일 만에 `app-1.0.9249/9250/9251` 세 경로). 경로는 민감 정보(규칙 5)라
  저장하지 않습니다. 컬럼 추가는 나중에 쉽고 반대는 어렵습니다.
- **`observed_s`(그날 관측 시간)를 함께 저장합니다.** 원본은 보존 기한이 지나면 지워져
  분모를 나중에 복원할 수 없는데, "6시간"은 그날 PC 를 8시간 켰는지 20시간 켰는지에
  따라 뜻이 다릅니다.
- **세션 경계가 함정입니다.** `unclean_shutdown` 은 다음 기동에 기록되지만 `ts` 는 마지막
  데이터 시각입니다. 모르고 짜면 세션이 겹쳐 관측 합계가 11,873시간(달력 16일 = 384시간)이
  됩니다 — 시제품이 그랬고, 이 PC 는 unclean 종료가 37번이라 이 처리가 값을 좌우합니다.
- **보존 연동이 핵심입니다.** `process_events` 삭제가 이 롤업의 워터마크를 넘지 못합니다.
  여기가 조용히 깨지면 그 날짜의 사용시간은 영구히 복원 불가능합니다.
- 설정: `rollup.program_usage_interval_s`(3600) · `rollup.program_usage_days_per_run`(7).
- mutation 4종(합집합 · 세션 클램프 · 보존 워터마크 · 실행 횟수) 전부 [잡힘].

### 보존 정리가 수집을 멈추지 않게 — 삭제를 나눈다 (2026-08-12)

`db._lock` 은 **읽기·쓰기를 함께 덮는 전역 락**이다. DELETE 하나가 끝날 때까지 놓지
않으면 그 시간이 그대로 수집 쓰기의 지연이 된다. 로그 17일:

```
"DB 쓰기가 느리다" 154건 — 0.5~1초 69 · 1~3초 42 · 3~10초 26 · 10초+ 17
10초 넘은 17건 중 12건이 보존 정리·지문 갱신 근처 (최대 109초)
```

한 번에 지우면 락 보유가 **밀린 양에 비례한다.** 평소는 몇 초치라 짧지만 PC 를 며칠
껐거나 롤업이 늦으면 수십만 행이 한 트랜잭션에 들어간다. 사본 벤치:

```
net_connections 606,320행   한 번에 779ms  →  2,000행씩 나누면 최악 113ms
                            (전체는 1,240ms 로 늘지만 그건 수집을 멈추지 않는 시간)
```

- `retention.delete_chunk_rows`(기본 2000): 한 트랜잭션에 지울 행 수. 0 은 옛 동작.
- `retention.delete_max_rows_per_tick`(기본 200,000): 큰 백로그를 한 틱에 다 지우려
  하면 그 틱이 길어진다. 남은 것은 다음 틱(300초)이 가져간다 — 지우는 일은 급하지 않다.

무력화해도 행은 똑같이 지워지고 예외도 안 나므로(달라지는 것은 락 보유 시간뿐) 결과만
보는 테스트는 통과한다. 그래서 **락 획득 횟수와 트랜잭션당 행 수를 직접 잰다.**
mutation 3개 — 그중 `delete_chunk_wiring` 은 처음에 안 잡혔다. 배선 테스트가 설정을
직접 써서 재는 탓에 **기본값이 0 으로 뒤집혀도 조용했다**(남의 PC 에서는 청크가 영영
돌지 않는다). "배포 기본값이 켜져 있는가"는 배선과 다른 것이라 따로 재야 했다.

**남은 것**: `Database.query()` 도 같은 전역 락을 잡는다 — WAL 을 택한 이유가 주석에
"읽기와 쓰기가 서로를 막지 않는다"로 적혀 있는데 락이 그것을 무효화하고 있다. 읽기를
별도 커넥션으로 떼면 구조적으로 해결되지만, 지금 락 하나에 의존하는 read-modify-write
(워터마크 읽고 삭제, 사건 갱신)의 원자성이 함께 걸려 있어 별도 작업으로 둔다.

### 발화가 불가능했던 GPU 고온 룰을 되살린다 — 부하 조건부 베이스라인 (2026-08-12)

"알림이 안 뜬다"를 확인하다 찾았다. 최근 5일 사건 10건이 전부 `info` 로 문턱 아래였고,
그중 GPU 발열은 **룰이 구조적으로 발화 불가능한 상태**였다. 08-11 의 90도+ 317초 구간을
실제 엔진에 재생해 확정했다:

```
90도 초과 표본 1,102개 → 상대 조건을 넘은 것 14개 → 발화 0건
엔진이 계산한 문턱 102~120도   (부하 시 GPU 는 96도를 넘은 적이 없다)
```

`median + 3 * sigma` 의 k=3 은 08-02 에 σ=4.2도를 근거로 골랐는데 **실측 σ 는 8.9~11.9도**
였다(유휴~부하가 한 창에 섞여 MAD 6~8도). 마지막 발화는 08-03이다.

**온도는 상한이 걸린 지표라 k 를 낮추는 것으로는 해결되지 않는다.** 스로틀링이 온도를
열 평형점에서 잡아 주므로, 그 평형점이 곧 median 이 되는 순간 `median + k*sigma` 는
어떤 k 로도 도달 불가가 된다. k 를 아주 작게 잡으면 절대 조건만 남는 08-02 이전
상태(시간의 19.7% 발화)로 되돌아간다.

- **부하 조건부 베이스라인**(`baseline.LoadGate`): 게이트가 열린 구간의 표본만으로 평소를
  세운다. 룰은 `median` 대신 `load_median` 을 참조한다. 부하의 정의는 config
  (`detection.load_gates`)에 있고 문턱은 룰 파일(`load_median + 5`)에 있다.
- **전역으로 폴백하지 않는다.** 프로그램 축은 폴백이 맞았지만 여기는 반대다 — 유휴가
  섞인 전역으로 조용히 물러나면 문턱이 다시 도달 불가가 되고, 룰이 서 있는 척하며
  발화하지 않는 바로 그 상태로 돌아간다. 모르면 판정하지 않는다.
- **워밍은 가장 긴 축에 맞춘다.** 전역 창(30분)만 읽으면 부하 축(6시간)은 매 기동 백지다.
  이 PC 는 하루 3~4회 재시작하므로(대부분 unclean) 그러면 사실상 영구 공백이다.

측정(저장된 9.6일 전 구간을 두 엔진에 같은 스트림으로):

```
GPU 고온 지속   before 0건 → after 0건      전체 룰 발화 총량  2건 → 2건
부하 축         median 92.0도 · 표본 69개 → 문턱 97.0도
```

**이 기계에서 발화 수가 0건 그대로인 것이 채택 근거다.** 냉각이 실제로 정상이기
때문이다 — 13일 실측에서 부하 시 온도가 93.0도로 완전히 평평했고 전력은 +2.1% 였다.
바뀐 것은 *평소 65도로 도는 PC 에서 90도가 되면 울린다*는 것이고, 그건 이 룰이 원래
하려던 일이다.

합성 데이터로 처음 쓴 테스트가 전제를 깨뜨렸다 — 한 값으로 채우면 MAD 가 0 이 되어
실제 데이터의 σ 부풀림이 재현되지 않는다. 산포를 실측 형태로 넣어 다시 썼다.
mutation 5개 전부 잡힘(그중 `load_warm_span` 은 처음에 안 잡혀서, 헬퍼만 보고 실제
워밍 구간은 보지 않던 테스트를 고쳤다).

### 알림을 누르면 그 사건이 열린다 — 피드백 0건을 끊는다 (2026-08-09)

**14일 · 사건 158건 · 알림 11건 · 피드백 0건.** "이 알림이 맞았나"에 답할 근거가
하나도 없었고, 그래서 문턱을 고칠 근거도 없었다. 이유는 기능이 없어서가 아니라
**가는 길이 없어서**였다 — 평가하려면 창을 열고 사건 목록에서 그 시각을 손으로 찾아야
했다. 알림 하나 때문에 그럴 사람은 없다.

```
풍선 클릭 → NIN_BALLOONUSERCLICK → argus-ui.exe --incident <id> → 그 사건이 펴진 채로
```

- **알림 전달자 규약이 바뀌었다**: `notify(title, message, severity, incident_id=None)`.
  풍선을 띄우는 시점에 어느 사건인지 붙잡아 둔다 — **Windows 는 클릭 알림에 아무
  정보도 실어 주지 않아** 그 방법밖에 없다.
- **구간 밖이면 한 번 넓힌다.** 목록 기본은 7일인데 알림은 그보다 오래된 것을 가리킬
  수 있다(창을 며칠 만에 열면 그렇다).
- **없으면 없다고 말한다.** 30일 안에도 없으면 보존 정리로 사라진 것이다. 이때 조용히
  목록 첫 줄을 펴 두면 사용자는 그것을 방금 누른 알림으로 알고 **틀린 라벨**을 남긴다 —
  없는 것보다 나쁜 데이터다. 실제로 구현 중에 그 상태가 나왔고, 테스트가 잡았다.

전부 조용히 되돌아가는 자리다 — 끊겨도 알림은 뜨고 창도 열리며, 안 생기는 것은
피드백뿐인데 그건 원래도 0건이라 아무 신호가 없다. mutation 4개를 넣었다.

**아직 남은 것**: 창이 이미 떠 있어도 새 프로세스가 하나 더 뜬다. 라벨을 모으는 데는
지장이 없어 이번에는 손대지 않았다 — 단일 인스턴스는 별도 작업이다.

### UI 가 하나가 됐다 — Streamlit 판 삭제 (2026-08-09)

네이티브 창이 다섯 페이지를 다 덮은 뒤(08-03) 두 UI 를 함께 끌고 왔다. 지운 것은
`dashboard/pages/`·`app.py`·`common.py`·`__main__.py`·`.streamlit/` — **1,089줄**.

**먼저 마지막 격차를 옮겼다 — 첫 실행 안내.** DB 가 없을 때 Streamlit 홈은 경로와 시작
방법을 말했지만 네이티브는 페이지마다 "…기다리는 중"만 띄웠다. **안 쌓인 것과 고장난
것이 같은 화면**이었고(설계 규칙 4) 그게 배포 사용자가 처음 볼 화면이다.

- 찾은 **경로를 함께** 보여 준다 — "상주를 안 켰다 / 창이 엉뚱한 곳을 본다 / 진짜 고장"
  셋을 가르는 정보가 그것뿐이다.
- 시작 방법이 **실행 형태에 따라 다르다.** 배포판 사용자에게 `python -m argus` 는
  실행할 수 없는 명령이다.
- DB 가 생기면 **사라진다** — 창을 열어 둔 채 상주를 켜는 경우가 있다.
- 셋 다 조용히 되돌아가고 개발 PC 에는 DB 가 있어 영영 안 보이는 경로라, mutation
  3개를 넣었다 — **3/3 잡힘.**

**남긴 것은 조회(`data.py`)와 색(`theme.py`) 뿐이다.** 08-03 에 캐시를 `st.cache_data`
에서 `ttl_cache` 로 바꿔 UI 의존을 떼어 놨던 것이 여기서 값을 했다 — **화면을 통째로
갈아 끼우면서 조회 코드를 한 줄도 건드리지 않았다.** 그 성질을 지키려고, 페이지를
헤드리스로 돌리던 테스트 자리에 **조회·색이 UI 를 import 하지 않는지 보는 검사**를 뒀다.

**패키지 이름 `dashboard/` 는 그대로 뒀다.** 안이 조회 계층만 남아 이름이 어긋나지만,
바꾸면 `desktop/` 전체의 import 가 함께 움직여 삭제와 이름 변경을 한 번에 되돌리게 된다.

plotly 전용 `layout()`·`line()` 과 쓰이지 않던 `severity_color()` 도 함께 지웠다 —
**UI 를 지웠는데 그 UI 전용 헬퍼가 남으면 다음 사람이 쓰이는 줄 안다.** spec 의
`excludes` 에서도 streamlit·plotly·altair 를 뺐다(같은 이유).

**spec 을 건드려 다시 빌드했더니 별개의 문제가 드러났다 — 상주 exe 에 Qt 102MB.**

```
상주 dist   301MB → 198MB      exe 13.1MB → 11.8MB
```

`hiddenimports += collect_submodules("argus")` 가 `argus.desktop` 까지 끌어왔다.
상주는 창을 in-process 로 부르지 않는데(트레이가 `argus-ui.exe` 를 별도로 띄운다)
그 한 줄이 사정을 모른 것이다. **이 삭제와는 무관한 회귀이고, README 의 "195MB" 는
창 페이지가 생기기 전(08-03)에 잰 값이라 그동안 아무도 다시 재지 않았다** — 빌드
수치는 코드가 바뀌면 같이 늙는다.

이름으로 거르고 `excludes` 에도 넣었다. **둘 다 필요하다** — 거르기만 하면 다른
import 경로로 다시 딸려 온다. 재빌드 후 `argus.exe --check` 통과,
`argus-ui.exe --seconds 12` 실시간 12개.

### 창이 그려지긴 하는데 읽을 수가 없었다 — 레이아웃 (2026-08-06)

`--seconds` 스모크는 전부 통과하고 있었다. **표본 수는 "갱신이 도는가"만 답한다** —
겹침·잘림·눌림은 그 숫자가 정상인 상태에서 일어난다. 실제 화면은 이랬다.

```
보조 차트 4개   55px 로 눌려 선이 뭉개짐
StatTile        100px 짜리가 460px 로 부풀어 화면 절반
표 3개          헤더 + 한 줄만 남음
주 차트         stretch=3 인데 stretch=4 인 보조 넷보다 작음
```

**원인은 하나였다 — `stretch` 가 먹지 않았다.** `stretch` 는 sizeHint 를 채우고 남은
여유만 나누는데 pyqtgraph 의 sizeHint 가 커서 나눌 여유가 없었다. `sizeHint` 를
최소 높이로 낮추고, 정책을 `Expanding` 으로 바꾸고, 상한 없는 위젯(StatTile)에
`Fixed` 를 준 뒤에야 비율이 실제 크기가 됐다.

- **차트 높이는 상수로**(`MIN_PLOT_HEIGHT` 132 · `MAIN_PLOT_HEIGHT` 190 · `SUB_PLOT_MAX` 200).
  주 차트와 보조 차트를 페이지마다 눈대중으로 잡던 것을 한 곳으로 모았다.
- **표 크기는 픽셀이 아니라 행 수로.** `setMaximumHeight(170)` 이 5행(≈187px)을 잘랐다 —
  픽셀 상한은 폰트·DPI 가 달라지면 최소 높이와 싸운다.
- **범례를 플롯 밖 제목 줄로.** pyqtgraph 범례는 플롯 위에 떠서, 차트가 작아지면
  하필 가장 읽기 어려운 순간에 데이터를 덮는다.
- **차트 설명의 `wordWrap` 을 껐다.** `heightForWidth` 를 가진 위젯이 `QGridLayout`
  한 행에만 있으면 행 배분 계산이 달라져 `setRowStretch` 가 무시된다
  (격리 실험: 주석 있으면 518 대 154, 없으면 336 대 336 — 같은 `stretch=1`).
- **창 기본 크기를 화면에 맞춰 연다.** 1420x960 을 원하되 화면의 92% 를 넘지 않는다.
  고정하면 1366x768 에서 창이 화면 밖으로 나가 스크롤조차 못 한다(설계 규칙 2).
- `QFrame` 에 테두리를 주던 것을 카드로 쓰는 것만 골라 주도록 고쳤다. `QLabel` 이
  `QFrame` 을 상속해 **화면의 모든 글자에 테두리가 붙고** 그만큼 수치가 잘리고 있었다.

**`tools/ui_snapshot.py` 를 만들었다.** 페이지를 그림으로 뽑아 눈으로 판정한다.
마우스를 움직이는 자동화가 아니라 Qt 가 스스로 그려 넘겨주는 `QWidget.grab()` 이다
(CLAUDE.md). 스타일 적용을 `apply_theme()` 로 뺀 이유가 여기 있다 — 창을 띄우는 경로가
둘인데 스타일이 한쪽에만 붙으면 **캡처가 실제와 다른 화면을 찍고**, 그 그림을 보고
없는 문제를 고치게 된다.

### 알림을 재시작 없이 켜고 끈다 (2026-08-06)

사용자 요청 ①. 지금까지는 `%APPDATA%\Argus\settings.yaml` 을 편집하고 상주를
재시작해야 했다 — **배포 대상 사용자에게 요구할 수 없는 절차**다.

- 트레이 우클릭 메뉴에 "알림 받기" 체크 항목
- 창에 **설정 페이지** 추가. 창은 별도 프로세스라 파일을 통로로 쓴다
- `argus/runtime/livecfg.py` — 실행 중에 바뀔 수 있는 값만 담는 `runtime.yaml`

**설정 전체를 다시 읽는 구조는 만들지 않았다.** 수집 주기나 큐 크기가 실행 중에
바뀌면 컴포넌트를 재구성해야 하는데, 그건 재시작보다 위험하다. `LIVE_KEYS` 에 있는
값만 받고 나머지는 무시한다.

**`settings.yaml` 을 프로그램이 덮어쓰지 않는다.** 그 파일은 `defaults.yaml` 사본이라
**주석이 곧 설명서**인데, YAML 을 다시 쓰면 전부 날아간다. 사람이 쓰는 파일과
프로그램이 쓰는 파일을 나눴다. 우선순위는 `runtime.yaml` 이 세다(방금 누른 것이 가장
최근 의사표시다) — 그래서 `settings.yaml` 을 고쳤는데 안 먹는 상황이 생기고,
**어느 쪽이 이겼는지를 트레이 툴팁·창·`describe()` 가 드러낸다**(규칙 4).

**끄는 것은 발송뿐이다.** 탐지·판정(`notified`)·기록은 그대로 돈다 — 그 분리가
"켜면 몇 건이 올 것인가"를 꺼진 상태로 재게 해 준 방법 자체였다. 이게 깨지면
**측정이 죽는데 알림이 원래 안 오는 상태라 아무 신호가 없다.** mutation 대상으로
넣었다(5개 추가, **5/5 잡힘**).

**검증 중에 드러난 것**: `TrayIcon.describe()` 에 키를 하나 더했더니 기존 테스트
둘이 깨졌는데, **파일별로 돌릴 때는 안 보였다.** mutation 의 기준선 검사가
"무력화 전부터 실패한다"고 멈춰 세워 잡혔다 — 전체를 돌리는 단계가 따로 있어야 한다.

### 파이썬이 아니라 Argus 로 보인다 — 아이콘과 앱 정체 (2026-08-06)

사용자 요청 ②. 지금까지 트레이는 시스템 느낌표(`IDI_INFORMATION`)를 달고 있었고,
작업표시줄과 알림에는 **파이썬이 발신자로** 붙었다.

- `argus/assets/argus.ico` — 눈 모티프(아르고스는 눈이 백 개인 파수꾼이다).
  **크기마다 따로 그렸다**(16·24·32·48·64·128·256). 한 장을 축소하면 트레이 크기에서
  형체가 뭉개진다 — 트레이는 16px, 작업표시줄은 32px, Alt+Tab 은 256px 를 고른다.
- `argus/branding.py` — `AppUserModelID` 를 상주와 창 **양쪽에서 같은 값**으로 설정.
  따로 두면 작업표시줄이 두 그룹으로 갈리고 알림 발신자도 다른 앱이 된다.
- 트레이는 `LoadIcon` 이 아니라 **`LoadImage`** 를 쓴다. `LoadIcon` 은 파일을 못 읽고
  크기도 못 고른다. 크기는 `SM_CXSMICON` 으로 시스템에 물어본다(고DPI 에서 흐려지지 않게).

**아이콘 생성에 Pillow 를 쓰지만 의존성으로 넣지 않았다.** `tools/make_icon.py` 는
일회성 도구이고 산출물 `.ico` 를 커밋한다 — Pillow 는 선언된 의존성이 아니라 개발
환경에 우연히 들어와 있는 것뿐이라, 거기 기대면 남의 PC 에서 빌드가 깨진다.

**이 세 가지는 전부 예외 없이 조용히 되돌아간다.** 자원이 빌드에서 빠지거나,
`set_app_id()` 호출이 사라지거나, 호출 순서가 뒤집혀도 프로그램은 정상 동작하고
아이콘만 파이썬으로 남는다. 그래서 mutation 대상 4개를 새로 넣었다 — **4/4 잡힘**.

특히 **순서를 따로 재는 이유**: Windows 는 창이 처음 생길 때 `AppUserModelID` 를 읽는다.
나중에 불러도 API 는 성공(True)을 돌려주므로 **반환값만 보는 검사는 순서가 뒤집혀도
통과한다.** 진입점 두 곳의 호출 순서를 AST 로 고정했다(문자열 검색과 달리 주석과
문서 문자열에 속지 않는다).

### Argus 가 자기 예산을 1.7배 넘긴 채로 8일을 돌았다 (2026-08-04)

원인은 **DuckDB 파라미터 바인딩** 하나였다.

```
지문 빌드의 웜 조회   바인딩:  private +416MB · 0.36초
                     리터럴:  private + 18MB · 0.06초    ← 결과는 1,396칸 전부 동일
```

DuckDB 는 값을 바인딩하면 `read_parquet` 의 필터 푸시다운을 하지 못해 Parquet 을
통째로 메모리에 올린다. 메모리 **23배**, 속도 **6배** 차이가 값의 표현 방식 하나에서 났다.
숫자만 SQL 에 박도록 바꿨다 — 문자열이 오면 예외를 던진다(인젝션 경로를 열지 않고,
조용히 바인딩으로 되돌아가지도 않는다).

**지문 빌드 전체가 private 439MB → 38MB 가 됐다.** 상주에서도 확인했다 —

```
                  RSS      private   peak_wset
재시작 전        160.8MB   525.7MB    224.8MB
재시작 후        142.9MB   115.4MB    188.3MB
```

private **506 → 116MB**, RSS 는 예산 300MB 의 절반에서 안정적이다. **예산을 올리지 않는다.**

**진단이 네 번 뒤집혔다.** 코드를 보고 세운 가설의 적중률이 낮았고, 매번 측정이 죽였다.

| 가설 | 어떻게 죽었나 |
|---|---|
| "RSS 순간값만 튄다" | 700MB 가 2분 지속. 순간이 아니었다 |
| `self_telemetry.active` 로 잡는다 | throttle>0 표본의 **99.7%가 None** — tick 이 5초 표본보다 짧다 |
| DuckDB 버퍼 풀 | `memory_limit` 을 128MB 로 걸어도 +442MB |
| `COUNT(DISTINCT)` | 무죄. 세 방식 모두 +15~17MB, 결과도 동일 |

`exclude` 구간 수를 0·5·10·20·30·43 으로 늘려 재자 **0→5 에서 계단처럼 뛰고 그 뒤로
평평**했다. "조건이 많아서"가 아니라 "params 가 있느냐 없느냐"였고, 거기서 좁혀졌다.

**계획서의 진단도 틀렸다.** "RSS 순간값만 튄다 · `private_mb` 는 501MB 로 평탄"이라고
적혀 있었는데, **평탄한 505MB 자체가 예산의 1.7배**였다. "평탄하다"를 "정상이다"로
읽은 것이 원인 추적을 하루 늦췄다.

### 스키마가 바뀐 날 과거가 사라지고 있었다 (2026-08-04)

`read_parquet` 에 `union_by_name` 이 없어, 08-03 에 `gpu_clock_sm_*` 을 추가한 뒤로
**그 컬럼이 없는 07-27~08-02 파티션과 합쳐 읽을 때 `Binder Error` 로 터졌다.**
타임라인 화면(대시보드·네이티브 창 양쪽)이 이틀 넘은 날짜를 통째로 못 그리고 있었다 —
예외가 UI 안에서 삼켜져 **화면만 조용히 빈** 상태였다.

`PLAN.md` 에 "기존 파티션에는 이 두 컬럼이 없다. 이름 기준 병합이 필요하다"고 미리
적어 두고도 못 잡았다. **적어 두는 것으로는 안 되고 테스트가 있어야 한다** — 스키마가
다른 파티션 둘을 만들어 합쳐 읽는 회귀 테스트를 넣었다. 337 passed.

### 베이스라인이 "지금 무엇을 하는 중인가"를 본다 — 아직 꺼진 채로 (2026-08-04)

"게임 중 CPU 60%"와 "브라우징 중 CPU 60%"는 다른 사건이다. 포어그라운드 프로그램으로
베이스라인을 나누면 리소스 변동계수가 **0.61 → 0.38** 로 준다(같은 편차에 대해 z 가
약 1.6배). 레짐을 리소스로 클러스터링해 나누는 계획서 원안(GMM+HMM)은 오히려 나빴다
— 세 번의 측정과 기각 근거는 `docs/DONE.md` 에 있다.

**기본은 꺼져 있다**(`detection.per_program: false`). 켜면 안 잡히던 것이 잡히는 만큼
오탐도 늘고, 그건 규칙 1(오탐이 미탐보다 비싸다)에 직접 걸린다. 리플레이 before/after
로 채택을 판정한 뒤에 켠다 — 오탐이 늘고 TP 가 그대로면 되돌린다.

**메모리가 이 설계의 제약이었다.** 전역과 같은 방식으로 프로그램 16개를 들면 90MB 로
예산 300MB 에 넣을 수 없다. 셋으로 줄였다 — 룰이 **상대 조건으로 쓰는 메트릭만**
나누고(실측 9개 중 6개), 창은 길게 잡되 표본을 5초 간격으로 솎고, 프로그램 수에
LRU 상한을 뒀다. **약 7MB.** 메트릭 목록은 코드에 박지 않고 `rules.yaml` 에서 뽑는다
(규칙 3) — 룰이 바뀌면 따라가야 한다.

폴백은 조용히 한다. 처음 보는 프로그램이나 표본이 덜 찬 프로그램은 전역으로 돌아간다 —
**모르는 것을 막는 방향으로는 틀지 않는다**(지문 억제와 같은 원칙). 새 게임을 깔 때마다
한 시간씩 탐지 공백이 생기면 안 된다.

**작업 중에 배선 구멍을 하나 찾아 고쳤다.** 레지스트리에 `RuleEngine` **클래스**가
그대로 등록돼 있어 `build("rules")` 가 인자 없이 불렀고, 그래서 `detection.*` 설정이
룰 엔진에 전달된 적이 없다. 코드 기본값과 YAML 기본값이 우연히 같아서(1800초/60표본)
아무 신호도 없었다 — `baseline_window_s` 를 고쳐도 판정이 안 바뀌는 상태였다(규칙 3
위반). `procleak` 은 처음부터 config 를 읽는 `build()` 형태였다.

이 기능은 끊겨도 전역으로 폴백해 **예외 없이 조용히 죽는다.** 그래서 학습/판정이 같은
이름을 쓰는지, 폴백이 의도한 자리에서만 도는지, 상한이 실제로 걸리는지를 각각 mutation
으로 잰다.

**그리고 그 배선을 검증하려고 쓴 테스트가 처음엔 아무것도 검증하지 않았다.** 배선을
끊는 mutation 에 330개가 전부 통과했다 — `assert engine.baselines.window_s ==
cfg.baseline_window_s` 로 썼는데 **양쪽 기본값이 같으니 배선이 끊겨도 참이다.**
방금 그 함정을 지적하며 고친 코드를, 그 함정에 빠진 테스트로 검증하고 있었다.
기본값이 아닌 값(4242초/37표본/7개)을 넣어 그게 도착하는지 보도록 고쳤다.

오늘만 같은 유형이 네 번째다(`procleak` 단조성 08-03, `MIN_DAY_HOURS`, 레지스트리
배선, 그 배선 테스트 자신). 전부 "코드 기본값과 YAML 기본값이 우연히 같아 아무 신호가
없던" 경우다. **통과는 증거가 아니다** — 매번 mutation 이 잡았다. 330 passed.

**판정: 이 PC 에만 켰다.** `argus.eval` 로는 못 쟀다 — 스냅샷의 정답이 `handle_leak`
인데 `rules` 는 그걸 원래 못 잡아 before/after 가 양쪽 다 TP0/FP0/FN8 로 똑같이 나왔다.
대신 같은 구간을 **한 번 재생해 두 엔진에 먹이고** 발화 수를 셌다(상주 DB 44.5시간,
60,986틱).

```
발화  per_program=false 7건  →  true 7건   (1.00x)
실질 차이 2건 — 1분 해상도 매칭에 걸린 중복을 걷어낸 뒤
  사라짐 : GPU 고온 지속 — 게임 중(deltaforce)
  새로 남: 메모리 이상 증가 1건 (chrome)
```

사라진 쪽이 켤 근거다. 이 PC 는 매일 93도로 게임하고 그게 평소라, 08-02 에 같은
이유로 GPU 절대 온도 룰에 상대 조건을 걸었다. **진짜 발열 악화는 `thermal_drift` 가
따로 본다**("같은 부하에서 예전보다 뜨거운가") — 이 건은 묻어도 되는 종류다.

**배포 기본값은 꺼진 채로 둔다.** 검증이 이 PC 한 대·44.5시간뿐이라 남의 PC 에 밀
근거로는 약하다. 사용자 `settings.yaml` 에만 켜고 며칠 관찰한 뒤 기본값을 다시 정한다.

### 5분만 켜 둔 날은 하루로 세지 않는다 — 그리고 그 문턱은 6시간이 아니었다 (2026-08-04)

`MIN_DAY_HOURS = 6.0` 은 2026-07-30 부터 **상수만 있고 판정이 없었다.** `xfail(strict)`
하나가 그 사실을 고정하고 있었다. 이제 `build()` 가 실제로 센다 — 짧게 켠 날이 하루로
세어지면 "3일 이상 관측"이 사실상 **"3번 실행"** 이 되고, 그 표본으로 세운 p99 는 그
프로그램의 평소가 아니다.

**그런데 값 6.0 은 전제가 틀린 값이었다.** 여기서 세는 버킷은 *켜 둔 시간*이 아니라
**활성 집합(버킷당 상위 40)에 있던 시간**이다 — `ProcessRollup` 이 그렇게 접는다.
실측 분포에서 하루 최대가 209버킷(17.4시간)이므로 6시간은 최대치의 1/3 이다.

```
min_day_hours   지문    프로세스종
        0~1.0    179          90     ← min_buckets=100 이 이미 하는 일
          2.0    165          83
          6.0    105          53     ← 계획서 값. 41% 탈락
```

6.0 을 적용하면 **게임 클라이언트가 전부 빠진다**(이 PC 의 게임 사용은 하루 1.4~2시간).
탈락 37종 중 8종은 실제 사건에 등장한 프로세스다 — `fczf` 7건, `leagueclient*` 18건,
`deltaforceclient` 2건. 게임은 핸들·RSS 변동이 커 `procleak` 오탐의 주 원천이라,
거기서 억제를 걷어내는 것은 **"오탐이 미탐보다 비싸다"의 반대 방향**이다.

**1.0 으로 정했다.** 원래 의도(짧은 날 배제)를 달성하면서 현재 지문을 하나도 잃지
않는다. 짧은 날만으로 자격을 넘보는 프로세스가 생기면 그때부터 실제로 일한다.
알림량이 늘지 않으므로 리플레이 before/after 도 필요 없다 — 동작이 같다.

경계 포함(`>=`)과 config 배선을 따로 테스트한다. 로직만 보면 YAML 을 고쳐도 판정이
안 바뀌는 상태를 놓친다(2026-08-03 `procleak` 단조성). 316 passed.

### 스로틀이 풀리면 자던 스레드가 깬다 (2026-08-04)

**5분 롤업이 46분 멈춰 있었다.** 원시 수집은 멀쩡한데(간격 1.01초, `drop_count` 0)
`process_5m`·`net_activity_5m` 만 갱신이 없었다. `metrics_1m` 은 60초 주기라 정상이어서
티가 안 났다.

```
23:12  RSS 472MB > 예산 300MB, 3회 연속 → 스로틀 3 (배수 10.0)
23:42  롤업 실행 후 `self._stop.wait(3000)` — 50분 대기 진입
00:06  스로틀 0 으로 회복 — 그러나 이미 잠든 스레드는 깨지 않는다
```

`Event.wait()` 는 종료 신호에만 반응한다. **스로틀은 주기를 늦추라는 뜻이지, 회복된
뒤까지 늦춘 채로 두라는 뜻이 아니다.** 남은 대기를 매번 다시 계산하며 잘게 자르도록
바꿔, 배수가 내려가면 일찍 깨고 올라가면 더 자게 했다.

**`wake_granularity_s: 5.0` 은 실측으로 정했다.** `Event.wait` 타임아웃 1회가 CPU
**7.8µs** 라, 컴포넌트 10개가 하루 17만 회를 깨워도 **0.0016%** — 예산 2% 의 1/1000 이다.
비용이 사실상 없으므로 크게 잡을 이유가 없어, 관측이 비는 시간을 줄이는 쪽으로 5초를
골랐다. 스로틀이 풀린 뒤 최대 5초면 정상 주기로 돌아온다.

**이걸 늦게 안 이유가 따로 있었다 — 로그가 새고 있었다.** `extra={"level": 3}`
(스로틀 단계)이 JSON 로그의 로그레벨 자리를 덮어써, 예산 초과 경고가 `"level": 3` 으로
찍혔다. `level=WARNING` 으로 거른 점검에서 **전부 누락된다.** 포매터가 자기 자리
(`ts·level·logger·msg·thread·exc`)를 지키고 충돌한 `extra` 는 `extra_*` 로 옆에 두도록
고쳤다. 호출부도 `throttle_level` 로 개명했다.

기존 예약키 테스트는 이걸 못 잡았다. `level` 은 `LogRecord` 속성이 아니라(속성명은
`levelname`) 검사를 통과하는데, **JSON 출력에서만 이름이 겹쳐 예외 없이 값만 틀어졌다.**
조용히 깨지는 종류라 mutation 대상에 둘 다 넣었고 `throttle_wakeup`·`log_level_field`
모두 잡혔다. 스로틀이 유지될 때는 늘어난 주기를 지키는지(반대 방향)와 config 배선도
따로 잰다 — 로직만 보면 YAML 을 고쳐도 안 바뀌는 상태를 놓친다.

303 passed, 1 xfailed.

### RSS 봉우리의 주인을 찾을 근거를 남긴다 (2026-08-04)

위 사고의 **원인**은 아직 모른다. 예산(300MB)을 넘긴 시점의 실측 RSS 는
447 / 469 / 553 / 588MB 인데 `self_telemetry` 평균은 65MB 이고 `private_mb` 는
501MB 로 평탄하다. **순간값만 튄다** — 5초 표본이 봉우리를 놓치고 있다는 뜻이다.

표본 주기를 줄이는 것은 **관측자가 예산을 먹는 방향**이라 규칙 1 에 걸린다. tick 마다
RSS 를 재는 델타 측정도 psutil 호출이 초당 수십 번 늘어 같은 이유로 뺐다. 대신
`Supervisor` 가 `_active` 를 유지해(tick 당 dict 쓰기 두 번) **표본을 찍은 그 순간
tick 중이던 컴포넌트**를 `self_telemetry.active` 에 남긴다(마이그레이션 013).
`throttle_level > 0` 표본만 모아 분포를 보면 범인이 드러난다.

**계측이 거짓말할 수 있는 자리 둘을 막았다.** 예외로 끝난 tick 을 `finally` 없이
지우면 한 번 터진 컴포넌트가 영원히 "실행 중"으로 남아 모든 표본에 그 이름이 박힌다 —
아무나 가리키는 계측은 없는 것보다 나쁘다. 그리고 `active` 조회 실패가 계측 자체를
멈추면 예산·누수 판정의 유일한 근거가 사라지므로, 실패해도 행은 쓴다.

**예산 300MB 를 올릴지는 정하지 않았다.** 설계 규칙 1 에 직접 걸리는 값이라, 데이터
없이 올리면 "모니터가 병목이 되지 않는다"가 숫자만 커진 채로 남는다.

### 트레이가 네이티브 창을 연다 (2026-08-03)

네이티브 창을 다 만들어 놓고 정작 트레이 메뉴는 Streamlit 을 띄우고 있었다. 메뉴 이름도
"대시보드 열기" → **"Argus 창 열기"** 로 바꿨다.

**묶인 상태와 소스 실행이 다르다.** exe 에서 창은 `argus-ui.exe` 라는 별도 실행 파일이다
(상주와 프로세스를 나눠 창이 죽어도 수집이 계속되게 했다). 상주 exe 옆과 `argus-ui/`
하위를 둘 다 찾고, 없으면 **조용히 실패하는 대신 말한다**(규칙 4).

**종단 검증은 상주와 같은 환경에서 했다.** venv 파이썬으로 재면 거기엔 PySide6 가 원래
있어 무엇을 고치든 통과한다 — base 인터프리터에 `soak_entry` 와 같은 경로 설정을 하고
트레이의 실행 경로를 그대로 태웠다. 다섯 페이지가 전부 뜬다.

296 passed, 1 xfailed.

### 알림을 켰다 (2026-08-03, Phase 9 마무리)

`detection.notify: false` → `true`. **8일 만에 Argus 가 사용자에게 말을 건다.**

**켤 근거는 꺼진 상태로 쌓았다.** 판정(`notified`)과 발송을 분리해 둔 덕에, 알림을 보내지
않으면서도 "켜면 몇 건이 올 것인가"가 계속 측정됐다.

```
20번 수정(GPU 룰 상대조건) 이전 6일   하루 6.2건 · 최다가 GPU 고온 지속
20번 수정 이후 누적 약 8시간          알림 대상 0건 (121분 게임 세션 포함)
```

GPU 고온이 최다였던 이유는 **매일 93도로 게임하는 이 기계에서 그게 "평소"** 였기
때문이다. 절대 온도 룰이 정상 동작을 하루 9.3회 알리고 있었고, 상대 조건을 AND 로
걸어 잡았다.

**기동 알림을 함께 넣었다.** 0건이라는 게 양날이다 — 오탐이 없다는 뜻이지만, 그 상태로는
"알림이 켜져 있는지" 자체를 사용자가 알 수 없다. 기동마다 풍선 하나면 하루 한두 번이라
소음이 아니고, **안 뜨면 그 자체가 신호다.** 등급은 `info` 다 — 이상이 아닌데 `warning`
으로 올리면 등급이 뜻을 잃는다.

되돌리기는 `detection.notify: false` 한 줄이다. 그 비용이 낮다는 것이 켤 수 있는
이유이기도 하다.

294 passed, 1 xfailed.

### 타임라인·자기 상태 이식 — 다섯 페이지가 다 찼다 (2026-08-03)

**타임라인**은 긴 구간용이라 실시간과 데이터 성격이 다르다. x 축을 "몇 초 전"이 아니라
절대 시각(`DateAxisItem`)으로 두고, 구간을 바꾸면 통째로 다시 읽는다 — 최대 일주일/분
단위라 증분이 의미 없다. 웜(Parquet)까지 합쳐 읽으므로 조회는 워커 스레드에서 한다.

이 페이지의 값은 **오버레이**다. 결함 주입 구간(세로 밴드)과 탐지 신호(세로선)를 지표
위에 겹쳐야 "그때 무엇을 넣었고 무엇이 울렸나"가 한눈에 보인다. **증상이 관측되지 않은
주입은 흐리게 그린다** — 채점에서 빠지는 구간인데 같은 진하기로 그리면 "탐지 실패"처럼
읽힌다.

**자기 상태**는 "관측자가 병목이 되고 있지 않은가"를 답한다. 누수 증가율은 RSS 가 아니라
**private** 으로 잰다 — RSS 는 워킹셋 트림에 따라 내려가 누수를 가린다(07-27 실측:
RSS 63 → 18MB 가 반납이 아니라 트림이었다). 유실·스로틀은 규칙 1 이 깨지는 신호라
평소엔 조용하다가 그때만 경고로 드러낸다.

**같은 실수를 세 번 했고, 셋 다 mutation 이 잡았다.**

| 회차 | 헛돈 이유 |
|---|---|
| 프로세스 | 테스트 데이터가 우연히 정렬 결과와 같았다 |
| 사건 | `cache_clear` 의 *존재*만 보고 *호출*을 안 봤다 |
| 자기 상태 | `isVisible()` 은 창을 띄우지 않으면 **항상 False** 다 |

셋 다 통과했고 셋 다 아무것도 확인하지 않았다. **"통과했다"와 "검증했다"는 다르다** —
PLAN 함정 목록에 넣었고, 세 번째는 mutation 전에 스스로 발견해 고쳤다.

292 passed, 1 xfailed. mutation 4/4 잡힘. exe 298MB 유지.

### 사건 페이지 이식 — 피드백 버튼이 죽어 있었다 (2026-08-03)

**이 페이지가 Argus 의 산출물이다.** 수집도 탐지도 여기서 문장이 되지 않으면 사용자에게
닿지 않는다. 구간 선택 · 요약 타일 4개 · 목록 + 상세(마크다운 리포트, 원인 후보 표) ·
피드백 버튼.

**Streamlit 판의 `expander` 대신 좌측 목록 + 우측 상세로 갔다.** 목록을 훑으며 상세를
갈아 끼우는 편이 "어느 사건이 문제였나"를 찾는 동작에 맞고, 접힌 것을 하나씩 여는
수고가 없다. 표 위젯은 프로세스 페이지에서 만든 것을 그대로 썼다 — 이식 전 남은
미지수가 마크다운 렌더링 하나였는데 `QTextBrowser.setMarkdown()` 으로 끝났다.

**회귀 하나를 잡았다 — 내가 만든 것이다.** `st.cache_data` 를 `ttl_cache` 로 바꾸면서
`.clear()` 가 `cache_clear()` 로 이름이 달라졌는데 호출부를 안 고쳤다. 그대로 뒀으면
**피드백 버튼을 누를 때마다 `AttributeError`** 였고, Streamlit 판에서도 똑같이 깨져
있었을 것이다.

**그 회귀용 테스트가 처음엔 아무것도 검증하지 않았다.** `cache_clear` 가 *존재하는지*만
봤는데 규칙은 "`set_user_label` 이 그걸 *부르는가*"였다. mutation 에서 `[안 잡힘]` 이
나와 드러났고, 실제 DB 를 만들어 호출 여부와 저장 결과를 함께 확인하도록 고쳤다.
**프로세스 페이지에서 한 실수와 같은 종류다**(그때는 데이터가 우연히 정렬 결과와 같아
통과했다) — 습관 문제로 보고 PLAN 함정 목록에 넣었다.

`isVisible()` 로 안내 문구를 확인하려던 테스트도 헛돌 뻔했다. 부모 창이 `show()` 되어야
True 라, 창을 띄우지 않는 테스트에서는 **항상 False** 다. `isHidden()` 으로 바꿨다.

282 passed, 1 xfailed. mutation 2/2 잡힘. exe 298MB 유지.

### 프로세스 페이지 이식 — 표 위젯이 두 UI 버그를 드러냈다 (2026-08-03)

집계 창 선택 · CPU 가로 막대 랭킹 · 전체 표 · 개별 추이(CPU / 메모리·핸들 + 증가율).
표 위젯(`Column`·`TableModel`·`DataTable`)은 **사건 페이지가 재사용할 것**이라 처음부터
따로 만들었다.

**테스트가 실제 UI 버그를 잡았다.** `setSortingEnabled(True)` 는 기본 인디케이터
(0번 열 내림차순)로 **즉시 정렬을 건다.** 조회는 `ORDER BY cpu DESC` 로 오는데 표가
그걸 뒤집어, 프로세스 표가 CPU 순이 아니라 **이름 역순**으로 뜨고 있었다. 창을 눈으로만
봤으면 그냥 넘어갔을 종류다. 지정이 없으면 조회 순서를 지키도록 고쳤다.

**정렬은 원본 값으로 한다.** 표시 문자열로 정렬하면 `9 MB` 가 `10 MB` 뒤에 온다 —
가장 많이 쓰는 프로그램을 찾으려고 정렬하는데 답이 틀린다.

**그 버그용으로 쓴 테스트가 처음엔 아무것도 검증하지 않았다.** 데이터가 우연히 정렬
결과와 같은 순서라 규칙을 지워도 통과했고, mutation 에서 *다른* 테스트만 잡는 것을 보고
드러났다. 이름이 오름차순인 데이터로 바꿔 고쳤다.

**Qt 특유의 함정 하나.** 테스트에서 만든 위젯이 `QThread` 를 물고 GC 되면 인터프리터가
통째로 죽는다(pytest exit 9, 같은 코드가 단독 실행에서는 멀쩡). 세션 끝에 한꺼번에
정리해도 같은 크래시가 나서, C++ 쪽 소멸은 프로세스 종료에 맡겼다.

**갱신 주기는 5초다.** 집계 창이 최소 30초라 1초마다 다시 물어도 값이 거의 그대로다 —
관측자가 자기 부하를 만들 이유가 없다(설계 규칙 1).

277 passed, 1 xfailed. mutation 2/2 잡힘. exe 298MB 유지.

### 실시간 페이지를 네이티브로 이식했다 (2026-08-03)

수치 타일 5개 + 차트 5개(CPU·메모리 / 디스크 처리량 / 디스크 응답 / 네트워크 / GPU).
왼쪽 탭으로 페이지를 나누는 골격도 함께 세웠다 — 나머지 넷은 "옮기는 중" 안내를
띄운다. **빈 화면 대신 지금 어디서 볼 수 있는지 말한다.**

```
argus/desktop/widgets.py         StatTile · TimeSeriesChart (재사용)
argus/desktop/pages/realtime.py  실시간 페이지 + 조회 워커
argus/desktop/app.py             창 골격 + 페이지 전환
```

**과거는 한 번만 읽는다.** 창을 열 때 최근 10분을 백필하고 그 뒤로는 새 표본만
덧붙인다. 매 초 600행을 다시 읽으면 관측자가 관측 대상을 오염시킨다(설계 규칙 1).

**측정 지표가 또 문제를 잡았다.** 첫 실행이 `608개`로 나와 통과처럼 보였는데,
그중 **600개가 백필**이고 실시간은 8개뿐이었다. "창을 열었다"와 "갱신이 돈다"는
다른 사실인데 한 숫자에 섞여 있었다. 둘을 분리하고 기대치(초당 한 점의 60%)를
넣으니 판정이 명확해졌다.

```
소스: 백필 594개 · 실시간 14개 (15초)
exe : 백필 594개 · 실시간 14개 (15초) · 298MB
```

270 passed, 1 xfailed. mutation 2/2 잡힘(백필/실시간 분리 · 중복 타임스탬프 무시).

### 네이티브 데스크톱 창 — 1단계 예광탄 (2026-08-03)

대시보드를 브라우저가 아니라 **완전한 데스크톱 앱**으로 만들기로 했다. 크게 투자하기
전에 판정할 것 넷을 예광탄 하나로 확인했고, **넷 다 통과했다.**

| 판정 항목 | 결과 |
|---|---|
| 창이 뜨는가 | ✅ (`ARGUS_UI_SCREEN` 으로 보조 모니터 배치) |
| 실시간 갱신 | ✅ **12초에 12개** — 정확히 1초 주기 |
| 조회가 UI 를 막지 않는가 | ✅ 워커 스레드(`QThread`) 분리 |
| exe 번들 | ✅ **298MB** (실패 신호로 잡아 둔 500MB 아래) |

**PySide6 + pyqtgraph** 를 골랐다. Qt 는 20년 넘은 상용급이고, `pyqtgraph` 는 초당 수천
포인트 갱신용으로 설계돼 1초 주기 모니터에 맞는다. Flet 은 신생, Dear PyGui 는 위젯
생태계가 작고, tkinter 는 차트가 빈약해 목표와 반대다.

**설계 판단 하나를 검토 중에 뒤집었다.** 처음엔 트레이를 `QSystemTrayIcon` 으로 옮기려
했는데, 그러면 상주 프로세스에 Qt 이벤트 루프가 들어온다. 지금 `main()` 은
`sup.wait()` 로 메인 스레드를 쓰고 Qt 는 `QApplication.exec()` 를 요구하니 **둘이
메인 스레드를 두고 다툰다.** 무엇보다 관측자를 무겁게 만드는 것이라 설계 규칙 1 과
부딪친다 → **창은 별도 프로세스, 트레이는 win32 유지.**

**조회 계층을 UI 에서 떼어냈다.** 캐시가 `st.cache_data` 여서 `data.py` 가 Streamlit
없이는 import 조차 안 됐다. `ttl_cache` 로 바꾸니 286줄이 그대로 재사용된다 — 창을
바꿔도 조회 코드는 따라 옮길 필요가 없다.

**예광탄이 곧바로 값을 했다.** 첫 실행이 12초에 **6개**였고, 원인이 `latest_metrics` 의
캐시 TTL 2초였다. Streamlit 의 rerun 폭주를 막으려던 값이 네이티브 창에서는 갱신을
절반으로 떨어뜨리고 있었다. 1초로 고쳤다 — **눈으로만 봤으면 "좀 굼뜬가"로 넘어갔을
문제다.** 검증에 "몇 개를 그렸는가"를 숫자로 넣은 덕에 잡혔다.

267 passed, 1 xfailed. mutation 1/1 잡힘.

**남은 것**: 페이지 5개 이식(실시간 → 사건 → 프로세스 → 타임라인 → 자기상태) 뒤
Streamlit 제거. 제거는 마지막이다 — 그 전까지 둘 다 살려 두면 이식본과 원본을 나란히
비교할 수 있고 언제든 되돌릴 수 있다.

### 트레이의 "대시보드 열기"가 아무 일도 하지 않았다 (2026-08-03)

메뉴를 눌러도 조용했다. **조용한 실패를 내가 만들었다**(규칙 4).

상주는 base `pythonw.exe` 로 돈다 — venv 트램폴린이 콘솔 창을 만들기 때문에
`tools/soak_entry.py` 가 경로를 직접 세우고 base 인터프리터를 쓴다. 그런데
`_open_dashboard` 가 `sys.executable` 로 자식을 띄웠고, **그 인터프리터에는
`streamlit` 이 없다.** `ModuleNotFoundError` 는 `CREATE_NO_WINDOW` 뒤로 사라졌고
`Popen` 은 즉시 돌아오므로 실패를 아무도 몰랐다.

```
수정 전 (base 인터프리터)   exit 1
수정 후                     포트 8599 리스닝 확인
```

**셋을 고쳤다.**

1. **현재 프로세스의 `sys.path` 를 `PYTHONPATH` 로 물려준다.** `soak_entry` 가
   `site.addsitedir` 로 얹어 둔 venv 경로가 거기 들어 있다.
2. **띄운 것과 뜬 것은 다르다.** `Popen` 뒤에 감시 스레드를 붙여, 곧바로 죽으면
   그 이유를 풍선으로 알린다. 감시를 별도 스레드에 둔 이유는 트레이 메시지 펌프를
   막으면 아이콘이 얼어붙기 때문이다.
3. **exe 에서는 시도하지 않고 안내한다.** 대시보드는 아직 번들에 없다.

**검증은 상주와 같은 환경에서 했다.** venv 파이썬으로 재면 거기엔 streamlit 이 원래
있어 무엇을 고치든 통과한다 — base 인터프리터에 `soak_entry` 와 같은 경로 설정을 하고,
포트 리스닝으로 판정했다. GUI 는 띄우지 않았다.

260 passed, 1 xfailed. mutation 2/2 잡힘.

### 지문 갱신 스레드가 몇 주 동안 조용히 죽어 있었다 (2026-08-03)

**exe 빌드가 콘솔을 띄운 덕에 드러났다.** 상주는 `pythonw` 로 돌아 stderr 가 없고,
스레드 사망은 기본적으로 stderr 로만 알려진다 — 그래서 아무 신호도 없었다.

```
AttributeError: 'FingerprintBuilder' object has no attribute 'throttleable'
```

`FingerprintBuilder` 가 `Component` 를 상속하지 않고 덕 타이핑으로 `name`·`interval_s`·
`tick` 만 갖췄다. 그런데 수퍼바이저는 매 틱 `throttleable` 도 읽고, **그 참조는 `tick()`
을 감싼 `try` 바깥**이다. 결과적으로 이 컴포넌트는 **첫 틱 직후 항상 죽었다** — 6시간
주기 지문 갱신이 사실상 "기동 시 1회"로 줄어든 채 돌았다.

실질 피해는 운이 좋아 작았다. 재기동이 잦아 그때마다 한 번씩은 갱신됐고, 오늘 오전
지문 오염(경계 버킷)이 해소된 것도 그 "기동 시 1회" 덕이다. 하지만 며칠 연속 켜 두면
`procleak` 억제가 낡은 지문으로 돌게 된다.

**셋을 고쳤다 — 원인, 재발 방지, 그리고 "죽었다는 사실".**

1. `FingerprintBuilder(Component)` 로 상속. 전수 조사에서 규약을 어긴 컴포넌트는
   이것 하나뿐이었다.
2. `Supervisor.add()` 가 규약을 검사해 **등록 시점에 터진다.** 기동 시점의 시끄러운
   실패가 런타임의 조용한 죽음보다 낫다. 검사 범위는 **`try` 바깥에서 읽는 것만**
   이다(`name`·`interval_s`·`tick`·`throttleable`) — `setup`·`teardown` 은 try 안이라
   없어도 조용히 죽지 않고, 과하게 잡으면 덕 타이핑 테스트 대역까지 거부한다(실제로
   그랬다).
3. 컴포넌트 스레드 전체를 감싸 사망을 로그와 크래시 파일에 남긴다. **죽더라도 죽었다는
   것은 알아야 한다.**

254 passed, 1 xfailed. mutation 1/1 잡힘. 실제 생존도 직접 쟀다 — 수정 전 1회에서
멈추던 것이 0.6초에 10회 돈다.

### 트레이 아이콘과 풍선 알림 (2026-08-03)

**이 프로그램이 사용자에게 말하는 경로가 0개였다.** 8일간 수집하고 탐지하고 귀인까지
하면서, 대시보드를 직접 열지 않으면 아무 일도 일어나지 않았다.

**의존성을 늘리지 않았다.** `pywin32` 는 PDH 때문에 이미 필수라 `Shell_NotifyIcon` 을
직접 쓰면 아이콘과 알림을 한 API 로 해결한다. pystray(+Pillow)나 win10toast 는 둘 중
하나만 주면서 의존성을 늘리고, WinRT 토스트는 AUMID 앱 등록이 필요해 배포 절차가 는다.

`argus/ui/tray.py` 는 `Component` 규약에 얹었다 — 창 메시지는 창을 만든 스레드에서만
처리되는데, `setup`(생성)·`tick`(펌프)·`teardown`(제거)이 한 스레드에서 도는 구조가
그 요구를 이미 만족한다. 펌프는 `PumpWaitingMessages` 로 논블로킹이다.

**세 가지를 규칙으로 못 박았다.**

- 트레이 실패가 수집을 죽이지 않는다(설계 규칙 1). 대신 조용히 죽지도 않는다 —
  `describe()` 로 상태를 드러낸다(규칙 4).
- 종료 시 아이콘을 반드시 지운다. 안 지우면 죽은 아이콘이 트레이에 남는다.
- **알림 실패가 융합을 죽이지 않는다.** 탐지가 알림보다 중요하다 — 알림은 못 받아도
  대시보드에서 볼 수 있지만, 사건이 기록되지 않으면 그 시간대는 영영 설명할 수 없다.

**판정과 발송을 분리했다.** `notified` 판정은 항상 돌고(대시보드와 채점이 쓴다),
`detection.notify` 는 실제로 띄울지만 정한다. 이 분리가 없으면 "알림을 켜면 몇 건이
올 것인가"를 켜기 전에 잴 수 없다. 기본값은 여전히 꺼짐이다.

알림 전달자 인터페이스는 `notify(title, message, severity) -> bool` 하나뿐이라,
나중에 Discord 등을 붙일 때 `fusion` 을 고치지 않아도 된다.

251 passed, 1 xfailed. mutation 2/2 잡힘(발송 게이트 · 실패 격리).

**아직 검증하지 못한 것**: 아이콘과 풍선이 실제로 화면에 뜨는지는 사람이 봐야 하고,
마우스를 움직이는 자동화는 쓰지 않는다(CLAUDE.md). 사용자가 게임 중이라 화면을 쓰는
스모크(`python -m argus.ui.tray`)와 exe 재빌드를 미뤘다.

### exe 빌드가 실제로 된다 (2026-08-03, Phase 14 착수)

**8일째 미지수로 두고 있던 리스크였다.** 배포가 전제인 프로젝트인데 빌드를 한 번도
시도하지 않아, 안 되면 그동안 쌓은 것이 전달되지 않는 상태였다. 남은 계획이 전부
정확도 개선인데 정작 막힌 곳은 거기가 아니라는 판단으로 순서를 바꿔 먼저 확인했다.

```
빌드 37~48초 · dist 195MB · exe 9.9MB · 파일 1,429개
```

**검증은 빌드 성공이 아니라 실행으로 했다.** 격리 디렉터리(`ARGUS_DATA_DIR`)에서 30초
돌려 실제 수집을 확인했다 — `metrics_raw` 30행(1Hz 정확), `process_metrics` 1,031행,
drop 0, 새 DB 를 스키마 v12 로 생성.

가장 큰 리스크였던 **PDH·NVML 이 둘 다 살아 있다.** `soak_entry.py` 가 남긴 교훈대로
pywin32 가 빠지면 예외 없이 psutil 폴백으로 내려가 `disk_resp_ms` 가 전부 NULL 이 되므로,
`[OK]` 표시가 아니라 **실제 값**을 봤다 — PDH 유래 4개 컬럼 30/30행, NVML 클럭 30/30행,
`ctx_switches_ps` 112,599 같은 실측값.

**1차 빌드는 실패했고 그 실패가 이 작업의 값이다.** `pathex=[".."]` 가 spec 파일 위치가
아니라 **빌드를 실행한 작업 디렉터리** 기준으로 풀려 `argus` 패키지가 통째로 빠졌다.
빌드는 성공(exit 0)하고 실행에서 `ModuleNotFoundError: No module named 'argus'` 로 죽었다
— CLAUDE.md 의 "상대경로는 exe 에서 조용히 깨진다"가 리소스가 아니라 **빌드 정의 자체**에서
재현된 것이다. `SPECPATH` 기준 절대경로로 고쳤다.

**범위를 나눴다.** 대시보드(Streamlit)는 자체 자원 파일·동적 import 가 많아 별도 문제이고,
그것 때문에 전체가 막히면 무엇이 진짜 장애물인지 알 수 없게 된다. 지금 exe 는 콘솔 창이
뜬 채 수집만 한다 — 트레이·알림 발송은 미구현이다.

### 등급을 두 축으로 — 위험 축 먼저 (2026-08-03, PLAN §8 의 10번 · 절반)

등급이 **탐지기 종류**에 붙어 있었다. `procleak` 은 무조건 warning, GPU 룰은 `rules.yaml`
에 적힌 값. 그래서 누수 규모나 실제 성능 손실과 무관하게 결정됐다.

**축이 하나면 반드시 한쪽이 왜곡된다.** GPU 스로틀은 지금 클럭이 깎이는 *현재 손실*이고,
핸들 누수는 지금 증상이 없지만 방치하면 프로세스가 죽는 *미래 위험*이다. 한 자에 억지로
놓으면 증상 없는 누수가 영원히 info 로 묻히거나 정상 스로틀링이 상시 warning 이 된다.
그래서 `등급 = max(현재 손실, 위험)` 으로 간다.

**후보 축 넷 중 둘은 실측에서 탈락했다** (procleak 신호 22건).

```
주입 python 핸들 : ratio 3.3~8.4배  · delta 2,700~3,700개   ← 진짜 결함
medal   메모리   : ratio 6.5~27.8배 · delta 1,321~2,693MB   ← 게임 녹화, 정상 동작
```

- **배수**: 가장 높은 배수(27.8)가 정상 프로세스다. 07-30 판단이 재확인됐다.
- **절대 증가량**: 핸들 3,664개와 메모리 2,693MB 를 같은 자에 놓을 수 없다. 단위가 다르다.
- **지문 대비 위치**: 실제로 갈린다 — medal 은 자기 p99 의 12~98%, 주입은 286~357%.
- **소진까지 남은 시간**: 상한을 알아야 하는데 핸들의 실질 상한은 시스템 전역이라 뺐다.

이번에 넣은 것은 **위험 축**뿐이다(`decide/severity.py`, 문턱은 `config` 의 `severity` 절).
`procleak` 의 warning 고정이 `p99` 대비 위치로 바뀌어 warning/critical 이 갈린다.

**첫 측정이 실패 신호에 걸렸고, 그게 선행 버그를 드러냈다.** 22건 전부 info 로 내려가
알림이 22 → 0 이 됐다. 축이 아니라 지문이 오염돼 있었다 — 별도 커밋으로 정리했다
("주입 구간과 겹치는 경계 버킷"). 고친 뒤 주입 16건이 critical, medal 6건이 info 로 갈렸다.

**문턱은 뜻으로 정했다.** 스윕을 돌려 보니 어디에 둬도 결과가 거의 같다(알림 16~17건) —
medal 최대 98% 와 주입 최소 286% 사이가 비어 있기 때문이다. `warning = 1.0`(평소 상한),
`critical = 2.0`(두 배)로 두되, **critical 문턱은 실측 근거가 없다**: 표본이 주입 하나뿐이고
실사용 누수는 medal 밖에 없는데 그건 info 로 갔다. 실사용 누수가 쌓이면 재평가한다.

**남은 절반(현재 손실 축)은 데이터를 기다린다.** 클럭 기준선을 세우려면 며칠치가 필요한데
원본은 24시간 보존이라, 컬럼부터 넣어 수집을 시작했다(별도 커밋).

회귀 없음 — `procleak` TP 8 / FP 2 / FN 0 · F1 0.889 · 지연 320s (기준선과 동일).
247 passed, 1 xfailed. mutation 3/3 잡힘(축·문턱 배선·롤업 클럭).

### mutation 측정을 스크립트로 다시 돌렸다 — 검증 구멍 하나 (2026-08-03, PLAN §8 의 11번)

07-29 의 측정은 `_SCORE_SATURATION_Z` 가 1.0 으로 남은 캐시 오염 상태에서 나왔다. 그때
"잡힌다"고 분류된 규칙들의 판정도 근거가 약했으므로 전부 다시 쟀다. 이번에는 손이 아니라
`tools/mutation_sweep.py` 로 한다 — **손으로 하면 또 같은 사고가 난다.**

기준선 240 passed / 1 xfailed, 대상 15개 중 **13개가 잡혔다.**

| 안 잡힌 것 | 판정 |
|---|---|
| `min_day_hours` | **예상대로.** 무력화할 구현 자체가 없다(§8 의 1번). `xfail(strict)` 이 그 사실을 고정한다 |
| `procleak_monotonic` | **구멍이었다.** 아래 |

**단조성 조건은 로직만 검증되고 배선은 비어 있었다.** `test_procleak.py` 의 톱니 케이스는
`MetricRule(monotonic_ratio=0.85)` 를 테스트 안에서 직접 만든다. 그래서 `defaults.yaml` 과
코드 기본값을 양쪽 다 0 으로 만들어도 241개가 전부 통과한다 — **YAML 을 고쳐도 판정이 안
바뀌는데 아무 신호가 없는 상태**이고, 규칙 3 이 막으려는 것이 정확히 이것이다.

`test_config_thresholds.py` 에 배선 테스트 둘(`monotonic_ratio`, `growth_ratio`·`min_delta`)을
넣고, YAML↔코드 일치 검사를 `process_leak`·`fingerprint` 절까지 넓혔다(중첩 절 재귀 비교).
로직과 배선을 **별도 mutation 으로 나눠** 다시 쟀다 — 4/4 잡힘, 배선 쪽은 새 테스트 하나가
정확히 잡는다. 242 passed / 1 xfailed.

**스윕 스크립트 자체에도 결함이 둘 있었다.** 첫 실행에서 드러났다.

- 마지막 `pyc_audit` 이 `UnicodeDecodeError` 로 죽었다(subprocess 인코딩 미지정).
- 더 나쁜 쪽: 감사 직전에 `__pycache__` 를 전부 지우므로 **"검사한 모듈 0개"로 [OK]** 가
  났다. 07-29 사고를 막겠다는 검사가 아무것도 보지 않고 통과한다 — 이 문서가 반복해서
  적어 온 "아무것도 검증하지 않는 테스트도 통과한다"가 검사 도구 쪽에서 재현된 것이다.
  복원 후 pytest 를 한 번 더 돌려 캐시를 다시 만든 뒤 감사한다(이제 64개 모듈을 본다).

스크립트는 치환 원문이 **정확히 1회** 나오지 않으면 중단하고(무엇을 무력화했는지 말할 수
없는 상태를 막는다), `finally` 에서 소스와 캐시를 복원한다.

### 룰의 절대 문턱을 이 PC 의 능력으로 표현한다 (2026-08-02, 규칙 2 마무리)

`ctx_switches_ps > 50000` → `cores * 8000`, `swap_used_mb > 512` → `ram_mb * 0.05`.
표현식에서 `cores`·`physical_cores`·`ram_gb`·`ram_mb`·`swap_total_mb` 를 쓸 수 있다
(`rules.machine_variables()`, 머신 프로파일에서 읽어 캐시).

**두 문턱 다 이 PC 에서 이미 뜻을 잃고 있었다.**

```
ctx_switches_ps  중앙값 64,566 · p95 171,627    문턱 50,000 → 시간의 66.2% 가 초과
                 → cores * 8000 = 96,000 (12코어)
swap_used_mb     RAM 64GB 에서 512MB 는 0.8%, RAM 4GB 에서는 12.5%
                 → ram_mb * 0.05 = 4GB→200MB · 64GB→3.2GB
```

컨텍스트 스위치 쪽은 상대 조건(`median + 8 * sigma`)이 AND 로 걸려 있어 발화까지 가진
않았지만, **하한으로서 아무 일도 하지 않고 있었다.** 스왑 쪽은 반대로 작은 PC 에서
상시 참이 된다.

**머신 값을 못 읽으면 판정 불가로 두고 경고한다.** 조용히 `False` 면 그 룰이 영영
발화하지 않고 아무도 모르며, 조용히 `True` 면 상시 오탐이다 — 규칙 4 의 "없으면 드러내
놓고 비활성화"에 해당한다. 경고는 룰 표현식당 한 번만 낸다(룰 평가는 초당 여러 번 돈다).

**이 PC 에서는 수치가 바뀌지 않는다**(평가용 스냅샷: `rules` FP 0 유지, `procleak` 8/2/0
그대로). 12코어·64GB 에서는 두 문턱 모두 이미 안전한 쪽이었기 때문이다. 효과는 4코어·
4GB 같은 기계에서 나타나고, **그쪽은 아직 단위 테스트로만 확인된 상태다** — 그런 하드웨어가
없어 리플레이로는 재현할 수 없다.

### 냉각 열화 탐지 (`detection/thermal.py`) — 조치할 수 있는 신호 (2026-08-02)

**같은 부하에서 예전보다 뜨거운가**를 본다. 절대 온도는 사용자가 조치할 수 없는 정보다 —
`GPU 93도` 를 들으면 할 일이 없고, 그 온도는 팬·클럭이 균형을 잡은 결과이지 고장이 아니다.
`같은 부하에서 지난주보다 7도 높다` 는 청소·서멀 재도포로 이어진다.

**하드웨어를 가정하지 않는다(규칙 2).** 절대 온도 문턱이 하나도 없고 자기 과거와만
비교하므로, 노트북(평소 87도)이든 데스크탑(평소 70도)이든 같은 코드가 그대로 맞는다.
같은 상승폭이 60도대와 90도대에서 똑같이 판정되는지를 테스트로 고정했다.

**부하를 맞춰 비교하는 것이 핵심이다.** 유휴를 섞으면 "요즘 게임을 많이 했다"가 "냉각이
나빠졌다"로 둔갑한다. `gpu_util_mean >= 80` 인 1분 버킷만 보고, 그런 버킷이 하루 20분
미만인 날은 표본에서 통째로 뺀다.

새 저장소도 새 수집기도 필요 없었다 — 롤업(`metrics_1m` + 웜 Parquet)에 `gpu_util_mean`·
`gpu_temp_max`·`gpu_power_mean` 이 처음부터 1분 단위로 들어 있고 웜은 영구 보존이다.

**결과를 이미 아는 데이터 두 개로 보정했다.** 실제 데이터(7일, 부하 시 온도 93.0도 불변)
에서는 "상승 없음", 합성 열화 데이터에서는 발화. 한쪽만 보면 "아무것도 발화하지 않는"
구현과 "무조건 발화하는" 구현이 각각 통과한다. 상승 문턱과 부트스트랩 조건을 각각 지워
실패를 확인했다.

표본이 `min_days`(기본 7일)에 못 미치면 **"정상"이라고 답하지 않고 침묵한다** — 데이터가
없는데 정상이라고 하면 그것도 거짓말이고, 사용자는 확인받았다고 믿는다.

### GPU 온도 룰이 정상 동작을 하루 9.3회 알리던 것을 고친다 (2026-08-02, PLAN §8 2번)

`GPU 열 스로틀링`(80도, info)을 없애고 `GPU 고온 지속`(90도)에 상대 조건을 AND 로 걸었다.

**실측이 근거다** (5.9일, GPU 표본 65,695).

```
GPU 온도  중앙값 83도 · p95 92도 · 최대 96도
  80도 초과 : 52.6% 의 시간      ← `GPU 열 스로틀링` 조건
  90도 초과 : 19.7% 의 시간      ← `GPU 고온 지속` 조건
  95도 초과 : 0.003% (2 표본)

발화  GPU 열 스로틀링 55건 · GPU 고온 지속 16건 → 하루 12.7회
      `rules` 신호 117건 중 GPU 룰이 75건 = 64%
```

없앤 룰은 주석에 스스로 "부하 시 흔한 동작이다"라고 적어 두고 그것을 알리고 있었다.
`notify: false` 라 사용자가 못 느꼈을 뿐, Phase 9 에서 알림을 켜면 그날로 알림을 끄게 된다.

**이 발화들이 오탐이라는 것도 데이터로 확인했다.** 웜 Parquet 에서 GPU 부하 구간
(`gpu_util_mean >= 80`)의 온도를 날짜별로 재면 **6일 내내 정확히 93.0도**다(사용률
89~98%, 전력 219~228W). 냉각이 열화됐다면 이 값이 올라간다 — 전혀 움직이지 않는다.
93도는 이 GPU 의 팬·클럭이 균형을 잡는 열 평형점이고, 90도 문턱은 그 **아래**에 있었다.

**효과 (평가용 스냅샷으로 측정 — 같은 입력 비교).**

| 탐지기 | | FP | 오탐/시간 |
|---|---|---|---|
| `rules` | 전 | 4 | 0.97 |
| | 후 | **0** | **0.00** |
| `procleak` | 전 | 2 | 0.48 |
| | 후 | 2 | 0.50 |

`GPU 위험 온도`(95도, critical)는 그대로 뒀다 — 실리콘 안전 한계는 벤더가 정하고
사용자 PC 마다 다르지 않다. 실측에서도 2 표본뿐이다.

### 평가용 스냅샷 (`tools/eval_snapshot.py`) — 같은 입력으로 반복 채점 (2026-08-02)

리플레이 평가의 재생 구간이 실행마다 줄어든다. 보존 정리가 앞부분을 계속 지우기 때문이다.
같은 날 세 번 돌렸더니 1401.6 → 1371.6 → 1346.6분이었고, 그래서 `rules` 의 오탐이
22 → 21 → 20 으로 준 것이 **고친 효과인지 구간이 짧아진 효과인지 가릴 수 없었다.**

주입 구간 ±`fault_guard_s` 를 별도 SQLite 로 떠 두고 `python -m argus.eval --db <경로>` 로
그것만 재생한다. 같은 스냅샷으로 두 번 돌려 수치가 완전히 일치하는 것을 확인했다
(재생 구간 · 관측 틱 · 모든 탐지기 지표).

**조용한 시간을 함께 담는다(`--quiet-hours`, 기본 6).** 처음에는 주입 구간만 담았는데,
그러자 `procleak` 의 정밀도가 80.0%(FP 2)에서 **100.0%(FP 0)** 로 올라가고 오탐/시간은
아예 계산되지 않았다. 오탐은 "평소에 얼마나 조용한가"를 재는 지표라 평소가 스냅샷에
없으면 잴 것이 없다 — 하필 문턱 튜닝에서 근거로 삼아야 할 수치다. 조용한 6시간을 넣자
전체 DB 와 같은 FP 2 · 정밀도 80.0% 가 나왔다.

**이 실패는 조용하다.** 스냅샷은 멀쩡히 만들어지고 채점도 정상 종료하며, 수치는 **더 좋게**
나온다 — 좋아 보이니 의심할 이유가 없다.

**파일 복사가 아니라 `sqlite3.backup()` 을 쓴다.** DB 가 WAL 모드라 최근 데이터는 아직
`-wal` 에 있고 `shutil.copy2` 는 `.db` 만 가져간다. 테스트에서 4KB 짜리 빈 스냅샷이 나와
드러났고, 바꾼 뒤 실제 스냅샷의 `metrics_raw` 가 28,491 → 28,564행이 됐다.

`net_connections` 는 담지 않는다 — 채점이 쓰지 않고, 네트워크 목적지는 규칙 5 가 말하는
민감 정보다. 스냅샷은 파일 하나라 원본 DB 보다 훨씬 쉽게 옮겨 다닌다.

### 판정 문턱을 config 로 옮긴다 (2026-08-02, PLAN §8 3번 — 규칙 3)

`bottleneck`·`fusion`·`report`·`attribution` 의 판정 문턱이 코드에 박혀 있어 "튜닝은 코드
수정 없이 YAML 만 고쳐서 되어야 한다"가 지켜지지 않았다. 두 절을 신설했다.

- **`bottleneck`** (13개) — `z_high`, `disk_resp_floor_ms`, cpu/mem/queue/gpu 문턱,
  `contention_cpu_ceiling_percent`, `override_ratio`·`override_margin`
- **`incident`** (5개) — `max_extend_before_s`·`max_extend_after_s`, `lead_min_share`·
  `lead_resolution_s`, `stock_drop_reset_ratio`

**점수 가중치(0.5·0.3·0.7 …)는 옮기지 않았다.** 그것은 튜닝 값이 아니라 분류 알고리즘의
구조다 — YAML 로 노출하면 사용자가 병목 판정을 망가뜨릴 수 있고 되돌릴 안전장치가 없다.
규칙 3 이 요구하는 것은 임계값 튜닝이지 알고리즘 노출이 아니다.

**한 개념에 두 값이던 디스크 응답 하한을 통일했다** — 코드 5.0 vs `rules.yaml` 3. 엄격한
쪽(5)으로 맞췄다. 3ms 는 NVMe 에서 흔해 룰만 먼저 울리고 분류는 IO 라 하지 않는 구간이
생긴다. 사용자에게는 "디스크 경고가 떴는데 원인은 디스크가 아니라는 리포트"로 보인다.

**값을 YAML 에 적는 것만으로는 규칙 3 이 지켜지지 않는다.** 배선이 한 군데라도 끊기면
조용히 코드 기본값으로 돈다 — 예외도 로그도 없다. 그래서 `tests/test_config_thresholds.py`
는 "값이 있다"가 아니라 **"바꾸면 판정이 달라진다"** 를 본다. 배선을 끊는 mutation 과
`defaults.yaml` 값을 어긋내는 mutation 으로 각각 확인했다. 후자가 특히 필요한데,
**pydantic 은 모르는 키를 조용히 무시하므로 YAML 오타가 아무 경고 없이 넘어간다.**

`Fusion`·채점(`eval.attribution`)·`tools/rescore_incidents.py` 가 모두 같은 문턱을 쓴다.
채점이 기본값으로 돌면 스코어보드가 제품을 재지 않게 된다.

**회귀 확인.** 디스크 하한 통일은 동작 변경이라 리플레이 평가를 돌렸다. `procleak` 이
소수점까지 같다(TP 8 / FP 2 / FN 0, 80.0% / 100.0% / F1 0.889 / 지연 320s).
`rules` 는 FP 21 → 20, 오탐/시간 1.39 → 1.36.

**다만 이 비교에는 한계가 있다.** 재생 구간이 실행마다 줄어든다(1401.6 → 1371.6 →
1346.6분) — 보존 정리가 앞부분을 계속 지우기 때문이다. `rules` 의 FP 감소가 디스크 문턱을
올린 효과인지 구간이 짧아진 효과인지 이 수치만으로는 가를 수 없다. 같은 구간을 두 번 재려면
평가용 스냅샷을 따로 떠야 하는데, 지금은 그 장치가 없다.

### 주입 배치 8회차 — 탐지 사슬은 닫혔고, 병목 분류에서 끊긴다 (2026-08-02 14:15~16:14)

`handle_leak` 을 rate 6/10/15/20/8/12/25/6 으로 각 720초씩 돌렸다. **`procleak` 판정은
8/8 통과**(배수 5.2~5.6, 문턱 3.0), 첫 표본 지연 +0~1초, 표본 97~344행. 12·13 번 수정이
새 데이터에서도 그대로 작동한다.

```
함수 경로 1순위 지목률 : 18/18 = 100.0%  [OK]  DoD 85%
제품 경로 1순위 지목률 :  1/8  =  12.5%  [FAIL]
```

제품 경로 8건이 전부 `메모리 압박` 으로 발표됐고 1위는 `stovecefhelper 63%`·`chrome 70%`·
`msmpeng 42%` 처럼 주입과 무관한 프로세스였다. **그 메모리 압박은 실재하지 않는다** —
주입 구간 안팎의 `mem_percent` 가 똑같다(중앙값 27.8 vs 27.8). 원인은 아래.

### 스왑이 0 이 아니면 항상 메모리 병목이 되던 것을 고친다 (2026-08-02, PLAN §8 18번)

조건을 `if swap:` → `if swap and (mem or 0) >= 60.0:` 으로 바꿔 **스왑을 독립 근거에서
보조 근거로 강등**했다. 아래 "발견" 항목이 배경이다.

**제품 경로 귀인이 12.5% → 100% 가 됐다.**

```
                    수정 전                수정 후
제품 1순위 지목률   1/8 = 12.5%  [FAIL]   8/8 = 100.0%  [OK]  DoD 85%
자원                전부 rss               전부 handles
1위 기여자          chrome·msmpeng 등      전부 python (65~84%)
```

`표본 → first → 배수 → 발화 → 사건 → 자원 → 귀인 1위` 가 처음으로 끝까지 닫혔다.
15번에 남겨 뒀던 MEMORY 계열 8건도 전부 이 버그의 산물이라 함께 사라졌다.

**`swap_z >= z_high` 안은 채택하지 않았다.** 스왑은 값이 거의 일정해 산포가 작고
448 → 449 가 큰 z 로 계산된다 — 같은 파일 `DISK_RESP_FLOOR_MS` 주석이 적어 둔
`0.1 → 0.2ms = 20σ` 와 같은 함정이다. 상대 조건만으로는 다시 빠진다.

스왑이 압박의 신호이려면 "메모리가 부족해서" 페이지아웃이 일어나야 한다. 진짜 신호는
페이지폴트 **속도**지 사용량이 아니고, 그건 지금 수집하지 않는다 — 수집하게 되면 조건을
그쪽으로 옮긴다.

### (배경) 스왑이 0 이 아니면 항상 메모리 병목이 된다 — 발견

`bottleneck.py:232` 의 `if swap:` 이 스왑 사용량이 0보다 크기만 하면 `MEMORY` 에 0.3점을
준다. 이 PC 실측에서 `swap_used_mb` 중앙값은 448MB 이고 0 인 표본은 9.2% 뿐이다 —
**Windows 는 메모리가 남아돌아도 페이지파일을 쓰므로, 시간의 90.8% 동안 `MEMORY` 점수가
붙어 있다.** 메모리는 32% 밖에 안 쓰는데 그렇다. 같은 줄의 다른 두 조건은 `mem >= 85%`·
`mem >= 60%` 를 요구하는데 스왑 조건만 하한이 없다.

CLAUDE.md 규칙 2(하드웨어를 가정하지 않는다) 위반이다 — "스왑이 돌면 메모리 압박"은
Linux 기준 가정이고 배포 대상인 Windows 에서는 항상 참이다.

**회귀 확인 (리플레이 평가, 재생 23시간·6만 틱, 정답 8건).** `procleak` 이 소수점까지 같다 —
탐지는 그대로 두고 설명만 바로잡았다는 뜻이다. 18번은 병목 분류(설명 계층)라 탐지기
발화와 경로가 다르고, 그것이 수치로 확인됐다.

| 탐지기 | | TP | FP | FN | 정밀도 | 재현율 | F1 | 지연 | 오탐/시간 |
|---|---|---|---|---|---|---|---|---|---|
| `procleak` | 전 | 8 | 2 | 0 | 80.0% | 100.0% | 0.889 | 320s | 0.13 |
| | 후 | 8 | 2 | 0 | 80.0% | 100.0% | 0.889 | 320s | 0.13 |
| `rules` | 전 | 0 | 22 | 8 | 0.0% | 0.0% | 0.000 | — | 1.41 |
| | 후 | 0 | 21 | 8 | 0.0% | 0.0% | 0.000 | — | 1.39 |

`rules` 의 FP 절대값 차이는 재생 구간이 30분 짧아진 것(보존 정리)에서 온다.

### 경합 분류가 탐지기의 주장을 덮던 것을 고친다 (2026-08-02, PLAN §8 15번)

탐지기 주장(`claim`)이 병목 분류를 이기는 경우를 `NONE` 에서 `CONTENTION` 까지 넓혔다
(`_CLAIM_WINS_OVER`). 07-30 배치는 신호가 `handles`/`python` 인데 사건 4건 중 3건이
`경합 (자원 포화 없이 지연) — 컨텍스트 스위치 평소의 19.7σ` 로 발표됐다. 그 경합은 주입과
인과가 없다 — 주입 구간의 컨텍스트 스위치 중앙값이 **오히려 더 낮다**(28,431/s vs 구간 밖
103,580/s). 분류기는 구간 안 최악 시점 한 행만 보는데 그 행이 잡음이었다.

**`GPU`·`THERMAL` 은 넣지 않았다.** 넷 다 `attributable=False` 지만 `NONE`·`CONTENTION`
은 "원인을 짚지 못했다"는 선언이고, `GPU`·`THERMAL` 은 특정 하드웨어 상태를 실제로 관측한
것이다. 거기까지 넓히면 `발열 스로틀링 — GPU 92°C` 가 프로세스 하나의 핸들 이야기로 바뀐다.

첫 시나리오 테스트는 **아무것도 보지 않은 채 통과했다.** 경합을 만들려 했는데 분류가
`NONE` 을 냈고 `NONE` 은 원래 주장을 채택하므로, 규칙을 지워도 통과했다. `_peak_and_baselines`
가 `max(rows, key=cpu_total)` 로 한 행만 뽑기 때문에 스파이크의 CPU 도 함께 올려야 그 행이
뽑힌다. 이제 테스트가 `bottleneck == "CONTENTION"` 을 먼저 고정한다.

MEMORY 계열(`attributable=True` 인데 자원이 틀린 경우)은 손대지 않았다 — 새 문턱이 필요해
`config` 로 나가야 하고(규칙 3), 방향 자체가 8번에서 일부러 피한 것이다.

### 채점이 판정 불능을 실패로 세던 것을 고친다 (2026-08-02)

`score_fault_product` 가 전역 지표(`metrics_raw`)가 지워진 구간을 "재분석 불능"으로
분류한다. 그 구간은 병목이 `관측 없음` 이 되고 자원이 기본값으로 돌아가 결과가 무조건
미지목인데, 이를 0점으로 세면 탐지기가 아니라 보존 정책을 채점하는 셈이다.

**표본에 남아 이후 실행을 영구히 끌어내리는 것이 더 큰 문제였다** — 07-30 표본 7건이
전부 이 상태라, 새 배치가 5/5 를 맞혀도 5/12 = 42% 로 DoD 85% 에 닿을 수 없었다.

**사건 유무를 먼저 본 뒤에 검사한다.** 사건이 없다는 사실은 `incidents` 가 보존 정리
대상이 아니라 언제 확인해도 확정이고, "사용자가 아무것도 받지 못했다"는 진짜 실패라
데이터가 지워졌다는 이유로 덮으면 안 된다.

### 주입 구간에서 전역 지표가 지워져 채점이 불능이 되던 것을 고친다 (2026-08-02)

`FAULT_PROTECTED` 에 `metrics_raw`·`gpu_metrics` 를 더했다. 채점은 네 갈래를 함께 읽는다 —
기여도(`process_metrics`), 정답 PID 트리(`process_events`), **병목 분류(전역 지표 둘)**.
뒤의 둘이 빠져 있어 `raw_hours`(24시간)가 지나면 주입 구간의 절반이 사라졌다.

07-30 배치를 08-02 에 채점하다 드러났다. 제품 경로 귀인이 **0/7 = 0.0%** 였고 7건 전부
`자원 cpu / 제목 "관측 없음" / 기여자 없음` 이었다. 탐지가 퇴행한 것처럼 보였지만, 저장된
사건에는 `경합 — 컨텍스트 스위치 8.1σ` 라는 제목이 남아 있다 — **그때는 분류가 됐다.**
`process_metrics` 는 보호되어 온전한데(#44 343행 … #47 118행) `metrics_raw` 는 그 구간이
0행이었다. 같은 실행의 함수 경로는 10/10 = 100% 로, 자원을 라벨에서 받으면 여전히 맞힌다.

**이 실패는 조용하다.** 정리는 정상 종료하고 `fault_windows_held` 로그도 정상이며 라벨도
남아 있다. 며칠 뒤 채점할 때에야 수치로 나타나고, 그때는 원인 데이터가 이미 없다.
`tests/test_retention_fault_tables.py` 가 세 가지를 고정한다 — 구간 안 행이 세 테이블 모두
남는다 / 구간 밖 기한 초과 행은 그대로 지워진다(아무것도 안 지우는 정리로는 통과하지
못한다) / 보호 목록이 채점이 읽는 이름을 덮는다. 둘을 빼고 돌려 실패를 확인했다.

여파: `PLAN.md` §8 의 15번 DoD("저장된 4건을 재분석해 판정, 재주입 불필요")가 성립하지
않게 됐다. 순서를 **17 → 16(재실행) → 15(판정)** 으로 바꿨다.

### 주입 배치 재실행 — 탐지 사슬이 닫혔다 (2026-07-30 15:30~16:25)

12·13 번 수정이 **함께 작동하는지**를 새 데이터로 확인했다. `handle_leak` 을 rate 6/10/15/20
으로 각 720초씩 돌렸다(5회차는 전원 강제 종료로 미실행).

| | 오전 (수정 전) | 오후 (수정 후) |
|---|---|---|
| 표본 (720초당) | 29~37행 | **118~343행** |
| 첫 표본 지연 | +40~50초 | **+0~1초** |
| `first` | 809~1,864 (부풀었다) | 704~772 (실제 시작값) |
| 배수 (문턱 3.0) | 2.25~2.89 | **5.4~5.5** |
| `procleak` 발화 | 2/5 (리플레이 0/5) | **4/4** |
| 사건 생성 | — | **4/4** (각각 그 신호 하나로만 구성) |

`표본 → first → 배수 → 발화 → 사건` 까지 닫혔다. 다음 고리에서 끊긴다 — 아래.

### 사건의 자원·제목이 탐지기의 주장을 버린다 (미해결, 설계 합의 필요)

**증상**: 신호는 `handles`/`python` 인데 사건은 `경합 — 컨텍스트 스위치 19.7σ`(3건)·
`메모리 압박 — python 30%`(1건) 로 발표됐다.

**원인**: 8번 수정이 탐지기의 주장을 `bottleneck.kind == "NONE"` 일 때만 쓴다
(`fusion.py:236`·`:277`). 오전 4건은 전역 지표가 조용해 `NONE` 이었지만, 이번에는 분류기가
뭔가를 찾아내 claim 이 통째로 버려졌다 — 자원이 `cpu`/`rss` 로 돌아가 귀인이 엉뚱한 축에서 돌았다.

그 "뭔가"는 주입과 인과가 없다. 주입 구간의 컨텍스트 스위치 중앙값이 오히려 **더 낮다**
(28,431/s vs 구간 밖 103,580/s). 분류기가 구간 안 최악 시점 한 점을 잡는데 그 점이 잡음이었다.

설계 논점과 DoD 는 `PLAN.md` §8 의 15번에 있다.

### 닫히지 못한 주입 라벨이 보존 구간을 무한히 넓히던 것을 고친다 (2026-07-30)

주입기는 종료 시 `finally` 에서 `ts_end` 를 쓰지만 **전원이 끊기면 그 코드가 돌지 않는다.**
16:25:54 에 실제로 그렇게 되어 `#47` 이 열린 채 남았고, `_fault_windows()` 가 `ts_end IS NULL`
을 `now` 로 대체하므로 **보호 구간이 시간과 함께 자랐다**(두 시간 뒤 2시간 24분). 그 구간의
보존 정리가 통째로 멈춘다.

행만 보고는 "지금 도는 중"과 "죽은 뒤 남은 것"을 구분할 수 없다. 그래서 **주입기가 라벨에
남긴 계획 길이(`params.limits.duration_s`)를 상한으로 쓴다** — 계획을 넘겨 도는 주입은 없다
(길이가 곧 종료 조건이다). 계획이 없는 `manual` 라벨은 끝이 열린 것이 의도이므로 그대로 둔다.

조용히 깨지는 규칙이라(예외가 아니라 구간 길이만 틀어진다) 상한을 지워 보고 확인했다 —
`tests/test_retention_fault_window.py` 의 고아 라벨 테스트만 실패하고 나머지 3건은 통과한다
(그 3건은 원래 동작을 고정한 것이라 맞다).

**PID 재사용이 두 번째 피해였다.** 열린 라벨은 `ts <= ts_end` 상한을 세울 수 없어, 그 PID 를
나중에 받은 다른 프로세스의 행이 섞여 읽힌다 — `inject_progress.py` 가 `#47` 을 `146분 /
12분 (1220%)` 로 찍었다. 라벨을 닫자 `9분 50초 · 118행` 으로 정상화됐다.

### 주입 배치 진행 상황 도구 (`tools/inject_progress.py`)

75분을 태우기 전에 실패를 잡기 위한 것이다. 오전에 5회 중 3회가 미탐이었는데 그 사실을
75분이 지나서야 알았다 — 실패는 1회차 2분 시점에 이미 관측 가능했다.

**판정에 `procleak.judge()` 자체를 부른다.** 근사값으로 재려던 시도가 세 번 틀렸고, 셋 다
"결과를 이미 아는 데이터 두 개로 보정"하는 과정에서 잡혔다.

| 재려던 것 | 왜 틀렸나 |
|---|---|
| 표본 밀도(행/초) | 사슬 밖의 대리 지표. 주입이 상한에 닿으면 증가가 멈춰 핸들 증가량 상위에서 빠지므로 밀도가 떨어지는 것이 **정상**이다 — 정상 실행에 경고를 띄웠다 |
| `first`·배수를 SQL 로 계산 | 이것도 대리 지표. `procleak` 은 자기 추적 창의 표본으로 판정한다(#39: SQL 15.9 통과 vs 실제 5.2). 방향까지 갈려 **발화한 주입에 ❌** 를 띄웠다 |
| 상한 없는 PID 조회 | PID 재사용. #42 가 "표본 부족 4/20" 으로 나왔다(실제 30행) |

보정 후 오전 데이터(미탐 확정)와 오후 데이터(정상 확정)의 판정이 리플레이와 소수점까지
일치한다. 절차(실패 신호 사전 정의 · 예광탄 1회차 · 보정 · 침묵을 성공으로 읽지 않기)는
`PLAN.md` §8 의 "주입 배치를 다시 돌릴 때 알아 둘 것" 에 박았다.

### 지문이 결함을 학습하지 않게 한다 (2026-07-30)

**주입 구간을 학습하면 지문이 결함을 "평소"로 배운다.** 07-29 의 주입(상한 5,000)이
`python` 의 `handles_max` p99 를 4,755 로 올려놓아, 07-30 의 주입(최대 4,194)이 그 범위
안으로 들어가 억제됐다. **같은 데이터를 리플레이해도 발화하지 않아 회귀 판정이 성립하지
않았다** — 채점이 채점 대상을 망가뜨리는 구조였다.

`build(exclude=...)` 로 결함 주입 구간을 학습에서 뺀다. 라벨이 이미 있고(`fault_injections`),
분포와 자격 판정(며칠·몇 버킷)에 **똑같이** 적용한다 — 한쪽만 빼면 서로 어긋난다.
구간은 정확히 주입 시간만 뺀다. `retention` 의 `fault_guard_s` 는 채점이 보는 비교 창을
지키기 위한 것이고, 여기서 뺄 것은 결함이 진행된 구간뿐이다.

실측 효과:

| | 전 | 후 |
|---|---|---|
| `python` `handles_max` | p95 4,266 · p99 4,755 | **자격 미달로 사라짐** (표본 120 → 부족) |
| `medal` `handles_max` | p95 4,203 · p99 12,466 | p95 4,206 · p99 13,323 (사실상 유지) |
| 리플레이 억제 | 3~42건 | **0건** |

`python` 관측의 대부분이 주입이었으므로 그것을 빼면 자격에 못 미친다. "지문이 없으면
억제하지 않는다"는 규칙에 따라 누수가 다시 보인다. 정당한 지문(medal 등)은 그대로다.

**`save()` 가 사라진 지문을 지우지 않던 것도 고쳤다.** `replace=True` 는 덮어쓰기만
하므로, 자격을 잃은 `python` 의 오염된 p99 가 DB 에 영구히 남아 억제를 계속했다. 빌드가
"지금 자격이 되는 것 전부"를 내는 순수 함수이므로 저장도 그 전체로 맞춘다. **빈 목록으로는
지우지 않는다** — 롤업이 안 돌았거나 웜 조회가 실패해 결과가 빌 수 있고, 그때 테이블을
비우면 억제가 통째로 사라져 오탐이 쏟아진다.

**(나) "억제 축 분리"는 하지 않았다 — 수치가 불가능하다고 말한다.**

| | p50 | p95 | p99 | 도달 | p95 대비 |
|---|---|---|---|---|---|
| medal | 4,030 | 4,203 | 12,466 | 1,395 | 0.33 |
| python | 530 | 4,266 | 4,755 | 4,194 | **0.98** |

억제 기준을 p99 → p95 로 좁혀도 python 은 여전히 억제된다(4,194 ≤ 4,266) — p95 자체가
오염됐기 때문이다. p50 으로 내리면 python 은 풀리지만 medal(4,030)도 통과해 기준이
무의미해진다. **두 사례를 가를 신호가 수준 통계에 없다.** 차이는 medal 의 통계가 정당하고
python 의 통계가 오염됐다는 것뿐이므로 (가)가 본질이다. medal 사례를 회귀 테스트로
남겨 두었다 — 억제 축을 손볼 때 이 선을 넘으면 그 변경은 틀린 것이다.

**아직 검증되지 않은 것**: 억제는 리플레이로 확인했지만 `배수 부족`(2.25~2.89 < 3.0)은
옛 구간에 그대로다. 표본이 이미 희소하게 저장돼 있어 되돌릴 수 없고, 핸들 증가량 축은
**앞으로 수집되는 데이터**에만 적용된다. **두 수정이 함께 작동하는지는 새 주입 배치로만
확인된다.**

### 활성 집합에 핸들 증가량 축을 넣는다 — 누수 미탐 3/5 의 원인 (2026-07-30)

**`procleak` 의 존재 이유가 핸들 누수인데 수집이 그 지표를 우선하지 않았다.** 활성 집합은
CPU 상위 15 + 메모리 상위 10 + 포어그라운드였고 나머지는 30초에 한 번만 저장됐다. 핸들
누수 프로세스는 CPU·메모리를 거의 쓰지 않아 어느 상위에도 못 든다 — 그래서 720초 주입
구간에 표본이 **29~37행**뿐이었고(초당 수집인데), 첫 표본이 40~50초 늦게 잡혀 그 사이 이미
1,400~1,800 핸들이 늘었다.

`procleak` 이 보는 `first` 가 부풀면 배수가 무너진다. 리플레이로 확인한 판정이다.

| 주입 | first | last | 배수 | 판정 |
|---|---|---|---|---|
| #39 | 809 | 4,194 | 5.2 | 누수 |
| #40 | 1,820 | 4,194 | **2.30** | **배수 부족 (< 3.0)** |
| #41 | 1,864 | 4,194 | **2.25** | **배수 부족** |
| #42 | 1,452 | 4,194 | **2.89** | **배수 부족** |
| #43 | 1,122 | 4,194 | 3.7 | 누수 |

실제 시작값은 200~500 이다. **문턱을 낮춰 덮을 문제가 아니라 표본이 없는 문제였다.**

**보유량이 아니라 증가량으로 고른다.** 상시 핸들이 많은 프로그램(브라우저 등)을 고르면
자리를 다 차지해 400개에서 시작해 새는 프로세스가 밀려난다. 늘어나는 것이 누수다.
추가 조회 비용은 없다 — 스냅샷이 이미 전체 프로세스를 담고 있어 직전 틱의 핸들 수만
기억하면 된다.

**비용 실측**: 전체 348 프로세스에서 활성 집합이 **20.6 → 24.8개/틱**(+20%), 하루 행수
추정 대비 **약 +12%** 다. 대안이었던 `full_store_interval_s` 단축(30초 → 5~10초)은 3~6배라
비교가 안 된다.

절전 복귀 시 증가량 기준을 버린다. 공백을 사이에 둔 두 값의 차는 "지금 늘어나는 중"이
아니고, 그대로 두면 복귀 직후 수백 개가 증가량 상위로 잡혀 활성 집합이 뒤집힌다. 이
초기화는 소스 상태를 확인하는 조기 반환 **앞**에 둔다 — 테스트가 그걸 잡았다.

테스트 5개. 축을 끄면 누수가 활성 집합에서 빠지는 것까지 고정했다(그래야 위 테스트가
무엇을 검증하는지 말할 수 있다).

### 채점이 제품과 같은 경로를 재게 했다 (2026-07-30)

기존 채점은 `SCENARIO_RESOURCE` 에서 자원을 **입력으로 받았다** — "핸들 누수니까
`handles` 로 보라"고 알려주고 시작했다. 제품에는 그런 입력이 없어 병목 분류와 탐지기
주장으로 추론해야 한다. 그래서 함수 경로 100% 와 제품 0% 가 공존할 수 있었다.

`score_fault_product` 를 추가했다. **라벨은 정답으로만 쓰고 입력으로 쓰지 않는다.**
저장된 제목이 아니라 현재 코드로 다시 분석한 결과를 보므로, 고친 것이 수치에 나타난다.
스코어보드는 두 수치를 나란히 낸다 — 하나를 다른 하나로 대체하면 "자원을 알려주면
찾는가"라는 정보가 사라진다.

```
[OK]   함수 경로 DoD 충족 — 1순위 지목률 100.0% ≥ 85% (표본 7건)
[보류] 제품 경로 표본 4건 — 7건 미만이라 판정하지 않는다 (지목률은 100.0%)
[주의] 주입 3건은 사건이 만들어지지 않아 사용자가 아무것도 받지 못한다
```

**테스트가 처음에 아무것도 검증하지 않았다.** 라벨과 추론이 둘 다 `handles` 가 되는
시나리오만 썼더니, 라벨을 그대로 쓰도록 되돌려도 통과했다. 라벨은 `handle_leak` 인데
구간에 CPU 가 치솟아 추론 결과가 `cpu` 가 되는 경우를 만들어야 구분된다.

### 주입 7건 중 3건은 사건이 만들어지지 않는다 (2026-07-30, 조사 중)

위 채점을 붙이자마자 드러났다. #41 구간의 신호는 **0건** — 귀인이 틀린 게 아니라
`procleak` 이 발화하지 않았다. 쿨다운은 아니다(트랙 키가 PID 별이다).

유력한 가설: **`python` 의 `handles_max` 지문 p99 가 4,755 인데 오늘 주입 최대값이
4,194 다.** 07-29 의 cap 5000 주입이 만든 지문이 오늘 cap 2000 주입을 "평소"로 판정한다.
맞다면 구조적 문제다 — 결함 주입을 반복하면 지문이 결함을 학습해 다음 주입을 못 잡고,
실사용에서도 누수가 반복되는 프로그램은 누수가 "평소"가 된다. 자세한 것은 `PLAN.md` 12번.

### 제품이 원인을 맞게 지목한다 — 0/4 → 4/4 (2026-07-30)

**탐지기가 아는 것을 산출물까지 전달한다.** `procleak` 은 신호에 `metric: "handles"`,
범인 프로세스, 지속 시간, 그리고 완성된 설명 문장까지 실어 보내고 있었는데 `fusion` 이
그것을 읽고도 쓰지 않았다. 그래서 핸들 누수는 병목이 `NONE` 이 되고, `NONE` → `cpu` 매핑
때문에 **구간에 CPU 를 많이 쓴 무관한 프로세스**가 원인으로 발표됐다.

저장된 주입 4건을 새 코드로 다시 분석한 결과(`tools/rescore_incidents.py`):

| 전 | 후 |
|---|---|
| `병목 없음 — claude 17%` | `python (PID 33616) 핸들 530 → 4,194개 (7.9배, 7분간 줄지 않음)` · 1위 python 70% |
| `병목 없음 — system 27%` | `python (PID 35736) 핸들 1,282 → 4,194개` · 1위 python 61% |
| `병목 없음 — compattelrunner 72%` | `python (PID 25196) 핸들 1,167 → 4,194개` · 1위 python 71% |
| `병목 없음 — l-connect-service 34%` | `python (PID 24504) 핸들 458 → 3,838개` · 1위 python 77% |

**DoD 두 단계 모두 충족** — 필수(1위 기여자가 주입 프로세스) 4/4, 목표(제목이 탐지기
문장) 4/4. CLAUDE.md 의 "탐지가 아니라 설명이 산출물이다"를 이 경로에서 처음 만족한다.

바꾼 것:

- `_trigger_rules` 가 `DetectorClaim`(자원·프로세스·문장·지속시간·점수)을 함께 돌려준다.
  탐지기가 여럿이면 점수가 큰 쪽을 택한다.
- **병목이 `NONE` 일 때만** 탐지기 자원으로 귀인한다. 구체적 병목(CPU·IO·MEMORY)은
  유지한다 — 전역 지표가 실제로 증상을 보이는 경우이고 사용자가 느낀 것은 그쪽이다.
- `_RESOURCE_BY_KIND["NONE"]` 의 `attributable` 을 `False` 로. "전역 지표에서 아무것도
  못 찾았다"가 확신을 갖고 CPU 1위를 원인이라 부르면 안 된다. **표만 고쳤을 때는 안
  바뀌었다** — `classify` 의 이른 반환(`scores` 가 빈 경우)이 dataclass 기본값 `True` 를
  그대로 써서, 값이 두 곳에서 정해지고 있었다.
- **탐지기가 센 지속 시간으로 앞쪽 경계를 늘린다.** `_refine_bounds` 는 전역 지표만
  보므로(`_BOUND_METRICS`) 핸들 누수에서 보정이 실패하고, 룰 쿨다운 때문에 신호가 몇
  개뿐이라 **12분짜리 주입이 18초로 기록됐다.** 그 창에서 귀인하면 누수 프로세스의
  증가분도 18초분뿐이라 마침 그때 뜬 프로세스에 1위를 빼앗긴다(첫 수정 후 2/4 가 그랬다).
  `MAX_EXTEND_BEFORE_S` 를 적용하지 않은 이유는 그 상한이 **지표에서 추정한** 경계를
  겨냥한 것이고, 여기 값은 탐지기가 자기 창에서 직접 센 것이라 추정이 아니기 때문이다.
- 계산을 `analyze_incident`(읽기 전용)로 분리했다. 저장된 사건을 새 코드로 다시 분석해
  전후를 비교하려면 계산이 저장과 붙어 있으면 안 되고, 도구에 계산을 복사하는 것은
  규칙을 두 곳에 두는 일이다.

테스트 7개 추가. 자원 전달·제목·`NONE` attributable·구간 확장을 각각 무력화해 실패를
확인했다. **제목과 1위 기여자가 같은 프로세스를 가리키는지도 고정했다** — 두 경로가
따로 오므로 갈라질 수 있고, 첫 수정 직후 실데이터에서 실제로 갈라져 있었다.

### GPU 베이스라인만 재시작마다 백지에서 다시 배웠다 (2026-07-30)

`BaselineSet.warm_from()` 이 `obs.metrics` 만 넣고 `obs.gpus` 를 무시했다. 리플레이
스트림은 `gpus` 를 채워 주고 `RuleEngine.observe` 는 그것을 `gpu_*` 로 펼쳐 쓰는데
**워밍 경로만 빠져 있었다.** 이 모듈은 "재시작해도 처음부터 다시 배우지 않는다"고
말하는데 GPU 는 예외였다.

부하 중에 재시작하면 부하 상태를 "평소"로 학습한다. 07-30 에 게임 중 재시작했고, 같은
룰의 근거가 3분 사이에 갈렸다 — 09:57 기동 인스턴스는 중앙값 69도로 88도를 z=+5.51 로
봤고, 10:07 기동 인스턴스는 중앙값 85도로 83도를 **z=−0.47** 로 봤다. SM 클럭이
1,918 → 1,715MHz(−10.6%)로 깎이는 중에 점수가 0.0588 이었다.

펼치는 규칙을 `Observation.flatten_gpus()` 한 곳으로 옮기고 양쪽이 그것을 쓰게 했다.

### GPU 수집이 드라이버 재시작에서 복구된다 (2026-07-30)

`nvmlInit()` 이 `setup()` 에서 한 번뿐이라 드라이버 업데이트·GPU 리셋 뒤에는 **재기동
전까지 GPU 가 통째로 비었다.** 07-29 03:58~05:02 에 `access violation` 3,845번 연속,
그 구간 `gpu_metrics` 0행이었고 실패가 `log.debug` 라 아무 신호가 없었다.

- 조회가 연속 `gpu_recover_after_failures`(기본 5)회 실패하면 **WARNING 을 한 번 올리고
  NVML 을 다시 초기화한다.** 실패하면 `gpu_recover_backoff_s`(기본 60초) 후 재시도 —
  드라이버가 아직 안 올라온 동안 매 초 시도하면 관측자가 비용이 된다.
- 복구되면 WARNING 으로 알리고(다운 시간·시도 횟수 포함) 상태를 초기화한다.
- 두 값은 **config 로 넣었다**(`collector.gpu_recover_*`). 모듈 상수로 두면 규칙 3
  위반을 새로 만드는 셈이다.

**함께 고친 두 번째 결함** — `_try()` 가 **어떤 예외든 그 항목을 영구히 "미지원"으로
낙인**했다. 드라이버 재시작 중 온도 조회가 한 번 실패하면 복구된 뒤에도 온도가 영영
비고 아무도 이유를 모른다. NVML 이 미지원을 별도 예외(`NVMLError_NotSupported`)로
알려주므로 그것만 낙인하도록 바꿨고, 재초기화 성공 시 낙인을 지운다(드라이버 버전이
바뀌면 지원 여부도 바뀔 수 있다).

**구현 중 스스로 만든 결함을 테스트가 잡았다.** `_init_nvml()` 이 시작할 때 핸들을
비우게 썼더니, 재초기화가 실패하면 핸들이 빈 채로 남아 `collect()` 루프가 아무것도
돌지 않고 **실패 카운터가 멈춰 상태가 얼어붙었다** — 고치려던 조용한 실패를 다른 모양으로
다시 만든 것이다. 성공할 때만 교체하도록 바꿨다. 죽은 핸들이라도 들고 있으면 조회가
계속 실패하며 그 사실이 계속 드러난다.

가짜 NVML 로 드라이버 재시작을 재현하는 테스트 8개를 붙였고, 고친 규칙 세 개(재초기화·
낙인 구분·백오프)를 각각 무력화해 실제로 실패하는지 확인했다. 실제 GPU 스모크
(`python -m argus.collector.gpu`)도 `[OK]`.

**남은 것**: `describe()` 의 건강 상태를 **대시보드가 읽지 않는다**(단독 스모크만 읽는다).
사용자가 "GPU 수집이 지금 실패 중"을 UI 에서 볼 수는 없으므로 규칙 4 를 완전히 만족하지
못했다. PLAN.md 에 후속 항목으로 적었다.

### Phase 8 DoD 충족 — 1순위 지목률 7/7 = 100% (2026-07-30)

`handle_leak` 을 rate 6/10/15/20/8 로 각 720초씩 5회 주입하고(08:38~09:51, 사이 180초는
비교 창이 앞 주입에 오염되지 않게 둔 간격) 기여도를 채점했다. 어제 살아남은 표본 2건과
합쳐 **7건 전부 주입 프로세스를 1순위로 지목**했다. 3위 이내도 7/7 이다.

| 주입 | rate | 지목 1위 | 기여율 |
|---|---|---|---|
| #39 | 6 | ✅ 1위 | 56% |
| #40 | 10 | ✅ 1위 | 47% |
| #41 | 15 | ✅ 1위 | 56% |
| #42 | 20 | ✅ 1위 | 64% |
| #43 | 8 | ✅ 1위 | 64% |
| #23·#24 | (07-29, cap 5000) | ✅ 1위 | 64% · 81% |

**07-30 01:32 의 수정(4618efd, 새로 뜬 프로세스의 기본 보유량을 증가분으로 세지 않음)이
실제로 작동한다.** 그 전에는 `compattelrunner` 13개가 각자 들고 온 400 핸들이 전부
"늘어난 것"으로 계산돼 5,200 으로 1위를 차지했었다.

이번 표본은 **어제보다 엄격한 조건에서 통과했다.** `--handles` 를 주지 않아 상한이
기본값 2,000(어제는 5,000)이었고, 그래서 기여율이 47~56% 로 낮다. 규모가 절반인데도
1순위를 지켰으므로 더 큰 주입으로 재확인할 필요는 없다.

**표본 무결성**: 상주 인스턴스 pid 35084 가 08:17:37 부터 주입 구간 내내 유지됐다
(재시작 없음 = 구간에 구멍 없음). 제외 1건(#10)은 "구간에 프로세스 메트릭이 없음"으로,
07-29 에 확인된 보존 정리 문제의 잔재다 — 스코어보드가 조용히 통과시키지 않고 제외로
드러냈다.

**이 수치의 의미는 좁다 — 같은 날 오후에 확인했다.** 채점은 시나리오 라벨에서 자원을
`handles` 로 **받아서** 귀인한다. 제품은 병목 분류기로 자원을 **추론**하는데 핸들 누수는
전역 지표에 안 드러나 `NONE` 이 되고, `NONE` 은 `cpu` 로 매핑되어 **CPU 기여도 1위**를
원인으로 발표한다. 그래서 같은 주입 구간에서 제품은 4건 모두(claude·system·
compattelrunner·l-connect-service) 엉뚱한 프로세스를 경고로 냈다. **7/7 은 "자원을
알려주면 원인을 찾는다"이고 "제품이 원인을 맞게 지목한다"가 아니다.** 자세한 것은
`PLAN.md` 8번.

**착수점 기록의 결함도 함께 고쳤다.** `PLAN.md` 는 5회를 돌리라고 적었지만 판정 문턱은
`MIN_SCORED = 7` 이다. 어제 표본 2건이 우연히 남아 7건에 닿았고, 보존 정리가 그것까지
지웠으면 75분을 쓰고도 `[보류]` 만 나왔다. "7건 이상이 남도록 돌린다"로 고쳤다.

### 검증되지 않던 상수에 테스트를 붙였다 — 그 과정에서 결함 둘 (2026-07-30)

07-30 점검에서 "무력화해도 안 잡힘"으로 나온 7개 중 6개를 고정했다. 각 테스트는 상수를
실제로 바꿔 실패하는지 확인한 뒤 되돌렸다. **171 passed, 1 xfailed.**

`MAD_TO_SIGMA` 가 안 잡힌 원인은 `sigma = max(mad*계수, floor, median*0.05)` 라
기존 표본에서는 `median*0.05` 항이 이겨 계수가 결과에 닿지 않았기 때문이다. 산포를
중앙값 대비 크게 잡아 첫 항이 이기게 만들고, 정규분포에서 σ 를 복원하는지(성질)와
환산값 14.826(리터럴)을 각각 고정했다. `_OVERRIDE_RATIO`·`_OVERRIDE_MARGIN` 은 AND
조건이라 한쪽만 모자란 경우를 각각 넣었고, `MAX_EXTEND_BEFORE_S` 는 상한을 늘렸을 때와
줄였을 때 양방향으로 조였다.

**결함 1 — `MIN_DAY_HOURS` 는 테스트가 없던 게 아니라 구현이 없었다.** `fingerprint.py`
에 상수와 "2시간 켜 둔 날과 12시간 켜 둔 날은 같은 하루가 아니다"라는 주석이 있는데
**이 값을 읽는 코드가 없다.** `build()` 는 `days = len(by_day)` 로 날짜 개수만 세므로
**5분만 켜 둔 날도 온전한 하루로 계산된다.** 0 으로 바꿔도 통과한 이유가 이것이다 —
무력화할 대상 자체가 없었다. 조건을 명시한 테스트를 `xfail(strict)` 로 남겼다.
같은 이름의 상수가 `tools/readiness.py` 에 따로 또 정의돼 실제로 쓰이고 있어, 한
개념에 정의가 둘이고 그중 하나가 죽어 있다.

**결함 2 — 바이트코드 캐시가 소스와 어긋난 채 며칠 실행됐다.** 소스는
`_SCORE_SATURATION_Z = 8.0` 인데 **실행되는 값이 1.0** 이었다. Python 은 `.pyc` 유효성을
(소스 mtime 초 단위, 소스 크기)로만 판단한다. 어젯밤 02:25 의 mutation 이 상수를 바꿔
컴파일한 뒤 **같은 초 안에** 되돌렸고 `8.0` 과 `1.0` 은 글자 수가 같아 **크기까지
동일**해서, 캐시가 무효화되지 않았다. 그때부터 z 가 1 만 넘으면 모든 룰 발화가 점수
1.0 이 되어 **등급 구분이 사라진 상태로 재기동마다 돌았다** — 알람 품질 ② 의 유력한
원인이다. 더 나쁜 것은 **"36개 통과"라는 그날의 mutation 측정 자체가 오염됐다**는
점이다. 못 잡은 게 아니라 되돌림이 반영되지 않았던 것이다.

`tools/pyc_audit.py` 로 모듈 수준 상수를 캐시와 대조하고(단독 실행 `[OK]/[FAIL]`),
같은 검사를 테스트로 고정했다. 감사가 실제로 잡는지는 크기를 유지한 가짜 어긋남을
심어 확인했다. CLAUDE.md 검증 절에 "상수를 되돌린 뒤 `__pycache__` 를 지운다"를 넣었다.

### 문서 — 구조 절을 채웠다 (2026-07-30)

CLAUDE.md 에 "아직 코드 착수 전"이 남아 있었다. 패키지별 역할표를 실제 구조로 채우고,
"알아 둘 것" 열에 세션마다 다시 밟는 함정을 적었다(장기 데이터는 `history` 경유,
실시간과 리플레이는 같은 경로).

### 전체 점검 — 검증되지 않는 규칙 7개를 찾았다 (2026-07-30)

규칙을 하나씩 무력화하고 테스트가 잡는지 측정했다(mutation). **"통과는 증거가 아니다"를
실제로 재 본 것이다.** 7개가 안 잡혔고, 그중 `MAD_TO_SIGMA = 1.4826` 은 **151개 전부**
통과했다 — 모든 z 점수의 기반이라 이 값이 틀어지면 탐지 민감도가 통째로 바뀌는데
아무 신호가 없다. 지문 자격 조건 셋(`MIN_DAYS`·`MIN_BUCKETS`·`MIN_DAY_HOURS`)도 전부
0 으로 만들어도 통과해서, Phase 6-B 의 핵심 조건은 검증된 적이 없다.

함께 드러난 것:
- **GPU 수집이 조용히 1시간 죽었다.** 07-29 03:58~05:02 에 `access violation` 3,845번
  연속, 그 구간 `gpu_metrics` 0행. `nvmlInit()` 이 `setup()` 에서 한 번뿐이라 드라이버
  재시작 시 복구 경로가 없고, 실패가 `log.debug` 라 경고도 대시보드 노출도 없었다.
- 튜닝 상수가 config 밖(규칙 3). `DISK_RESP_FLOOR_MS = 5.0` 은 `rules.yaml` 이 같은
  개념을 `3` 으로 써서 한 개념에 두 값이다.
- GPU 온도 룰만 상대 조건 없이 절대값(80/90/95)이다. 노트북 GPU 는 87도가 정상이라
  그런 PC 에서 상시 경고가 된다 — 알람 품질 ③ 의 구조적 원인이다.

문제 없음을 확인한 것: `eval()` 금지, PDH 인덱스 조회, `_MEIPASS`, 외부 전송 0건,
예외 삼킴 13곳 전부 정당, 자기 예산 준수(CPU 평균 0.3% / RSS 최대 201.9MB / drop 0).

### 채점의 근거가 스스로 사라지고 있었다

보존 정리가 `process_metrics` 를 24시간으로 자르면서 **결함 주입 24건 중 22건이 채점
불가**가 됐다. 롤업은 이 원본을 대신하지 못한다 — `process_5m` 은 이름 단위로 접혀
`pid` 가 없고, 귀인 채점은 정답을 PID 집합으로 매칭하기 때문이다. 남은 표본 2건으로는
DoD 85% 를 판정할 수 없었다(나올 수 있는 값이 0·50·100% 뿐이다).

주입 구간을 앞뒤 900초 여유와 함께 삭제에서 제외한다. 진행 중인 주입(`ts_end` 가 NULL)도
구간으로 잡는다 — 12분짜리 주입 도중에 정리가 두 번 돌기 때문이다.

채점기에도 최소 표본 조건을 넣었다. 2건에서 100% 가 나와도 우연과 구별되지 않는데 그때도
"DoD 충족"을 찍고 있었다. **근거가 사라진 것을 합격으로 보고하는 스코어보드는 없느니만
못하다.**

### 귀인이 새로 뜬 프로세스의 기본 보유량을 증가분으로 셌다

`compattelrunner` 13개가 주입 도중 뜨면서 각자 원래 갖고 있던 400 핸들이 전부 "늘어난
것"으로 계산돼 5,200 이 됐고, 혼자 +8,313 을 낸 실제 원인을 밀어내고 1위를 차지했다.
이 13개가 구간 동안 실제로 늘린 핸들은 합쳐서 300 남짓이었다.

자원을 **유량과 저량**으로 나눴다. `cpu`·`io` 는 유량이라 현행이 옳다 — 새로 뜬 프로세스가
CPU 30% 를 먹으면 그 30% 전부가 진짜 새 부하다. `rss`·`handles` 는 보유량이라, 전/후 창
비교 대신 **자기 시계열 상승분**으로 잰다. 새로 뜬 프로세스는 자기 첫 관측값이 기준이 되어
기본 보유량이 빠지면서도, 뜨면서 실제로 자원을 먹어치우는 프로세스는 여전히 잡힌다.

곁들여 둘을 고쳤다. PID 재사용 시 시계열을 끊고(이미 반납한 양을 증가분으로 세지 않게),
`is_new` 를 "비교 창에 관측됐는가"로 바꿨다 — 옛 정의는 가만히 있던 프로세스를 "이상
구간에 새로 시작됨"으로 표시했다.

`handle_leak` 1순위 지목률 50% → 100%(2/2). **표본이 7건 미만이라 판정은 보류 상태다.**

### 착수 판정이 끝난 작업을 계속 내밀고 있었다

`readiness.py` 가 Phase 6 을 완료 후에도 **"착수 가능"으로 표시**했다. 착수 조건만 보고
완료 여부는 모르는 도구라서인데, 세션을 여는 루틴이 이걸 돌리므로 그대로 두면 다음 세션이
끝난 일을 다시 시작한다. `done` 을 두어 `[완료]` 로 표시하고 "착수 가능" 집계에서 뺐다.
조건 현황 자체는 계속 보여 준다 — 지문 자격을 갖춘 프로세스가 몇 종인지는 여전히 쓸모가
있기 때문이다.

같은 실행에서 알람 품질 ②의 warning 표본이 **11건으로 조건을 채운 것**도 확인했다.
남은 것은 사용 패턴 다양성 하나다(관측 3일이 전부 고부하).

### 프로그램마다 "평소"를 배워 오탐을 가린다 (Phase 6-B)

6-A 는 자기 과거만 보므로 **"평소에도 핸들을 4천 개씩 쓰는 프로그램"과 "평소 200개인데
4천 개가 된 프로그램"을 구분하지 못한다.** 실측에서 medal(게임 녹화)이 핸들 383 → 1,539 로
늘어 발화했는데, medal 의 평소 `handles_max` p99 는 **13,323** 이었다. 완전히 정상 범위였다.

- **`process_fingerprint`** — 프로그램별 `p50/p95/p99/최대`. `procleak` 이 보는 지표에
  대응하는 버킷 통계량만 담는다(`handles` → `handles_max`, `rss_mb` → `rss_p95`).
- **억제는 한쪽으로만 작동한다.** 지문이 있고 도달값이 평소 p99 이내일 때만 발화를 막는다.
  **지문이 없으면 아무것도 하지 않는다** — 6-A 에서 추적 상한 때문에 신규 프로세스를 통째로
  놓친 적이 있어, 모르는 것을 막는 방향으로는 틀지 않았다. 누수는 대개 새로 뜬 프로세스에서
  생긴다.
- 지문 빌드는 6시간 주기다. 3일 이상 관측된 것만 자격이 있어 자주 다시 만들 이유가 없고,
  웜(Parquet)까지 훑는 작업이라 싸지도 않다.

**같은 세 구간을 억제 전/후로 재생한 결과:**

```
medal 핸들   383 → 1,539개  (4.0배)   →  억제됨   (평소 p99 13,323)
medal 메모리  94 → 2,164MB (22.9배)   →  유지됨   (평소 p99 915MB)
주입 python  879 → 5,977개  (6.8배)   →  유지됨   (평소 p99 2,768)
```

**FP 2건 중 1건이 사라지고 TP 는 그대로다**(오탐 0.24 → 0.12/시간). 남은 1건은 medal 의
메모리인데, 2,164MB 는 **medal 자신에게도 이례적**이라 애초에 오탐이 아니었을 가능성이 크다.
지문이 두 건을 갈라 준 셈이다 — 이것이 6-B 가 6-A 위에 얹혀 실제로 더한 값이다.

#### 분위수가 조용히 틀려 있었다

`quantile` 을 `round(p × (n-1))` 로 짜서 **1~100 의 p50 이 51** 이었다. 문서에는 "최근접
순위"라고 적어 놓고 다른 계산을 하고 있었다. 큰 차이는 아니지만 **예외도 안 나고 숫자만
그럴듯하게 달라지는** 종류라, 알려진 분포를 넣어 기대값과 대조하는 테스트가 없었으면
못 잡았다. `ceil(p × n)` 으로 고치니 medal 의 핸들 p99 가 12,466 → 13,323 으로 바뀌었다.

`statistics.quantiles` 를 쓰지 않는 이유도 같다. 표본이 적으면 보간으로 **관측된 적 없는
값**을 만들어 내는데, 지문의 기준은 "이 프로그램이 실제로 도달한 수준"이다.

#### 변이 테스트 자체가 틀려 있었다

억제 규칙 여섯 개를 하나씩 깨뜨려 봤는데 "지표 대조 제거"만 통과했다. 확인해 보니
**변이 스크립트가 잘못돼 있었다** — `STAT_FOR` 의 첫 값을 쓰게 했는데 그게 마침 대조군과
같은 `handles_max` 라 아무것도 바뀌지 않았다. 변이가 무효면 "검증됐다"는 결론도 무효다.
고쳐서 다시 걸어 6/6 이 됐다.

### 탐지기를 여러 개 함께 돌린다

`DetectionComponent` 가 `detection.detector` **하나**만 읽어 탐지기를 만들고 있었다. 그래서
`procleak` 은 스코어보드에서만 이기고 **상주 앱에서는 한 건도 돌지 않는 상태**였다. 설정을
`procleak` 으로 바꾸면 이번엔 `rules` 가 죽는데, 둘은 보는 영역이 달라(전역 메트릭 vs
프로세스별) 서로 대체재가 아니다.

- `detection.detector` 에 쉼표로 여러 개를 적을 수 있다. 기본값은 `rules,procleak`.
  하나만 적던 기존 설정도 그대로 동작한다.
- **관측은 `ObservationTail` 하나로 한 번만 읽어 모든 탐지기가 나눠 쓴다.** 탐지기마다
  자기 tail 을 두면 같은 행을 탐지기 수만큼 다시 읽게 된다(설계 규칙 1).
- 탐지기 하나가 못 뜨거나 예외를 내도 나머지는 계속 돈다. 룰 파일 오타 하나로 프로세스
  누수 탐지까지 같이 죽으면 안 된다.

스모크가 **설정에 적힌 탐지기가 전부 떴는지**를 확인한다. 하나가 조용히 빠지면 그 영역은
영영 안 보는데, 로그만 봐서는 알 수 없다.

### 시스템 룰이 닿지 못하는 프로세스 누수를 잡는다 (Phase 6-A)

`handle_leak` 은 계획서에서 계속 미탐으로 남아 있었다. 룰 엔진은 `obs.metrics`(시스템
전역)만 보는데 **핸들 누수는 전역 메트릭에 거의 드러나지 않기 때문이다** — 한 프로세스가
핸들 8천 개를 쥐고 있어도 CPU·메모리·디스크는 평온하다. 이번 스코어보드가 그것을 수치로
확인했다: 같은 구간에서 `rules` 의 F1 이 **0.000**(2건 중 0건 탐지)이다.

**Phase 6 을 6-A(누수 탐지)와 6-B(지문)로 쪼갰다.** 원안은 "프로세스명 × 레짐 단위
p50/p95/p99 지문"을 먼저 세우는 것이었는데, 누수는 **남과 비교하는 게 아니라 자기 과거와
비교하는 문제**다. 크롬의 핸들이 평소 몇 개인지 몰라도, *지금 이 인스턴스가* 10분간 한 번도
안 줄고 6배로 불었으면 누수다. 3일치 지문도 웜 병합 조회도 필요 없다. 지문부터 지으면
**이길 상대가 없는 채로 무거운 쪽을 먼저 짓는 것**이 되어 PLAN 정렬 원칙 4 와 "수치 없이
모델을 추가하지 않는다"에 걸린다. 6-B 를 미뤄도 잃는 게 없다 — `process_5m` 이 이미 데이터를
보존하고 있어 **지연 비용이 0 인 드문 경우**다.

- **`argus/detection/procleak.py`** — 네 조건을 모두 만족해야 발화한다. 창 시작 대비 배수,
  최소 증가량, 비감소 표본 비율, 지속 시간. 여기에 급락 리셋(PID 재사용 차단)과 프로세스별
  쿨다운이 붙는다. 판정은 전부 상대값이고, 예외인 핸들 최소 증가량만 절대값이다 — 핸들 수는
  RAM 이나 코어 수에 비례하지 않으므로 그게 맞다. RSS 는 비례하므로 **이 PC 의 RAM 비율**로
  환산한다(실측 63.65GB → 1304MB, 4GB PC 라면 164MB).
- 임계값은 전부 `defaults.yaml` 의 `process_leak` 섹션에 있다.

**스코어보드** (24시간 재생 30,460틱, 정답 2건):

| 탐지기 | TP | FP | FN | 정밀도 | 재현율 | F1 | 지연 | 오탐/시간 |
|---|---|---|---|---|---|---|---|---|
| **procleak** | 1 | 2 | 1 | 33.3% | 50.0% | **0.400** | 543s | **0.24** |
| always | 2 | 492 | 0 | 0.4% | 100.0% | 0.008 | 0s | 59.77 |
| rules | 0 | 6 | 2 | 0.0% | 0.0% | 0.000 | — | 0.73 |

놓친 1건은 **2분짜리 주입**이다. 지속 조건 300초를 의도적으로 못 채우는 케이스이고,
"2분 급증은 누수가 아니다"가 바로 오탐을 막는 규칙이라 이건 설계대로 걸러진 것이다.
오탐률은 `rules` 보다 낮다(0.24 vs 0.73/시간).

**FP 2건은 오탐이라 단정할 수 없다.** 둘 다 `medal`(게임 녹화)이었다.

```
04:53  medal (PID 28676) 핸들 383 → 1,395개   (3.6배, 15분간 줄지 않음)
18:05  medal (PID 26044) 메모리 94 → 1,919MB (20.4배, 15분간 줄지 않음)
```

결함주입 라벨 밖이라 채점기가 자동으로 FP 로 셌지만, "녹화 프로그램이 15분간 메모리를
20배로 늘리며 한 번도 안 줄였다"는 **사용자가 알고 싶어할 만한 사실**이다. 채점상의 FP 와
실제 오탐을 여기서 구분할 방법이 없다 — 정답 라벨이 주입 구간에만 있기 때문이다.
가르려면 사용자 피드백(Phase 11)이나 "이 프로그램의 평소 성장 폭"(6-B 지문)이 필요하고,
**이것이 6-B 가 6-A 위에 얹혀 실제로 개선할 여지**다. 6-B 착수 시 이 두 건을 판정 기준으로
쓴다.

#### 결함주입 한 번이 설계 결함 넷을 연달아 드러냈다

전부 **조용히 실패하는** 종류였고, 넷 다 첫 판 테스트를 통과했다. 실제 주입 없이는
드러나지 않았을 것들이다.

1. **상한이 찬 뒤 등장한 프로세스를 영영 못 본다.** 추적 상한 400 이 트랙 수(프로세스
   200종 × 지표 2개)와 정확히 같아, 뒤에 뜬 주입 프로세스가 자리를 못 얻었다. 12분간
   핸들이 210 → 8523 으로 늘도록 **아무 일도 일어나지 않았다.** 누수는 대개 새로 뜬
   프로세스에서 생기므로, 봐야 할 것을 정확히 못 보는 상태였다.
2. **LRU 축출이 자라는 트랙까지 버린다.** 대부분의 프로세스가 같은 틱에 함께 갱신되는
   탓에 "가장 오래 갱신 안 된 것"이 사실상 삽입 순이 된다. 축출 기준을 **가장 평평한
   트랙**으로 바꿨다 — 평평한 트랙은 어차피 발화하지 않으므로 버려도 잃는 게 없다.
3. **순회 도중 축출하면 도미노가 돈다.** 쫓겨난 프로세스가 다음 순번에서 자리를 되찾으며
   그다음 것을 쫓아낸다. 실측에서 한 틱에 축출이 **201회** 일어나 전체 트랙이 매 틱
   리셋됐고, 모든 트랙의 표본이 영원히 1개였다. **상한에 닿기만 해도 탐지기가 통째로
   무력화된다.** 정리를 틱 경계에서 한 번만 하고, 그 틱 동안은 잠시 초과를 허용한다.
4. **판정이 관측자를 병목으로 만든다.** 매 틱(1초) 추적 중인 트랙 전부에 표본 전체의
   중앙값을 냈다. 24시간 리플레이가 **CPU 617초를 쓰고도 20분 넘게 끝나지 않았다** —
   실시간에서도 예산 2% 를 못 지킨다는 뜻이다(설계 규칙 1). 누수는 분 단위 현상이라
   초당 판정할 이유가 없어 **판정·정리 주기를 30초**로 두고, 중앙값 전에 `최대/최소` 로
   명백히 아닌 것을 O(n) 한 번에 거르는 **사전 배제**를 넣었다. 앞 1/4 중앙값은 최솟값
   이상이고 뒤 1/4 중앙값은 최댓값 이하이므로 이 상계가 문턱에 못 미치면 정식 판정도
   반드시 탈락한다 — **놓치는 쪽으로 틀리지 않는다.** 같은 구간이 11초로 줄었다.

#### 첫 테스트가 규칙 여섯 중 넷을 검증하지 못했다

작성 직후 변이 테스트(규칙을 하나씩 제거)를 돌렸더니 **네 개가 통과했다.** 케이스들이
여러 조건을 동시에 위반해서, 조건 하나를 지워도 나머지가 막아 준 탓이다. 조건을 **정확히
하나씩만** 어기는 케이스로 다시 짜서 다섯이 잡혔고, 남은 단조성 하나는 톱니파 진폭이 너무
커서 **급락 리셋이 먼저 걸려** 판정까지 도달조차 못 하고 있었다. 진폭을 리셋 문턱 아래로
낮춰 6/6 이 됐다.

성능 회귀도 테스트로 고정했다. 시간이 아니라 **판정 호출 횟수**를 센다 — 시간은 기계
사양에 따라 흔들려 회귀를 가리지만 호출 횟수는 결정적이다.

#### 주입기: 완만한 누수를 만들 수 없었다

`handle_leak` 의 개방 속도가 20/s 로 고정이라 **120초면 상한에 닿고 뒤가 평평해진다.**
그 모양은 실제 누수(시간 단위로 완만히 오름)와 다르고, 평평한 구간이 길면 앞 1/4 중앙값이
상승 한복판을 잡아 배수가 **오히려 2.0 으로 떨어진다.** 탐지기 문턱을 낮춰 맞추면 검증이
무의미해지므로 주입기에 `--handle-rate` 를 추가했다.

실측도 하나 얻었다 — 핸들만 여는 프로세스는 CPU 가 낮아 활성 집합(1초)에 간헐적으로만 들고
**실제 해상도가 30초**다. 표본 20개를 채우려면 최소 10분이 필요하다는 뜻이고, 탐지 지연
543초의 하한이 여기서 온다.

#### 귀인이 새로 뜬 프로세스의 보유량을 통째로 증가분으로 센다 (Phase 8 재개봉)

`--attribution` 채점이 12분 주입에서 **`compattelrunner` 를 1위(40%)로, 실제 원인을
2위로** 올렸다. 확인해 보니 `compattelrunner` 13개 프로세스는 각자 핸들이 +20~29 늘었을
뿐이고(합계 약 +300), 주입 프로세스는 혼자 **+8313** 이었다. 원인은 이 13개가 주입 직전
창에는 **0행**이었다는 것이다 — 도중에 새로 뜨면서 각자 원래 갖고 있던 400 핸들이 전부
"늘어난 것"으로 계산돼 5200 이 됐다.

Phase 8 은 완료로 표시돼 있지만 이건 오늘 생긴 게 아니라 **원래 있던 결함이 드러난 것**이다.
`PLAN.md` §8 에 남긴다. procleak 은 이 함정에 빠지지 않는다 — 자기 시계열의 배수를 보므로
`compattelrunner` 의 1.06배는 걸러지고 주입 프로세스만 6.8배로 남는다.

### 웜으로 옮겨간 데이터를 화면에 드러낸다

저장소 화면의 테이블 목록은 **핫(SQLite) 보유분**인데 화면에 그 말이 없었다. 롤업은 이틀이
지나면 웜으로 옮겨가므로 이 표에서는 보유 시간이 줄어든다 — 사용자가 보기에 데이터가
사라진다. 바로 앞에서 `process_5m`·`net_activity_5m` 을 이 표에 추가하면서 오해 소지를
더 키웠다. 설계 규칙 4("조용히 실패하지 않는다")를 **코드 주석에만 적고 화면에는 안 적은**
셈이다.

- 표 위에 캡션을 단다 — 핫 보유분이며 이틀 지난 롤업은 웜으로 이동하고, 대시보드와 착수
  판정은 두 계층을 합쳐 읽는다는 것.
- "웜 스토어" 지표를 크기(KB)에서 **보유 일수**로 바꾸고 구간·크기를 캡션에 붙였다
  (`2일치` / `07-27~07-28 · 902 KB`). 정의만 있고 호출처가 없던 `warm_exports()` 를
  여기서 쓴다 — 며칠치가 있는지 보여줄 재료가 이미 있는데 죽어 있었다.

### 착수 판정이 전 구간을 메모리에 올리지 않는다

`readiness.py` 가 롤업 전 구간을 파이썬으로 올려 날짜별로 세고 있었다. 지금은 1,440행이라
즉시 끝나지만 30일이면 4만 행이 된다. 날짜별 카운트만 필요하므로 집계를 저장소로 내렸다
(`history.busy_minutes` · `has_column_data`).

그 과정에서 병합 규칙이 세 곳에 복사될 뻔해 `_by_day` 헬퍼로 뽑았다. `coverage` ·
`process_day_index` · `busy_minutes` 가 같은 규칙을 쓴다. 컬럼명이 SQL 에 그대로 들어가는
자리는 롤업 스키마에 있는 이름만 받는다.

### 테스트가 규칙을 검증하지 못하고 있었다

위 리팩터 뒤 **변이 테스트**(병합 필터를 일부러 제거)를 돌렸더니 10개가 전부 통과했다.
되살린 핫 데이터가 웜과 똑같이 10버킷이라, 필터가 없어 핫이 웜을 덮어써도 값이 같아
구분되지 않았던 것이다. 두 계층 버킷 수를 다르게(웜 10 / 핫 5·3) 만들어 **어느 쪽이
채택됐는지가 값으로 드러나게** 고쳤다. 이제 필터를 지우면 두 테스트가 실패한다.

같은 날 "통과하지만 검증하지 못하는 테스트"를 두 번 만났다 — 실 DB 에 의존하는 스모크와
이것. **테스트를 새로 붙이면 일부러 깨뜨려 실패하는지 확인한다.**

### 미뤄 둔 DoD 둘을 실측으로 닫는다

기능 추가가 아니라 **판정**이다. 둘 다 "데이터가 더 필요하다"고 적어 뒀는데, 확인해 보니
필요한 데이터가 이미 쌓여 있었다.

- **Phase 1 (수집 레이어)** — 6시간 무중단 소크를 기다리고 있었으나, 자동 시작이 붙은
  뒤로는 **실사용 자체가 소크**였다. 2026-07-28 12:55~20:27 의 7.53시간 세션(표본 5,411개)
  으로 판정: drop_count 0, CPU 평균 0.259%(최대 0.91%), 스로틀 0회.
  메모리는 07-27 에 세운 원칙대로 Private 으로 봤다 — **핸들 428~429·스레드 19 가 7.5시간
  내내 평탄**하고 Private 은 71.0 → 73.3MB(+0.31MB/시간)다. RSS 는 워킹셋 트리밍 때문에
  32.8~54.9MB 로 출렁여 선형 회귀가 시작·끝 값과 어긋나므로 판정에 쓰지 않았다.
- **Phase 10 (대시보드)** — 미달 사유가 "실시간 탐지가 조용해서 사건 0건, 리플레이 사건
  으로만 검증"이었다. 실사용 사건이 11건(warning 이상 6건) 쌓여 `AppTest` 로 실제 DB 를
  렌더해 확인했다. 5개 페이지 전부 예외 없이 서고, 사건 페이지는 집계(11 · 0 · 6)와 함께
  병목·체감 영향·근거·원인 후보(기여도/증가/선행/신규)를 한 화면에 보여준다.

`PLAN.md` 의 현재 상태 표가 §8 순서표와 어긋나 있던 것도 함께 고쳤다(순서표는 Phase 10 을
✅ 로 적어 뒀는데 상태 표는 "DoD 미달"이었다). 표에 **데이터 대기** 칸을 나눠, 착수해서
못 끝낸 것과 아직 시작할 수 없는 것을 구분했다.

### 장기 데이터를 절반만 보고 있었다

롤업은 두 곳에 나뉘어 산다. 최근 이틀은 SQLite 에, 그보다 오래된 날짜는 Parquet 으로
내보낸 뒤 **SQLite 에서 지워진다**(`purge_after_export`). 그런데 **읽는 쪽은 전부
SQLite 만 보고 있었다.** 웜을 읽는 `WarmStore.query` 는 Phase 4-A 때 만들어 뒀는데
소비자가 아무도 쓰지 않았다.

2026-07-29 관측에서 드러난 증상 둘:

- `tools/readiness.py` 가 "3일 이상 관측된 프로세스"를 `process_5m` 에서만 세고 있었다.
  그 테이블이 담을 수 있는 날짜는 구조적으로 최대 이틀이라 **영원히 0종**이었다. 날짜를
  더 기다려서 풀릴 문제가 아니었고, Phase 6·4-B 의 착수 조건이 도달 불가능했다.
- 대시보드 타임라인에서 07-28 이 통째로 빈 채 그려지고 있었다. 데이터는 Parquet 에
  7,456행 멀쩡히 있었다.

- `argus/storage/history.py` 신설 — 핫과 웜을 하나로 보이게 하는 **단일 진입점**.
  장기 데이터를 보는 코드는 전부 여기를 거친다. 소비자마다 각자 합치면 병합 규칙이
  제각각이 되고 같은 버그를 소비자 수만큼 다시 만든다.
- **병합 규칙: 날짜 단위로 웜이 정본이다.** 한 날짜가 양쪽에 있으면 웜만 쓴다. 그냥
  더하면 그날이 두 배로 세어져 고부하 판정과 지문 통계가 조용히 틀어진다. 07-27 이 실제로
  양쪽에 있었다(아래 백필 항목).
- 웜 날짜에 걸치는 조회에서만 DuckDB 를 켠다. 대시보드 기본 조회는 24시간이라 대부분
  핫만으로 끝나는데, 매번 커넥션을 열면 관측자가 무거워진다.
- `warm.query`·`has_partitions` 를 모듈 함수로 승격. 웜 조회는 SQLite 를 건드리지 않는데
  메서드로 두면 호출자가 쓰지도 않을 `Database` 를 열어야 했다.

### 착수 조건이 날짜 대신 관측 시간을 센다

앞 커밋에서 "날짜가 아니라 표본으로 판정한다"고 해 놓고 **Phase 6·4-B 조건만 여전히
날짜를 세고 있었다.** Argus 는 PC 가 켜져 있을 때만 도므로 관측 시간은 곧 사용 시간이고
날마다 크게 다르다 — 실측 3일이 10.4h / 11.3h / **2.1h** 였다. 2시간짜리 날을 1일로 세면
"3일 관측"이 실질 하루치가 된다.

- `MIN_DAY_HOURS = 6.0` — 이만큼 관측된 날만 '하루'로 센다. 하루의 1/4 이고, PC 사용은
  시간대별로 성격이 갈리는데(업무 낮·게임 밤) 이 정도는 돼야 한 구간을 온전히 덮는다.
  실측 분포에서 4~10시간 어디로 잡아도 판정이 같아 경계가 민감하지 않다.
- `FINGERPRINT_MIN_BUCKETS = 100` — 지문은 "며칠 보였나"만으로 부족하다. 3일에 걸쳐
  보였어도 매번 5분씩이면 표본이 15개다. p99 를 세우려면 100 표본은 있어야 한다.
- 알람 품질 조건에는 이 규칙을 적용하지 않는다. 사건은 켜져 있던 시간에 비례해 나오므로
  짧은 날도 표본으로는 유효하다.

고친 결과 Phase 6 은 여전히 [대기]다 — 웜까지 합쳐 3일치가 보이지만 그중 하루가 2.2시간
이라 조건상 2일이다. **고치기 전과 판정이 같지만 이유가 다르다**: 전에는 도달 불가능한
조건이었고 지금은 하루만 더 모으면 된다(조건 충족 시 92종 예상).

### 백필이 내보낸 날짜를 되살리지 않는다

`tools/backfill_rollup.py` 는 워터마크를 원본 최소 시각까지 되돌려 다시 접는다. 그런데
이미 Parquet 으로 내보내고 SQLite 에서 지운 날짜까지 되돌리면 **그 날짜가 SQLite 에
되살아난다.** `warm_exports` 에는 '완료'로 찍혀 있어 다시 내보내지지도 지워지지도 않는다.
07-27 이 그렇게 남아 두 곳에 존재했고, readiness 가 그걸 세어 "2일 관측"으로 보였다.

- `_export_floor` — 내보낸 마지막 날의 다음 날 00:00 아래로는 되돌리지 않는다. 진단
  출력에도 그 경계를 찍는다(조용히 건너뛰면 데이터 손실로 오해한다).
- 누락 판정에서 내보낸 구간을 제외한다. 빼지 않으면 그 구간이 영원히 "누락"으로 보고되어
  고칠 수 없는 경고를 보고 `--apply` 를 반복하게 된다.
- **워터마크 이후도 누락이 아니다.** 아직 접힐 차례가 오지 않았을 뿐이다. 이걸 빼지 않아
  정상 가동 중에도 항상 "누락 1개"가 떴다 — 늘 켜져 있는 경고는 아무도 읽지 않는다.

### 정상적으로 끌 방법을 만든다

`--stop` 을 붙이기 전까지 **Argus 를 깨끗하게 끄는 방법이 아예 없었다.** 정상 종료 경로는
시그널 핸들러뿐인데, Windows 에는 SIGTERM 이 없고 `pythonw` 로 콘솔 없이 도는 프로세스에는
Ctrl 이벤트도 닿지 않으며, 창이 없으니 `taskkill` 의 WM_CLOSE 도 무의미하다. 남는 것은
강제 종료뿐이었고, 그러면 **사용자가 의도적으로 끈 것까지 전부 `unclean_shutdown` 으로
기록된다** — 바로 앞 커밋에서 만든 사후 진단이 크래시와 사용자 의도를 구분하지 못한다.

- `%APPDATA%\Argus\STOP` 파일을 종료 신호로 쓴다 (`argus/runtime/stopfile.py`).
  권한도 창도 콘솔도 필요 없다. `python -m argus --stop` 이 그 파일을 만든다.
- **읽는 쪽이 지운다.** 감시자가 파일을 먼저 지우고 종료를 요청한다. 순서가 반대면
  지우기 전에 프로세스가 내려가 파일이 남고 다음 기동이 곧바로 다시 죽는다.
- 기동 시 남은 신호를 치운다(`clear_stale`). 직전 세션이 소비 전에 강제 종료됐을 때를
  위한 것으로, 없으면 사용자는 "실행이 안 된다"만 보게 된다.
- `Supervisor.request_stop()` 추가. `stop()` 은 모든 스레드를 join 하므로 컴포넌트 스레드
  안에서 부르면 자기 자신을 기다린다. 실제 정리는 `wait()` 에서 풀린 메인 스레드가 한다.
- 감시자는 `throttleable=False` 다. 스로틀이 걸린 상태(= 이미 부하가 높은 상태)에서야말로
  사용자가 끄고 싶어 하는데 종료 반응까지 느려지면 안 된다.

### 병목 분류가 방아쇠를 따른다

앞선 작업에서 `detectors` 에 발화한 룰 이름을 남기자 어긋남이 드러났다. 그 뒤 실측
7시간에서 사건 6건이 더 쌓였고, **3건이 방아쇠와 다른 병목을 보고했다.** 그중 하나는
심각도가 warning 이라 **알림 대상으로 판정됐다**(`notified=1`) — 대시보드 사건 페이지에
"CPU 병목 — deltaforceclient 53%" 로 보이는데, 그 사건을 연 것은 GPU 온도 룰이었다.
*(2026-07-30 정정: 원래 "실제로 발송됐다 / 사용자가 받은 알림"으로 적었지만 발송 기능은
아직 없다 — `notified` 는 판정 기록이다. 아래 "발송은 만들지 않았다" 절과 어긋났다.)*

원인은 판정 경로가 둘로 갈라져 있던 것이다. 룰은 "평소와 다른가"를 보고 발화하는데,
병목 분류는 그와 무관하게 구간 지표에서 "지금 무엇이 높은가"만 다시 계산했다. 게임 중에는
CPU 가 늘 70% 를 넘어 CPU 점수(0.8)가 THERMAL(0.7)을 **상시 근소하게** 앞선다. 그래서
GPU·메모리로 열린 사건이 전부 CPU 로 덮였다.

- **방아쇠가 지목한 자원을 우선한다** (`argus/explain/bottleneck.py`). 룰 *이름*이 아니라
  룰이 참조한 **지표 이름**으로 매핑한다 — 룰 이름은 사용자가 `%APPDATA%\Argus\rules.yaml`
  에서 바꿀 수 있어 코드에 박으면 사용자 룰에서 조용히 깨지지만, 지표 이름은 룰 정의에서
  그대로 따라오고 로케일에 독립적이다.
- **뒤집을 수는 있되 말없이는 안 된다.** 다른 자원의 근거가 배수(1.5배)와 절대차(0.3)를
  둘 다 넘을 때만 방아쇠를 뒤집고, 그때는 리포트에 "이 사건을 연 것은 X 신호였지만
  구간의 지표는 Y 가 더 뚜렷했다"를 남긴다. 근소한 우위는 뒤집을 근거가 못 된다.
- 방아쇠 자원의 근거 점수가 **0 인 경우도 고지 대상**이다. 평탄한 지표는 σ 가 0 이라 z 가
  서지 않아 점수가 비는데 실제로 흔하다. 조용히 넘어가면 사용자가 기다린 이야기가 사라진다.

실측 사건 10건 재생: **4건이 `CPU 병목` → `발열 스로틀링` 으로 바뀌었고 나머지 6건은
그대로다.** 알림 대상으로 판정됐던 사건 7 의 제목은 "CPU 병목 — deltaforceclient 53%" 에서
"발열 스로틀링 — GPU 91°C 열 스로틀링" 이 됐다. 107 tests passed (신규 4).

### 상주 프로그램이 어떻게 죽었는지 남긴다

실측 이력을 보니 `system_events` 에 **`startup` 17건, `shutdown` 3건**이었다. `shutdown` 은
시그널 핸들러를 거친 정상 종료에서만 기록되는데, 상주 프로그램이 실제로 죽는 방식은 대부분
그게 아니다 — Windows 종료, 전원 차단, 강제 종료, 크래시. 나머지 14건은 아무 흔적도 없었다.

남의 PC 에서 "어제 밤부터 안 돌았다"는 제보를 받았을 때, 사용자가 껐는지 재부팅됐는지
크래시인지 구분할 근거가 없으면 진단이 성립하지 않는다.

- **다음 기동 시점에 직전 세션을 역추적한다** (`argus/runtime/session.py`). 마지막 `startup`
  뒤에 종결 이벤트가 없으면 미종결로 보고, 그 뒤 마지막 데이터 행의 시각을 사망 시각으로
  추정해 `unclean_shutdown` 을 남긴다. `self_telemetry` 가 5초 주기라 해상도도 그 수준이다.
- **부팅 시각과 비교해 원인을 가른다.** 부팅이 사망보다 뒤면 `reboot_or_power_loss`,
  아니면 같은 부팅 세션 안에서 프로세스만 죽은 `process_killed_or_crash` — 후자가 크래시를
  의심할 쪽이다. `psutil` 을 못 쓰면 `unknown` 으로 두고 단정하지 않는다.
- 이벤트의 `ts` 는 '지금'이 아니라 **사망 추정 시각**이다. 그래야 시간순 정렬이 유지되고
  관측 공백 구간의 시작점을 그대로 읽을 수 있다.
- 자기상태 페이지의 시스템 사건 표에 **추정 원인** 열을 붙였다. 사건 이름만으로는
  절전인지 재부팅인지 크래시인지가 안 보였다.

판정 실패가 기동을 막지 않도록 전 구간을 감싼다. 실기 검증: 정상 종료 → `shutdown`,
`taskkill /F` → 다음 기동에서 `unclean_shutdown` / `process_killed_or_crash` 확인.

### 알람 품질 — 방아쇠와 설명이 다른 말을 하고 있었다

Phase 6 설계를 검토하는 사이 실시간 탐지가 **12분 만에 critical 2건**을 냈다. 그 4건을
들여다본 결과 Phase 6 보다 먼저 고칠 것이 나왔다.

**실측 21시간, 신호 6건 중 4건이 `GPU 온도 스로틀링` 이었고 전부 critical 이었다.**
게임하는 동안 쿨다운(30분)마다 영구히 울리는 구조다. 그런데 점수는 0.027~0.607 로
제각각이었다 — 베이스라인이 뜨거운 GPU 에 적응해 z 가 0.2 까지 내려가도 절대 조건만으로
critical 이 나갔다.

- **GPU 온도 룰을 심각도 계단으로 나눴다.** 80°C 스로틀 = `info`(부하 시 정상 동작이다.
  측정된 `SW_THERMAL` 2965 샘플이 전부 부하 구간이었고 유휴 시엔 한 번도 없었다),
  90°C 5분 = `warning`, 95°C = `critical`. 쿨다운도 60분으로 늘렸다.
- **귀인이 불가능한 병목을 값으로 표시한다** (`Bottleneck.attributable`). GPU·발열·경합은
  프로세스별 사용량을 얻을 수 없어 CPU 상위로 대신 분해하는데, 그것을 **원인이라고
  말하고 있었다.**

      이전: 발열 스로틀링 — svchost 19%       ← svchost 는 CPU 를 2% 썼을 뿐이다
      이후: 발열 스로틀링 — GPU 86°C 열 스로틀링

  기여도 목록은 지우지 않고 "참고 — 이 병목의 원인 프로세스는 특정할 수 없습니다"로
  강등했다. 조용히 감추는 것도 아는 척하는 것만큼 나쁘다.
- **사건에 발화한 룰 이름을 남긴다.** `detectors` 에 `["rules"]` 만 있어 아무것도
  말해 주지 않았다. 이제 어긋남이 보인다 — 사건 3 은 *메모리 이상 증가 · GPU 온도
  스로틀링* 이 울렸는데 제목은 `CPU 병목 — op.gg 22%` 였다. 둘 다 사실이지만 인과가
  이어지지 않고, 사용자는 GPU 발열 알림을 보고 op.gg 를 의심하게 된다.

#### 조사하면서 내가 틀린 것

처음에 "score 0.06 인데 critical 은 불일치", "7초짜리가 사건이 됐다", "THERMAL 에 CPU
귀인은 고장"이라고 셋을 지적했는데 **앞의 둘은 틀렸다.** severity 는 룰이 지정하는 것이
설계고(score 와 독립), `for: 60s` 는 충족돼 있었으며 7초는 신호 1건의 시간 폭이었다.
세 번째도 폴백 자체는 의도된 것이고(`bottleneck.py`), 결함은 그 결과를 확신에 찬 답으로
제시한 것이었다. **"#3 은 게임 기동 CPU 오탐"이라는 첫 진단도 틀렸다** — 그 사건을 연
신호는 메모리 증가(info)였고 critical 로 만든 것은 GPU 온도였다. CPU 는 사후 분류기가
붙인 라벨이다.

### 롤업 소급 집계 — 지문 학습 데이터 21시간을 회수했다

Phase 6(프로세스 지문) 착수 가능성을 재려고 데이터를 실측하다 발견했다. `process_5m` 에
**4.2시간 구간에 버킷이 6개뿐**이었다. 5분 버킷이면 50개가 나와야 한다.

원인은 롤업이 워터마크 **이후**만 접는다는 것(`_RollupBase._pending_range`). 워터마크가
앞서 있는데 그 뒤에 원본이 남아 있으면 그 구간은 접힐 기회를 영영 얻지 못한다. 보존
정리에는 "롤업 워터마크를 넘어 지우지 않는다"는 보호 장치가 있지만, **워터마크가 이미
앞서 있으므로 그 장치는 통과 상태다.** 즉 보호받지 못한 채 24시간 뒤 삭제를 기다리고
있었다. 발견 시점에 원본의 보존 기한까지 3시간 남아 있었다.

- 셋 다 같은 상태였다. `metrics_1m` 627버킷(10.4시간), `process_5m` 129버킷(10.8시간)
  누락. `net_activity_5m` 만 연속이라 대조군이 됐다.
- `tools/backfill_rollup.py` — 워터마크를 원본 `MIN(ts)` 로 되돌리고 끝까지 반복 집계한다.
  저장이 `INSERT OR REPLACE` 라 이미 접힌 버킷과 겹쳐도 멱등하다.
- 결과: `process_5m` 380 → **7321행(버킷 136개, 21.0시간, 154종)**, `metrics_1m` 33 → 661행.

#### 도구를 만들면서 두 번 틀린 곳

1. **누락을 행 수로 셌다.** 워터마크 이전 원본을 세니 정상인 `net_activity_5m` 이
   224780행으로 나왔다 — 원본은 접힌 뒤에도 보존 기한까지 남기 때문이다. 누락과
   구분되지 않는 지표였다. **원본에는 있는데 집계에 없는 버킷**을 세도록 바꿨다.
2. **전후 비교가 각자 `time.time()` 을 불렀다.** 그 사이 분 경계를 넘으면 진행 중이라
   원래 접지 않는 마지막 버킷이 누락으로 잡혀, **성공한 백필이 `[FAIL]` 로 보고됐다.**
   같은 기준선을 넘기도록 고쳤다.

**원인은 확정하지 못했다.** 워터마크가 왜 원본보다 앞서 있었는지는 `rollup_state` 에
`updated_at` 만 남아 재구성할 수 없다. 대신 진단 모드를 상시 점검에 쓸 수 있게 남겼다.

### 자동 시작 + 중복 실행 방지 — Phase 14 에서 앞당김

**왜 앞당겼나.** 남은 작업(Phase 6 지문, 4-B 레짐)이 전부 데이터 축적을 기다린다.
그런데 상주 실행이 수동이라 PC 를 켤 때마다 띄우는 것을 잊으면 그날치가 통째로 빈다.
실행 순서 원칙 1(*지연 비용이 있는 것을 먼저 — 데이터를 잃는 작업은 하루 늦으면 하루치가
영구히 사라진다*)에 걸리는 유일한 항목이라, 패키징(Phase 14)까지 끌고 가지 않았다.

- `tools/argus_task.xml` · `tools/install_autostart.ps1` — 로그온 1분 뒤 시작, 실패 시
  1분 간격 3회 재시작. 관리자 권한 불필요. 파이썬 경로는 `pyvenv.cfg` 에서 읽어 주입한다.
- 소크 검증용 `soak_task.xml` 은 건드리지 않았다. 수동 트리거 전용이라 용도가 다르다.

#### 중복 실행 방지가 선결이었다 (`argus/runtime/singleton.py`)

자동 시작을 붙이면 스케줄러 인스턴스와 손으로 띄운 인스턴스가 같은 `argus.db` 를 동시에
쓴다. WAL 이라 즉시 터지지 않고 **수집이 두 배로 들어가며 융합 워터마크·예산 가드·자기
계측이 서로 덮어쓴다.** 조용히 오염되는 쪽이라 며칠 뒤 데이터를 열어 보고서야 안다.

- 커널 named mutex. PID 파일과 달리 강제 종료돼도 유령 잠금이 남지 않는다. pywin32 가
  없으면 PID 파일로 내려가며, 이때 stale 판정은 PID 재사용을 고려해 **생성 시각까지** 본다.
- 이름에 데이터 디렉터리 해시를 섞어 `ARGUS_DATA_DIR` 로 분리한 테스트 인스턴스는 막지 않는다.
- 이미 실행 중이면 **종료 코드 0**. 자동 시작과 수동 실행이 겹치는 것은 실수가 아니다.
- `--check` 는 잠금을 잡지 않는다(쓰지 않으므로). 진단용 강제 겹침은 `--allow-multi`.

#### 설치 스크립트에서 실제로 밟은 함정

1. `pyvenv.cfg` 를 인코딩 지정 없이 읽어 경로의 한글이 깨졌고 → 폴백이 Microsoft Store
   파이썬 스텁(3.13)을 골라 → venv(3.12)의 `pydantic_core` 가 ABI 불일치로 죽었다.
   폴백이 조용히 틀린 것을 고르는 구조라 폴백 자체를 좁혔다(WindowsApps 제외, venv 가
   있으면 폴백 금지).
2. `pythonw.exe` 로 기동 점검을 돌리면 GUI 서브시스템이라 PowerShell 이 기다리지 않아
   `$LASTEXITCODE` 가 비어 **점검이 늘 통과한 것처럼 보인다.** 콘솔형 `python.exe` 로 바꿨다.
3. XML **주석에 적어 둔 `{{ }}` 예시**가 미치환 검사에 걸려 등록이 막혔다. 검사가 주석까지
   훑는다. 검사를 느슨하게 하는 대신 주석 표기를 바꿨다.
4. `.ps1` 에 BOM 이 없어 PowerShell 5.1 이 ANSI 로 읽었고, 깨진 한글 바이트가 따옴표 경계를
   무너뜨려 **뒤쪽 `Write-Host` 줄들이 코드가 아니라 문자열로 출력됐다.** 등록 자체는
   성공해서 오류로 보이지 않는다. UTF-8 BOM 으로 저장.

#### 실기 검증 (2026-07-28 04:28~04:30)

작업 등록 → `Status: Running`, `pythonw.exe` PID 28228 생존. `metrics_raw`·`process_metrics`·
`gpu_metrics` 가 1초 이내로 갱신되고 `self_telemetry` 도 들어온다. 상주 중에 손으로 하나 더
띄우니 뮤텍스에 걸려 **종료 코드 0** 으로 물러났고, `--check` 는 같은 시각에 정상 통과했다.

### Phase 13 조사 — 보류하고 데이터 계층만

Phase 13(보안 라이트)이 "데이터 의존이 없어 지금 가능하다"고 판단했는데 **틀렸다.**
착수 전 실측이 계획서의 규칙을 그대로 쓸 수 없음을 보여 줬다.

| 규칙 | 이 PC 에서의 빈도 |
|---|---|
| 셸·스크립트 호스트를 낳은 부모 (`winword → powershell` 류) | 시간당 **37.8건** |
| 신규 부모→자식 조합 | 시간당 **21.2건** |
| 신규 실행 경로 | 시간당 **12.1건** |
| 신규 원격 주소 | 시간당 **87건** |

절대 규칙은 쓸 수 없다 — `claude → powershell` 99건이 전부 정상이다. **"처음 보는
것만"으로 바꿔도 12시간 데이터로는 시간당 수십 건**이라 "정보성 보고"조차 소음이 된다.
신규 조합은 학습이 쌓이며 줄어드는 추세(53 → 4)라 며칠 뒤 재평가할 수 있다.

Phase 5·7 과 같은 구조다 — 이길 상대가 없는 게 아니라 **판정 기준을 세울 근거가 없다.**

#### 네트워크는 학습으로도 해결되지 않는다

CDN 주소가 계속 바뀌어 신규가 줄지 않고, `/24` 로 묶어도 (프로그램 × 대상) 조합이
1,214 → 1,029 로 거의 그대로다. 의미 있는 단위는 ASN·도메인인데 그건 외부 조회를
요구하고, "개인정보는 로컬을 벗어나지 않는다"와 충돌한다.

#### 추가 — `net_activity_5m` (010)

`net_connections` 는 72시간만 보존된다. 프로세스 롤업 때와 같은 문제로, 접지 않으면
Phase 13 이 나중에 학습할 것이 남지 않는다.

**개별 원격 주소를 저장하지 않는다.** 신호가 되지 않을 뿐 아니라, 원본이 72시간 뒤
사라지는데 집계가 주소를 영구 보관하면 **보존 정책을 우회하는 개인정보 보관**이 된다.
계획서가 원하는 "단시간 다수 신규 원격 IP"는 **개수만으로** 잡힌다 — "크롬이 평소
대상 200개인데 지금 2,000개"에 개별 주소는 필요 없다.

남기는 것: 프로그램별 고유 대상 수·포트 수·연결 수·LISTEN 수.
실측 3,201행 / 12.5시간(하루 약 6,100행).

회귀 테스트는 컬럼 이름이 아니라 **저장된 값**을 검사한다 — 이름은 바꿀 수 있어도
값은 못 속인다.

### Phase 9 — 융합·억제·예산 (발송 제외)

Phase 8 이 만든 리포트가 **메모리에만 있었다.** 계산하고 버렸으므로 대시보드도,
피드백도, 알림도 붙일 곳이 없었다. 이 단계가 넷을 동시에 풀었다.

end-to-end 실측(주입 #15 구간 리플레이): 룰 신호 3건 → 사건 3건, 각각 병목·원인
후보·근거가 붙었다. 주입 구간과 대응이 맞는다.

```
[2] warning  14:42:04 ~ 14:46:04  (3분 59초)
    CPU 병목 — python 82%
    체감 영향: CPU 21% → 96% (평소의 8.4σ)
      1. python (프로세스 12개)   기여도 82%  (43.6%, +29.6%)  ← 거의 동시
      2. discord (프로세스 6개)   기여도  5%  (3.7%, +1.8%)
```

(주입 `cpu_spin` 은 14:40:58~14:45:59, `memory_leak` 은 사건 `python 95% (+6.9GB)` 로 잡혔다.)

#### 추가

- **`009_incidents.sql`** — `incidents`, `incident_signals`
- **`argus/decide/`** — `fusion`(신호→사건) · `suppression`(억제) · `budget`(예산)
- **대시보드 Incidents 페이지** — 사건 목록, 리포트 전문, 피드백 버튼

#### 핵심 — 경계를 신호 시각이 아니라 지표에서 다시 찾는다

처음 구현은 사건의 끝을 "마지막 신호 시각"으로 잡았다. 그랬더니 실측에서
**5분짜리 문제가 "0초"로 기록됐고, 원인 후보가 통째로 비었다.**

원인: 룰은 쿨다운(10분) 때문에 지속되는 문제에도 신호를 **한 번만** 낸다. 신호 1건
= 순간이 되고, 비교 창 길이가 0 이라 귀인이 볼 것이 없다. 사용자에게는 5분간
느렸던 일이 순간 스파이크로 보인다.

Phase 8 에서 만들어 놓고 **쓰지 않던 `changepoint` 를 여기 연결**해 해결했다.
`find_recovery` 를 추가해 시작뿐 아니라 끝도 지표에서 찾는다. 결과: 0초 → 3분 59초,
원인 후보 5개.

이 과정에서 둘을 더 배웠다.

- **한 지표만 보면 안 된다.** 처음에는 `cpu_total` 하나로 경계를 잡았는데, 메모리 누수
  구간을 CPU 로 재니 **4초**로 나왔다. 어느 지표로 재야 하는지는 병목 판정이 알려주지만
  그 판정은 구간이 정해진 뒤에야 할 수 있어 순환이다. **전부 보고 가장 오래 벗어난
  것**을 쓰면 순환 없이 답이 나온다(4초 → 2분 48초).
- **그러나 상한이 필요하다.** 전부 보게 하자 오래 높은 지표 하나가 사건을 삼켜
  4분짜리가 **23분 46초**가 되고 뒤따르는 사건과 겹쳤다. 보정의 목적은 탐지 지연을
  되돌리는 것이지 사건을 병합하는 것이 아니다 — 앞 5분/뒤 10분으로 제한했다.

#### 억제와 예산 — 지우지 않고, 조용해지지 않는다

- **억제한 사건을 지우지 않는다.** `suppressed_by` 로 표시만 한다. 왜 안 알렸는지
  설명할 수 있어야 하고, 억제 규칙이 틀렸는지 나중에 검증하려면 기록이 남아야 한다.
- **예산을 다 쓰면 조용해지는 게 아니라 기준을 올린다.** 남은 것은 대시보드에만 남기고
  그보다 심각한 것은 여전히 알린다. 진짜 중요한 일이 예산 때문에 묻히면 예산 자체가
  위험해진다.
- **안 보낸 이유를 저장한다**(`notify_skipped`). 조용히 사라지면 왜 알림이 오지
  않았는지 아무도 설명할 수 없다.

#### 발송은 만들지 않았다

`severity` 결정과 `notified` 판단까지만 한다. Toast·Discord 경로는 오탐률이 검증된
뒤에 붙인다 — 알림은 되돌릴 수 없다.

#### 대시보드의 유일한 쓰기

조회 계층은 읽기 전용을 유지하되, 피드백 저장만 별도 함수로 쓰기 연결을 열고 닫는다.
피드백은 사용자가 화면에서 주는 것이라 다른 경로가 없다. 그 라벨이 Phase 11 의 입력이다.

### Phase 4-A 보강 — 프로세스 롤업 (4-A 에서 빠뜨린 절반)

4-A 는 `metrics_raw` 의 증발만 막았다. **`process_metrics` 는 24시간 보존 그대로였고**,
`metrics_1m` 에 남는 프로세스 정보는 `foreground_proc`·`top_cpu_proc`·`top_cpu_share`
셋뿐이라 분포를 복원할 수 없었다. Phase 6(지문)이 요구하는 "프로세스명 단위
p50/p95/p99" 를 학습할 데이터가 매일 사라지고 있었다.

계획서 3.2 에 `process.parquet (5분 집계, 상위 프로세스만)` 이 이미 적혀 있었는데
4-A 에서 구현하지 않았다.

#### 보호 장치가 헛돌고 있었다

이게 더 나빴다. `process_metrics` 는 보존 규칙에서 롤업 워터마크에 묶여 있었지만
(`needs_rollup=True`), 그 워터마크는 **`metrics_1m` 것**이었고 1분 롤업은 프로세스를
접지 않는다. **접히지도 않은 채 "롤업이 지나갔으니 안전하다"는 이유로 지워지고
있었다.** 보호 장치가 있다고 믿는 편이 없다고 아는 것보다 나쁘다.

이제 테이블마다 **자기를 접는 롤업의** 워터마크를 본다.
`tests/test_rollup.py::test_retention_uses_the_rollup_that_folds_it` 이 고정한다.

#### 추가

- **`007_process_rollup.sql`** — `process_5m`(20컬럼)
- **`008_warm_kind.sql`** — `warm_exports` 에 `kind`(metrics/process). 한 날짜에
  Parquet 이 둘이 되므로 `date_key` 만으로는 키가 안 된다
- **`ProcessRollup`** — 5분 단위, 워터마크 분리. `_RollupBase` 로 공통 골격 분리
- **웜 스토어**가 종류별 파티션과 종류별 DuckDB 뷰(`warm`, `warm_process`)를 다룬다

#### 시각별로 먼저 합친 뒤 분위수를 낸다

표본을 그냥 모아 분위수를 내면 **"개별 프로세스의 p95"** 가 나온다. 지문에 필요한
것은 **"크롬 전체가 평소 얼마나 쓰나"** 다. 탭이 30개면 둘은 30배 다르다.
`GROUP BY ts, name` 으로 시각별 합계를 만든 뒤 그 시계열의 분위수를 낸다.

검증(실 데이터): chrome 13:30 버킷 롤업 `p50=4.899 max=19.177 pids=14`,
원본 재계산 `p50=4.899 max=19.177 PID 14` — 일치.

#### 조건 필터가 아니라 상위 N

`cpu > 0` 같은 조건으로 거르면 프로세스가 500개인 PC 에서 저장량이 예측 불가로
늘어난다. **배포 대상의 하드웨어를 가정하지 않으려면 행 수에 상한이 있어야 한다.**
CPU 상위 N 과 메모리 상위 N 을 따로 뽑아 합치는 이유는, 메모리만 먹고 CPU 는 안 쓰는
프로세스(누수 후보)를 CPU 순위로만 자르면 놓치기 때문이다.

#### 실측

| 항목 | 결과 |
|---|---|
| 12시간 백필 | 4,006행 **2.2초** |
| 버킷당 프로그램 | 54개 (top_n=40 기준) |
| 하루 환산 | 15,591행 |
| Parquet(zstd) | 행당 **46 bytes** |
| 하루 / 1년 | **705 KB / 251 MB** |

지문 후보 샘플: `chrome` cpu p50 0.73 / p95 5.01, rss 최대 2,899MB —
분포가 실제로 남는다.

#### 고친 것 — 적용된 마이그레이션을 수정했다

`warm_exports` 변경을 007 에 덧붙였는데, 007 은 이미 이 머신에 적용된 뒤였다.
적용된 마이그레이션은 다시 실행되지 않으므로(`PRAGMA user_version`) 반영되지 않았고,
`no such column: kind` 로 터졌다. 개발 중에는 DB 를 지우면 그만이지만 **배포 후라면
그 사용자는 변경분을 영영 받지 못한다.** 008 로 분리했다 — 적용된 마이그레이션은
고치지 않는다.

### Phase 8 — 귀인 엔진

"이상 감지"는 알림을 끄게 만들고, "CPU 병목, 기여도 1위 python 82%"는 행동을 만든다.
**이 단계가 제품이다.**

실측: **1순위 지목률 88.9%(8/9), 3위 이내 100%** — DoD 85% 충족.
`python -m argus.eval --attribution`

#### 추가

- **`argus/explain/`** — `changepoint`(시작점 역추적) · `bottleneck`(병목 분류) ·
  `attribution`(기여도 분해·선행성) · `report`(자연어 리포트)
- **`argus/eval/attribution.py`** — 1순위 지목률 채점
- 새 의존성 없음

#### 계획서와 다르게 간 것 — Shapley 를 쓰지 않는다

Shapley 가 값을 하는 것은 참여자의 기여가 **상호작용할 때**다. CPU 시간·디스크
바이트·메모리는 가산적이라 A 의 30% 와 B 의 20% 를 더하면 50% 이고, 둘이 함께여야만
생기는 시너지가 없다. 그런 자원에서 Shapley 값은 단순 델타 비율과 **수학적으로 같다.**
복잡도만 늘고 답은 같은 것을 넣지 않는다. `ruptures`·BOCPD 도 같은 이유로 뺐다 —
이상이 있다는 것과 대략 언제인지를 이미 아는 상태에서 필요한 건 경계를 다듬는
일이고, 기존 중앙값/MAD 로 충분했다.

#### 정답을 프로세스 트리로 확장했다

`fault_injections.pid` 는 주입기 부모 하나뿐인데 실제 부하는 자식들이 낸다 —
CPU 스핀은 GIL 때문에 프로세스로 fork 하므로 부모는 CPU 를 0.1% 쓰고 자식 8개가
각 4.2% 를 쓴다. 실측에서 정답 PID 는 5분 구간에 **12행만 관측됐고 CPU 는 0** 이었다.
부모만 정답으로 두면 어떤 귀인 엔진도 통과할 수 없다. `process_events.ppid` 로
트리를 복원해 정답 집합을 만든다.

#### 채점 70% → 88.9%, 고친 것 셋

첫 채점은 70% 였다. 세 가지가 원인이었고 셋 다 성격이 달랐다.

- **`svchost` 91개가 한 덩어리로 묶여 1위를 차지했다.** 이름이 같아도 서로 다른
  서비스다. 합산하면 주입 프로세스를 제치고, 게다가 "svchost 가 41%"는 사용자에게
  **쓸모없는 답**이다 — 어떤 서비스인지 알 방법이 없다. 공유 호스트 프로세스
  (`svchost`·`dllhost`·`rundll32`·`wmiprvse` 등)는 PID 단위로 분리한다.
- **비교 창의 평균이 스파이크에 끌려갔다.** "평소"를 정하는 창은 중앙값으로 본다 —
  베이스라인이 평균 대신 중앙값을 쓰는 이유와 같은 이유다.
- **`manual` 시나리오는 귀인 대상이 아니다.** 이 시나리오는 부하를 만들지 않고
  사용자의 실제 활동에 라벨만 붙인다. 주입기 프로세스는 아무 일도 하지 않으므로
  (실측: 25개 시각 중 1행, CPU 0) "주입 PID 를 지목하라"는 요구 자체가 성립하지
  않는다. 원인은 게임이지 라벨을 붙인 도구가 아니다.

#### 리포트를 실제로 출력해 보고 고친 것 넷

숫자가 DoD 를 넘었다고 끝이 아니었다. 리포트를 눈으로 읽자 넷이 더 나왔다.

- **판정과 무관한 근거가 붙었다** — "CPU 병목 — 근거: CPU 96% · 스왑 220MB".
  근거를 판정별로 모으도록 고쳤다. 근거가 섞이면 읽는 사람이 판단을 검증할 수 없다.
- **원인 프로세스가 "34초 후행"으로 나왔다.** 원인이 결과보다 늦었다는 말이 안 되는
  결론이다. 변화점 역추적이 임계 미만 표본 하나에 즉시 끊어져, 서서히 오르는 램프
  구간에서 시작점을 한참 뒤로 잡고 있었다. 임계 미만이 연속으로 나와야 끊도록 고쳤다.
- **선행성의 기준이 서로 달랐다.** 시스템은 "평소를 처음 벗어난 시점", 프로세스는
  "상승폭의 절반 도달"이었다. 5분 램프의 절반 지점은 당연히 늦어서 원인이 일관되게
  후행으로 나왔다. 둘 다 "오르기 시작한 시점"으로 맞췄다.
- **가짜 선행성이 대량으로 있었다.** `discord 255초 선행`, `system 285초 선행` —
  전부 조회 창 경계값이었다. 창 시작부터 이미 높으면 첫 표본이 반환되던 것으로,
  실제 뜻은 "원래 그 수준이었다"이다. 모르는 것은 모른다고 하도록 고쳤고,
  기여도가 낮은 후보는 선행성을 아예 표시하지 않는다(잡음이다).

#### 테스트가 잡은 것 — 상대 조건만으로는 안 된다

`0.1ms → 0.2ms` 가 **20σ** 로 계산돼 "IO 병목"으로 판정됐다. NVMe 의 평소 응답은
산포가 거의 0 이라 아주 작은 변화가 거대한 z 가 된다. Phase 3 에서 "룰은 상대 조건과
절대 조건을 함께 가져야 한다"고 정해 놓고 병목 분류에는 적용하지 않았던 것이다.
체감 하한(5ms)을 함께 요구하도록 고쳤다 — 평소와 다르고(상대), 실제로 아플 만큼
느리다(절대).

#### 남긴 한계

- **표본 9건은 얇다.** 1건 차이로 78%↔89% 가 갈린다.
- **채점이 실사용보다 쉬운 조건이다.** 채점은 시나리오별로 볼 자원을 알려주고
  시작하지만, 실사용에서는 병목 분류가 자원을 정해야 한다. 이 PC 의 `disk_thrash` 는
  응답시간 증상이 없어 "CPU 병목"으로 분류되고, 그러면 귀인도 엉뚱한 프로세스를
  지목한다. 증상이 나오는 하드웨어에서 확인해야 한다.

### Phase 10 — 대시보드 (부분)

Phase 4-B(레짐 추론)는 데이터가 며칠 쌓여야 착수할 수 있어, 축적이 필요 없는
대시보드를 앞당겼다. 실용적인 이유가 하나 더 있다 — **하루 동안 일회성 조회
스크립트를 다섯 개 짰다**(소크 상태 확인, 데이터 조사, 압축률 실측…). 그 일이
반복될 것이 분명했다.

7개 페이지 중 **데이터가 이미 있는 4개만** 만들었다. Regimes(4-B)·Incidents(Phase 9)·
Models(학습 후)는 빈 화면으로 만들지 않았다 — "아직 없음"과 "고장남"이 구분되지 않는다.

#### 추가

- **`argus/dashboard/`** — Streamlit 멀티페이지. `python -m argus.dashboard`
  - **Health** — 자기 계측. RSS 와 private 을 나란히 그린다
  - **Live** — 현재 상태와 최근 10분
  - **Timeline** — 1분 집계 위에 결함 주입 구간(정답)과 탐지 신호(판정)를 겹친다
  - **Processes** — 프로그램 단위 랭킹(PID 가 아니라 이름으로 합친다), 개별 추이
- 의존성: `streamlit`, `plotly`

#### 색은 눈으로 고르지 않았다

`dataviz` 검증기를 실제로 돌려 통과한 조합만 쓴다.

```
node scripts/validate_palette.js "#3987e5,#d95926,#199e70,#c98500" --mode dark
→ 명도 대역 PASS · 채도 하한 PASS · 색각 분리 PASS(최악 ΔE 8.4)
  · 일반 시야 하한 PASS(19.8) · 서피스 대비 PASS(전부 3:1 이상)
```

**다크로 고정한 것도 검증 결과다.** 같은 팔레트가 라이트 서피스에서는 aqua·yellow 가
3:1 미만이라 별도 완화(가시 라벨·표 병기)가 필요해진다. 축이 다른 지표는 한 차트에
겹치지 않는다 — CPU %(0~100)와 디스크 MB/s 를 한 그림에 넣으면 "어느 선이 어느 축인가"를
읽는 비용이 데이터를 읽는 비용보다 커진다.

#### HTTP 200 은 아무것도 증명하지 못했다

헤드리스로 띄워 `HTTP 200 · 10,626 bytes` 를 받고 넘어갈 뻔했다. Streamlit 은 SPA 라
**페이지 스크립트가 통째로 터져도 셸 HTML 은 정상으로 돌아온다.**

`AppTest` 로 페이지를 실제 실행해 보니 **네 페이지 전부 즉시 예외**였다.

```
ImportError: attempted relative import with no known parent package
```

Streamlit 은 페이지 파일을 `exec` 으로 돌려서 패키지 컨텍스트가 없다 — `from . import`
가 통째로 실패한다. 절대 import 로 바꾸고 각 페이지가 프로젝트 루트를 직접 세우게 했다.
**검증 방법이 틀리면 통과가 실패보다 나쁘다** — 고장난 채로 완료 보고를 하게 된다.
`tests/test_dashboard.py` 가 다섯 페이지를 빈 DB 로 실제 실행해 이 회귀를 막는다
(마우스 자동화 없이, 예외 수집으로).

#### 실측

| 페이지 | 실제 데이터 렌더 |
|---|---|
| app | 345ms |
| Live | 185ms |
| Timeline | 486ms |
| Processes | 142ms |
| Health | 283ms |

첫 측정에서 Timeline 이 5.9초로 나왔으나 반복 측정하니 150ms 였다 — plotly 최초
초기화 비용이며 지속되는 지연이 아니다. 쿼리 자체는 6시간 구간 124행에 7.5ms.

### Phase 4-A — 1분 롤업과 웜 스토어

Phase 4(레짐 추론) 착수 직전 조사에서 **레짐을 학습할 데이터가 존재할 수 없는
구조**임이 드러났다. `retention.raw_hours` 가 24시간이라 원본이 매일 지워지는데,
Phase 4 는 며칠치를, Phase 5 는 "최근 14일"을, Phase 7 은 "2주 축적"을 전제한다.
GMM 을 아무리 잘 만들어도 학습할 것이 없고, DoD 도 측정되지 않는다.

그래서 Phase 4 를 **4-A(데이터 계층) → 4-B(레짐 추론)** 로 나누고 4-A 를 먼저 했다.
롤업은 우회로가 아니다 — 계획서 Phase 4 파이프라인의 1번 단계가 "1분 윈도우 피처
벡터 생성"이므로, **이건 레짐 추론의 입력 계층 그 자체다.**

#### 추가

- **`storage/migrations/006_rollup.sql`** — `metrics_1m`(43컬럼), `rollup_state`, `warm_exports`
- **`storage/rollup.py`** — 완결된 1분 버킷을 접는다.
- **`storage/warm.py`** — 끝난 날짜를 Parquet 일별 파티션으로 내보내고 DuckDB 로 조회
- 의존성 추가: `pyarrow`, `duckdb`

#### 평균만 남기면 레짐을 구분할 수 없다

접고 나면 원본이 없다. **여기서 안 남긴 것은 영원히 못 본다.**

게임과 빌드는 평균 CPU 가 같아도 분포가 다르다 — 게임은 고르게 눌려 있고 빌드는
코어별로 튄다. 그래서 표준편차·최대·p95 와 **코어 불균형**(가장 바쁜 코어 − 전체 평균)을
함께 접는다. 계획서 3.3 의 "코어 수와 무관한 집계값(평균·최대·분산·불균형도)"이
요구하는 것도 이것이다. 포어그라운드 프로세스의 1분 최빈값과 그 비율도 남긴다 —
"무엇을 하는 중인가"는 리소스 수치만으로는 알 수 없다.

p95 는 보간하지 않고 최근접 순위로 낸다. 관측된 값만 남기기 위해서다 — "응답시간
p95 = 95.05ms"처럼 실제로 없던 값을 만들지 않는다.

#### Parquet 이 append 를 못 해서 저장 경로가 2단이 됐다

```
metrics_raw(초, 24h) → metrics_1m(SQLite, 최근 며칠) → warm/date=.../metrics.parquet
```

한 줄을 더하려면 파일 전체를 다시 써야 하므로 1분마다 직접 쓸 수 없다. **완전히 끝난
날짜만** 내보내 파일을 불변으로 유지한다(오늘·어제는 건드리지 않는다). 임시 파일에
쓰고 원자적으로 옮기며, **읽기가 검증된 뒤에야** SQLite 에서 지운다 — 쓰기 성공과
읽기 가능은 다르다.

#### 이 작업의 핵심은 집계가 아니라 삭제 순서다

**보존 정리는 롤업 워터마크를 넘지 못한다.** 접히기 전에 지워진 원본은 어디에도 남지
않는다. 롤업이 멈추면(예외·설정 오류) 삭제도 함께 멈추고 DB 가 커지는데, **그게 맞다** —
디스크가 차는 것은 눈에 보이고 고칠 수 있지만 지워진 2주치는 되돌릴 방법이 없다.
`tests/test_rollup.py` 가 이 결합을 고정한다(워터마크 중간 정지, 멱등성, 진행 중인 분
제외, Parquet 왕복 포함).

#### 실측

| 항목 | 결과 |
|---|---|
| 실제 8시간 데이터 롤업 | 141버킷 **239ms** |
| 압축비 | **59:1** (8,351행 → 141행) |
| Parquet(zstd) | 행당 308 bytes |
| 하루치 | **434 KB/일** (원본 292 MB/일) |
| 1년 누적 | **155 MB** |
| 보존된 신호 | `fg=mahjong-jp(0.85)`, `gpu=20.5`, `cpu σ=7.6`, `imb=20.7` |

#### 고친 것

- `retention.py` 스모크가 워터마크를 현재 시각으로 남기고 끝나, 그 뒤로 롤업이 과거를
  영영 건너뛰게 만들었다. **스모크가 운영 상태를 바꾸면 안 된다** — 원래 값으로
  되돌리거나(없었으면) 행을 지우도록 했다.
- 워터마크 부재 경고가 대상 테이블마다 한 줄씩(3줄) 나와 로그를 채웠다. 한 줄로 합쳤다.

### Phase 1 보강 — 자기 계측의 메모리 지표를 고침

Phase 1 의 마지막 미완 항목이 "RSS 장기 추세"였는데, **그 지표로는 답을 낼 수 없다는
것이 소크 도중에 드러났다.**

6시간 소크의 10분 버킷 집계에서 RSS 가 이렇게 움직였다.

```
+400m  63.7MB   +410m  29.4MB   +440m  27.2MB   +470m  18.8MB   +480m  17.9MB
```

내려가고 있으니 누수는 없어 보인다. 그런데 이건 메모리를 반납한 게 아니라, 백그라운드
(`pythonw`)로 도는 프로세스의 워킹셋을 Windows 가 트림해 페이지를 디스크로 내보낸
결과다. **누수가 실제로 있어도 트림이 그걸 가린다.** 이대로 6시간을 채우고 "RSS 안정,
누수 없음"으로 Phase 1 을 닫았다면, 측정하지 않은 것을 측정했다고 적는 셈이 된다.

#### 추가

- **`self_telemetry` 에 컬럼 셋** (`005_selftel_memory.sql`)
  - `private_mb` — 커밋된 사설 메모리. **누수 판정의 정본.** 트림의 영향을 받지 않는다.
  - `peak_wset_mb` — 워킹셋 최대치. 되돌아가지 않으므로 트림된 구간에서도 실제 사용량이 남는다.
  - `page_faults` — 누적 페이지 폴트. 트림이 실제로 일어났는지 확인하는 근거.
- **`BudgetGuard.last_memory()`** — `memory_info()` 한 번으로 위 값을 모두 뽑는다.
  psutil 의 Windows 전용 필드라 다른 플랫폼에서는 `getattr` 로 받아 `None` 을 남긴다.

#### 예산 판정은 계속 RSS 로 한다

바꾸고 싶은 유혹이 있지만 바꾸지 않았다. 예산 "RSS 300MB"는 *지금 물리 메모리를 얼마나
붙들고 있는가*라는 뜻이고, 트림된 뒤 줄어든 값이 그 질문에는 맞는 답이다.
**지표마다 답하는 질문이 다르다** — 예산은 RSS, 누수는 private.

#### USS 를 넣지 않은 이유

`memory_full_info().uss` 가 후보였지만 뺐다. 워킹셋 기반이라 트림되면 RSS 와 함께 줄어
**같은 문제를 그대로 겪고**, Windows 에서 계산 비용이 가장 크다(전 페이지 스캔).
관측자는 가벼워야 한다.

#### 검증 — 트림을 인위로 일으켰다

`SetProcessWorkingSetSize(-1, -1)` 로 유휴 백그라운드 프로세스에서 Windows 가 하는 일을
즉시 재현했다. 64MB 를 실제로 건드린 뒤 트림:

| | RSS | private | peak_wset | page_faults |
|---|---|---|---|---|
| 트림 전 | 95.8MB | 85.4MB | 95.8MB | 24,709 |
| 트림 후 | **1.0MB** | **85.4MB** | 95.9MB | 24,979 |

RSS 는 95.8 → 1.0MB 로 무너지고 private 은 85.4MB 그대로다. 소크에서 본 하강 곡선의
정체가 이것이다. `tests/test_selftel_memory.py` 에 회귀 테스트로 고정했다
(트림이 재현되지 않는 환경에서는 skip 이 아니라 실패로 드러나도록 `argtypes` 를 명시했다 —
64비트에서 `SIZE_T(-1)` 이 잘려 호출이 조용히 실패하면 검증이 사라진 걸 모른다).

### Phase 3 — 통계 베이스라인 + 룰 엔진

절대 임계값은 배포 프로그램에서 쓸 수 없다. "디스크 응답 50ms 이상은 병목"은 HDD
사용자에게 평소이고, "CPU 85%"는 12코어 PC 에서 좀처럼 닿지 않는다. **이 PC 의 평소
대비 얼마나 벗어났는가**로 말한다.

#### 추가

- **`argus/detection/expr.py`** — 룰 표현식 평가기. `eval()` 을 쓰지 않는다.
  룰 파일은 사용자가 편집하고 나중에는 남이 만든 룰을 받게 되는데, `eval()` 이면
  `__import__('os').system(...)` 한 줄로 성능 모니터가 실행 통로가 된다. `ast.parse`
  로 트리를 만들고 **허용한 노드만** 통과시킨다(화이트리스트라 새 문법도 자동 차단).
  회귀 방지로 11가지 공격 표현식을 pytest 에 고정했다.
- **`argus/detection/baseline.py`** — 롤링 중앙값/MAD 기반 베이스라인.
  - **평균/표준편차를 쓰지 않는다.** 이상 구간이 베이스라인에 섞이면 평균이 끌려가고,
    오염된 평균으로 다음 이상을 재면 점점 둔감해진다. 30% 오염 실측에서 중앙값은
    20 → 21.7 로 버텼다.
  - **산포가 없는 메트릭은 판정하지 않는다**(`degenerate`). 내내 0 인 `disk_queue` 에
    억지로 바닥값을 깔면 0 → 1 이 z 수십으로 튀어 오탐 폭풍이 된다. 이런 지표는
    룰에서 절대 임계값으로 다룬다 — 큐 길이는 단위 자체가 하드웨어에 독립적이다.
- **`argus/detection/rules.py` + `argus/config/rules.yaml`** — 룰 엔진과 기본 룰 9개.
  모든 룰이 지속 조건(`for`)과 쿨다운을 **강제로** 갖는다. GPU 지표는 `obs.gpus` 에서
  `gpu_*` 로 펼쳐 룰이 볼 수 있게 한다.
- **`argus/detection/registry.py`** — 탐지기 이름 표를 한 곳에. 스코어보드와 실시간이
  같은 표를 봐야 "평가한 것"과 "실제로 도는 것"이 갈라지지 않는다.

#### 계획서와 다르게 간 것 — 재시작 후 부트스트랩 폐지

`PLAN.md` 는 "부트스트랩 첫 2시간 탐지 비활성"이었다. 그대로 하면 PC 를 켤 때마다
2시간 동안 아무것도 못 잡는데, 사용자는 PC 를 매일 끈다.

베이스라인을 메모리에만 두지 않고 **시작할 때 `metrics_raw` 에서 최근 30분을 읽어
즉시 채운다.** 데이터는 이미 DB 에 있으므로 별도 상태 테이블도 마이그레이션도 필요
없다. 실측 **1,172틱을 60ms 에 채우고 곧바로 `ready`**. 2시간 부트스트랩은 진짜 첫
설치일 때만 남는다.

#### 룰이 상대 조건과 절대 조건을 함께 갖는 이유

상대값만 쓰면 조용한 PC 에서 오탐이 쏟아진다. 하루 종일 CPU 5% 인 사무용 PC 는
중앙값 5 / σ 0.5 라서 "5σ"가 겨우 7.5% 다. 절대값만 쓰면 하드웨어를 가정하게 된다.
그래서 둘 다 요구한다 — **평소와 다르고(상대), 실제로 아플 만큼 높다(절대).**

예외가 하나 있다. **메모리 누수는 아파지기 전에 잡아야 의미가 있다.** 64GB PC 에서
누수가 18%p 를 먹어도 사용률은 46% 라 압박 룰(80%)에 안 걸린다. "메모리 이상 증가"
룰을 따로 두고, 절대 조건을 사용률이 아니라 **평소 대비 증가폭(%p)** 으로 잡았다.

#### 채점 결과 — 기준선을 넘었다

| 탐지기 | TP | FP | FN | 정밀도 | 재현율 | F1 | 지연 |
|---|---|---|---|---|---|---|---|
| **`rules`** | **4** | **0** | 5 | **100.0%** | **44.4%** | **0.615** | 224s |
| `always` | 9 | 28 | 0 | 24.3% | 100.0% | 0.391 | 0s |
| `fixed_cpu` | 2 | 0 | 7 | 100.0% | 22.2% | 0.364 | 100s |
| `fixed_mem` | 0 | 0 | 9 | 0.0% | 0.0% | 0.000 | — |

#### 고친 것 — 테스트 설계와 계측 오류

처음 채점에서 `rules` 가 F1 0.444 로 기준선과 동률이었다. 원인을 파고들어 보니
룰이 아니라 **검증 방법이 틀린 경우가 섞여 있었다.**

- **90초 램프는 "서서히 나빠짐"이 아니다.** 계획서가 말하는 점진적 열화는 30~90분
  짜리다. 90초 램프는 임계 위에 22초만 머물러 지속 조건을 못 채운다. 지속 조건을
  줄이면 숫자는 오르지만 그건 튜닝이 아니라 조작이다. **5분 램프로 시나리오를 고쳤고**,
  그때 F1 이 0.444 → 0.615 가 됐다.
- **`soak_entry.py` 가 PDH 를 죽이고 있었다.** `sys.path.insert` 로는 `.pth` 파일이
  실행되지 않아 pywin32 가 import 되지 않았고, 프로세스 수집이 psutil 폴백(15초 주기)
  으로 내려가며 `disk_resp_ms`·`disk_queue` 가 전부 NULL 로 쌓였다. `site.addsitedir()`
  로 고치고, 진입점이 의존성을 직접 확인하도록 했다.
- **주입기의 디스크 효과 판정을 처리량 → 응답시간으로 바꿨다.** 600MB/s 를 퍼부어도
  NVMe 가 삼키면 사용자는 아무것도 못 느끼고, 탐지기가 안 잡는 게 맞다(증상 없는
  원인은 알릴 가치가 없다). 처음엔 `psutil` 의 `write_time` 을 썼는데 **Windows 에서
  사실상 0 만 준다**(2초에 96회 쓰기, 델타 0ms). 그 값으로 "이 PC 는 디스크 병목을
  만들 수 없다"고 결론 낼 뻔했다. Argus 가 이미 PDH 로 읽는 `disk_resp_ms` 로 교체한 뒤
  다시 재니 0.1ms → 0.2ms — **결론은 같았지만 이제 근거가 있다.**

#### DoD 문구 수정 — 고정 개수 기준을 버렸다

원래 DoD 는 "시나리오 6개 중 4개 이상 탐지"였다. 실제 주입기는 4종이고 그중 2종은
**이 하드웨어에서 증상 자체가 발생하지 않는다.** 증상 없는 구간을 탐지하라는 요구는
오탐을 요구하는 것과 같다. 하드웨어마다 만들 수 있는 증상이 다르다는 것이 이 프로젝트의
전제인데, 고정 개수를 요구하는 기준은 그 전제와 모순이다. **"증상이 관측되는 시나리오는
전부 탐지"** 로 바꿨다.

| 항목 | 결과 |
|---|---|
| 기준선 초과 | ✅ F1 0.615 > 0.364 / 0.214 |
| 증상 있는 시나리오 전부 | ✅ 2/2 (`cpu_spin`·`memory_leak`) |
| 오탐률 < 1/h | ✅ **0.00/h** (`always` 는 60.83/h) |

탐지되지 않은 2종은 이유가 다르고, 둘 다 룰의 결함이 아니다.

- **`disk_thrash`**: 이 NVMe 에서 응답시간이 0.1 → 0.2ms 밖에 안 올라 **증상 자체가
  없다.** 라벨이 `completed=0` 으로 자동 제외된다. SATA·HDD 장비에서 확인해야 한다.
- **`handle_leak`**: 핸들 수는 프로세스별 지표라 시스템 메트릭 룰로는 닿지 않는다.
  프로세스 단위 탐지는 Phase 8(귀인) 범위다.

#### 주입기에 `manual` 시나리오 추가

GPU 점유 시나리오를 만들려다 방향을 바꿨다. GPU 를 실제로 태우려면 torch(약 2.5GB)나
D3D/OpenGL 직접 호출이 필요한데 시나리오 하나 늘리자고 치를 비용이 아니다. 그리고
**인위로 만든 부하보다 진짜 게임이 더 나은 데이터다** — 실제 사용에서 나오는 열·클럭·
프레임 패턴을 흉내로는 만들 수 없다.

`manual` 은 부하를 만들지 않고 **지금 벌어지는 일에 라벨만 붙인다.**

    python tools/fault_injector.py manual --label GAME --duration 1800

Phase 4 레짐 인식이 요구하는 것도 정확히 이 데이터다.

#### 실시간 연동 — 리플레이와 같은 경로로

오탐률 0.00/h 를 확인한 뒤 붙였다.

- **`argus/detection/live.py` + `replay_source.py`** — 실시간 탐지는 **미래를 따라잡지
  못하는 리플레이어**다. 수집 스레드에서 관측을 따로 조립하지 않는다. 그러면 "평가할 때
  쓰는 관측"과 "실제로 도는 관측"이 두 벌이 되고, 둘이 조금씩 달라지는 순간 스코어보드
  숫자가 거짓말이 된다. 같은 `Replayer`·같은 `Observation`·같은 `run_detector` 를 쓴다.
  대가는 몇 초의 지연인데, 룰의 지속 조건이 30초 이상이라 문제가 되지 않는다.
- 뒤늦게 도착하는 행 때문에 `lag_s`(기본 5초)만큼 물러서서 읽는다. `now` 까지 읽으면
  방금 수집됐지만 아직 안 쓰인 행을 영영 건너뛴다.
- 절전 복귀 시 탐지 상태를 버린다. 지속 시계를 그대로 두면 "3시간 동안 조건이 참이었다"가
  되어 자고 일어난 직후 알림이 터진다 — 실제로는 그 3시간을 보지 못했다.
- **알림은 보내지 않는다.** `anomaly_signals` 에 기록만 한다(`detection.notify: false`).
  무엇을 언제 알릴지는 Phase 9 의 문제다.

실행 확인: 수퍼바이저 컴포넌트로 기동, 베이스라인 1,780틱 워밍, 45초 정상 구간에서 발화 0건.

### Phase 2 — 평가 인프라

**이 단계가 없으면 이후 전부가 감(感)에 의존한다.** 이상 탐지에는 정답이 없어서
탐지기를 만들어도 잘 맞는지 잴 수 없기 때문이다. 그래서 정답을 **만든다** — 결함을
인위로 주입하면 언제부터 언제까지가 이상인지 아는 구간이 생기고, 그것이 유일한 채점
기준이 된다.

#### 추가

- **`argus/eval/replay.py`** — 리플레이 하네스. 저장된 구간을 관측 스트림으로 되돌린다.
  실측 **17만~27만 배속**(358분 → 0.08초), 두 번 재생 결과 동일(결정론).
  시각은 항상 데이터에서 오고, `system_events` 의 공백 구간은 `suspect` 로 표시해 넘긴다.
- **`argus/eval/scoring.py`** — 채점. **채점 단위는 틱이 아니라 알람이다.** 틱으로 세면
  30분 주입에서 매 초 발화한 탐지기가 TP 1,800점을 얻어 긴 주입 하나가 점수를 지배한다.
  사용자가 겪는 단위는 알람 횟수다.
  - TP/FN 은 **구간 단위**("이 문제를 잡았나"), FP 는 **알림 단위**("쓸데없이 몇 번 울렸나").
    단위가 다른 것은 의도한 것이다.
  - 정상 구간 알람 시간을 쿨다운(60초)으로 나눠 FP 를 센다. 이렇게 하지 않으면
    **영원히 발화하는 탐지기가 FP 1건으로 만점을 받는다.**
- **`argus/eval/baselines.py`** — 기준선 탐지기(`always` / `fixed_cpu` / `fixed_mem`).
  일부러 순진하게 만들었다. F1 0.62 가 좋은 점수인지는 비교 대상이 없으면 답할 수 없다.
  `always` 는 재현율 100% 를 공짜로 받는 바보를 스코어보드에 상설 전시해, 재현율만 보고
  모델을 고르면 안 된다는 것을 계속 보여 준다.
- **`argus/eval/__main__.py`** — 스코어보드 CLI. `python -m argus.eval --detector all --save`
- 마이그레이션 `004_evaluation.sql` — `fault_injections`(정답 라벨) / `anomaly_signals`
  (탐지기 판정) / `eval_runs`(스코어보드 이력, 회귀 감시용).

#### 변경 — 결함 주입기를 상대 강도로

첫 실측에서 **주입 구간이 정상 구간보다 조용한** 일이 벌어졌다. 라벨은 있는데 대응하는
열화가 없으면 어떤 탐지기도 정답일 수 없고, Phase 3 튜닝이 통째로 무의미해진다.

| 실측 (12코어 / 64GB / NVMe) | 결과 |
|---|---|
| `cpu_spin --cpu-threads 3` | `cpu_total` 26.8% — 정상 구간 평균 20.7%, 최대 46.3% |
| `disk_thrash` 271MB/s | `disk_resp_ms` 최대 0.3ms, `disk_queue` 최대 1.0 |

- **`machine_profile` 기준 상대 강도로 전환.** `--cpu-threads N` → `--cpu-load 0.75`
  (논리 코어 대비 비율), `--memory-mb` → `--mem-load`, `--disk-mb` → `--disk-load`
  (실측 순차쓰기 대비 배수). CLAUDE.md 설계 규칙 2 를 주입기에도 적용한 것이다.
  4코어 노트북과 12코어 데스크톱에서 같은 세기의 이상을 만들려면 절대량으로는 안 된다.
- **CPU 스핀을 스레드 → 프로세스로.** GIL 때문에 파이썬 스레드로는 CPU 바운드 루프를
  아무리 늘려도 병렬로 안 돈다. 9스레드로 20.9% → 41.2% 였던 것이 9프로세스에서
  20.3% → **95.8%** 가 됐다.
- **디스크 폭주를 다중 워커로.** 단일 스레드 동기 쓰기는 미결 I/O 가 항상 1개라
  **어떤 장비에서도 `disk_queue` 를 못 올린다.** 워커마다 자기 파일을 쓴다(한 파일을
  공유하면 파일 락에 직렬화된다). 0.3 → **600MB/s** 가 장치까지 내려간다.
- **주입기가 스스로 효과를 검증한다.** 주입 전 3초 기준선을 재고, 끝날 때 최대·평균과
  비교해 관측 가능한 열화가 없으면 `[FAIL]` 로 끝나며 라벨을 `completed=0` 으로 남긴다.
  스코어보드는 `completed=1` 만 채점하므로 쓸모없는 라벨이 자동으로 걸러진다.
  이 검증은 도입 첫 실행에서 위 GIL 버그를 잡아냈다.

#### 채점 로직에서 고친 결함 3개

작업 중 실데이터로 돌려 보고 찾은 것들이다. 셋 다 조용히 틀리는 종류였다.

1. **한 알람이 여러 정답 구간에 걸치면 첫 구간에만 귀속시켰다** → `always` 기준선이
   재현율 50% 로 나왔다(정의상 100% 여야 한다). 구간마다 독립적으로 판정하도록 바꿨다.
2. **오탐률 분모에 창 길이를 썼다** → 수집기가 죽어 있던 5.7분까지 "정상이었던 시간"으로
   세어 오탐률이 실제보다 낮게 나왔다. 실제 관측 틱이 덮은 시간으로 바꿨다.
3. **관측이 없는 정답 구간을 FN 으로 셌다** → 탐지기가 못 잡은 게 아니라 볼 것이 없었던
   것이다. `filter_covered()` 로 제외하고, 제외한 사실을 리포트에 드러낸다.

추가로 정상 구간이 30분 미만이면 오탐률을 숫자로 내지 않는다. 90초 관측에서 오탐 2건을
80건/시간으로 환산하는 것은 산술이지 측정이 아니다.

#### DoD 확인

`python -m argus.eval --detector all` 실측 출력:

| 탐지기 | TP | FP | FN | 정밀도 | 재현율 | F1 | 지연 |
|---|---|---|---|---|---|---|---|
| `always` | 7 | 10 | 0 | 41.2% | 100.0% | 0.583 | 0s |
| `fixed_cpu` | 2 | 0 | 5 | 100.0% | 28.6% | 0.444 | 100s |
| `fixed_mem` | 0 | 0 | 7 | 0.0% | 0.0% | 0.000 | — |

Phase 3 룰 엔진은 이 두 줄을 넘어야 채택한다.

#### 장시간 실행 — 하루에 네 번 죽고 알아낸 것

소크 테스트가 네 번 죽었다. 네 번 다 "백그라운드로 띄웠다"고 생각했지만 한 번도
분리된 적이 없었다. 원인이 매번 달랐다.

| 방식 | 죽은 이유 |
|---|---|
| `Start-Process -WindowStyle Hidden` | 부모 세션이 닫히자 같이 죽음(16분). 숨김은 분리가 아니다 |
| 스케줄러 + `.cmd` 래퍼 | 래퍼의 cmd.exe 가 대화형 콘솔에 붙어 Ctrl+C 를 같이 맞음 |
| `DETACHED_PROCESS` + 새 프로세스 그룹 | 콘솔 신호는 막았지만 **job object 상속은 못 막음** |
| 스케줄러 + `.venv\Scripts\pythonw.exe` | uv 의 venv 실행파일은 트램폴린이라 콘솔형 `python.exe` 를 다시 띄움. `0xC000013A`(STATUS_CONTROL_C_EXIT) |

해법은 **작업 스케줄러가 base `pythonw.exe` 를 직접 실행**하는 것이다(`tools/soak_entry.py`).
스케줄러 서비스가 만들므로 개발 셸의 job 밖에 있고, GUI 서브시스템이라 콘솔이 없어
창을 닫을 수도 Ctrl+C 가 닿을 수도 없다. 상세는 `tools/README.md`.

> ⚠️ **Phase 1 의 RSS 장기 추세는 아직 미확정이다.** 위 사망들 때문에 6시간 연속
> 구간을 아직 못 모았다. 현재 실행분(14:13 시작)이 그 측정이다.

### Phase 1 보강 — 절전 복귀 처리와 보존 정리 검증

상주 프로그램은 PC 가 자는 것을 반드시 겪는데, `PLAN.md` 에 그 처리가 아예 없었다.

#### 추가

- **`argus.runtime.gapmon`** — 시간 공백 감지. 벽시계와 단조시계를 함께 보고, **둘 중
  하나라도** 예상 주기보다 크게 튀면 공백으로 판정한다. Windows 에서 단조시계가 절전
  시간을 포함하는지는 버전·API 에 따라 달라 어느 한쪽에 의존할 수 없다. 절전뿐 아니라
  시각 수동 변경·NTP 보정·시각 역행·심한 스톨도 같이 잡힌다.
- **`Collector.on_time_gap()`** — 복귀 훅. 수집기별 처리:
  - `system`: 속도 추적 초기화 + **PDH 질의 재개방**(절전 중 카운터 제공자가 재시작되면
    기존 핸들이 무효가 될 수 있고, 속도형 카운터는 표본을 다시 쌓아야 한다)
  - `process`: **프로세스 목록 조용히 재기준**. 이게 없으면 복귀 직후 수백 건의 생성·종료
    이벤트가 쏟아진다. 실제로는 그 사이를 못 본 것뿐이고, 없던 일을 지어내면 Phase 13
    보안 탐지가 통째로 오작동한다. 공백 사실은 `system_events` 에 남으므로 정보는 잃지 않는다.
  - `network`: pid 재사용 대비 이름 캐시 폐기
- **`Supervisor.broadcast_time_gap()`** — 컴포넌트별 복구 실패를 격리한다.
- 마이그레이션 `003_system_events.sql` — `startup`/`shutdown`/`time_gap` 기록.
  무엇이 언제 끊겼는지는 사후에 복원할 수 없어 그 시점에 남겨야 한다.
  Phase 3 베이스라인과 Phase 8 변화점 탐지가 이 구간을 제외하는 근거가 된다.
- **`tests/test_time_gap.py`** — 시계 기준점을 3시간 뒤로 밀어 절전을 흉내 낸다.
  정상 틱 오인 없음 / 절전·시각변경·역행 구분 / 이벤트 폭주 억제 / 복구 실패 격리.

#### 검증 — 24시간 대신 압축 테스트

24시간 무중단 검증의 목적은 시간 그 자체가 아니라 **그 시간에만 드러나는 것**을 보는
것이다. 보존 기간을 10분으로 압축하면 삭제 경로를 40분 안에 실제로 실행시킬 수 있다.

40분 실행 결과 (보존 10분 압축):

| 항목 | 결과 |
|---|---|
| 보존 삭제 경로 | ✅ 주기 실행, 전 테이블이 10.8분에서 절단 |
| DB 크기 안정화 | ✅ 2.03 → 2.05MB 에서 증가 정지 |
| WAL | ✅ 3.95MB 고정(무한 성장 아님), 정상 종료 시 0으로 체크포인트 |
| 행 수 정상 상태 | ✅ `process_metrics` 19.5k~20.3k 진동 (유입 = 삭제) |
| `drop_count` | ✅ 0 |
| CPU | ✅ 평균 0.187% / 최대 0.333% |
| 핸들 | ✅ 기울기 -3.3/시간 (누수 없음) |
| 쓰기 지연 | 평균 0.27ms / 최대 4.82ms |

⚠️ **RSS 장기 추세는 여전히 미확정.** 40분 시점에 78.0~78.8MB 로, 초기 68MB 에서
올라온 뒤 거의 평탄해졌으나 완만한 상승(+3.7MB/시간)이 남아 있다. 다만 이 기울기는
10분 구간의 노이즈를 외삽한 것이라 신뢰도가 낮다 — **압축 테스트가 `self_telemetry`
까지 10분만 남기도록 설정해 전체 곡선을 볼 수 없었던 것이 테스트 설계의 실수다.**
실제 운영 설정은 자기 계측을 7일 보존하므로 24시간 실행 시 확인 가능하다.

### Phase 1 — 수집 레이어

실제 메트릭을 수집해 SQLite 에 쌓기 시작한다.

#### 추가

- **`argus.collector.system`** — 1Hz 시스템 스냅샷. psutil(사용률·처리량) + PDH(응답시간·큐·실효 클럭).
- **`argus.collector.pdh`** — Windows 성능 카운터. 디스크 응답시간처럼 psutil 로 얻을 수 없는
  "증상" 지표를 담당한다.
- **`argus.collector.procsource`** — 프로세스 스냅샷 소스 2종(PDH / psutil 폴백).
- **`argus.collector.process`** — 전 프로세스 매 틱 수집, 저장은 선별(활성 집합 1초 / 전체 30초).
  프로세스 생성·종료 이벤트 기록.
- **`argus.collector.gpu`** — NVML. 사용률·VRAM·온도·전력·P-State·클럭·**스로틀 사유**.
- **`argus.collector.network`** — 연결 스냅샷(30초). DNS 역조회는 하지 않는다.
- **`argus.storage.queue`** — 상한 도달 시 오래된 것부터 버리는 비블로킹 큐.
- **`argus.storage.writer`** — 200ms/500행 배치 삽입, 종료 시 잔여 데이터 flush.
- **`argus.storage.retention`** — 기한 지난 원본 삭제. VACUUM 은 하지 않는다(우리가 관측 대상인
  디스크에 큰 IO 를 만들어 스스로 이상을 유발한다).
- 마이그레이션 `002_metrics.sql` — `metrics_raw`, `gpu_metrics`, `process_metrics`,
  `process_events`, `net_connections`.

#### 설계 변경 — 계층을 수집이 아니라 저장에 둔다

`PLAN.md` 의 원안은 "전체는 10초마다, 활성 집합만 1초마다 **수집**"이었다. psutil 로는
전체 스캔이 1.4초나 걸려 수집 자체를 아껴야 했기 때문이다.

측정해 보니 전제가 틀렸다. PDH `Process V2` 와일드카드 질의는 전 프로세스를 **13ms** 에
가져온다(psutil 1,380ms 대비 106배). 수집을 아낄 이유가 없어졌다.

비싼 것은 저장이다. 프로세스 330개를 1초마다 넣으면 하루 2,800만 행이다. 그래서 계층을
저장 단계로 옮겼다 — 매 틱 전부 수집하되, 활성 집합만 1초 해상도로 저장하고 전체는 30초마다
저장한다. 전체 그림과 문제 프로세스 추적을 둘 다 얻으면서 행 수는 원안 수준으로 유지된다.

#### 실측으로 잡은 함정 3가지

이 세 가지는 모두 **틀린 채로도 그럴듯한 숫자가 나와** 눈으로는 알아챌 수 없었다.

1. **PDH 카운터 인덱스 하드코딩은 위험하다.** 널리 알려진 값인 `Context Switches/sec = 146`,
   `Avg. Disk sec/Transfer = 208` 을 넣었으나 이 시스템의 실제 값은 각각 **14340, 206** 이었고
   208 은 `Avg. Disk sec/Read` 였다. 그런데도 그럴듯한 수치가 나왔다.
   → 레지스트리의 영문(009) 카운터 목록에서 **이름으로 인덱스를 역조회**한 뒤, 그 인덱스로
   지역화된 이름을 얻어 경로를 조립하도록 바꿨다. 인덱스를 추측하지 않으면서 로케일 독립성도 유지된다.
2. **PDH `Process` (V1) 객체는 프로세스를 대량 누락한다.** 인스턴스 이름이 프로세스명뿐이라
   같은 이름이 여러 개면 합쳐진다. 실측 결과 336개 중 **200개(총 11GB RSS)** 를 놓쳤고,
   하필 chrome·Discord·Steam·claude 처럼 가장 보고 싶은 것들이었다.
   → `Process V2`(인스턴스명이 `name:pid`)를 쓴다. 커버리지 336/336, 누락 0.
3. **PDH 는 백분율을 100 에서 자른다.** `NOCAP100` 없이 읽으면 1코어 넘게 쓰는 프로세스가
   전부 100 으로 보고된다(실측: Idle 이 capped 100 / nocap 976). 기여도 계산(Phase 8)의
   근간이 조용히 망가질 뻔했다.

#### 결정

- **쓰지 않는 인덱스는 만들지 않는다.** `process_metrics(name, ts)` 인덱스가 데이터 172KB 에
  대해 98KB(DB 의 21%)를 차지했는데, 이를 쓰는 질의는 Phase 6 이전에는 없다. 이 테이블은
  보존 기한이 24시간이라 나중에 추가해도 재구축이 빠르다.
- **DNS 역조회를 하지 않는다.** 연결마다 이름을 물으면 그 자체가 네트워크 트래픽을 만들어
  관측 대상을 오염시키고, 조회 기록이 외부에 남는다.
- **두 프로세스 소스의 메모리 지표는 완전히 같지 않다.** PDH 는 `Working Set - Private`,
  psutil 폴백은 `Private Bytes` 다(실측 152MB vs 236MB). Windows 가 대응하는 값을 psutil 로
  싸게 주지 않는다. 한 머신에서 소스는 바뀌지 않으므로 베이스라인 일관성은 유지되며,
  머신 간 절대값 비교는 어차피 하지 않는다(하드웨어 무가정 원칙).

### Phase 0 — 골격과 자기 계측

첫 실행 가능한 형태. 아직 시스템 메트릭은 수집하지 않고 **Argus 자신만 관측한다.**
모니터가 병목이 되는 것이 이런 도구의 1순위 실패 모드라, 수집기보다 자기 계측을 먼저 만들었다.

#### 추가

- **`argus.paths`** — `%APPDATA%\Argus` 데이터 경로, PyInstaller `_MEIPASS` 대응 리소스 경로.
  `ARGUS_DATA_DIR` 로 덮어쓸 수 있어 개발 중 실사용 데이터를 건드리지 않는다.
- **`argus.logging_setup`** — 콘솔(사람용) + JSON Lines 파일(기계용) 이중 출력, 회전 로그,
  크래시 별도 파일 기록. 한국어 Windows 콘솔(cp949)에서 한글이 깨지지 않도록 UTF-8 로 전환.
- **`argus.config`** — 기본값 → 사용자 `settings.yaml` → 환경변수(`ARGUS_*`) 순 병합,
  pydantic 검증. 첫 실행 시 주석 포함 설정 파일을 사용자 폴더에 생성한다.
- **`argus.machine.capabilities`** — 관리자 권한·psutil·PDH·NVML·ETW·서명검증 가용성 탐지.
  없으면 이유(`reason`)와 함께 기록해 조용히 실패하지 않는다.
- **`argus.machine.calibration`** — CPU/메모리/디스크 짧은 벤치(약 3초)로 `machine_profile.json`
  생성. 하드웨어를 가정하지 않기 위한 기준선으로, 하드웨어 시그니처가 바뀌면 자동 재측정.
- **`argus.storage.hot`** — SQLite WAL, 번호순 SQL 마이그레이션 프레임(`PRAGMA user_version`),
  `meta`·`self_telemetry` 테이블.
- **`argus.runtime.supervisor`** — 컴포넌트 스레드 생명주기, 에러 격리(하나가 죽어도 나머지 지속),
  지수 백오프, 시그널 기반 정상 종료.
- **`argus.runtime.budget`** — 자기 CPU/RSS 예산 감시, 히스테리시스가 있는 4단계 스로틀.
  `cpu_percent` 를 논리 코어 수로 정규화해 "머신 전체의 N%" 의미가 되게 했다.
- **`argus.runtime.selftel`** — 자기 계측을 `self_telemetry` 에 기록 (핸들 수 포함 — Windows 에서
  핸들 누수는 메모리보다 먼저 드러난다).
- **`argus.__main__`** — `python -m argus`, `--check`, `--duration`, `--recalibrate`.
- **`tests/test_shutdown.py`** — 실제 시그널을 보내 정상 종료와 DB 무결성을 검증.

#### 결정

- **Python 3.12 고정** (`>=3.12,<3.13`). 시스템 기본은 3.14 였으나 PyTorch·river 지원이
  불확실해 Phase 7 에서 막힐 위험이 있어 회피했다. `PLAN.md` 의 3.11 표기도 3.12 로 수정.
- **스키마 마이그레이션을 첫 테이블과 함께 도입.** 배포 후에는 스키마 변경이 "남의 PC 에 있는
  DB 를 고치는 일"이 되므로 나중에 붙일 수 없다.
- **전체 스키마 파일을 두지 않는다.** 정본은 `migrations/NNN_*.sql` 뿐. 두 벌이 되면 어긋난다.

#### 수정

- 시그널 핸들러가 `_stop` 을 세운 뒤 `Supervisor.stop()` 이 조기 반환해 **스레드 join 과
  `teardown()` 이 건너뛰어지던 문제.** 데몬 스레드라 프로세스는 종료됐지만 진행 중인 DB 쓰기를
  기다리지 않았다. "종료 요청"과 "정리 완료"를 별도 플래그로 분리. (`tests/test_shutdown.py` 가 발견)

#### 측정 (Phase 0 완료 기준)

60초 상주 실행 기준 — AMD 6C/12T · 64GB · SSD:

| 항목 | 실측 | 예산 |
|---|---|---|
| CPU | 평균 0.013% / 최대 0.050% | 2.0% |
| RSS | 평균 67.5MB | 300MB |
| 기록 간격 | 평균 5.009초 (설정 5초) | — |
| 핸들 | 281 → 286 (안정) | 누수 없음 |
| `drop_count` | 0 | 0 |

24시간 연속 가동 검증은 Phase 1 에서 실제 수집기를 붙인 뒤 함께 수행한다.
