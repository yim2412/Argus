# 변경 이력

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고,
버전은 [유의적 버전](https://semver.org/lang/ko/)을 따른다.

배포 대상 프로그램이라 첫 코드 커밋부터 이력을 남긴다.

## [Unreleased]

### Phase 3 — 통계 베이스라인 + 룰 엔진 (진행 중)

절대 임계값은 배포 프로그램에서 쓸 수 없다. "디스크 응답 50ms 이상은 병목"은 HDD
사용자에게 평소이고, "CPU 85%"는 12코어 PC 에서 좀처럼 닿지 않는다. **이 PC 의 평소
대비 얼마나 벗어났는가**로 말한다.

#### 추가

- **`argus/detection/expr.py`** — 룰 표현식 평가기. `eval()` 을 쓰지 않는다.
  룰 파일은 사용자가 편집하고 나중에는 남이 만든 룰을 받게 되는데, `eval()` 이면
  `__import__('os').system(...)` 한 줄로 성능 모니터가 실행 통로가 된다. `ast.parse`
  로 트리를 만들고 **허용한 노드만** 통과시킨다(화이트리스트라 새 문법도 자동 차단).
  회귀 방지로 11가지 공격 표현식을 pytest 에 고정했다.
- **`argus/detection/baseline.py`** — 롤링 중앙값/MAD 기반 베이스라인.
  - **평균/표준편차를 쓰지 않는다.** 이상 구간이 베이스라인에 섞이면 평균이 끌려가고,
    오염된 평균으로 다음 이상을 재면 점점 둔감해진다. 30% 오염 실측에서 중앙값은
    20 → 21.7 로 버텼다.
  - **산포가 없는 메트릭은 판정하지 않는다**(`degenerate`). 내내 0 인 `disk_queue` 에
    억지로 바닥값을 깔면 0 → 1 이 z 수십으로 튀어 오탐 폭풍이 된다. 이런 지표는
    룰에서 절대 임계값으로 다룬다 — 큐 길이는 단위 자체가 하드웨어에 독립적이다.
- **`argus/detection/rules.py` + `argus/config/rules.yaml`** — 룰 엔진과 기본 룰 9개.
  모든 룰이 지속 조건(`for`)과 쿨다운을 **강제로** 갖는다. GPU 지표는 `obs.gpus` 에서
  `gpu_*` 로 펼쳐 룰이 볼 수 있게 한다.
- **`argus/detection/registry.py`** — 탐지기 이름 표를 한 곳에. 스코어보드와 실시간이
  같은 표를 봐야 "평가한 것"과 "실제로 도는 것"이 갈라지지 않는다.

#### 계획서와 다르게 간 것 — 재시작 후 부트스트랩 폐지

`PLAN.md` 는 "부트스트랩 첫 2시간 탐지 비활성"이었다. 그대로 하면 PC 를 켤 때마다
2시간 동안 아무것도 못 잡는데, 사용자는 PC 를 매일 끈다.

베이스라인을 메모리에만 두지 않고 **시작할 때 `metrics_raw` 에서 최근 30분을 읽어
즉시 채운다.** 데이터는 이미 DB 에 있으므로 별도 상태 테이블도 마이그레이션도 필요
없다. 실측 **1,172틱을 60ms 에 채우고 곧바로 `ready`**. 2시간 부트스트랩은 진짜 첫
설치일 때만 남는다.

#### 룰이 상대 조건과 절대 조건을 함께 갖는 이유

상대값만 쓰면 조용한 PC 에서 오탐이 쏟아진다. 하루 종일 CPU 5% 인 사무용 PC 는
중앙값 5 / σ 0.5 라서 "5σ"가 겨우 7.5% 다. 절대값만 쓰면 하드웨어를 가정하게 된다.
그래서 둘 다 요구한다 — **평소와 다르고(상대), 실제로 아플 만큼 높다(절대).**

예외가 하나 있다. **메모리 누수는 아파지기 전에 잡아야 의미가 있다.** 64GB PC 에서
누수가 18%p 를 먹어도 사용률은 46% 라 압박 룰(80%)에 안 걸린다. "메모리 이상 증가"
룰을 따로 두고, 절대 조건을 사용률이 아니라 **평소 대비 증가폭(%p)** 으로 잡았다.

#### 채점 결과 — 기준선을 넘었다

| 탐지기 | TP | FP | FN | 정밀도 | 재현율 | F1 | 지연 |
|---|---|---|---|---|---|---|---|
| **`rules`** | **4** | **0** | 5 | **100.0%** | **44.4%** | **0.615** | 224s |
| `always` | 9 | 28 | 0 | 24.3% | 100.0% | 0.391 | 0s |
| `fixed_cpu` | 2 | 0 | 7 | 100.0% | 22.2% | 0.364 | 100s |
| `fixed_mem` | 0 | 0 | 9 | 0.0% | 0.0% | 0.000 | — |

#### 고친 것 — 테스트 설계와 계측 오류

처음 채점에서 `rules` 가 F1 0.444 로 기준선과 동률이었다. 원인을 파고들어 보니
룰이 아니라 **검증 방법이 틀린 경우가 섞여 있었다.**

- **90초 램프는 "서서히 나빠짐"이 아니다.** 계획서가 말하는 점진적 열화는 30~90분
  짜리다. 90초 램프는 임계 위에 22초만 머물러 지속 조건을 못 채운다. 지속 조건을
  줄이면 숫자는 오르지만 그건 튜닝이 아니라 조작이다. **5분 램프로 시나리오를 고쳤고**,
  그때 F1 이 0.444 → 0.615 가 됐다.
- **`soak_entry.py` 가 PDH 를 죽이고 있었다.** `sys.path.insert` 로는 `.pth` 파일이
  실행되지 않아 pywin32 가 import 되지 않았고, 프로세스 수집이 psutil 폴백(15초 주기)
  으로 내려가며 `disk_resp_ms`·`disk_queue` 가 전부 NULL 로 쌓였다. `site.addsitedir()`
  로 고치고, 진입점이 의존성을 직접 확인하도록 했다.
- **주입기의 디스크 효과 판정을 처리량 → 응답시간으로 바꿨다.** 600MB/s 를 퍼부어도
  NVMe 가 삼키면 사용자는 아무것도 못 느끼고, 탐지기가 안 잡는 게 맞다(증상 없는
  원인은 알릴 가치가 없다). 처음엔 `psutil` 의 `write_time` 을 썼는데 **Windows 에서
  사실상 0 만 준다**(2초에 96회 쓰기, 델타 0ms). 그 값으로 "이 PC 는 디스크 병목을
  만들 수 없다"고 결론 낼 뻔했다. Argus 가 이미 PDH 로 읽는 `disk_resp_ms` 로 교체한 뒤
  다시 재니 0.1ms → 0.2ms — **결론은 같았지만 이제 근거가 있다.**

#### DoD 미충족 — 아직 Phase 3 완료가 아니다

**DoD**: 시나리오 6개 중 4개 이상 탐지, 정상 구간 오탐 < 1/h.

| 항목 | 상태 |
|---|---|
| 기준선 초과 | ✅ F1 0.615 > 0.391 / 0.364 |
| 오탐 0건 | ✅ FP 0 (정밀도 100%) |
| 오탐률 < 1/h | ⚠️ **미측정.** 정상 구간 26분으로 30분 문턱 미달 |
| 시나리오 4개 이상 | ❌ **2종만.** `cpu_spin`·`memory_leak` 만 탐지 |

남은 두 시나리오는 이유가 다르다.

- **`disk_thrash`**: 이 NVMe 에서 응답시간이 0.1 → 0.2ms 밖에 안 올라 **증상 자체가
  없다.** 라벨이 `completed=0` 으로 자동 제외된다. 룰의 문제가 아니라 하드웨어 특성이고,
  SATA·HDD 장비에서 확인해야 한다.
- **`handle_leak`**: 핸들 수는 프로세스별 지표라 시스템 메트릭 룰로는 닿지 않는다.
  프로세스 단위 탐지는 Phase 8(귀인) 범위다.

**실시간 연동은 아직 하지 않았다.** 오탐률이 확인되기 전에 붙이면 사용자에게 알림이
갈 수 있고, 그건 되돌릴 수 없다. 소크가 정상 구간 30분을 채우면 재채점 후 붙인다.

### Phase 2 — 평가 인프라

**이 단계가 없으면 이후 전부가 감(感)에 의존한다.** 이상 탐지에는 정답이 없어서
탐지기를 만들어도 잘 맞는지 잴 수 없기 때문이다. 그래서 정답을 **만든다** — 결함을
인위로 주입하면 언제부터 언제까지가 이상인지 아는 구간이 생기고, 그것이 유일한 채점
기준이 된다.

#### 추가

- **`argus/eval/replay.py`** — 리플레이 하네스. 저장된 구간을 관측 스트림으로 되돌린다.
  실측 **17만~27만 배속**(358분 → 0.08초), 두 번 재생 결과 동일(결정론).
  시각은 항상 데이터에서 오고, `system_events` 의 공백 구간은 `suspect` 로 표시해 넘긴다.
- **`argus/eval/scoring.py`** — 채점. **채점 단위는 틱이 아니라 알람이다.** 틱으로 세면
  30분 주입에서 매 초 발화한 탐지기가 TP 1,800점을 얻어 긴 주입 하나가 점수를 지배한다.
  사용자가 겪는 단위는 알람 횟수다.
  - TP/FN 은 **구간 단위**("이 문제를 잡았나"), FP 는 **알림 단위**("쓸데없이 몇 번 울렸나").
    단위가 다른 것은 의도한 것이다.
  - 정상 구간 알람 시간을 쿨다운(60초)으로 나눠 FP 를 센다. 이렇게 하지 않으면
    **영원히 발화하는 탐지기가 FP 1건으로 만점을 받는다.**
- **`argus/eval/baselines.py`** — 기준선 탐지기(`always` / `fixed_cpu` / `fixed_mem`).
  일부러 순진하게 만들었다. F1 0.62 가 좋은 점수인지는 비교 대상이 없으면 답할 수 없다.
  `always` 는 재현율 100% 를 공짜로 받는 바보를 스코어보드에 상설 전시해, 재현율만 보고
  모델을 고르면 안 된다는 것을 계속 보여 준다.
- **`argus/eval/__main__.py`** — 스코어보드 CLI. `python -m argus.eval --detector all --save`
- 마이그레이션 `004_evaluation.sql` — `fault_injections`(정답 라벨) / `anomaly_signals`
  (탐지기 판정) / `eval_runs`(스코어보드 이력, 회귀 감시용).

#### 변경 — 결함 주입기를 상대 강도로

첫 실측에서 **주입 구간이 정상 구간보다 조용한** 일이 벌어졌다. 라벨은 있는데 대응하는
열화가 없으면 어떤 탐지기도 정답일 수 없고, Phase 3 튜닝이 통째로 무의미해진다.

| 실측 (12코어 / 64GB / NVMe) | 결과 |
|---|---|
| `cpu_spin --cpu-threads 3` | `cpu_total` 26.8% — 정상 구간 평균 20.7%, 최대 46.3% |
| `disk_thrash` 271MB/s | `disk_resp_ms` 최대 0.3ms, `disk_queue` 최대 1.0 |

- **`machine_profile` 기준 상대 강도로 전환.** `--cpu-threads N` → `--cpu-load 0.75`
  (논리 코어 대비 비율), `--memory-mb` → `--mem-load`, `--disk-mb` → `--disk-load`
  (실측 순차쓰기 대비 배수). CLAUDE.md 설계 규칙 2 를 주입기에도 적용한 것이다.
  4코어 노트북과 12코어 데스크톱에서 같은 세기의 이상을 만들려면 절대량으로는 안 된다.
- **CPU 스핀을 스레드 → 프로세스로.** GIL 때문에 파이썬 스레드로는 CPU 바운드 루프를
  아무리 늘려도 병렬로 안 돈다. 9스레드로 20.9% → 41.2% 였던 것이 9프로세스에서
  20.3% → **95.8%** 가 됐다.
- **디스크 폭주를 다중 워커로.** 단일 스레드 동기 쓰기는 미결 I/O 가 항상 1개라
  **어떤 장비에서도 `disk_queue` 를 못 올린다.** 워커마다 자기 파일을 쓴다(한 파일을
  공유하면 파일 락에 직렬화된다). 0.3 → **600MB/s** 가 장치까지 내려간다.
- **주입기가 스스로 효과를 검증한다.** 주입 전 3초 기준선을 재고, 끝날 때 최대·평균과
  비교해 관측 가능한 열화가 없으면 `[FAIL]` 로 끝나며 라벨을 `completed=0` 으로 남긴다.
  스코어보드는 `completed=1` 만 채점하므로 쓸모없는 라벨이 자동으로 걸러진다.
  이 검증은 도입 첫 실행에서 위 GIL 버그를 잡아냈다.

#### 채점 로직에서 고친 결함 3개

작업 중 실데이터로 돌려 보고 찾은 것들이다. 셋 다 조용히 틀리는 종류였다.

1. **한 알람이 여러 정답 구간에 걸치면 첫 구간에만 귀속시켰다** → `always` 기준선이
   재현율 50% 로 나왔다(정의상 100% 여야 한다). 구간마다 독립적으로 판정하도록 바꿨다.
2. **오탐률 분모에 창 길이를 썼다** → 수집기가 죽어 있던 5.7분까지 "정상이었던 시간"으로
   세어 오탐률이 실제보다 낮게 나왔다. 실제 관측 틱이 덮은 시간으로 바꿨다.
3. **관측이 없는 정답 구간을 FN 으로 셌다** → 탐지기가 못 잡은 게 아니라 볼 것이 없었던
   것이다. `filter_covered()` 로 제외하고, 제외한 사실을 리포트에 드러낸다.

추가로 정상 구간이 30분 미만이면 오탐률을 숫자로 내지 않는다. 90초 관측에서 오탐 2건을
80건/시간으로 환산하는 것은 산술이지 측정이 아니다.

#### DoD 확인

`python -m argus.eval --detector all` 실측 출력:

| 탐지기 | TP | FP | FN | 정밀도 | 재현율 | F1 | 지연 |
|---|---|---|---|---|---|---|---|
| `always` | 7 | 10 | 0 | 41.2% | 100.0% | 0.583 | 0s |
| `fixed_cpu` | 2 | 0 | 5 | 100.0% | 28.6% | 0.444 | 100s |
| `fixed_mem` | 0 | 0 | 7 | 0.0% | 0.0% | 0.000 | — |

Phase 3 룰 엔진은 이 두 줄을 넘어야 채택한다.

#### 장시간 실행 — 하루에 네 번 죽고 알아낸 것

소크 테스트가 네 번 죽었다. 네 번 다 "백그라운드로 띄웠다"고 생각했지만 한 번도
분리된 적이 없었다. 원인이 매번 달랐다.

| 방식 | 죽은 이유 |
|---|---|
| `Start-Process -WindowStyle Hidden` | 부모 세션이 닫히자 같이 죽음(16분). 숨김은 분리가 아니다 |
| 스케줄러 + `.cmd` 래퍼 | 래퍼의 cmd.exe 가 대화형 콘솔에 붙어 Ctrl+C 를 같이 맞음 |
| `DETACHED_PROCESS` + 새 프로세스 그룹 | 콘솔 신호는 막았지만 **job object 상속은 못 막음** |
| 스케줄러 + `.venv\Scripts\pythonw.exe` | uv 의 venv 실행파일은 트램폴린이라 콘솔형 `python.exe` 를 다시 띄움. `0xC000013A`(STATUS_CONTROL_C_EXIT) |

해법은 **작업 스케줄러가 base `pythonw.exe` 를 직접 실행**하는 것이다(`tools/soak_entry.py`).
스케줄러 서비스가 만들므로 개발 셸의 job 밖에 있고, GUI 서브시스템이라 콘솔이 없어
창을 닫을 수도 Ctrl+C 가 닿을 수도 없다. 상세는 `tools/README.md`.

> ⚠️ **Phase 1 의 RSS 장기 추세는 아직 미확정이다.** 위 사망들 때문에 6시간 연속
> 구간을 아직 못 모았다. 현재 실행분(14:13 시작)이 그 측정이다.

### Phase 1 보강 — 절전 복귀 처리와 보존 정리 검증

상주 프로그램은 PC 가 자는 것을 반드시 겪는데, `PLAN.md` 에 그 처리가 아예 없었다.

#### 추가

- **`argus.runtime.gapmon`** — 시간 공백 감지. 벽시계와 단조시계를 함께 보고, **둘 중
  하나라도** 예상 주기보다 크게 튀면 공백으로 판정한다. Windows 에서 단조시계가 절전
  시간을 포함하는지는 버전·API 에 따라 달라 어느 한쪽에 의존할 수 없다. 절전뿐 아니라
  시각 수동 변경·NTP 보정·시각 역행·심한 스톨도 같이 잡힌다.
- **`Collector.on_time_gap()`** — 복귀 훅. 수집기별 처리:
  - `system`: 속도 추적 초기화 + **PDH 질의 재개방**(절전 중 카운터 제공자가 재시작되면
    기존 핸들이 무효가 될 수 있고, 속도형 카운터는 표본을 다시 쌓아야 한다)
  - `process`: **프로세스 목록 조용히 재기준**. 이게 없으면 복귀 직후 수백 건의 생성·종료
    이벤트가 쏟아진다. 실제로는 그 사이를 못 본 것뿐이고, 없던 일을 지어내면 Phase 13
    보안 탐지가 통째로 오작동한다. 공백 사실은 `system_events` 에 남으므로 정보는 잃지 않는다.
  - `network`: pid 재사용 대비 이름 캐시 폐기
- **`Supervisor.broadcast_time_gap()`** — 컴포넌트별 복구 실패를 격리한다.
- 마이그레이션 `003_system_events.sql` — `startup`/`shutdown`/`time_gap` 기록.
  무엇이 언제 끊겼는지는 사후에 복원할 수 없어 그 시점에 남겨야 한다.
  Phase 3 베이스라인과 Phase 8 변화점 탐지가 이 구간을 제외하는 근거가 된다.
- **`tests/test_time_gap.py`** — 시계 기준점을 3시간 뒤로 밀어 절전을 흉내 낸다.
  정상 틱 오인 없음 / 절전·시각변경·역행 구분 / 이벤트 폭주 억제 / 복구 실패 격리.

#### 검증 — 24시간 대신 압축 테스트

24시간 무중단 검증의 목적은 시간 그 자체가 아니라 **그 시간에만 드러나는 것**을 보는
것이다. 보존 기간을 10분으로 압축하면 삭제 경로를 40분 안에 실제로 실행시킬 수 있다.

40분 실행 결과 (보존 10분 압축):

| 항목 | 결과 |
|---|---|
| 보존 삭제 경로 | ✅ 주기 실행, 전 테이블이 10.8분에서 절단 |
| DB 크기 안정화 | ✅ 2.03 → 2.05MB 에서 증가 정지 |
| WAL | ✅ 3.95MB 고정(무한 성장 아님), 정상 종료 시 0으로 체크포인트 |
| 행 수 정상 상태 | ✅ `process_metrics` 19.5k~20.3k 진동 (유입 = 삭제) |
| `drop_count` | ✅ 0 |
| CPU | ✅ 평균 0.187% / 최대 0.333% |
| 핸들 | ✅ 기울기 -3.3/시간 (누수 없음) |
| 쓰기 지연 | 평균 0.27ms / 최대 4.82ms |

⚠️ **RSS 장기 추세는 여전히 미확정.** 40분 시점에 78.0~78.8MB 로, 초기 68MB 에서
올라온 뒤 거의 평탄해졌으나 완만한 상승(+3.7MB/시간)이 남아 있다. 다만 이 기울기는
10분 구간의 노이즈를 외삽한 것이라 신뢰도가 낮다 — **압축 테스트가 `self_telemetry`
까지 10분만 남기도록 설정해 전체 곡선을 볼 수 없었던 것이 테스트 설계의 실수다.**
실제 운영 설정은 자기 계측을 7일 보존하므로 24시간 실행 시 확인 가능하다.

### Phase 1 — 수집 레이어

실제 메트릭을 수집해 SQLite 에 쌓기 시작한다.

#### 추가

- **`argus.collector.system`** — 1Hz 시스템 스냅샷. psutil(사용률·처리량) + PDH(응답시간·큐·실효 클럭).
- **`argus.collector.pdh`** — Windows 성능 카운터. 디스크 응답시간처럼 psutil 로 얻을 수 없는
  "증상" 지표를 담당한다.
- **`argus.collector.procsource`** — 프로세스 스냅샷 소스 2종(PDH / psutil 폴백).
- **`argus.collector.process`** — 전 프로세스 매 틱 수집, 저장은 선별(활성 집합 1초 / 전체 30초).
  프로세스 생성·종료 이벤트 기록.
- **`argus.collector.gpu`** — NVML. 사용률·VRAM·온도·전력·P-State·클럭·**스로틀 사유**.
- **`argus.collector.network`** — 연결 스냅샷(30초). DNS 역조회는 하지 않는다.
- **`argus.storage.queue`** — 상한 도달 시 오래된 것부터 버리는 비블로킹 큐.
- **`argus.storage.writer`** — 200ms/500행 배치 삽입, 종료 시 잔여 데이터 flush.
- **`argus.storage.retention`** — 기한 지난 원본 삭제. VACUUM 은 하지 않는다(우리가 관측 대상인
  디스크에 큰 IO 를 만들어 스스로 이상을 유발한다).
- 마이그레이션 `002_metrics.sql` — `metrics_raw`, `gpu_metrics`, `process_metrics`,
  `process_events`, `net_connections`.

#### 설계 변경 — 계층을 수집이 아니라 저장에 둔다

`PLAN.md` 의 원안은 "전체는 10초마다, 활성 집합만 1초마다 **수집**"이었다. psutil 로는
전체 스캔이 1.4초나 걸려 수집 자체를 아껴야 했기 때문이다.

측정해 보니 전제가 틀렸다. PDH `Process V2` 와일드카드 질의는 전 프로세스를 **13ms** 에
가져온다(psutil 1,380ms 대비 106배). 수집을 아낄 이유가 없어졌다.

비싼 것은 저장이다. 프로세스 330개를 1초마다 넣으면 하루 2,800만 행이다. 그래서 계층을
저장 단계로 옮겼다 — 매 틱 전부 수집하되, 활성 집합만 1초 해상도로 저장하고 전체는 30초마다
저장한다. 전체 그림과 문제 프로세스 추적을 둘 다 얻으면서 행 수는 원안 수준으로 유지된다.

#### 실측으로 잡은 함정 3가지

이 세 가지는 모두 **틀린 채로도 그럴듯한 숫자가 나와** 눈으로는 알아챌 수 없었다.

1. **PDH 카운터 인덱스 하드코딩은 위험하다.** 널리 알려진 값인 `Context Switches/sec = 146`,
   `Avg. Disk sec/Transfer = 208` 을 넣었으나 이 시스템의 실제 값은 각각 **14340, 206** 이었고
   208 은 `Avg. Disk sec/Read` 였다. 그런데도 그럴듯한 수치가 나왔다.
   → 레지스트리의 영문(009) 카운터 목록에서 **이름으로 인덱스를 역조회**한 뒤, 그 인덱스로
   지역화된 이름을 얻어 경로를 조립하도록 바꿨다. 인덱스를 추측하지 않으면서 로케일 독립성도 유지된다.
2. **PDH `Process` (V1) 객체는 프로세스를 대량 누락한다.** 인스턴스 이름이 프로세스명뿐이라
   같은 이름이 여러 개면 합쳐진다. 실측 결과 336개 중 **200개(총 11GB RSS)** 를 놓쳤고,
   하필 chrome·Discord·Steam·claude 처럼 가장 보고 싶은 것들이었다.
   → `Process V2`(인스턴스명이 `name:pid`)를 쓴다. 커버리지 336/336, 누락 0.
3. **PDH 는 백분율을 100 에서 자른다.** `NOCAP100` 없이 읽으면 1코어 넘게 쓰는 프로세스가
   전부 100 으로 보고된다(실측: Idle 이 capped 100 / nocap 976). 기여도 계산(Phase 8)의
   근간이 조용히 망가질 뻔했다.

#### 결정

- **쓰지 않는 인덱스는 만들지 않는다.** `process_metrics(name, ts)` 인덱스가 데이터 172KB 에
  대해 98KB(DB 의 21%)를 차지했는데, 이를 쓰는 질의는 Phase 6 이전에는 없다. 이 테이블은
  보존 기한이 24시간이라 나중에 추가해도 재구축이 빠르다.
- **DNS 역조회를 하지 않는다.** 연결마다 이름을 물으면 그 자체가 네트워크 트래픽을 만들어
  관측 대상을 오염시키고, 조회 기록이 외부에 남는다.
- **두 프로세스 소스의 메모리 지표는 완전히 같지 않다.** PDH 는 `Working Set - Private`,
  psutil 폴백은 `Private Bytes` 다(실측 152MB vs 236MB). Windows 가 대응하는 값을 psutil 로
  싸게 주지 않는다. 한 머신에서 소스는 바뀌지 않으므로 베이스라인 일관성은 유지되며,
  머신 간 절대값 비교는 어차피 하지 않는다(하드웨어 무가정 원칙).

### Phase 0 — 골격과 자기 계측

첫 실행 가능한 형태. 아직 시스템 메트릭은 수집하지 않고 **Argus 자신만 관측한다.**
모니터가 병목이 되는 것이 이런 도구의 1순위 실패 모드라, 수집기보다 자기 계측을 먼저 만들었다.

#### 추가

- **`argus.paths`** — `%APPDATA%\Argus` 데이터 경로, PyInstaller `_MEIPASS` 대응 리소스 경로.
  `ARGUS_DATA_DIR` 로 덮어쓸 수 있어 개발 중 실사용 데이터를 건드리지 않는다.
- **`argus.logging_setup`** — 콘솔(사람용) + JSON Lines 파일(기계용) 이중 출력, 회전 로그,
  크래시 별도 파일 기록. 한국어 Windows 콘솔(cp949)에서 한글이 깨지지 않도록 UTF-8 로 전환.
- **`argus.config`** — 기본값 → 사용자 `settings.yaml` → 환경변수(`ARGUS_*`) 순 병합,
  pydantic 검증. 첫 실행 시 주석 포함 설정 파일을 사용자 폴더에 생성한다.
- **`argus.machine.capabilities`** — 관리자 권한·psutil·PDH·NVML·ETW·서명검증 가용성 탐지.
  없으면 이유(`reason`)와 함께 기록해 조용히 실패하지 않는다.
- **`argus.machine.calibration`** — CPU/메모리/디스크 짧은 벤치(약 3초)로 `machine_profile.json`
  생성. 하드웨어를 가정하지 않기 위한 기준선으로, 하드웨어 시그니처가 바뀌면 자동 재측정.
- **`argus.storage.hot`** — SQLite WAL, 번호순 SQL 마이그레이션 프레임(`PRAGMA user_version`),
  `meta`·`self_telemetry` 테이블.
- **`argus.runtime.supervisor`** — 컴포넌트 스레드 생명주기, 에러 격리(하나가 죽어도 나머지 지속),
  지수 백오프, 시그널 기반 정상 종료.
- **`argus.runtime.budget`** — 자기 CPU/RSS 예산 감시, 히스테리시스가 있는 4단계 스로틀.
  `cpu_percent` 를 논리 코어 수로 정규화해 "머신 전체의 N%" 의미가 되게 했다.
- **`argus.runtime.selftel`** — 자기 계측을 `self_telemetry` 에 기록 (핸들 수 포함 — Windows 에서
  핸들 누수는 메모리보다 먼저 드러난다).
- **`argus.__main__`** — `python -m argus`, `--check`, `--duration`, `--recalibrate`.
- **`tests/test_shutdown.py`** — 실제 시그널을 보내 정상 종료와 DB 무결성을 검증.

#### 결정

- **Python 3.12 고정** (`>=3.12,<3.13`). 시스템 기본은 3.14 였으나 PyTorch·river 지원이
  불확실해 Phase 7 에서 막힐 위험이 있어 회피했다. `PLAN.md` 의 3.11 표기도 3.12 로 수정.
- **스키마 마이그레이션을 첫 테이블과 함께 도입.** 배포 후에는 스키마 변경이 "남의 PC 에 있는
  DB 를 고치는 일"이 되므로 나중에 붙일 수 없다.
- **전체 스키마 파일을 두지 않는다.** 정본은 `migrations/NNN_*.sql` 뿐. 두 벌이 되면 어긋난다.

#### 수정

- 시그널 핸들러가 `_stop` 을 세운 뒤 `Supervisor.stop()` 이 조기 반환해 **스레드 join 과
  `teardown()` 이 건너뛰어지던 문제.** 데몬 스레드라 프로세스는 종료됐지만 진행 중인 DB 쓰기를
  기다리지 않았다. "종료 요청"과 "정리 완료"를 별도 플래그로 분리. (`tests/test_shutdown.py` 가 발견)

#### 측정 (Phase 0 완료 기준)

60초 상주 실행 기준 — AMD 6C/12T · 64GB · SSD:

| 항목 | 실측 | 예산 |
|---|---|---|
| CPU | 평균 0.013% / 최대 0.050% | 2.0% |
| RSS | 평균 67.5MB | 300MB |
| 기록 간격 | 평균 5.009초 (설정 5초) | — |
| 핸들 | 281 → 286 (안정) | 누수 없음 |
| `drop_count` | 0 | 0 |

24시간 연속 가동 검증은 Phase 1 에서 실제 수집기를 붙인 뒤 함께 수행한다.
