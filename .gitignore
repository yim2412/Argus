# 파이썬
__pycache__/
*.py[cod]
.venv/
venv/

# PyInstaller 산출물
build/
dist/
*.spec
!packaging/*.spec

# 수집 데이터 — 프로세스명·실행경로·네트워크 목적지 등 개인정보가 들어있다. 절대 커밋 금지
data/
warm/
# ARGUS_DATA_DIR 로 지정하는 개발용 데이터 폴더
# (주의: .gitignore 는 줄 끝 주석을 지원하지 않는다. 반드시 별도 줄에 쓸 것)
.devdata/
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
*.parquet
machine_profile.json
capabilities.json

# 모델 아티팩트
models/
*.onnx
*.pt
*.pth
*.pkl
*.joblib

# 평가 산출물
eval_runs/
reports/

# 임시/작업 파일
_build_start.txt
_bgtask.meta
*.log
crash_*.json

# 에디터/OS
.vscode/
.idea/
Thumbs.db
desktop.ini

# uv add 부산물. 빌드 도구가 만드는 것이라 소스가 아니다.
argus.egg-info/
uv.lock
