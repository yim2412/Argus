# Argus — 구현 계획서 (v2)

> `Argus.md`의 스펙을 실제 구현 가능한 단계로 재설계한 문서.
> 원본 대비 추가된 축: **평가 인프라 우선**, **활동 레짐 조건부 탐지**, **체감 성능 지표**, **귀인(attribution) 엔진**.
>
> **여기에는 남은 일만 둔다.** 끝난 작업의 기록은 `docs/DONE.md` 로 옮겼다 — 결론만이
> 아니라 "왜 그 결론에 이르렀는가"가 거기 있다. 수치와 커밋은 `CHANGELOG.md` 에 있다.
> 지금 무엇부터 하면 되는지는 **§8 "다음 세션 착수점"** 이 답한다.

---

## 0. 설계 원칙

| 원칙 | 의미 |
|---|---|
| **관측자는 가벼워야 한다** | Argus 자신의 CPU/RAM/IO를 항상 계측하고, 예산 초과 시 자동으로 샘플링을 낮춘다. 모니터가 병목이 되는 순간 프로젝트는 실패다. |
| **측정할 수 없으면 만들지 않는다** | 탐지기보다 리플레이·결함주입 하네스를 먼저 만든다. 정밀도/재현율 숫자 없이 모델을 추가하지 않는다. |
| **탐지가 아니라 설명이 제품이다** | "이상함" 알림은 가치가 낮다. "14:32 디스크 큐 급증, 기여도 1위 `Chrome.exe`(68%), 2위 `MsMpEng.exe`(19%)"가 제품이다. |
| **알림은 예산제** | 하루 알림 총량에 상한을 둔다. 상한을 넘으면 severity 컷을 자동으로 올린다. 알림 피로가 시스템을 죽인다. |
| **모든 단계가 배포 가능** | 각 Phase 종료 시점에 항상 실행되는 바이너리가 존재한다. |

---

## 0.5 배포 전제 (타인 배포 O)

Argus는 **개발자 본인 PC 전용이 아니라 배포 대상**이다. 다음이 처음부터 설계 제약이다.

### 하드웨어/환경 가정 금지
`Argus.md`의 "RTX 3080 / RAM 64GB 기준"은 **개발 환경일 뿐 전제가 아니다.**

| 항목 | 대응 |
|---|---|
| GPU | NVIDIA(pynvml) / AMD·Intel / GPU 없음 — `GpuBackend` 인터페이스로 추상화, 없으면 관련 피처·탐지기 자동 비활성화 |
| 디스크 | NVMe / SATA SSD / HDD — 디스크 응답시간 임계값을 **하드코딩 금지**, 부트스트랩 기간에 실측 분포로 자동 캘리브레이션 |
| 코어 수 | per-core 배열 길이가 가변 → 피처 벡터는 코어 수와 무관한 집계값(평균·최대·분산·불균형도)으로 구성 |
| RAM | 절대값(GB) 대신 **비율·정규화 값**을 피처로 사용 |
| 권한 | 관리자 권한을 **요구하지 않고** 동작해야 함. ETW·서명검증은 권한 있을 때만 활성화되는 부가 기능 |
| Windows 버전 | 10/11 모두. PDH 카운터명은 로케일 의존 → **영문 카운터명 인덱스 조회** 방식 사용 (한글 Windows에서 카운터명 문자열 매칭은 실패한다) |

### 처음부터 필요한 것

- **피처 정규화 계층**: 모든 절대값은 해당 머신의 캘리브레이션 기준으로 정규화. 모델이 특정 하드웨어에 종속되지 않게.
- **부트스트랩 캘리브레이션**: 첫 실행 시 짧은 벤치(디스크 랜덤 읽기, 메모리 대역폭, 코어 성능)를 돌려 `machine_profile`을 만들고 저장. 모든 임계값의 기준점.
- **기능 탐지(feature detection)**: 시작 시 사용 가능한 계측 소스를 탐지 → `capabilities` 기록 → 대시보드에 "이 기능은 관리자 권한이 필요합니다" 형태로 노출. 조용히 실패하지 않는다.
- **크래시 리포팅**: 예외를 로컬 파일에 구조화 저장 + (동의 시) 익명 전송. 남의 PC에서 나는 버그는 재현이 불가능하므로 이게 없으면 디버깅이 불가능하다.
- **설정 마이그레이션**: 설정/DB 스키마에 버전 필드. 업데이트 시 자동 마이그레이션. Phase 1의 스키마 설계 시점에 이미 반영.
- **개인정보 경계**: 프로세스 경로·네트워크 목적지·창 제목은 민감 정보. **기본은 로컬 저장만**, 외부 전송 일절 없음. 리포트 내보내기 시 익명화 옵션.
- **라이선스/서드파티**: PresentMon(MIT), pynvml 등 번들 시 라이선스 고지 파일 포함.

### Phase 조정

- **Phase 0**에 `capabilities` 탐지 + `machine_profile` 캘리브레이션 추가
- **Phase 1**의 스키마에 버전 필드, 모든 임계값을 config로 외부화
- **Phase 12(ETW)** 는 순수 부가 기능으로 격리 — 없어도 제품이 성립해야 함
- **Phase 14** 확장: 설치 마법사, 언인스톨러, 업데이트 채널, 크래시 리포팅, 첫 실행 동의 화면

---

## 1. 아키텍처 (개정)

```
┌─ SENSING ─────────────────────────────────────────────┐
│ T0  ETW 커널 이벤트 스트림   (프로세스 생성/디스크/컨텍스트 스위치)  │
│ T1  시스템 카운터 1Hz        (psutil + PDH)                │
│ T2  프로세스 스냅샷 적응형    (활성 프로세스만 고빈도)            │
│ T3  체감 지표 (QoE)          (디스크 응답시간/DPC/프레임타임)     │
└───────────────────┬───────────────────────────────────┘
                    ↓  링버퍼 + 백프레셔 + 배치 writer
┌─ STORAGE ─────────────────────────────────────────────┐
│ HOT  인메모리 링버퍼(최근 10분) · SQLite WAL(최근 24h 원본)      │
│ WARM Parquet 일별 파티션 + DuckDB 쿼리 레이어(장기)             │
│ FEATURE STORE  파생 피처 캐시(롤링 통계·미분·비율)               │
└───────────────────┬───────────────────────────────────┘
                    ↓
┌─ CONTEXT ─────────────────────────────────────────────┐
│ 활동 레짐 추론: IDLE / BROWSE / GAME / BUILD / LLM / MEDIA │
│ (GMM 클러스터링 → HMM 평활화 → 포어그라운드 프로세스로 라벨링)     │
└───────────────────┬───────────────────────────────────┘
                    ↓  레짐 조건부
┌─ DETECTION (앙상블) ──────────────────────────────────┐
│ D1 통계 베이스라인 (레짐×시간대 EWMA + 로버스트 Z)              │
│ D2 룰 엔진 (YAML DSL)                                   │
│ D3 Isolation Forest / Half-Space Trees (다차원 점 이상)     │
│ D4 프로세스 지문 (프로그램별 정상 프로필 이탈)                   │
│ D5 시퀀스 모델 (TCN-AE → 필요시 LSTM-AE, 점진적 열화 탐지)      │
│ D6 변화점 탐지 (BOCPD, 레짐 전환과 구분)                      │
└───────────────────┬───────────────────────────────────┘
                    ↓
┌─ EXPLAIN ─────────────────────────────────────────────┐
│ 병목 분류 (CPU/IO/MEM/GPU/THERMAL-bound)                 │
│ 기여도 분해 (프로세스 단위 Shapley 근사)                      │
│ 인과 후보 랭킹 (선행성 + 기여도 + 지문 이탈도 결합)              │
└───────────────────┬───────────────────────────────────┘
                    ↓
┌─ DECIDE ──────────────────────────────────────────────┐
│ 이상 융합(동일 사건 dedup) · 억제(suppression) · 에스컬레이션    │
│ 알림 예산 관리 · 사용자 피드백 반영                            │
└───────────────────┬───────────────────────────────────┘
                    ↓
┌─ ACT ─────────────────────────────────────────────────┐
│ 대시보드(Streamlit) · Toast/Discord · 일간 리포트            │
└───────────────────────────────────────────────────────┘

┌─ 평가 인프라 (개발 전용, 항상 병렬 운영) ──────────────────┐
│ 리플레이 하네스: 저장 데이터로 탐지기 오프라인 재현             │
│ 결함 주입기: 메모리 누수/CPU 스핀/디스크 폭주 인위 생성          │
│ 스코어보드: 탐지기별 정밀도·재현율·탐지지연·오탐률 추적           │
└───────────────────────────────────────────────────────┘
```

---

## 2. 원본 스펙 대비 변경점

| # | 원본 | 개정 | 이유 |
|---|---|---|---|
| 1 | 1초 전체 프로세스 순회 | **적응형 계층 샘플링** — 전체는 10초, 상위 N개 활성 프로세스만 1초 | 400개 프로세스 × `cpu_percent()` 1Hz는 그 자체로 CPU 3~8% 상시 점유 |
| 2 | 시간대별 베이스라인 | **활동 레짐 조건부 베이스라인** | 시각보다 "무엇을 하는 중인가"가 압도적으로 강한 조건 변수 |
| 3 | 리소스 사용률만 수집 | **체감 지표(QoE) 추가** | 사용률은 원인, 레이턴시가 증상. 증상 없는 원인은 알릴 가치가 없다 |
| 4 | SQLite 단일 저장소 | **SQLite(핫) + Parquet/DuckDB(웜)** | 초당 삽입 × 다차원 = 주당 수백만 행. 장기 분석 쿼리가 SQLite에서 무너짐 |
| 5 | LSTM Autoencoder | **TCN-AE 우선, LSTM은 대안** | 학습 3~10배 빠르고 병렬화됨. 성능 동등 이상. PyTorch 의존은 유지 |
| 6 | 평가 방법 없음 | **리플레이 + 결함주입 하네스를 Phase 2에 배치** | 튜닝 불가능한 시스템은 운영 불가능 |
| 7 | 상관분석 = 원인 | **변화점 + 기여도 분해 + 선행성** | 상관은 인과가 아니다. 귀인을 별도 엔진으로 분리 |
| 8 | PyInstaller + PyTorch | **ONNX Runtime으로 추론 분리** | PyTorch 번들 시 .exe가 2GB+. ONNX면 ~50MB |
| 9 | 알림 정책 없음 | **알림 예산 + 억제 + dedup** | 오탐 3회면 사용자는 알림을 끈다. 그 순간 제품은 죽는다 |
| 10 | 자기 계측 없음 | **self-telemetry + 자동 스로틀** | 모니터링 도구의 1순위 실패 모드 |

---

## 3. 데이터 모델

### 3.1 핫 스토어 (SQLite, WAL)

```sql
-- 1초 원본, 24시간 보존
metrics_raw(ts INTEGER PK, cpu_total REAL, cpu_per_core BLOB,
            mem_used, mem_avail, swap_used,
            disk_read_bps, disk_write_bps, disk_queue, disk_resp_ms,
            net_rx_bps, net_tx_bps,
            gpu_util, gpu_vram_used, gpu_temp, gpu_power, gpu_pstate,
            dpc_latency_us, ctx_switches, interrupts)

-- 적응형 주기 프로세스 메트릭
process_metrics(ts, pid, name, cpu, rss, io_read, io_write,
                handles, threads, gpu_util, tier)

-- ETW 이벤트 (구조화)
kernel_events(ts, event_type, pid, ppid, image_path, signed,
              detail_json)

-- 네트워크 연결
net_connections(ts, pid, laddr, raddr, rport, state, remote_asn, resolved_host)

-- 활동 레짐 타임라인
regimes(ts_start, ts_end, regime, confidence, foreground_proc)

-- 이상 (융합 전 원시)
anomaly_signals(ts, detector, score, features_json, regime)

-- 이상 (융합 후, 사용자에게 보이는 단위)
incidents(id PK, ts_start, ts_end, severity, bottleneck_class,
          title, explanation_md, contributors_json,
          notified, user_label, labeled_at)

-- 프로세스 지문
process_fingerprints(name, regime, cpu_p50, cpu_p95, cpu_p99,
                     rss_p50, rss_p95, io_profile_json,
                     net_targets_json, sample_count, updated_at)

-- 모델 레지스트리
models(id, kind, version, trained_at, train_window, metrics_json,
       artifact_path, active)

-- 자기 계측
self_telemetry(ts, cpu, rss, queue_depth, drop_count, write_latency_ms)
```

### 3.2 웜 스토어

- `warm/date=YYYY-MM-DD/metrics.parquet` (1분 집계)
- `warm/date=YYYY-MM-DD/process.parquet` (5분 집계, 상위 프로세스만)
- DuckDB로 직접 쿼리 (`SELECT ... FROM 'warm/**/*.parquet'`)
- 보존: 원본 24h → 1분 집계 30일 → 1시간 집계 무기한

> ✅ **`net_activity_5m` 추가 (2026-07-27).** `net_connections` 는 72시간 보존이라
> 접지 않으면 네트워크 이력이 3일마다 사라진다. **개별 원격 주소는 저장하지 않고**
> 프로그램별 고유 대상 수·포트 수·연결 수만 남긴다 — 개별 주소는 신호가 되지 않고
> (신규가 시간당 87건), 원본의 보존 정책을 우회하는 개인정보 보관이 된다.
> 실측 3,201행/12.5시간(하루 약 6,100행).
>
> ✅ **`process.parquet` 추가 (2026-07-27).** 4-A 는 `metrics` 만 만들어 프로세스
> 데이터가 여전히 24시간 뒤 사라지고 있었다. `process_5m`(5분 집계, 버킷당 상위 N)
> 과 `warm/date=.../process.parquet` 을 붙였다. 실측: 하루 15,591행,
> Parquet 행당 46 bytes → **하루 705KB, 1년 251MB**. 상세는 `CHANGELOG.md`.
>
> **구현 시 확정 (Phase 4-A)**: 저장 경로가 **2단**이 된다. Parquet 은 append 를
> 지원하지 않아 1분마다 쓸 수 없기 때문이다.
>
> ```
> metrics_raw(초, 24h) → metrics_1m(SQLite, 최근 며칠) → warm/date=.../metrics.parquet
> ```
>
> **완전히 끝난 날짜만** 내보내 파일을 불변으로 유지한다. 그래야 재작성도, 쓰는 도중
> 크래시로 반쪽 파일이 남는 일도 없다. 임시 파일에 쓰고 원자적으로 옮기며, **읽기가
> 검증된 뒤에야** SQLite 에서 지운다.
>
> **삭제는 롤업 워터마크를 넘지 못한다.** 접히기 전에 지워진 원본은 어디에도 남지
> 않는다. 롤업이 멈추면 삭제도 멈추고 DB 가 커지는데, 그게 맞는 선택이다 — 디스크가
> 차는 것은 눈에 보이고 고칠 수 있지만 지워진 2주치는 되돌릴 방법이 없다.

### 3.3 피처 파이프라인

원시 메트릭을 그대로 모델에 넣지 않는다. 파생 피처:

- 롤링 통계: 10s/60s/300s 창의 mean·std·p95·min·max
- 1차 미분(변화율), 2차 미분(가속도) — 메모리 누수는 여기서 보인다
- 비율 피처: `disk_queue / disk_throughput`, `gpu_util / gpu_power` (효율), `mem_used / mem_total`
- 레짐 원-핫 + 레짐 지속시간
- 시간 피처: 요일, 시각의 sin/cos 인코딩
- 프로세스 집계: 상위 5개 프로세스의 CPU 합/최대, 프로세스 총 개수, 신규 프로세스 수

---

## 4. Phase별 계획

각 Phase는 **완료 기준(DoD)** 을 만족해야 다음으로 넘어간다.

---

### Phase 0 — 골격과 자기 계측 (1~2일)

- 프로젝트 구조, `pyproject.toml`, 로깅(구조화 JSON 로그), 설정 로더(YAML + 환경변수 오버라이드)
- 프로세스 수퍼바이저: 수집/분석/UI 스레드 생명주기 관리, graceful shutdown
- **self-telemetry 먼저 구현** — Argus 자신의 CPU/RAM/큐 깊이를 DB에 기록
- 리소스 예산 가드: CPU 2% / RSS 300MB 초과 지속 시 자동 스로틀 다운

**DoD**: `python -m argus` 실행 → 자기 자신의 리소스 사용량이 SQLite에 쌓이고, 로그가 남고, Ctrl+C로 깨끗하게 종료된다.

> ✅ **완료.** 60초 실행 실측: CPU 평균 0.013%(최대 0.050%), RSS 67.5MB, 기록 간격 5.009초,
> 핸들 안정, `drop_count` 0, 시그널 종료 후 DB 무결성 OK. 상세는 `CHANGELOG.md`.
> 24시간 연속 가동 검증은 실제 수집기가 붙는 Phase 1 에서 함께 한다.

---

### Phase 1 — 수집 레이어 (3~5일)

**T1 시스템 카운터 (1Hz)**
- psutil: CPU per-core, 메모리, 스왑, 디스크 IO, 네트워크 IO
- `win32pdh` (PDH): `\LogicalDisk(*)\Avg. Disk sec/Transfer`, `Current Disk Queue Length`, `\System\Context Switches/sec`, `\Processor Information(*)\% Processor Performance` (실클럭)

**T2 프로세스 (적응형)**
- 10초마다 전체 스캔 → CPU/메모리 상위 20개 + 포어그라운드 프로세스를 "활성 집합"으로 승격
- 활성 집합만 1초 주기 폴링
- `cpu_percent()`의 첫 호출은 항상 0.0 → 캐시된 이전 스냅샷과의 델타로 직접 계산

**T3 GPU**
- pynvml: 사용률, VRAM, 온도, 전력, P-State, 스로틀 사유(`nvmlDeviceGetCurrentClocksThrottleReasons`)
- 프로세스별 GPU: `nvmlDeviceGetProcessUtilization`
- NVML 미탑재 환경 fallback (조용히 비활성화)

**수집 → 저장 경로**
- 각 수집기는 `queue.Queue(maxsize=N)` 에 push, 가득 차면 **오래된 것부터 버리고 drop_count 증가** (블로킹 금지)
- DB writer 스레드가 200ms 또는 500행 단위로 배치 INSERT
- 크래시 안전: WAL + `synchronous=NORMAL`

**DoD**: ~~24시간~~ **6시간** 무중단 실행, drop_count = 0, Argus 자체 CPU 평균 1.5% 미만, DB 증가량 하루 500MB 미만.

> **DoD 수정 (2026-07-27)**: 무중단 요구를 24시간에서 6시간으로 낮춘다. 24시간은 서버 기준이고,
> 이건 **데스크톱 상주 프로그램**이다. 배포 대상 사용자도 PC 를 켜고 쓰다 끄지 24시간 켜두지 않으므로
> 6~8시간 세션이 오히려 실사용에 가깝다. 24시간을 요구하면 검증 조건이 실제 사용 패턴과 어긋난 채
> 개발만 막는다. 재부팅을 건너 이어지는지는 자동 시작(Phase 14) 범위로 옮긴다.

> ✅ **대부분 완료.** 300초 실행 실측: CPU 평균 0.216%(최대 0.367%), `drop_count` 0,
> DB 292MB/일 환산(3.5M행/일, 행당 87 bytes), RSS 75.6MB, 쓰기 지연 0.35ms,
> 핸들 기울기 -0.024/초(누수 없음).
> **장시간 항목은 압축 테스트로 검증했다.** 보존 기간을 10분으로 줄여 40분 실행하면
> 삭제 경로가 실제로 돈다. 결과: 전 테이블 10.8분에서 절단 ✅, DB 2.05MB 에서 안정 ✅,
> WAL 3.95MB 고정 후 종료 시 0 으로 체크포인트 ✅, 행 수 정상 상태 진동 ✅, drop 0 ✅.
>
> ✅ **DoD 충족 (2026-07-29 판정).** 별도 소크가 아니라 **실사용 세션**으로 판정했다 —
> 2026-07-28 12:55~20:27 의 **7.53시간 무중단 세션**(표본 5,411개). 자동 시작이 붙은 뒤
> 실사용 자체가 소크가 됐고, 인위적 소크보다 실제 사용 패턴에 가깝다.
>
> | 항목 | 실측 | 기준 |
> |---|---|---|
> | 무중단 | **7.53시간** | 6시간 ✅ |
> | `drop_count` | **0** | 0 ✅ |
> | CPU 평균 | **0.259%** (최대 0.91%) | 1.5% 미만 ✅ |
> | 스로틀 발동 | 0회 | — |
>
> **메모리 장기 추세 — 누수 없음.** 07-27 에 세운 원칙대로 **Private(커밋)** 으로 판정했다
> (RSS 는 워킹셋 트리밍이 가린다 — 아래 이전 기록 참조). 이번 세션에서도 RSS 는
> 32.8~54.9MB 로 출렁여 선형 회귀가 시작·끝 값과 어긋났다. 시간대별 중앙값으로 보면:
>
> - **핸들 428~429 · 스레드 19 — 7.5시간 내내 평탄.** 누수의 가장 흔한 형태가 여기서 걸린다.
> - **Private 71.0 → 73.3MB (+0.31MB/시간).** 0 은 아니지만 300MB 예산 기준 40일 연속
>   가동해야 닿는 속도다. 데스크톱 상주 프로그램은 그만큼 연속으로 켜져 있지 않다
>   (DoD 를 24시간에서 6시간으로 낮춘 것과 같은 논리).
>
> 검증용 `ArgusSoak` 작업은 남겨 둔다 — 수집 주기나 보존 설정을 바꿨을 때 다시 필요하다.
>
> <details><summary>이전 기록: 6시간 소크 시도 (2026-07-27)</summary>
>
> **6시간 소크 진행 중** (2026-07-27 15:45:26 시작 → 21:45 판정, 작업 스케줄러 `ArgusSoak`).
> 그날 앞선 시도 네 번이 전부 죽었고 원인이 매번 달랐다 — 세션 종속, 콘솔 Ctrl+C,
> job object 상속, uv 트램폴린이 만든 콘솔. 해법은 스케줄러가 base `pythonw.exe` 를
> 직접 실행하는 것이다(`tools/soak_entry.py`, 원인 정리는 `tools/README.md`).
>
> **측정 지표를 고치고 다시 시작했다.** 그전 실행에서 RSS 가 63 → 18MB 로 내려갔는데,
> 메모리를 반납한 게 아니라 백그라운드 프로세스의 워킹셋을 Windows 가 트림한 것이었다
> (강제 트림 실측: RSS 95.8 → 1.0MB, private 85.4MB 유지). **RSS 로는 누수를 판정할 수
> 없다** — 누수가 있어도 트림이 가린다. `self_telemetry` 에 `private_mb`(판정 정본)·
> `peak_wset_mb`·`page_faults` 를 추가했고, 새 지표를 남은 시간에 쌓기 위해 1시간을
> 버리고 재시작했다. 상세는 `CHANGELOG.md`.
>
> **추가 구현**: 절전 복귀 처리(`runtime/gapmon.py`). PC 가 자는 것은 상주 프로그램이
> 반드시 겪는데 원안에 없었다. 복귀 시 속도 추적 초기화·PDH 재개방·프로세스 목록 재기준을
> 하고, 공백 구간을 `system_events` 에 남겨 이후 단계가 제외할 수 있게 한다.
>
> **설계 변경**: 계층 샘플링을 수집이 아니라 **저장** 단계에 두기로 했다. PDH `Process V2`
> 와일드카드 질의가 전 프로세스를 13ms 에 주므로(psutil 1,380ms 대비 106배) 수집을 아낄 이유가
> 없어졌다. 매 틱 전부 수집하고, 활성 집합만 1초 해상도로 저장한다. 상세는 `CHANGELOG.md`.
>
> </details>

---

### Phase 2 — 평가 인프라 ★ (3~4일)

> **이 단계를 건너뛰면 이후 전부가 감(感)에 의존하게 된다.**

**2-1. 리플레이 하네스**
- 저장된 기간을 지정해 실시간처럼 재생 → 탐지기에 주입 → 결과 수집
- 시간 배속(1x ~ 1000x), 결정론적 (같은 입력 = 같은 출력)
- 탐지기는 "지금 시각"을 직접 읽지 않고 주입된 clock을 사용하도록 인터페이스 강제

**2-2. 결함 주입기 (`tools/fault_injector.py`)**
| 시나리오 | 구현 |
|---|---|
| 메모리 누수 | 초당 N MB씩 리스트에 append, 30분간 |
| CPU 스핀 | k개 코어 busy loop, 가변 duty cycle |
| 디스크 폭주 | 랜덤 4K 쓰기 폭탄, 큐 길이 상승 유도 |
| GPU 점유 | 대형 텐서 반복 연산 |
| 핸들 누수 | 파일 핸들 미해제 반복 |
| 서서히 나빠짐 | 위 항목을 30~90분에 걸쳐 선형 증가 (가장 중요) |

주입 구간은 ground-truth 라벨로 자동 기록된다.

**2-3. 스코어보드**
- 탐지기별: Precision, Recall, F1, **탐지 지연(주입 시작 → 첫 알림)**, 정상구간 오탐률(FP/hour)
- CLI: `python -m argus.eval --detector all --scenario leak_slow`
- 결과를 `eval_runs` 테이블에 축적 → 회귀 감시

**DoD**: 결함 주입 → 리플레이 → "탐지기 X가 메모리 누수를 12분 만에 잡음, 오탐 0.3/h" 형태 리포트가 자동 출력된다.

> ✅ **완료.** `python -m argus.eval --detector all` 실측: `fixed_cpu` TP 2 / FP 0 /
> 정밀도 100% / 재현율 28.6% / F1 0.444 / 지연 100초, `always` F1 0.583(재현율 100%,
> 정밀도 41.2%). 리플레이 실측 17만~27만 배속, 두 번 재생 결과 동일.
>
> **설계 변경 — 결함 주입기를 상대 강도로.** 첫 실측에서 주입 구간이 정상 구간보다
> 조용했다(12코어에 3스레드 → `cpu_total` 26.8%, 정상 평균 20.7%). 절대량 인자를
> `machine_profile` 기준 비율(`--cpu-load`/`--mem-load`/`--disk-load`)로 바꿨다.
> 함께 드러난 버그 둘: CPU 스핀이 GIL 때문에 스레드로는 병렬이 안 됨(→ 프로세스),
> 디스크 폭주가 단일 스레드 동기 쓰기라 큐 깊이가 항상 1(→ 다중 워커).
> 주입기가 끝날 때 효과를 스스로 검증하고, 열화가 없으면 라벨을 `completed=0` 으로
> 남겨 채점에서 빠지게 한다.
>
> **채점 단위는 틱이 아니라 알람이다.** TP/FN 은 구간 단위, FP 는 알림 단위로 센다.
> 정상 구간 알람 시간을 쿨다운으로 나누지 않으면 영원히 발화하는 탐지기가 만점을 받는다.
>
> ⚠️ **오탐률(FP/hour)은 아직 측정 못 했다.** 정상 구간이 30분 이상 쌓여야 하는데
> 소크 테스트가 반복해서 죽는 바람에 최대 25분까지만 모였다. 숫자를 지어내지 않고
> `—` 로 표시하도록 해 뒀다. 6시간 연속 실행이 끝나면 채워진다.

---

### Phase 3 — 통계 베이스라인 + 룰 엔진 (3~4일)

**베이스라인**
- 각 메트릭에 대해 EWMA(α 가변) + EW 표준편차
- **로버스트 통계 사용**: 평균/표준편차 대신 중앙값/MAD 기반 Modified Z-score (이상치가 베이스라인을 오염시키는 것 방지)
- 동적 임계값: `median + k * 1.4826 * MAD`, k는 메트릭별 설정
- 부트스트랩 기간(첫 2시간)에는 탐지 비활성, 수집만

**룰 엔진 DSL**
```yaml
rules:
  - name: "디스크 병목"
    when:
      all:
        - {metric: disk_resp_ms, op: ">", value: 50}
        - {metric: disk_queue, op: ">", value: "baseline * 3"}
    for: 30s              # 지속 조건 — 순간 스파이크 무시
    cooldown: 10m         # 재발화 억제
    severity: warning
    regime_scope: [ANY]
    explain: "디스크 응답시간 {disk_resp_ms}ms (평소 {baseline}ms)"

  - name: "온도 스로틀링"
    when:
      all:
        - {metric: gpu_temp, op: ">", value: 83}
        - {metric: gpu_throttle_reason, op: "contains", value: "THERMAL"}
    for: 60s
    severity: critical
```
- 표현식 평가는 **AST 화이트리스트 파서**로 (`eval()` 금지 — 사용자 편집 파일이므로 보안 이슈)
- `for`(지속시간)와 `cooldown`이 핵심. 이거 없으면 알림이 폭발한다.

**DoD**: ~~결함 주입 시나리오 6개 중 4개 이상~~ **이 PC 에서 증상이 관측되는 시나리오는
전부** 룰만으로 탐지, 정상 구간 오탐 < 1/h.

> **DoD 수정 (2026-07-27)**: "6개 중 4개"라는 고정 개수 기준을 버린다. 실제 주입기는
> 4종이고 그중 2종은 **이 하드웨어에서 증상 자체가 발생하지 않는다**(NVMe 는 2배 부하에도
> 응답 0.1→0.2ms). 증상 없는 구간을 탐지하라는 요구는 오탐을 요구하는 것과 같다
> (CLAUDE.md: 증상 없는 원인은 알릴 가치가 없다). 하드웨어마다 만들 수 있는 증상이
> 다르다는 것이 이 프로젝트의 전제이므로, 고정 개수를 요구하는 기준 자체가 틀렸다.
>
> ✅ **완료.**
>
> | 항목 | 결과 |
> |---|---|
> | 기준선 초과 | ✅ `rules` F1 **0.615** > `fixed_cpu` 0.364 > `always` 0.214 |
> | 증상 있는 시나리오 전부 탐지 | ✅ **2/2** (`cpu_spin`·`memory_leak`) |
> | 정상 구간 오탐 < 1/h | ✅ **0.00/h** (`always` 는 60.83/h — 비교군이 제 역할을 했다) |
> | 실시간 연동 | ✅ `anomaly_signals` 기록. 알림은 Phase 9 (`notify: false`) |
>
> 탐지되지 않은 2종은 이유가 다르고, 둘 다 룰의 결함이 아니다.
> - `disk_thrash`: 이 NVMe 에 증상이 없어 라벨이 `completed=0` 으로 자동 제외된다.
>   SATA·HDD 장비에서 확인해야 한다.
> - `handle_leak`: 핸들은 프로세스별 지표라 시스템 룰로 닿지 않는다 → Phase 8.
>
> **계획서 수정 — 재시작 후 부트스트랩 폐지.** "첫 2시간 탐지 비활성"을 그대로 두면
> PC 를 켤 때마다 2시간을 기다린다. 시작 시 `metrics_raw` 에서 최근 30분을 읽어 즉시
> 채운다(실측 1,172틱 / 60ms). 2시간 부트스트랩은 진짜 첫 설치일 때만 적용된다.
>
> **통계 방식 확정.** 계획서가 "EWMA + EW 표준편차"와 "중앙값/MAD"를 같이 적었는데
> 둘은 양립하지 않는다(EWMA 는 로버스트하지 않다). **중앙값/MAD** 로 간다.
> 산포가 없는 메트릭은 z 를 내지 않고(`degenerate`) 절대 임계값으로 다룬다.
>
> **주입기에 `manual` 시나리오 추가.** GPU 점유 시나리오를 만들려다 방향을 바꿨다.
> GPU 를 실제로 태우려면 torch(약 2.5GB)나 D3D 직접 호출이 필요한데 시나리오 하나에
> 치를 비용이 아니고, **인위로 만든 부하보다 진짜 게임이 더 나은 데이터다.**
> 사용자가 게임·빌드를 하는 구간에 라벨만 붙인다 — Phase 4 레짐 인식이 요구하는 것도
> 정확히 이 데이터다.
>
> - `disk_thrash`: 이 NVMe 는 2배 부하에도 응답시간이 0.1→0.2ms 라 **증상이 없다.**
>   룰 문제가 아니라 하드웨어 특성이며, SATA·HDD 에서 확인해야 한다.
> - `handle_leak`: 핸들은 프로세스별 지표라 시스템 룰로 닿지 않는다 → Phase 8 범위.
>
> **계획서 수정 — 재시작 후 부트스트랩 폐지.** "첫 2시간 탐지 비활성"을 그대로 두면
> PC 를 켤 때마다 2시간을 기다린다. 시작 시 `metrics_raw` 에서 최근 30분을 읽어 즉시
> 채운다(실측 1,172틱 / 60ms). 2시간 부트스트랩은 진짜 첫 설치일 때만 적용된다.
>
> **통계 방식 확정.** 계획서가 "EWMA + EW 표준편차"와 "중앙값/MAD"를 같이 적었는데
> 둘은 양립하지 않는다(EWMA 는 로버스트하지 않다). **중앙값/MAD** 로 간다.
> 산포가 없는 메트릭은 z 를 내지 않고(`degenerate`) 절대 임계값으로 다룬다.
>
> **실시간 연동은 오탐률 확인 후에 붙인다.** 알림은 되돌릴 수 없다.

---

### Phase 4 — 활동 레짐 추론 ★ (4~5일)

원본 스펙에 없던, 그러나 정확도를 가장 크게 끌어올리는 모듈.

> **4-A / 4-B 로 나눈다 (2026-07-27).** 착수 직전 조사에서 **레짐을 학습할 데이터가
> 존재할 수 없는 구조**임이 드러났다. 원본 보존이 24시간이라 매일 지워지는데,
> 이 Phase 는 며칠치를, Phase 5 는 "최근 14일"을, Phase 7 은 "2주 축적"을 전제한다.
> 데이터가 없으면 GMM 을 아무리 잘 만들어도 학습할 것이 없고, DoD 도 측정되지 않는다.
>
> - **4-A 1분 롤업 + 웜 스토어** — 이 Phase 의 1번 단계("1분 윈도우 피처 벡터")가
>   요구하는 입력 계층이자, 데이터 증발을 멈추는 조치. **선행 조건이다.**
> - **4-B GMM + HMM 레짐 추론** — 데이터가 며칠 쌓인 뒤.

> **파이프라인이 2026-08-04 실측으로 바뀌었다. GMM·HMM 을 쓰지 않는다.**
> 예광탄 3회의 수치와 기각 근거는 `docs/DONE.md` 에 있다. 아래는 그 결론이다.

**입력**: `metrics_1m.foreground_proc` (이미 수집 중). 리소스 피처는 클러스터링용이
아니라 필요 시 세분용으로만 남긴다.

**파이프라인 (개정)**
1. **조건 변수 = 포어그라운드 프로그램명.** 평활화도 클러스터링도 거치지 않는다.
   베이스라인은 `(program, metric)` 별로 쌓이므로 **시간 연속성이 필요 없다** —
   `chrome` 구간이 1,049조각으로 흩어져 있어도 합치면 1,729분이라 표본은 충분하다.
2. **표시용 타임라인은 롤링모드 5분** (경계 정확도 93%). 이건 사람이 보는 용도이고
   탐지는 1번을 쓴다. 둘을 섞지 않는다 — 평활화는 균질성을 해치기 때문이다(아래).
3. 사용자가 대시보드에서 라벨 수정 가능 (변경 없음)

**활용**
- 모든 베이스라인이 `(program, metric)` 조건부로 재계산
- 레짐 전환 자체는 이상이 아님 → 변화점 탐지가 레짐 전환과 진짜 이상을 구분해야 함
- 새 레짐 발견 시 "새로운 사용 패턴 감지" 정보성 알림 (경고 아님)

**DoD**: 게임 실행/종료, 빌드 시작/종료를 90% 이상 정확히 레짐 경계로 인식.
→ **표시용 타임라인 기준 93% 로 충족**(2026-08-04 예광탄 3회차). 탐지용 조건 변수는
경계 개념이 없으므로 이 DoD 가 적용되지 않는다 — 그쪽 판정 기준은 아래 4-B-2 다.

> **DoD 수정 (2026-07-27)**: "Phase 3 대비 오탐률 50% 이상 감소"를 삭제한다.
> **Phase 3 의 실측 오탐률이 0.00/h 라 50% 를 줄일 수 없다.** 이 기준으로는 Phase 4 가
> 아무리 좋아도 통과도 실패도 판정되지 않는다. 측정 불가능한 기준은 기준이 아니다.
> 레짐 경계 정확도는 그대로 측정 가능하므로 그것만 남긴다.
>
> ✅ **4-A 완료.** 실측: 실제 8시간 데이터 141버킷을 **239ms** 에 접었고, 포어그라운드
> (`mahjong-jp` 0.85)·GPU·CPU 표준편차·코어 불균형이 모두 살아남았다. 압축비 **59:1**,
> Parquet(zstd) 행당 308 bytes → **1분 집계 434KB/일, 1년 155MB**(원본은 292MB/일).
> 상세는 `CHANGELOG.md`.

#### 4-B 남은 일 — 켤지 말지 (2026-08-04, 구현 완료)

**코드는 들어갔고 꺼져 있다**(`detection.per_program: false`). 남은 것은 판정 하나다.

구현하며 정해진 것(상세는 `CHANGELOG.md`): 룰이 상대 조건으로 쓰는 메트릭만 나누고
(9개 중 6개), 창 3600초에 표본을 5초로 솎고, 프로그램 16개 LRU 상한. **약 7MB** —
전역과 같은 방식이면 90MB 라 예산에 못 넣는다. 표본이 덜 찼거나 처음 보는 프로그램은
전역으로 폴백한다. `regimes` 테이블은 만들지 않았고 필요 없다.

**남은 것: 켤지 정한다. 그런데 `argus.eval` 로는 못 잰다.**

**2026-08-04 에 그렇게 재려다 실패했다.** `phase8-handle-leak` 스냅샷으로
`--detector rules` 를 before/after 돌렸더니 **양쪽 다 TP0/FP0/FN8 로 똑같았다.**
당연하다 — 그 스냅샷의 정답은 `handle_leak` 이고 **`rules` 는 그걸 원래 못 잡는다**
(프로세스별 지표라 전역 메트릭에 안 드러난다. `procleak` 이 따로 있는 이유).
`defaults.yaml` 에 "rules 만으로는 F1 이 0.000이었다"고 이미 적혀 있었다.

**교훈은 예광탄 때와 같다 — 측정이 대상을 재는지 먼저 확인한다.** 스코어보드가
돌아가고 숫자가 나온다고 그 숫자가 내 변경을 잰 것은 아니다.

| 무엇을 재나 | 어떻게 | 필요한 것 |
|---|---|---|
| **오탐 위험** (주된 위험) | 같은 구간을 재생해 **발화 수**를 두 엔진으로 비교 | 정답 라벨 불필요 |
| 미탐 | `cpu_spin` 주입 배치 후 채점 | 100~120분 + **PC 가 실제로 느려진다** |

**비교 전에 조건부 축이 실제로 섰는지 본다** — `BaselineSet.program_readiness()` 가
비어 있으면 두 엔진이 같은 것이므로 "차이 없음"은 아무 뜻도 없다.

**실패 신호 — 되돌리는 조건**: 오탐(FP)이 늘고 TP 가 그대로면 끈다. 민감도만 올리는
변경은 규칙 1(오탐이 미탐보다 비싸다)에 걸린다. 되돌리기는 config 한 줄이다.

**측정해서 정할 것**: `program_min_samples`(지금 60)와 `program_window_s`(3600초)는
근거 없이 전역 값을 따온 것이다. 폴백이 얼마나 자주 도는지 보고 조정한다 —
`BaselineSet.program_readiness()` 가 프로그램별 표본 수를 돌려준다.

---

### Phase 5 — 다차원 이상탐지 (3~4일)

- **Isolation Forest** (scikit-learn): 레짐별로 별도 모델. 매일 새벽 3시 재학습, 학습 창 = 최근 14일 중 해당 레짐 구간
- **Half-Space Trees** (river): 온라인 학습, 재학습 없이 즉시 적응. IF와 앙상블
- 이상 점수 정규화: 원시 점수 → 최근 분포 기준 백분위로 변환 (모델 간 비교 가능하게)
- 피처 중요도: 이상 판정 시 어떤 차원이 기여했는지 (IF의 경로 길이 분해 or 대체로 permutation)

**DoD**: 리플레이 평가에서 룰 엔진 대비 F1 개선, 그리고 **룰이 못 잡던 시나리오를 최소 1개 잡음**. 개선이 없으면 채택하지 않는다(복잡도 비용이 크므로).

> ⏸️ **보류 (2026-07-27) — 이길 상대가 없다.**
>
> DoD 가 "룰이 못 잡던 시나리오를 최소 1개"인데, 실제로 그런 시나리오가 없다.
>
> | 시나리오 | 상태 |
> |---|---|
> | `cpu_spin` | 룰이 잡는다 |
> | `memory_leak` | 룰이 잡는다 |
> | `disk_thrash` | **이 하드웨어에 증상 자체가 없다** — 채점에서 자동 제외 |
> | `handle_leak` | 프로세스 지표라 시스템 룰로 닿지 않는다 → **Phase 6 영역** |
>
> 남는 것은 `handle_leak` 하나인데 그건 다차원 이상탐지가 아니라 프로세스 지문이
> 푸는 문제다. **이길 상대를 먼저 만든 뒤에 재평가한다** — 60분 램프 주입으로
> 룰의 한계를 실제로 확인하거나, 증상이 나오는 하드웨어에서 `disk_thrash` 를
> 재현하거나. 그 전에 착수하면 무엇을 이겼는지 말할 수 없는 모델이 남는다.
>
> **2026-08-16 — 60분 램프를 리플레이로 재봤고, 상대가 되지 못했다.**
> `tools/ramp_replay.py` 로 제품 탐지기에 태운 결과다.
>
> | 잰 것 | 결과 |
> |---|---|
> | `rules` 만 (배포 기본값) | 60분 램프 **미탐** (-1.85%p) · 5분 램프는 발화 |
> | `rules` 만 (이 PC, `per_program=on`) | 60분 램프도 **발화** (23.4분 뒤) |
> | **`rules,procleak` (상주의 실제 구성)** | **`procleak` 이 8.9분 뒤 발화** |
>
> 위 표가 이 항목의 요점이다 — **시스템 룰이 못 잡아도 제품은 잡는다.** 그러니
> `memory_leak` 은 램프로 늘려도 여전히 "룰이 잡는다" 칸에 있다. 표를 고칠 이유가
> 없다. **실제 60분 주입은 취소했다**(§8, 70분 절약).
>
> **이길 상대의 조건이 좁혀졌다.** `procleak` 이 잡는 것은 *"한 프로세스의 RSS 가
> 단조 증가"* 뿐이므로, 남은 후보는 **분산 누수 · 비단조 누수 · 프로세스 밖 누수**
> 셋이다. 셋 다 `fault_injector` 확장이 필요하고 아직 아무도 재지 않았다.
> **다음에 이 항목을 건드릴 때 60분 램프부터 다시 시작하지 않는다.**

---

### Phase 6 — 프로세스 지문 (3~4일)

- 프로세스명 × 레짐 단위로 리소스 분포 학습 (p50/p95/p99, IO 패턴, 네트워크 대상 집합)
- 실행 경로 + 서명자 + 파일 해시를 아이덴티티에 포함 (이름만으로는 위장 가능)
- 이탈 판정: 현재 값이 자기 자신의 p99를 지속 초과, 또는 KL divergence 기반 분포 이탈
- 신규 프로세스: 지문 없음 → 학습 모드(경고 안 함), N회 관측 후 지문 확정
- 버전 업데이트 감지: 해시 변경 시 지문을 폐기하지 않고 "변경 이후" 분포를 별도 추적 → 유의미하게 나빠졌으면 **"업데이트 후 성능 회귀"** 알림 (이게 실사용에서 굉장히 유용한 기능)

**DoD**: 브라우저·게임·에디터 각각의 정상 프로필이 구축되고, 인위적 과사용 주입 시 해당 프로세스가 1위 용의자로 지목된다.

> ✅ **DoD 충족 (2026-07-29).** 6-A / 6-B 를 모두 끝냈다.
> - **"1위 용의자 지목"** — 6-A 가 주입 프로세스를 PID 로 직접 지목한다(score 1.00).
> - **"정상 프로필 구축"** — 6-B 가 프로그램별 `p50/p95/p99` 를 세운다(실측 121건 /
>   60~61종). 브라우저(chrome)·게임(mahjong-jp)·녹화(medal) 프로필이 실제로 잡혔고,
>   그 프로필로 오탐 절반을 가렸다.
>
> 다만 **"에디터"는 확인하지 못했다** — 이 PC 에서 3일 이상 6시간씩 켜져 있는 에디터가
> 없어 자격을 채운 표본이 없다. 프로필 구축 자체는 프로그램 종류를 가리지 않으므로
> 구조적 문제는 아니지만, DoD 문구가 예로 든 셋 중 하나는 실측으로 못 보였다.

#### 6-A / 6-B 로 쪼갠다 (2026-07-29)

원안은 지문(p50/p95/p99)을 먼저 세우는 것이었는데, **누수는 남과 비교하는 문제가 아니라
자기 과거와 비교하는 문제**다. 크롬의 핸들이 평소 몇 개인지 몰라도, 지금 이 인스턴스가
10분간 한 번도 안 줄고 6배로 불었으면 누수다. 3일치 지문도 웜 병합 조회도 필요 없다.

| | 내용 | 상태 |
|---|---|---|
| **6-A** | 프로세스 누수 탐지 — 지문 불필요, 원본 스트림, 자기 과거와 비교 | ✅ 2026-07-29 |
| **6-B** | 지문 — **6-A 의 오탐을 가린다** | ✅ 2026-07-29 |

지문부터 지으면 **이길 상대가 없는 채로 무거운 쪽을 먼저 짓는 것**이라 §8 정렬 원칙 4 와
"수치 없이 모델을 추가하지 않는다"에 걸린다. 6-B 를 미뤄도 잃는 게 없다 — `process_5m` 이
이미 데이터를 보존하고 있어 **지연 비용이 0 인 드문 경우**다(정렬 원칙 1 이 걸리지 않는다).

**6-A 결과**: F1 0.400 (`rules` 0.000, `always` 0.008). 시스템 룰이 핸들 누수에 닿지
못한다는 것이 수치로 확인됐다. 오탐률도 `rules` 보다 낮다(0.24 vs 0.73/시간).

**6-B 결과 (2026-07-29)** — 원안(지문 기반 **독립 탐지기**, p99 이탈)을 바꿨다. 그러면
6-A 와 **같은 것을 다른 방식으로 재는** 셈이라 무엇이 나아졌는지 측정되지 않고, 지문은
5분 버킷 단위라 탐지 지연이 6-A(543초)보다 오히려 나쁘다. 대신 **6-A 의 오탐을 가리는
보조**로 넣어 성공/실패가 숫자로 갈리게 했다.

```
medal 핸들   383 → 1,539개  (4.0배)   →  억제됨   (평소 p99 13,323)
medal 메모리  94 → 2,164MB (22.9배)   →  유지됨   (평소 p99 915MB)
주입 python  879 → 5,977개  (6.8배)   →  유지됨   (평소 p99 2,768)
```

FP 2건 중 1건 제거, TP 유지(오탐 0.24 → 0.12/시간). 남은 1건은 medal 의 메모리인데
2,164MB 는 medal 자신에게도 이례적이라 애초에 오탐이 아니었을 가능성이 크다.

**"평소 성장 폭"으로 하려던 것은 불가능했다.** `process_5m` 에 버킷 간 변화율이 없어
절대 수준으로 갔다. 원본은 24시간만 남으므로 나중에 변화율 지문을 원하면 **롤업에
그 컬럼을 추가하는 것부터** 해야 한다 — 지금 없는 과거는 되살릴 수 없다.

**남은 한계**: 지문 단위가 프로그램 **이름**이라 `python` 처럼 범용적인 이름은 서로 다른
프로그램이 지문을 공유한다(실측: `python` 의 `rss_p95` p99 가 9GB 인데 개발 작업
프로세스들 탓이다). 실행 경로를 아이덴티티에 넣으면 갈리지만 Phase 13 과 함께 판단한다.

**원안의 남은 절반 (p99 이탈 탐지기)** 은 착수하지 않았다. 이길 상대가 불분명하다는 위
이유 때문이고, 필요해지면 그때 재평가한다.

**6-A 의 FP 2건** — 둘 다 `medal`(게임 녹화)이었다.
핸들 3.6배(15분), 메모리 20.4배(94 → 1,919MB, 15분). 결함주입 라벨 밖이라 채점기가 FP 로
셌지만 실제 오탐인지는 알 수 없다. **정답 라벨이 주입 구간에만 있어 가를 방법이 없다.**
"이 프로그램의 평소 성장 폭"을 알아야 판정되고, 그게 지문의 역할이다. 6-B 착수 시 이 두
건을 판정 기준으로 쓴다.

**6-B 를 할 때 정해야 할 것 (오늘 확인한 제약)**

- **분위수의 분위수를 만들면 안 된다.** 원본 `process_metrics` 는 24시간만 남으므로 3일치
  지문은 버킷 통계를 다시 집계할 수밖에 없는데, 버킷별 p99 를 모아 다시 p99 를 내면 그건
  분위수가 아니다 — 그런데 **조용히 그럴듯해 보인다.** 지문의 단위를 "이 프로그램의 5분
  버킷 통계량이 평소 어느 범위인가"로 못 박고 `stat` 컬럼에 명시해야 한다.
- **탐지 입력과 지문의 단위가 다르다.** `Replayer._load_processes` 가 싣는 것은 원본
  `process_metrics`(1초/30초)지 5분 버킷이 아니다. "현재 버킷 p95 를 지문과 비교"하려면
  탐지 지연이 5분이 된다.
- **경로·서명자·해시는 뺀다.** 해시는 게임 exe 수백 MB 를 읽어야 해 설계 규칙 1 과 충돌하고,
  서명 검증은 Phase 13(보류) 영역이다. 해시가 없으면 "업데이트 후 성능 회귀"도 성립하지
  않으므로 함께 뺀다. KL divergence 도 마찬가지 — p99 이탈이라는 기준선을 먼저 만들고
  그걸 못 이기면 넣지 않는다.
- **레짐 축은 비워 두되 스키마에는 넣는다.** 4-B 가 데이터 대기 중이라 기다리면 6-B 도
  같이 막힌다. `regime` 컬럼을 두고 지금은 `'all'` 고정, 4-B 가 오면 행이 늘어날 뿐이다.

> ✅ **선행 조건 해결 — 프로세스 롤업 (2026-07-27).**
>
> 이 Phase 는 "프로세스명 × 레짐 단위 p50/p95/p99" 를 학습하는데, 착수 직전까지
> **학습할 데이터가 매일 사라지고 있었다.** 4-A 는 `metrics_raw` 의 증발만 막았고
> `process_metrics` 는 24시간 보존 그대로였다.
>
> 더 나빴던 것은 **보호 장치가 헛돌았다는 점**이다. `process_metrics` 는 보존 규칙에서
> 롤업 워터마크에 묶여 있었지만 그 워터마크는 `metrics_1m` 것이었고, 1분 롤업은
> 프로세스를 접지 않는다. 접히지도 않은 채 "롤업이 지나갔으니 안전하다"는 이유로
> 지워지고 있었다. 이제 테이블마다 **자기를 접는 롤업의** 워터마크를 본다.
>
> `process_5m` 이 남기는 것: `cpu_p50/p95/p99`·`rss_p50/p95`·IO·핸들·스레드·
> 포어그라운드 비율. **시각별로 합친 뒤 분위수를 낸다** — 크롬 탭 30개를 그냥 모으면
> "개별 프로세스의 p95"가 되어 "크롬 전체"와 30배 어긋난다.

---

### Phase 7 — 시퀀스 모델 (5~7일, 데이터 2주 축적 후)

- **TCN Autoencoder** 우선 (dilated causal conv, 수용영역 ~10분)
- 입력: 1분 집계 다변량 시계열, 윈도우 60스텝
- 학습: 정상으로 라벨된 구간만 (사용자 피드백 + 룰/IF 모두 조용했던 구간)
- 판정: 재구성 오차의 채널별 분해 → 어떤 메트릭이 예측 불가였는지 바로 나옴
- LSTM-AE는 TCN이 부족할 때만 (비교 실험을 스코어보드에 기록)
- **주 타깃**: 서서히 진행되는 열화 — 메모리 누수, 팬 열화로 인한 점진적 스로틀, 디스크 노후화

**DoD**: `leak_slow` 시나리오(60분에 걸친 선형 누수)를 룰/IF보다 **먼저** 탐지. 이게 안 되면 이 모듈의 존재 이유가 없다.

> ⏸️ **보류 (2026-07-27) — DoD 대상이 존재하지 않는다.**
>
> `leak_slow`(60분 램프) 시나리오가 아직 없다. 현재 최장 램프는 5분이고, 그것도
> Phase 3 에서 90초 → 5분으로 늘린 것이다. **먼저 60분 램프를 주입해 룰이 실제로
> 못 잡는 것을 확인해야 한다.** 그 확인 없이 시작하면 무엇을 이겼는지 말할 수 없다.
>
> 비용도 크다 — torch(약 2.5GB) 학습 + ONNX 변환 파이프라인. 그리고 **이 Phase 를
> 채택하지 않으면 Phase 14 의 "PyTorch → ONNX 변환"도 불필요해진다.** exe 2GB 문제
> 자체가 사라지므로, 7 의 채택 여부는 14 의 범위까지 바꾼다.
>
> **2026-08-16 — `leak_slow` 를 리플레이로 만들어 봤고, DoD 대상이 되지 못했다.**
>
> 「메모리 이상 증가」에 안 걸린다는 예측은 **맞았다**(제품 리플레이 -1.85%p, 오전
> 추정 -6.34%p 와 방향 일치). 원인도 예측대로 베이스라인 창 30분이 램프를 "평소"로
> 학습하는 것이다. **그런데 `procleak` 이 램프 시작 8.9분 뒤에 잡는다** —
> `rss_mb` 를 보기 때문이다(`procleak.py:52`). 시스템 룰만 보고 "룰이 못 잡는다"고
> 읽은 것이 착시였다.
>
> DoD 가 *"`leak_slow` 를 룰/IF보다 **먼저** 탐지"* 인데, 이미 8.9분에 잡히는 것을
> TCN 이 더 먼저 잡아야 한다는 뜻이 된다. **60분 램프는 이 Phase 의 존재 이유가
> 되지 못한다.** 실제 주입은 취소했다(§8, 70분 절약).
>
> **DoD 를 만족시키려면 `procleak` 이 못 잡는 형태여야 한다** — 분산 누수 ·
> 비단조 누수(`monotonic_ratio=0.9` 미달) · 프로세스 밖 누수(커널·페이지드 풀).
> 셋 다 `fault_injector` 확장이 필요하다. torch 2.5GB 비용은 그대로이므로,
> **그 확장으로 미탐이 실제로 확인되기 전에는 이 Phase 를 열지 않는다.**

---

### Phase 8 — 귀인 엔진 ★ (4~5일)

"느려진 이유"를 실제로 만드는 단계.

**8-1. 변화점 탐지**
- BOCPD(Bayesian Online Changepoint Detection) 또는 `ruptures` PELT
- 이상 발생 시각을 점(point)이 아니라 **구간 시작점**으로 정확히 특정

**8-2. 병목 분류**
- 규칙 + 학습 결합. 판정 축:
  - CPU-bound: 높은 CPU, 낮은 IO 대기, 실클럭 최대
  - IO-bound: 높은 디스크 큐/응답시간, CPU는 IO wait
  - Memory-bound: 낮은 가용 메모리, 높은 페이지 폴트/스왑
  - GPU-bound: GPU 100%, VRAM 압박
  - Thermal-bound: 스로틀 사유 플래그, 클럭 하락 + 온도 상승
  - Contention: 특정 리소스 포화 없이 컨텍스트 스위치/DPC 급증

**8-3. 기여도 분해**
- 변화점 전후 구간에서, 각 프로세스의 해당 리소스 델타 기여율 계산
- Shapley 근사(프로세스 집합 소규모이므로 정확 계산도 가능)
- 지문 이탈도로 가중: "평소보다 이상한 정도"가 큰 프로세스에 가산점
- **선행성 검사**: 프로세스의 리소스 증가가 시스템 지표 악화보다 시간적으로 앞섰는가 (Granger 유사 lag 상관)

**8-4. 리포트 생성**
```
[WARNING] 14:32:10 ~ 14:38:44 — 디스크 IO 병목 (6분 34초)
체감 영향: 디스크 응답시간 8ms → 71ms (평소의 8.9배)

원인 후보:
  1. Chrome.exe (PID 8812)   기여도 68%  ← 지문 이탈: 쓰기량 p99의 4.2배
  2. MsMpEng.exe (PID 3104)  기여도 19%  ← 정상 범위 (실시간 검사 중)
  3. 기타                     기여도 13%

타임라인: 14:31:52 Chrome 탭 12개 → 47개 급증 (선행 40초)
레짐: BROWSE (평소 이 레짐의 디스크 쓰기: 2.1 MB/s, 현재: 88 MB/s)
```

**DoD**: 결함 주입 시 원인 프로세스를 1순위로 지목하는 비율 85% 이상.

> ✅ **완료 — 1순위 지목률 88.9% (8/9), 3위 이내 100%.** (07-30 재측정 7/7 = 100%)
> `python -m argus.eval --attribution`
>
> ⚠️ **이 수치는 자원을 알려준 상태의 것이다 (2026-07-30 확인).** 채점은 시나리오 라벨에서
> 자원(`handles`)을 **입력으로 받고**, 제품은 그것을 병목 분류기로 **추론**한다. 같은 주입
> 구간에서 제품은 4건 모두 엉뚱한 프로세스를 지목했다. **DoD 문장("원인 프로세스를 1순위로
> 지목")을 제품 기준으로 읽으면 아직 미충족이다.** §8 의 8·9번을 고친 뒤 다시 판정한다.
>
> **계획서 수정 — Shapley 를 쓰지 않는다.** Shapley 가 값을 하는 것은 참여자의 기여가
> *상호작용할 때*다. CPU 시간·디스크 바이트·메모리는 **가산적**이라(A 의 30% + B 의
> 20% = 50%, 둘이 함께여야만 생기는 시너지가 없다) Shapley 값이 단순 델타 비율과
> 수학적으로 같고 계산 비용만 늘어난다. 상호작용이 실제로 관측되면(캐시 경합, 큐
> 포화의 비선형 지연) 그때 도입한다.
>
> **`ruptures`·BOCPD 도 넣지 않았다.** 그 도구들은 "구간 어딘가에 변화가 있는가"를
> 모를 때 쓴다. 우리는 이상이 있다는 것과 대략 언제인지를 이미 안다 — 필요한 것은
> 경계를 다듬는 일이고 기존 중앙값/MAD 로 충분했다.
>
> **채점 정답을 프로세스 트리로 확장했다.** `fault_injections.pid` 는 주입기 부모
> 하나뿐인데 실제 부하는 자식들이 낸다(CPU 스핀은 GIL 때문에 프로세스로 fork).
> 실측에서 정답 PID 는 5분 구간에 12행만 관측됐고 CPU 는 0 이었다 — 부모만 정답으로
> 두면 **어떤 귀인 엔진도 통과할 수 없다.**
>
> ⚠️ **한계 두 가지를 남긴다.**
> - **표본이 9건이라 얇다.** 1건 차이로 78%↔89% 가 갈린다. 신뢰구간이 넓다.
> - **채점이 실사용보다 쉬운 조건이다.** 채점은 시나리오별로 볼 자원을 알려주고
>   시작하지만, 실사용에서는 병목 분류가 자원을 정해야 한다. 이 PC 의 `disk_thrash`
>   구간은 응답시간 증상이 없어 "CPU 병목"으로 분류되고, 그러면 귀인도 CPU 기준으로
>   엉뚱한 프로세스를 지목한다. 증상이 나오는 하드웨어(SATA·HDD)에서 확인해야 한다.

---

### Phase 9 — 융합·알림 정책 (2~3일)

- **신호 융합**: 여러 탐지기가 같은 시간대에 발화 → 하나의 `incident`로 병합. 탐지기 합의 수가 severity에 반영
- **억제 규칙**: 상위 사건이 있으면 하위 사건 알림 억제 (디스크 병목 알림 중 "Chrome CPU 높음"은 묻는다)
- **알림 예산**: 하루 기본 8건. 소진 시 severity 컷 자동 상향, 나머지는 대시보드에만
- **에스컬레이션**: `info` → 대시보드만 / `warning` → Toast / `critical` → Toast + Discord
- **디바운스**: 동일 서명(같은 원인+같은 프로세스)은 쿨다운 내 재알림 금지

**DoD**: 일주일 실사용에서 알림 8건/일 이하, 사용자 "정상이야" 피드백 비율 30% 이하.

> 🔓 **이 Phase 가 지금 가장 많은 것을 푼다 (2026-07-27).**
>
> **`incidents` 테이블이 아직 존재하지 않는다** — 마이그레이션에 정의 자체가 없다.
> Phase 8 이 만든 `Incident` 는 메모리에만 있고 어디에도 저장되지 않는다. 그 결과
> 넷이 동시에 막혀 있다.
>
> | 막힌 것 | 이유 |
> |---|---|
> | 귀인 리포트가 사라진다 | 저장소가 없다 |
> | 대시보드 Incidents 페이지 | 읽을 테이블이 없다 |
> | Phase 10 DoD("어제 왜 느렸는지") | 사건 목록이 없어 미달 상태 |
> | Phase 11 피드백 | 입력인 `incidents.user_label` 이 없다 |
>
> **DoD 는 나중에 판정한다.** "일주일 실사용"은 알림을 실제로 켠 뒤에나 잴 수 있고,
> 알림 발송은 오탐률이 검증된 뒤에 붙인다(현재 `notify: false`). 저장·융합·억제까지
> 먼저 만들고, 발송과 DoD 판정은 분리한다.
>
> ✅ **저장·융합·억제·예산 완료 (2026-07-27). 발송은 없다.**
>
> end-to-end 실측(주입 #15 구간 리플레이): 룰 신호 3건 → 사건 3건, 각 사건에
> 병목·원인 후보·근거가 붙었다. 주입 구간과 대응이 맞는다 —
> `cpu_spin`(14:40:58~14:45:59) → 사건 `14:42:04~14:46:04 python 82%`,
> `memory_leak` → 사건 `python 95% (+6.9GB)`.
>
> **경계를 신호 시각이 아니라 지표에서 다시 찾는다.** 이게 이 단계의 핵심이었다.
> 룰은 쿨다운(10분) 때문에 지속되는 문제에도 신호를 한 번만 내므로, 신호 시각을
> 그대로 쓰면 **5분짜리 문제가 "0초"로 기록되고 원인 후보가 통째로 빈다**(비교 창
> 길이가 0). Phase 8 의 `changepoint` 를 여기 연결해 해결했다 — 만들어 놓고 쓰지
> 않던 모듈이다. 상세는 `CHANGELOG.md`.

---

### Phase 10 — 대시보드 (4~5일)

Streamlit 멀티페이지:

1. **Live** — 실시간 게이지, 레짐 표시, 최근 10분 스파크라인
2. **Timeline** — 시간축 위에 레짐 밴드 + incident 마커 + 메트릭 오버레이. 구간 드래그 → 상세 분석
3. **Incidents** — 사건 목록, 리포트 전문, "정상이야 / 맞아 문제야" 피드백 버튼
4. **Processes** — 프로세스별 리소스 랭킹, 지문 대비 현재 상태 (정상범위 밴드 위에 현재값 표시)
5. **Regimes** — 레짐 타임라인, 라벨 편집, 레짐별 리소스 프로필 비교
6. **Health** — Argus 자기 계측 (수집 지연, drop, DB 크기, 모델 상태)
7. **Models** — 모델 레지스트리, 스코어보드 히스토리, 재학습 트리거

시각화는 대시보드 착수 시점에 `dataviz` 스킬 기준을 적용한다.

**DoD**: 대시보드만 보고 "어제 몇 시에 왜 느렸는지" 3클릭 내로 파악 가능.

> **순서 변경 (2026-07-27)**: Phase 4-B 가 데이터 축적을 기다리는 동안 대시보드를
> 앞당겼다. 축적이 필요 없고, 하루 동안 일회성 조회 스크립트를 다섯 개 짜면서
> 그 일이 반복될 것이 분명해졌기 때문이다.
>
> ✅ **5개 페이지 완료** — Live · Timeline · Processes · Health · **Incidents**.
> 실측 렌더 시간 142~486ms(실제 데이터). 색은 `dataviz` 검증기를 실제로 돌려
> 전 항목 통과한 다크 팔레트만 사용(최악 색각 ΔE 8.4, 대비 전부 3:1 이상).
>
> **나머지 2개는 만들지 않았다.** Regimes(4-B)·Models(학습 후)는 데이터가 없다.
> 빈 화면을 미리 두면 "아직 없음"과 "고장남"이 구분되지 않는다.
>
> ✅ **DoD 충족 (2026-07-29 판정).** 아래에 "실사용 데이터로는 확인하지 못했다"고 적어
> 뒀던 것이 해소됐다. **실사용 사건 11건**(warning 이상 6건)이 쌓인 뒤 `AppTest` 로
> 실제 DB 를 렌더해 확인했다 — 5개 페이지 전부 예외 없이 서고, 사건 페이지는
> `사건 11 · 진행 중 0 · 알림 대상 6` 을 정확히 집계한다. 사건 하나가 이렇게 보인다:
>
> ```
> ● warning · 신호 1건 · 탐지기 GPU 고온 지속
> **발열 스로틀링** — 19:35:22 ~ 19:35:28 (6초)
> 체감 영향: CPU 33% → 62% (평소의 2.4σ)
> 근거: GPU 92°C 열 스로틀링 / 발화한 룰: GPU 고온
> 원인 후보: op.gg 21% (+18.8, 선행 -1초) · league of legends 15% ● 신규
> ```
>
> "어제 몇 시에 왜 느렸는지"가 목록 → expander 1클릭으로 병목·체감 영향·근거·원인 후보
> (기여도/증가/선행/신규)까지 한 화면에 있다. 피드백 버튼도 붙어 있어 Phase 11 입력이 된다.
>
> <details><summary>이전 기록: 리플레이 사건으로만 검증하던 때</summary>
>
> **DoD 는 구조적으로 충족됐다.** Incidents 페이지에서 사건을 열면 병목·원인 후보·
> 근거가 한 화면에 있다(실측: 사건 3건 렌더, expander 1클릭으로 리포트 전문).
> 다만 **실사용 데이터로는 아직 확인하지 못했다** — 실시간 탐지가 조용해서 지금까지
> 사건이 0건이다. 리플레이로 만든 사건으로만 검증했다. 오탐률 0.00/h 의 다른 얼굴이다.
>
> </details>
>
> **검증 방법에서 배운 것**: 헤드리스 `HTTP 200` 은 증거가 되지 않았다. Streamlit 은
> SPA 라 페이지 스크립트가 터져도 셸 HTML 이 정상으로 돌아온다. `AppTest` 로 실제
> 실행해 보니 네 페이지 전부 `ImportError` 였다(페이지가 `exec` 으로 돌아 상대 import
> 불가). 상세는 `CHANGELOG.md`.

---

### Phase 11 — 피드백 & 지속 학습 (3~4일)

- 피드백 라벨 → `incidents.user_label` → 학습 데이터셋 재구성
- 오탐으로 라벨된 구간은 정상 데이터로 편입, 정탐 구간은 학습에서 제외
- **드리프트 감지**: 입력 분포가 학습 시점과 유의하게 달라지면(PSI/KS 검정) 재학습 트리거
- 하드웨어/OS 변경 감지 시 관련 베이스라인 리셋 (RAM 증설, GPU 교체, 드라이버 업데이트)
- 모델 레지스트리: 새 모델은 **섀도 모드**로 먼저 운영 → 스코어보드에서 기존 모델을 이기면 승격
- 일간/주간 리포트 자동 생성 (Markdown → 선택적 PDF)

**DoD**: 피드백 20건 반영 후 리플레이 평가에서 오탐률 개선이 수치로 확인됨.

---

### Phase 12 — 심층 계측: ETW (5~7일, 선택적 고위험)

> 여기서 진짜 커널 레벨 관측이 열린다. 다만 Python 생태계가 약해 리스크가 있으므로 **후반부 + fallback 필수**.

- **PresentMon** (Intel, 오픈소스) 연동 → 프레임타임/프레임 페이싱. 게임 렉을 실제로 측정하는 유일한 방법
- ETW 프로바이더:
  - `Microsoft-Windows-Kernel-Process` — 프로세스 생성/종료 (psutil 폴링보다 정확, 단명 프로세스 놓치지 않음)
  - `Microsoft-Windows-Kernel-Disk` — 개별 IO 요청의 지연시간 분포
  - `Microsoft-Windows-Kernel-Network`
  - DPC/ISR 레이턴시 — 드라이버 문제로 인한 스터터링 탐지
- 구현: `pywintrace` 시도 → 불안정하면 `xperf`/`wpr` 세션을 서브프로세스로 돌리고 결과 파싱하는 fallback
- 관리자 권한 필요 → 권한 없으면 자동 비활성화 + 대시보드에 안내

**DoD**: "리소스는 여유로운데 렉이 걸린" 케이스를 DPC 레이턴시 또는 프레임타임 분산으로 설명 가능.

---

### Phase 13 — 보안 라이트 (2~3일)

- 신규 프로세스: 서명 검증(`WinVerifyTrust`), 실행 경로 화이트리스트 이탈, 부모-자식 관계 이상 (`winword.exe → powershell.exe` 등)
- 네트워크: 단시간 다수 신규 원격 IP, 알려지지 않은 목적지, 지문에 없는 네트워크 대상
- 지속성 지점 변경 감시 (레지스트리 Run 키, 시작프로그램, 예약 작업)
- **정보성 알림만** — 판정하지 않고 보고한다. 오탐 비용이 성능 탐지보다 훨씬 크다

**DoD**: 새 프로그램 설치/실행 시 과도한 경고 없이, 요약 형태로 보고.

> ⏸️ **보류 (2026-07-27) — 판정 기준을 세울 근거가 아직 없다.**
>
> 착수 전 실측이 계획서의 규칙을 그대로 쓸 수 없음을 보여 줬다.
>
> | 규칙 | 이 PC 에서의 빈도 |
> |---|---|
> | 셸·스크립트 호스트를 낳은 부모 (`winword → powershell` 류) | 시간당 **37.8건** |
> | 임시·`ProgramData` 경로 실행 | 시간당 3.5건 |
> | 신규 부모→자식 조합 | 시간당 **21.2건** |
> | 신규 실행 경로 | 시간당 **12.1건** |
> | 신규 원격 주소 | 시간당 **87건** |
>
> 절대 규칙은 쓸 수 없고(`claude → powershell` 99건이 전부 정상), **"처음 보는 것만"으로
> 바꿔도 12시간 데이터로는 시간당 수십 건**이다. 신규 조합은 학습이 쌓이며 줄어드는
> 추세(53 → 4)라 며칠 뒤 재평가할 수 있다.
>
> **네트워크는 학습으로도 해결되지 않는다.** CDN 주소가 계속 바뀌어 신규가 줄지 않고,
> `/24` 로 묶어도 조합이 1,214 → 1,029 로 거의 그대로다. 의미 있는 단위는 ASN·도메인인데
> 그건 외부 조회를 요구하고, "개인정보는 로컬을 벗어나지 않는다"와 충돌한다.
>
> ✅ **대신 데이터 계층만 먼저 넣었다** — `net_activity_5m`(§3.2). `net_connections` 가
> 72시간만 보존되므로 접지 않으면 이 Phase 가 나중에 학습할 것이 남지 않는다.
> **개별 주소는 저장하지 않는다.** 계획서가 원하는 "단시간 다수 신규 원격 IP"는
> 개수만으로 잡히고, 주소를 영구 보관하면 원본의 72시간 보존 정책을 우회하는 셈이 된다.

---

### Phase 14 — 패키징 & 배포 (3~4일)

- **PyTorch → ONNX 변환**, 런타임은 `onnxruntime`만 번들 (.exe 크기 2GB+ → ~120MB)
- 실행 모드 2가지:
  - **트레이 앱** (기본): 사용자 세션에서 실행, 시스템 트레이 아이콘, 대시보드 열기 메뉴
  - **Windows 서비스** (선택): `pywin32` 서비스, 관리자 권한 ETW 수집 담당. 트레이 앱과 로컬 IPC(named pipe)
- PyInstaller `--onedir` (onefile은 시작이 느리고 DLL 문제 잦음)
- 첫 실행 마법사: 데이터 저장 위치, 알림 채널, 리소스 예산, 자동 시작 등록
- ~~자동 시작: 작업 스케줄러 등록 (레지스트리 Run 키보다 안정적)~~ ✅ **2026-07-28 에 앞당김.**
  남은 작업이 전부 데이터 축적 대기인데 상주 실행이 수동이라, 켜는 것을 잊으면 그날치가
  통째로 빈다 — §8 원칙 1 에 걸리는 유일한 항목이라 패키징까지 끌지 않았다.
  `tools/install_autostart.ps1`. 전제로 중복 실행 방지(`runtime/singleton.py`)가 먼저 들어갔다.
  exe 패키징 시에는 `<Command>` 가 pythonw 가 아니라 exe 를 가리키도록 바꾸면 된다.
- 무결성: DB 손상 감지 시 자동 복구, 설정 파일 스키마 검증
- 업데이트 채널(선택): GitHub Releases 폴링

**DoD**: 클린 Windows 환경에서 설치 → 부팅 시 자동 시작 → 7일 무중단 동작.

---

## 5. 폴더 구조

```
argus/
├── argus/
│   ├── __main__.py              # 엔트리포인트
│   ├── supervisor.py            # 스레드 생명주기 + 리소스 예산 가드
│   ├── config/
│   │   ├── loader.py            # YAML + env 오버라이드 + 스키마 검증
│   │   ├── settings.yaml
│   │   └── rules/
│   │       ├── performance.yaml
│   │       └── security.yaml
│   ├── collector/
│   │   ├── base.py              # Collector 인터페이스, 주기/에러 격리
│   │   ├── system.py            # psutil
│   │   ├── pdh.py               # Windows 성능 카운터
│   │   ├── gpu.py               # pynvml
│   │   ├── process.py           # 적응형 계층 샘플링
│   │   ├── network.py
│   │   ├── qoe.py               # 체감 지표
│   │   └── etw/                 # Phase 12
│   │       ├── session.py
│   │       ├── providers.py
│   │       └── presentmon.py
│   ├── storage/
│   │   ├── hot.py               # SQLite WAL
│   │   ├── warm.py              # Parquet + DuckDB
│   │   ├── schema.sql
│   │   ├── writer.py            # 배치 writer, 백프레셔
│   │   └── retention.py         # 다운샘플/압축 잡
│   ├── features/
│   │   ├── pipeline.py
│   │   ├── rolling.py
│   │   └── store.py
│   ├── context/
│   │   ├── regime.py            # GMM + HMM
│   │   └── labeler.py           # 레짐 자동 명명
│   ├── detection/
│   │   ├── base.py              # Detector 인터페이스 (clock 주입)
│   │   ├── baseline.py          # EWMA + robust Z
│   │   ├── rules/
│   │   │   ├── engine.py
│   │   │   └── expr.py          # 안전한 AST 표현식 평가기
│   │   ├── isolation.py
│   │   ├── streaming.py         # river Half-Space Trees
│   │   ├── fingerprint.py
│   │   ├── sequence.py          # TCN-AE
│   │   ├── changepoint.py       # BOCPD
│   │   └── security.py
│   ├── explain/
│   │   ├── bottleneck.py
│   │   ├── attribution.py       # Shapley 근사 + 선행성
│   │   └── report.py            # 자연어 리포트 생성
│   ├── decide/
│   │   ├── fusion.py            # 신호 → incident
│   │   ├── suppression.py
│   │   └── budget.py
│   ├── notify/
│   │   ├── toast.py
│   │   ├── discord.py
│   │   └── digest.py            # 일간/주간 요약
│   ├── learning/
│   │   ├── registry.py          # 모델 버전 관리 + 섀도 배포
│   │   ├── trainer.py
│   │   ├── drift.py
│   │   └── feedback.py
│   ├── dashboard/
│   │   ├── app.py
│   │   └── pages/
│   │       ├── 1_live.py
│   │       ├── 2_timeline.py
│   │       ├── 3_incidents.py
│   │       ├── 4_processes.py
│   │       ├── 5_regimes.py
│   │       ├── 6_health.py
│   │       └── 7_models.py
│   └── tray/
│       └── app.py               # 시스템 트레이
├── tools/
│   ├── fault_injector.py        # 결함 주입
│   ├── replay.py                # 리플레이 하네스
│   └── eval.py                  # 스코어보드
├── tests/
│   ├── unit/
│   ├── integration/
│   └── scenarios/               # 결함 시나리오 정의
├── packaging/
│   ├── argus.spec               # PyInstaller
│   └── installer.iss            # Inno Setup (선택)
├── docs/
│   ├── Argus.md                 # 원본 스펙
│   └── PLAN.md                  # 이 문서
├── pyproject.toml
└── README.md
```

---

## 6. 기술 스택 (개정)

| 영역 | 선택 | 비고 |
|---|---|---|
| 런타임 | **Python 3.12** | 3.14는 PyTorch·river 지원이 불확실해 회피. 3.12는 전 의존성이 안정 지원 |
| 시스템 메트릭 | psutil, pywin32(PDH) | |
| GPU | pynvml | NVML 부재 시 graceful degradation |
| 커널 이벤트 | pywintrace / xperf fallback, PresentMon | Phase 12, 고위험 |
| 핫 저장소 | SQLite (WAL) | 표준 라이브러리 |
| 웜 저장소 | pyarrow + duckdb | 장기 분석 쿼리 |
| 통계 | numpy, scipy | |
| 배치 ML | scikit-learn | IsolationForest, GMM |
| 스트리밍 ML | river | Half-Space Trees, 온라인 통계 |
| 시계열 DL | PyTorch → ONNX | 학습만 torch, 배포는 onnxruntime |
| 변화점 | ruptures + 자체 BOCPD | |
| HMM | hmmlearn | 레짐 평활화 |
| 설정/룰 | PyYAML + pydantic | 스키마 검증 필수 |
| 대시보드 | Streamlit + plotly | |
| 트레이 | pystray + Pillow | |
| 알림 | win11toast, requests(Discord) | |
| 패키징 | PyInstaller (onedir) | |
| 테스트 | pytest, hypothesis | 시계열 속성 기반 테스트 |

---

## 7. 주요 리스크

| 리스크 | 영향 | 대응 |
|---|---|---|
| **수집 오버헤드가 관측 대상을 오염** | 치명적 | self-telemetry + 자동 스로틀 (Phase 0에 배치) |
| **오탐 폭발로 사용자가 알림 차단** | 치명적 | 레짐 조건부 + 알림 예산 + `for`/`cooldown` |
| ETW 파이썬 바인딩 불안정 | 중 | Phase 12로 후치, xperf 서브프로세스 fallback |
| 라벨 데이터 부족으로 모델 평가 불가 | 높음 | 결함 주입기로 합성 ground-truth 생성 (Phase 2) |
| PyInstaller + PyTorch 번들 비대 | 중 | ONNX 변환 |
| SQLite 쓰기 경합 | 중 | WAL + 단일 writer 스레드 + 배치 |
| 하드웨어 변경 시 베이스라인 무효 | 중 | 드리프트 감지 + 자동 리셋 |
| 레짐 오분류가 하위 탐지 전체를 오염 | 높음 | HMM 평활화 + confidence 낮으면 무조건부 베이스라인으로 폴백 |

---

## 8. 실행 순서 (2026-07-27 재배치)

**번호 순서대로 하지 않는다.** Phase 번호는 주제 분류지 실행 순서가 아니다. 실제로
`4-A → 10(부분) → 8` 로 갔고 매번 근거가 있었다. 남은 것도 아래 원칙으로 정렬한다.

### Phase 8 — 제품 기준으로 통과 (2026-08-02 18:0x 채점)

```
제품 1순위 지목률 : 8/8  = 100.0%   [OK] DoD 85%    ← 자원을 알려주지 않은 수치
함수 1순위 지목률 : 18/18 = 100.0%  [OK]
```

**07-30 의 "7/7 = 100%" 는 자원을 알려준 상태의 수치였다.** 같은 구간에서 제품은 4건 모두
엉뚱한 프로세스를 지목하고 있었다 — 스코어보드가 제품이 하지 않는 일을 재고 있었던 것이다
(9번에서 채점을 제품 경로로 바꾸자 드러났다). 그 뒤 12·13·15·17·18 을 차례로 고쳐
`표본 → first → 배수 → 발화 → 사건 → 자원 → 귀인 1위` 를 끝까지 닫았다.

마지막 한 칸이 18번(스왑)이었다 — `if swap:` 한 줄 때문에 이 기계는 시간의 90.8% 동안
메모리 병목 판정을 달고 있었고, 그래서 핸들 누수 8건이 전부 `메모리 압박 — chrome 70%` 로
발표됐다. 고치자 12.5% → 100% 가 됐다. 수치와 근거는 CHANGELOG 에 있다.

**이 수치의 의미는 여전히 좁다.** 07-30 에 적어 둔 한계 중 "자원을 입력으로 받는다"는
9번이 해소했지만 나머지는 그대로다.

- **시나리오가 `handle_leak` 하나뿐이다.** 8건 전부 같은 종류다. `cpu_spin`·`disk_thrash`
  는 PC 가 실제로 느려져 자리를 비울 때만 돌릴 수 있고, 이 NVMe 에서 `disk_thrash` 는
  증상 자체를 만들지 못해 채점에서 빠진다.
- **하드웨어가 하나뿐이다.** 08-02 에 문턱을 머신 상대값으로 바꿨지만(20번) 4코어·4GB
  에서의 동작은 단위 테스트로만 확인됐다.
- **정답이 "주입 프로세스 트리"다.** 실사용의 "느린 이유"는 이보다 흐릿하다 — 라벨이
  없어 채점할 수 없는 영역이 남아 있다.

**주입 배치를 다시 돌릴 때 알아 둘 것.**

```
for r in 6 10 15 20 8; do
  .venv\Scripts\python.exe tools\fault_injector.py handle_leak --duration 720 --handle-rate $r
  sleep 180   # 비교 창(주입 150초 전부터)이 앞 주입에 오염되지 않게
done
```

- **두 주입이 겹치면 두 구간 다 못 쓴다.** 2026-08-02 에 두 번 겪었다. 겹치면 `procleak`
  이 두 `python` 을 함께 보고 귀인이 이름으로 묶어, 어느 구간의 결과인지 말할 수 없게 된다.
  - **원인 1: 종료 대기를 `tasklist` 로 했다.** pid 를 못 찾고 즉시 통과해 앞 주입이 도는
    중에 다음 회차가 시작됐다. **대기 루프를 아예 쓰지 말 것** — 주입기를 포그라운드로
    부르면 순차 실행이 구조적으로 보장된다(`for r in …; do python fault_injector.py …; done`).
  - **원인 2: 배치 스크립트를 죽였는데 자식이 살아남았다.** 래퍼 셸만 죽고 그 자식
    스크립트가 계속 돌아 30분 뒤 다음 회차를 시작했다. 중단할 때는 **프로세스 트리 전체**를
    확인한다 — `Get-CimInstance Win32_Process -Filter "Name='python.exe' or Name='bash.exe'"`
    로 `ParentProcessId` 까지 보고, 남은 것이 없는지 확인한 뒤에 다음 배치를 시작한다.
  - 겹쳐 버린 라벨은 `completed=0` 으로 닫으면 **채점에서 자동 제외**된다("증상 미관측").
    `notes` 에 이유를 적어 둔다 — 나중에 왜 표본이 비는지 설명할 수 있어야 한다.
- **채점 가능한 표본이 7건 이상 남도록 돌린다.** `MIN_SCORED = 7` 이라 6건이면 결과가
  아니라 `[보류]` 다(85% 를 가르는 최소 조합이 6/7). 위 5회는 그 자체로 부족하다 —
  07-30 에는 어제 표본 2건이 우연히 살아 있어 7건에 닿았고, 보존 정리가 그것까지
  지웠으면 75분을 쓰고도 판정이 안 나왔다. **돌리기 전에 먼저 채점을 한 번 돌려
  살아 있는 표본 수를 센다** — 읽기 전용이라 주입 없이도 확인된다.
- `--handles` 기본값은 **2,000** 이다(파일 1개당 핸들 메트릭 +2 → 약 +4,000). 07-29
  표본은 5,000 이었다. 2,000 으로도 1순위를 지켰으므로 기본값으로 충분하다.
- 75분간 **무거운 프로그램을 새로 켜지 않는다.** 게임이 뜨면서 핸들 수천 개를 열면
  그게 "구간 중 새로 뜬 프로세스"가 되어 주입 프로세스와 경쟁하고, 채점이 실패했을 때
  **로직 탓인지 게임 탓인지 구분할 수 없게 된다.** 이미 떠 있던 프로그램은 비교 창에도
  있으므로 계속 써도 된다(07-30 에 크롬 13개·디스코드 9개가 떠 있는 상태로 통과했다).
- **상주 인스턴스를 주입 중에 재시작하지 않는다** — 그 구간에 구멍이 나고 그게 바로
  채점할 표본이다. 끝난 뒤 시작 시각으로 재시작 여부를 확인해 둔다.
- 시작 전에 **예약 작업과 Defender 검사 일정을 확인한다.** 07-29 채점을 망친
  `compattelrunner` 는 사용자가 켠 프로그램이 아니라 Windows 예약 작업이었다.
- **끝내기 전에 라벨이 닫혔는지 본다.** 주입기는 `finally` 에서 `ts_end` 를 쓰지만
  **전원이 끊기면 그 코드가 돌지 않는다** — 07-30 16:25 에 그렇게 되어 `#47` 이 열린 채
  남았다. 열린 라벨은 두 곳을 망친다: 보존 정리가 그 구간을 계속 지키고(이제 계획 길이로
  상한을 걸어 무한히 자라지는 않는다), **PID 가 재사용되면 그 PID 의 남의 데이터가 섞여
  읽힌다**(`inject_progress.py` 가 `1220%` 를 찍었다). 확인은 한 줄이다 —
  `SELECT COUNT(*) FROM fault_injections WHERE ts_end IS NULL` 이 0 이어야 한다.
  `Stop-Process` 는 Windows 에서 강제 종료라 `finally` 를 돌리지 않으므로 라벨을 닫지 못한다.

**75분을 태우기 전에 — 실패를 일찍 잡는 두 가지.** 07-30 오전에 5회 중 3회가 미탐이었는데
그 사실을 75분이 지나서야 알았다. 실패는 1회차 2분 시점에 이미 관측 가능했다.

1. **실패 신호를 배치 시작 전에 적어 둔다.** "어떤 수치가, 언제, 얼마면 이 실행은 이미
   죽은 것인가." **그 값은 DoD 에서 유도한다** — 최종 판정이 "1위 기여자가 주입
   프로세스"이고 거기까지 가는 사슬은 `표본 → first → 배수 → 발화 → 사건 → 귀인 1위` 다.
   중간 점검은 이 사슬의 **고리**를 재야 한다. 사슬 밖의 대리 지표는 세 번 다 틀렸다
   (아래 `inject_progress.py` 항목).
2. **첫 회차를 예광탄으로 쓴다.** 5회를 한 덩어리로 던지지 말고 1회차만 돌려 판정하고,
   통과하면 나머지를 돌린다. 회차 사이에 이미 180초 대기가 있으므로 구조 변경이 아니다.
   비용은 대기 한 번, 이득은 실패 시 12분에서 끊는 것 — 오전 배치는 63분을 아꼈다.

**진행 상황은 `tools/inject_progress.py` 로 본다**(`--watch` 로 5초 갱신). 이 도구가 판정에
쓰는 것은 `procleak.judge()` **자체**다. 근사값으로 재려던 시도가 세 번 틀렸다.

| 재려던 것 | 왜 틀렸나 |
|---|---|
| 표본 밀도(행/초) | 사슬 밖의 대리 지표. 주입이 상한에 닿으면 증가가 멈춰 핸들 증가량 상위에서 빠지므로 **밀도가 떨어지는 것이 정상**이다 — 정상 실행에 경고를 띄웠다 |
| `first`·배수를 SQL 로 직접 계산 | 이것도 대리 지표. `procleak` 은 자기 추적 창의 표본으로 판정하므로 값이 다르다(#39: SQL 15.9 통과 vs 실제 5.2). 방향까지 갈려 **발화한 주입에 ❌** 를 띄웠다 |
| 상한 없는 PID 조회 | PID 재사용. `ts <= ts_end` 를 안 걸어 #42 가 "표본 부족 4/20" 으로 나왔다(실제 30행) |

**새 점검 도구는 결과를 이미 아는 데이터 두 개로 보정한다** — 미탐 확정 데이터에 ❌ 가,
정상 확정 데이터에 ✅ 가 나와야 쓴다. 위 세 개는 전부 이 보정에서 잡혔다. 보정 없이 썼으면
셋 다 놓쳤다.

**로그 감시(Monitor)의 침묵을 성공으로 읽지 않는다.** 로그 줄만 보는 감시는 "죽었나"를
답할 뿐이고, 조용히 이상해지면 아무 알림도 오지 않는다. 의미 판정은 회차 경계마다 따로 돌린다.

`cpu_spin`·`disk_thrash`(유량 회귀 확인)는 PC 가 실제로 느려지므로 자리를 비울 때 따로
돌린다. 07-30 수정이 유량 경로를 건드리지 않았다는 것은 아직 테스트로만 확인된 상태다.

### 다음 세션 착수점 (2026-08-16 기준)

> **여기에는 최신 착수점 하나만 둔다.** 08-04~08-15 의 일곱 개는 2026-08-16 에
> `docs/DONE.md` 의 "지난 세션 착수점"으로 옮겼다 — 지우지 않았다. 같은 자리를
> 다시 건드릴 때 **왜 그 결론에 이르렀는지**가 거기 있다.

**임시 폴더 건은 끝났다 (2026-08-16 오전).** `pytest-of-준-old-20260813` 삭제 완료,
`.claude/settings.local.json` 의 `defaultMode` 도 `auto` 로 되돌렸다.

> **재부팅으로는 안 지워졌다 — ACL/소유권 문제였다.** 일반 권한 세션에서는
> `takeown` 이 **최상위만** 성공하고 하위 `pytest-469/470/471` 은 전부
> `Access is denied`, `icacls /t` 는 exit 5 로 끝난다. 같은 형태가 또 생기면
> 처음부터 **관리자 터미널**에서 `takeown /r` → `icacls /t` → `Remove-Item` 이다.
> 일반 세션에서 시도하는 것은 시간 낭비다.

**오늘(08-15 밤 ~ 08-16 새벽) 커밋 9건.** 전부 푸시됨.

| | 무엇 | 커밋 |
|---|---|---|
| 1 | 백필 미리보기와 저장이 같은 판정을 지나게 | `af73bf7` |
| 2 | `tools/` 가 초록불 뒤에서 죽는 유형을 규칙에 | `ae468c6` |
| 3 | 원인 모르는 사건의 표를 "원인 후보"라 부르지 않는다 | `ff84c70` |
| 4 | 끝난 주입 배치를 "남음 17분"이라 말하지 않는다 | `8498223` |
| 5 | **알림이 안 나간 사건도 물어본다** (미탐 → 답 대기) | `e6a063c` |
| 6 | 목록에서 알림 발송 여부가 보인다 | `81af962` |
| 7 | 관측 공백을 선으로 잇지 않는다 | `f3cdb88` |
| 8 | 창 닫기 4.07초 → 0.20초 · 기본 크기 1600x900 | `8f14d74` |
| 9 | 승인 프롬프트 원인 기록 | `32f1ac3` |

**라벨이 7 → 11건이 됐다** (정상 8 · 비정상 3). 사용자가 5건을 직접 답했다.

#### 착수 조건을 "전체 20건"에서 "발열 축 6건"으로 바꿨다 (08-16 오전)

`e6a063c`(미탐도 답 대기에 올린다)가 실제로 사는지 검증하다 **조건 쪽이 틀렸다는
것이 나왔다.**

**먼저 `e6a063c` 는 정상이다.** `#164`(08-10, 발열, `notified=0`)가 그 경로로 답
대기에 올라와 사람이 `real` 로 답했다. 지금 대기에 미탐이 0건인 것은 코드가 아니라
**발열 사건이 08-10 이후 안 나서**다(최근 10일 4건). 필터를 단계별로 풀어 확인했다 —
`최근 7일 THERMAL 미탐 1건 → 사람 답 없음 0건`. 고칠 것이 없다.

**그런데 라벨이 늘어난 방향이 등급 역전을 비껴갔다.**

```
08-14  라벨  7건 = CPU·경합 4(전부 normal) + 발열 3(전부 real)
08-16  라벨 11건 = CPU·경합 8(전부 normal) + 발열 3(전부 real)
                   ~~~~~~~~~~~~~~~~ 늘어난 4건이 전부 여기
```

**등급 역전 축(발열 미탐)의 표본은 3건에서 하나도 안 늘었다.** 전체가 20건이 되어도
발열이 3건이면 그때 내리는 판단의 근거는 그대로 3건이다. 20건은 애초에 "7건은 아직
적다"에서 나온 어림수였고(08-14), **재려는 대상을 세는 수가 아니었다.**

그래서 `tools/readiness.py` 에 판정 항목을 추가했다 — `SEVERITY_AXIS_MIN_LABELS = 6`.
6인 이유는 상수 옆 주석에 있다(3건은 우연히도 12.5%, 6건이면 1.6%). **축은 여기서
다시 정하지 않고 `label.ask_unnotified_bottlenecks` 를 읽는다** — 같은 개념이 두 곳에
있으면 config 를 고쳐도 이 판정만 옛 축을 센다. 무력화 4/4 확인(문턱 3↔20 으로 판정
뒤집힘, 축을 기본값 아닌 `CPU` 로 바꾸면 `7건 normal 7` 로 세는 대상이 바뀜, 빈 목록은
별도 경로).

```
[대기] severity 등급 역전
      x THERMAL 축 사람 라벨 6건 이상
        └ 현재 3건 (real 3) · 전체 라벨은 11건 — 3건 부족
```

> **답 없이 7일 창을 지난 발열 미탐이 56건 있다** (`info` 51 · `warning` 5,
> 07-28~08-06). 표본이 없어서 못 세는 것이 아니라 **일부러 안 쓰는 것**이다 —
> 8~19일 전 미탐은 흔적이 없어 답이 짐작이 되고, 지금 발열 라벨 3/3 이 완벽히
> 일관돼 신뢰도가 높은데 거기에 흐린 답을 섞으면 그 일관성이 뭘 뜻하는지 알 수
> 없게 된다. **등급 역전을 재려고 재는 자를 망가뜨리는 셈이다.** 창을 넓히자는
> 말이 다시 나오면 이 문단을 먼저 읽는다.

**자동 라벨러가 미탐을 안 보는 것도 의도다** — `tools/autolabel_backfill.py:48` 의
`notified = 1`. 자동 라벨은 사람 답에서 뽑은 기준을 쓰므로 발열 미탐에 적용하면
기준이 자기를 확인할 뿐이다(순환). `auto_label` 34건이 전부 `notified=1` 인 것을
확인했다.

**`readiness.py` 는 여전히 "착수 가능: 없음"이다.** 이제 5개 항목 중 기각 2·완료 2·
**대기 1**(위)이다. 코드로 할 일이 없을 때 **`tools/` 를 실제로 돌려 보는 것**이 값이
있었다 — `--help` 로는 13개 중 0개가 걸리고 실행하면 2개가 걸렸다
(경위는 `docs/DONE.md` 의 08-15 착수점).

#### ▶ 다음에 할 일 — Phase 5·7 의 "이길 상대"를 실측으로 세운다 (08-16 오후 착수 예정)

**Phase 5 와 7 은 같은 하나의 선행 조건에 막혀 있다.**

| Phase | 보류 사유 (2026-07-27) |
|---|---|
| 5 — 다차원 이상탐지 | "이길 상대가 없다 — 룰이 못 잡는 시나리오가 없다" |
| 7 — 시퀀스 모델 | "DoD 대상이 존재하지 않는다 — `leak_slow`(60분 램프)가 없다" |

**둘 다 조건이 같다: 60분 램프를 주입해 룰이 못 잡는 것을 확인하는 것.** 확인해
보니 `fault_injector` 에 `--ramp` 가 이미 있어 **새 코드 없이 돌릴 수 있다.**

```
.venv\Scripts\python.exe tools\fault_injector.py memory_leak --ramp --duration 3600
    → 60분에 걸쳐 0 → 11883MB (가용 메모리 보호로 13035 → 11883 자동 하향)
    → 이 PC 에서 +18.2%p
```

**부하를 올리지 않는다.** `--mem-load` 기본 0.20 이 맞다 — 올리면 「메모리 압박」
(80%)에 걸려서 *"점진적 열화를 잡았나"* 가 아니라 *"메모리가 꽉 찼나"* 를 묻게 된다.
`rules.yaml:60` 의 주석이 이미 그 자리를 정확히 적어 뒀다("64GB PC 에서 누수가
18%p 를 먹어도 사용률은 46% 라 압박 룰에 걸리지 않는다 … Phase 7 의 주 타깃").

**60분을 태우기 전에 리플레이로 먼저 쟀다 (08-16 오전).** 실제 최근 90분
시계열에 램프를 얹어 `MetricBaseline` 실물에 먹였다.

```
[미탐] 60분 램프          문턱에 가장 가까웠던 순간이 -6.34%p (한 번도 못 넘음)

대조 — 룰이 죽은 게 아니다:
[발화] 같은 양을 5분에     램프 시작 5.1분 뒤
[미탐] 같은 양을 10분에    +0.74%p 까지 갔으나 90초 지속을 못 채움
[미탐] 같은 양을 20분에    -4.34%p
[미탐] 같은 양을 30분에    -7.37%p
```

**원인이 구조적이다.** 베이스라인 창 30분 · 램프 60분 → median 이 창의 절반
(15분)만큼 뒤처져 따라 오르고, 현재값과의 격차가 **4.5%p 에서 평형**이다. 문턱은
`median + max(5σ, 5%p)` 라 최소 5%p 가 벌어져야 한다. **4.5 < 5 — 영원히 안 걸린다.**
느린 누수는 베이스라인이 그것을 "평소"로 학습해 버려서 안 걸린다.

**그래서 Phase 5·7 의 "이길 상대"가 실재한다.** 다만 아직 확정이 아니다 — 아래
1번이 남았다.

**착수 순서.**

| | 무엇을 | 왜 | 시간 |
|---|---|---|---|
| ~~1~~ | ~~합성 램프를 **제품 리플레이 경로**에 태운다~~ | **끝났다 (08-16 오후). `tools/ramp_replay.py`. 결과는 아래.** | 실제 ~40분 |
| ~~2~~ | ~~예측을 먼저 적고 실제 60분 주입~~ | **취소 (08-16 오후).** `procleak` 이 8.9분 만에 잡아 미탐 라벨이 안 나온다 — 아래 참조. **70분 절약** | ~~70분~~ |
| 3 | Phase 5·7 의 보류 상자 갱신 | 보류 사유가 **유지**된다 — 다만 조건이 "60분 램프"가 아니라 아래 세 후보로 바뀐다 | — |

#### 1번 결과 — 답이 `per_program` 설정에 따라 갈린다 (08-16 오후)

고정 창 `09:40~11:15`(유휴, CPU 6%·mem 25.5% 평탄) · 램프 `+18.2%p` ·
`--end 1786846500 --warm-minutes 35`. **제품 탐지기를 `registry.build()` 로 세워
설정 배선 그대로 돌렸다.**

```
                per_program=off (배포 기본값)     per_program=on (이 PC)
  5분 램프      [발화] +11.67%p  3.3분 뒤          [발화] +11.67%p  3.3분 뒤
 10분 램프      [발화] +11.58%p  5.1분 뒤          [발화] +11.58%p  5.1분 뒤
 20분 램프      [발화]  +7.20%p  8.7분 뒤          [발화] +11.45%p  8.7분 뒤
 30분 램프      [발화]  +2.66%p 12.4분 뒤          [발화] +12.05%p 12.4분 뒤
 60분 램프      [미탐]  -1.85%p                    [발화]  +4.01%p 23.4분 뒤
```

**배포 기본값에서는 오전 시뮬이 옳았다** (-1.85%p vs 오전 -6.34%p — 창이 달라 크기는
다르지만 방향과 결론이 같다). **이길 상대가 실재한다.**

**그런데 이 PC 설정에서는 룰이 60분 램프를 잡는다.** 원인은 프로그램별 베이스라인이다 —
`program_window_s=3600` 에 `program_min_interval_s=5.0` 으로 솎아 담아서, 전역(30분 창)
보다 **훨씬 늦게** 램프를 학습한다. median 궤적 실측(chrome, 60분 램프):

```
       t=+0m   t=+16m   t=+24m   t=+32m   t=+40m   t=+58m
전역   25.70    25.77    28.33    30.67    33.14    39.15   ← 창의 절반만큼 뒤처져 따라온다
프로그램 25.70   25.70    25.80    26.22    28.64    33.94   ← 32분까지 사실상 고정
```

느린 누수를 못 잡는 원인이 "베이스라인이 그것을 평소로 학습해 버린다"였으므로,
**학습이 느린 축이 하나 더 있으면 그 축이 잡는다.** per_program 은 민감도를 올리려고
켠 것인데 여기서는 **점진적 열화 탐지**로 작동했다.

#### 그런데 `procleak` 이 잡는다 — 2번을 돌릴 이유가 사라졌다

위 표는 전부 `--detector rules` 다. **상주는 `rules,procleak` 둘을 돌린다**
(`defaults.yaml:173`). `procleak` 은 `rss_mb` 를 보므로(`procleak.py:52`)
램프를 시스템 지표에만 얹은 위 측정은 **procleak 에게 아무 일도 일어나지 않은
것과 같았다.** 램프를 프로세스 RSS 로도 넣어(`--with-process`) 다시 쟀다.

```
--detector rules,procleak --with-process --per-program off

  5분 램프   [발화] 메모리 이상 증가              — 램프 시작 3.3분 뒤
 60분 램프   [발화] procleak:python.exe(rss_mb)  — 램프 시작 8.9분 뒤
```

**시스템 룰이 못 잡는 것을 procleak 이 8.9분 만에 잡는다.** 산수로 먼저 예측한
5~7분과 근접하다 — 창 900초 · 최소 증가 512MB · 3배 · 단조성 0.9 인데, 분당
198MB 짜리 램프는 초반 15분 창에서 `ratio ≈ 6.8` · `delta ≈ 2.2GB` · 단조성 1.0 이다.
후반 창에서는 `ratio` 가 1.2 로 떨어져 못 잡지만, **초반에 이미 잡은 뒤다.**

> **그래서 Phase 5·7 의 보류 사유는 살아 있다.** "이길 상대가 없다"가 오전·오후에
> 두 번 뒤집힐 뻔했는데, 두 번 다 **재는 대상이 제품보다 좁아서** 생긴 착시였다
> (오전엔 `MetricBaseline` 만, 오후엔 `rules` 만).
>
> **2번(실제 60분 주입 70분)은 지금 형태로 돌릴 이유가 없다.** 목적이 "Phase 7 용
> 실제 미탐 라벨"인데 procleak 이 잡아 버려 미탐이 안 나온다. **70분을 아꼈다.**

**남은 진짜 질문은 이것이다 — `procleak` 이 못 잡는 느린 누수가 있는가.**
procleak 이 잡는 것은 *"한 프로세스의 RSS 가 단조 증가"* 하나뿐이다. 아래는
그 형태가 아니라서 **아직 아무도 재지 않았다.**

| 후보 | 왜 procleak 이 못 잡나 | 주입 가능? |
|---|---|---|
| 여러 프로세스에 분산된 누수 | 프로세스별 추적이라 각각은 문턱 미달 | `fault_injector` 확장 필요 |
| 비단조 누수 (썼다 지웠다 하며 우상향) | `monotonic_ratio=0.9` 를 못 채운다 | 확장 필요 |
| 프로세스 밖 누수 (커널·드라이버·페이지드 풀) | `process_metrics` 에 안 잡힌다 | 주입 자체가 어렵다 |

**Phase 5·7 을 여는 조건은 "60분 램프"가 아니라 이 셋 중 하나다.** 다음에 이
자리를 건드릴 때 60분 램프부터 다시 시작하지 않는다.

#### "베이스라인 창을 늘리면 되지 않나" — 안 된다 (08-16 오후 실측)

프로그램별 베이스라인(창 3600초)이 60분 램프를 잡은 것을 보고, **시스템 지표에도
긴 창을 두면 ML 없이 될 것**이라 판단해 재봤다. `ARGUS_DETECTION__BASELINE_WINDOW_S`
로 창만 바꿔 같은 리플레이를 돌린 결과다.

```
창 1800s  [미탐] -1.85%p
창 3600s  [발화] 램프 시작 23.0분 뒤
창 7200s  [미탐] -3.32%p      ← 더 늘렸는데 못 잡는다
```

**단조롭지 않다.** 램프 끝 시점의 문턱을 분해하면 이유가 나온다:

```
           끝값    median   sigma   5σ문턱   실질문턱    여유
 1800s    47.39    39.95    4.412    62.02    62.02    -14.62
 3600s    47.39    35.19    7.341    71.90    71.90    -24.50
 7200s    47.39    30.22    6.842    64.43    64.43    -17.03
```

**램프가 자기 σ 를 키워 자기를 숨긴다.** 창을 늘리면 median 은 확실히 덜 따라오지만
(39.95 → 30.22), 같은 창에 램프 구간이 들어오면서 σ 가 평소 1.3 에서 7.3 으로 뛰고
**5σ 문턱이 62~72 로 도망간다.** 3600초가 23분에 잡은 것은 *그 시점에는 σ 가 아직
안 컸다*는 타이밍 우연이지 창 길이가 만든 안정적 성질이 아니다 — 다른 시각·다른 PC
에서 재현된다고 볼 근거가 없다.

> **그래서 이 발견은 Phase 5·7 의 근거를 약화시키는 게 아니라 강화한다.**
> 중앙값/MAD 단변량 룰은 느린 램프에 **구조적으로** 약하다: median 이 따라 오르고
> σ 가 커져 **문턱이 양쪽에서 도망간다.** 창 튜닝으로 고칠 수 있는 종류가 아니다.
> (같은 질문이 다시 나오면 이 문단을 먼저 읽는다.)

**도구를 만들며 잡은 것 셋** — 전부 처음 판에서 **정반대 결론**을 낼 뻔했다.

| 무엇 | 어떻게 드러났나 |
|---|---|
| **대표 룰이 다른 룰을 가린다** | `rules.py:430` 이 한 틱의 발화 중 심각도 최고 하나만 대표(`rule`)로 세운다. 「메모리 이상 증가」는 `info` 라 게임 구간의 「CPU 과부하」에 자리를 뺏겨 **발화했는데 미탐으로 집계**됐다. → `rules=[...]` 를 본다 |
| **램프와 무관한 룰을 성공으로 셌다** | 입력이 실제 시계열이라 램프가 안 건드린 룰이 얼마든지 발화한다. 근접도가 `-1.24%p`(문턱 미달)인데 "발화"로 찍힌 것이 단서였다 |
| **창이 매 실행 밀려 재현이 안 됐다** | 기본값이 "최근 N분"이라 게임이 켜지자 수치가 통째로 바뀌었다. → `--end` 로 고정한다 |

**보정 2개로 도구를 검증했다**(결과를 이미 아는 입력):
`--minutes 5` → 발화해야 하고 발화했다 · `--delta 0` → 발화하면 안 되고 양쪽 다 미탐(-3.57/-3.30%p).

**돌릴 때 알아 둘 것.**

- **게임 금지 · 유튜브는 괜찮다.** 실측(08-16 08:4x): 전체 63.6GB 중 사용 17.5GB
  (27%), 크롬은 **이미** 1.49GB/14프로세스로 떠 있다. 주입 11.9GB 를 더해도 46%,
  유튜브 탭이 더하는 1GB 는 2%p 라 판정을 못 바꾼다. 다만 **주입 중에 크롬을 새로
  켜지 않는다** — 이미 떠 있던 것은 비교 창에도 있어 상쇄되지만 구간 중 새로 뜬
  프로세스는 경쟁자가 된다.
- **상주 재시작 금지** (구간에 구멍이 나고 그게 채점할 표본이다).
- 09:44 · 매일 `ScanForUpdates` 예약 작업이 있다. 결과가 애매하면 이것부터 의심한다.
- 끝나면 `SELECT COUNT(*) FROM fault_injections WHERE ts_end IS NULL` 이 0 인지 본다.

> **재현 파라미터** (스크래치 스크립트는 세션과 함께 사라진다). 램프 `+18.2%p` ·
> 베이스라인 창 `detection.baseline_window_s = 1800` · 「메모리 이상 증가」는
> `for: 90s`, `median + 5σ` AND `median + 5%p`. 판정은 `Stats.threshold(k)` 를
> 그대로 쓰고 손으로 다시 만들지 않는다. 입력은 지어낸 노이즈가 아니라 **실제
> `metrics_raw` 시계열**에 램프를 더한 것이다(이 PC: median 26.0% · σ 1.483,
> σ 하한 `|median|*0.05 = 1.3` 에 거의 붙어 있다 — 메모리가 매우 평탄하다).

**미뤄 둔 것** — 근거가 약하거나 조건이 안 찼다.

| 항목 | 왜 미뤘나 |
|---|---|
| `severity` 등급 역전 | **발열 축 라벨 6건 대기** (현재 3건). 전체 라벨 수로 세지 않는다 — 위 참조 |
| `Database.query()` 락 분리 | 근거 약함 — `DB 쓰기가 느리다` 는 기동 직후가 대부분 |
| 다른 PC 데이터 | 하드웨어가 하나뿐이라 문턱 일반화를 검증할 수 없다 |
| **"실제로 쓰던 시간"** | 사용시간이 지금은 "켜져 있던 시간"이다 — 게임을 켜 두고 자리를 비운 시간이 들어간다. `foreground_ratio` 로 가중하면 되지만 **근거가 없다**: 며칠 써 보고 "이 숫자가 실제와 다르다"고 느껴질 때 착수한다. (포어그라운드 필터로 절반은 08-12 에 끝냈다 — `8bf77a6`·`9def309`, 200종 → 61종) |
| Phase 5 · 7 | 60분 램프가 08-16 에 상대 자격을 잃었다(위). 남은 후보는 분산·비단조·프로세스 밖 누수 |

### 창을 개발 중에 띄울 때 (2026-08-16 — 세 번 헛짚고 정리한 것)

**결론부터: `pythonw` 로도 창은 뜬다.** 한때 "안 뜬다"고 적었으나 그 뒤 같은 방식으로
떴다. 재현 조건을 못 세웠으므로 **버그로 기록하지 않는다** — 확인 안 된 것을 확인했다고
적으면 다음 세션이 없는 버그를 쫓는다.

대신 실제로 확인된 것 셋만 남긴다.

| 확인된 것 | 근거 |
|---|---|
| **RSS 로 "창이 떴나"를 판정할 수 없다** | 5.6MB 일 때도 창은 떠 있었다. 게임처럼 메모리를 많이 쓰는 프로세스가 있으면 Windows 가 다른 프로세스의 워킹셋을 트림한다. 이걸로 세 번 오판했다 |
| **판정은 `EnumWindows` + `IsWindowVisible`** | `Get-Process` 의 `MainWindowTitle`·`MainWindowHandle` 도 창이 떠 있는데 비어 있었다 |
| **PowerShell `Start-Process` 에 `$env:` 가 안 실린다** | `$env:ARGUS_UI_SCREEN="1"` 을 같은 호출에서 세우고 띄웠는데 주 모니터(X=0)에 떴다. 게임 중이면 그게 곧 방해다 |

**그래서 개발 중 창은 이렇게 띄운다** — 환경변수를 프로세스 인자로 확실히 실어야 한다.

```bash
ARGUS_UI_SCREEN=1 .venv/Scripts/python.exe -m argus.desktop.app   # Bash 에서
```

`tools/ui_snapshot.py` 는 `QWidget.grab()` 이라 이 문제와 무관하고, 게임 중 화면
확인은 그쪽이 안전하다(창을 사용자 화면에 띄우지 않는다).

### 2026-07-30 전체 점검 결과

#### 검증되지 않는 규칙 7개 (mutation 테스트로 확인)

규칙을 무력화해도 테스트가 잡지 못한다. **통과는 증거가 아니다**를 실제로 측정한 것이다.

| 규칙 | 무력화했을 때 | 왜 중요한가 |
|---|---|---|
| **`MAD_TO_SIGMA = 1.4826`** | **151개 전부 통과** | 모든 z 점수의 기반. 1.0 으로 바꾸면 이상 점수가 48% 커져 탐지 민감도가 통째로 바뀐다 |
| `DEFAULT_MIN_DAYS = 3` | 10개 통과 | 지문 자격 |
| `DEFAULT_MIN_BUCKETS = 100` | 10개 통과 | 지문 자격 |
| `MIN_DAY_HOURS = 6.0` | 10개 통과 | 지문 자격 |
| `_SCORE_SATURATION_Z = 8.0` | 36개 통과 | 알람 품질 ② 와 직결 |
| `_OVERRIDE_RATIO = 1.5` | 16개 통과 | 병목 뒤집기 문턱 |
| `MAX_EXTEND_BEFORE_S` | 19개 통과 | 사건 구간 확장 상한 |

**지문 자격 조건 셋이 전부 안 잡힌다.** 세 값을 모두 0 으로 만들어도 통과하므로,
Phase 6-B 를 완료로 닫았지만 그 핵심 조건은 검증된 적이 없다.

잡히는 규칙(정상): 룰 지속조건·쿨다운, 디스크 체감하한, 선행성 최소기여도,
procleak 단조성·급락리셋, 롤업 워터마크.

> **2026-08-03 재측정으로 갱신됐다** (위 "11 결과"). 이 표의 7개 중 6개는 07-30 에
> 테스트가 붙어 회귀 없이 잡힌다. `MIN_DAY_HOURS` 만 그대로인데 구현이 없어서다.
> **그리고 "잡히는 규칙"으로 적힌 위 줄이 한 군데 틀렸다** — `procleak` 단조성은
> 로직만 잡히고 config 배선은 비어 있었다. 오염된 환경의 판정을 의심한 것이 맞았다.

### 정렬 원칙

1. **지연 비용이 있는 것을 먼저.** 대부분의 작업은 늦게 해도 손해가 없다. 그러나
   **데이터를 잃는 작업은 다르다** — 하루 늦으면 하루치가 영구히 사라진다.
2. **막힌 것을 푸는 것을 그다음.** 하나가 여럿을 동시에 여는 작업이 있다.
3. **데이터 대기가 필요한 것은 뒤로.** 대신 그 데이터를 *모으기 시작하는* 작업은 앞으로.
4. **이길 상대가 없는 것은 하지 않는다.** 입증 대상을 먼저 만들고 재평가한다.

### 현재 상태

| 완료 (DoD 확인) | 착수했으나 DoD 미달 | 데이터 대기 | 보류 | 미착수 |
|---|---|---|---|---|
| 0 · 1 · 2 · 3 · 4-A · 6 · **8** · 9 · 10 | — | 4-B | 5 · 7 · 13 | 12 · 14 |

> **2026-08-02 갱신 — Phase 8 을 완료로 올렸다.** 제품 경로 1순위 지목률 8/8 = 100%
> (DoD 85%), 함수 경로 18/18. **자원을 알려주지 않은 수치다.** 07-30 의 "7/7 = 100%" 는
> 자원을 입력으로 받은 것이었고 같은 구간에서 제품은 4건 다 틀렸다 — 그 차이가 9번
> (채점을 제품 경로로)에서 드러났고, 12·13·15·17·18 을 차례로 고쳐 닫았다.
>
> **착수했는데 못 끝낸 Phase 는 다시 없다.** 남은 것은 데이터를 기다리는 것(4-B · 11),
> 채택 여부를 재평가할 것(5 · 7 · 13), 순서상 마지막인 것(12 · 14)뿐이다.
>
> **Phase 9 는 "완료"지만 알림 발송은 꺼져 있다**(`notify: false`). 융합·억제·예산은
> 돌고 severity 와 `notified` 판정까지 하지만, 실제 발송(Toast·Discord)은 오탐률이
> 검증된 뒤에 붙인다. 그 검증의 마지막 조각이 §8 의 1번(알림량 측정)이다.

> **2026-07-29 밤 갱신.** **Phase 6** 은 6-A(누수 탐지) + 6-B(지문)로 완료했다.
> **Phase 8** 은 오늘 채점에서 원래 있던 결함이 드러나 완료에서 내렸다
> (경위는 `docs/DONE.md` 의 "Phase 8 재개봉"). 08-02 에 다시 올렸다.
> "착수했는데 못 끝낸 Phase 는 이제 없다"던 앞 문단은 Phase 8 때문에 더 이상 맞지 않다.
>
(12 ETW · 14 패키징은 미착수 — 판단 근거가 아직 없거나 마지막 순서다.)

> **2026-07-29 갱신.** 표가 순서표와 어긋나 있었다(순서표는 Phase 10 DoD 를 ✅ 로 적어
> 뒀는데 이 표는 "DoD 미달"이었다). 둘 다 실측으로 판정해 닫았다.
> - **Phase 1** — 실사용 7.53시간 세션으로 DoD 충족. 별도 소크를 기다릴 필요가 없었다.
>   자동 시작이 붙은 뒤로는 실사용 자체가 소크다.
> - **Phase 10** — 실사용 사건 11건으로 확인 완료. 미달 사유였던 "사건 0건"이 해소됐다.
>
> **착수했는데 못 끝낸 Phase 는 이제 없다.** 남은 것은 데이터를 기다리는 것과 보류 중인
> 판단뿐이다 — 열려 있는 작업을 붙들고 있는 상태가 아니다.

### 순서

| # | 작업 | 근거 | 여는 것 |
|---|---|---|---|
| 1 | ~~**프로세스 롤업**~~ ✅ | 지연 비용이 있어 먼저 했다. 손실 0 으로 마감 | Phase 6, 13 |
| 2 | ~~**Phase 9** 융합·알림 (+`incidents`)~~ ✅ | 넷을 풀었다. 발송만 남겨 뒀다 | 대시보드 Incidents ✅, Phase 10 DoD ✅, Phase 11 |
| 3 | **알람 품질** (2026-07-28 삽입) — 진행 중 | 실측 21시간에서 신호 6건 중 4건이 한 룰(GPU 온도)이었고 전부 critical 이었다. **오탐을 줄이기 전에 탐지기를 더 얹으면 사건만 늘고 품질은 안 오른다** — Phase 6 이 이길 상대가 가짜가 된다 | Phase 6 의 스코어보드가 의미를 갖는다 |

**알람 품질 — 남은 것.** ①(방아쇠·병목 일치)은 끝났다. 실측에서 뒤이어 드러난 둘은 남겼다.

- **② 심각도와 점수가 반대로 붙는다.** 사건 10 은 `peak_score` 0.063(z=0.51 — 평소와
  사실상 같음)인데 warning 이라 알림 대상이 됐고, 사건 9 는 1.0(z=8.28)인데 info 라 빠졌다.
  *가장 평범한 사건이 알림을 받고 가장 이례적인 사건은 조용하다.* severity 를 룰이 정하는
  설계는 유지하되 **발송 게이트에 점수 하한**을 넣을지가 판단 사안이다.
- **③ 같은 알림이 영구히 반복된다.** `GPU 고온 지속` 은 쿨다운 60분이라 게임 세션마다
  다시 운다. 사용자는 이미 "게임하면 92도"를 안다. 표본이 2건이라 며칠 더 보고 정한다.
- **peak 선택이 방아쇠와 어긋날 수 있다** (①에서 발견). `_peak_and_baselines` 는 구간에서
  `cpu_total` 이 최대인 행을 peak 로 잡는다. 사건 9 는 그 시점에 GPU 스로틀이 걸려 있지
  않아 방아쇠(GPU 온도)의 근거 점수가 0 이 됐다. 방아쇠 지표 기준으로 peak 를 고르는 편이
  옳아 보이지만, 지표가 여럿일 때 무엇을 기준 삼을지가 정해지지 않았다.
- **PC 종료 시에도 정상 종료로 남기기** (2026-07-28 판단: 지금은 넣지 않는다). Windows
  종료는 `unclean_shutdown` / `reboot_or_power_loss` 로 기록된다 — 창도 콘솔도 없어
  `WM_ENDSESSION` 도 `CTRL_SHUTDOWN_EVENT` 도 닿지 않는다. 크래시와는 라벨로 구분되므로
  진단 근거가 오염되지는 않아 급하지 않다. 넣을 때 반드시 지킬 것 둘:
  **`WM_QUERYENDSESSION` 에는 무조건 TRUE 를 즉시 반환**하고(질의 단계에서 일하면 Windows
  종료가 막힌다 — 성능 모니터가 낼 수 있는 최악의 버그다), 정리에는 **하드 타임아웃**을
  건다(유예 시간 안에 못 끝내면 기록을 포기하고 나간다. 종료를 붙잡는 것보다 낫다).

### 착수 조건은 날짜가 아니라 표본으로 (2026-07-28)

위 작업 여럿이 "데이터가 며칠 쌓인 뒤"를 전제하는데, **날짜를 세는 것은 답이 아니다.**
하루 종일 게임한 3일과 유휴 위주의 3일은 같은 3일이 아니다.
`.venv\Scripts\python.exe tools\readiness.py` (웜 스토어를 읽어야 해 가상환경이 필요하다)
가 실제 DB 에서 조건을 세어 판정한다. 첫 실행(관측 2일)이 곧바로 둘을 드러냈다.

- **warning 이상 사건이 하루 3건꼴이다** (2일간 6건). 추정했던 4건/일보다 적지만, 착수선
  10건까지는 이틀이면 닿는다.
- **관측된 2일이 모두 '고부하'였다.** 진짜 병목은 사건 수가 아니라 **사용 패턴 다양성**이다.
  매일 게임을 하면 유휴 위주의 날이 영영 안 섞이고, 그때는 "게임 아닌 날의 점수 분포"를
  하루 단위가 아니라 **구간 단위**(유휴 구간에서 발생한 사건)로 봐야 한다. 조건을 그렇게
  바꿀지는 3일 뒤 실제 분포를 보고 정한다.
  → ~~**2026-07-29 에 해소됐다.** 유휴 위주의 날(07-29)이 들어와 다양성 조건은 충족됐다.~~
  → **틀렸다 (2026-07-29 밤 정정).** 그날 늦게 게임을 해서 07-29 가 **고부하 8.1h** 로
  뒤집혔다. `readiness.py` 는 지금 관측 3일을 전부 고부하로 센다. 다양성 조건은 **여전히
  미충족**이고, 이건 "하루가 끝나기 전에 판정을 기록하면 뒤집힌다"는 뜻이다 — 착수 조건을
  볼 때는 **그날이 끝난 뒤의 값**만 믿어야 한다.

#### 판정 자체가 틀려 있었다 (2026-07-29)

착수 조건을 **날짜가 아니라 표본으로** 바꿔 놓고도, 정작 판정이 두 가지로 어긋나 있었다.
둘 다 "더 기다리면 된다"처럼 보였지만 기다려서 풀릴 문제가 아니었다.

1. **읽는 계층이 절반이었다.** `readiness.py` 가 `process_5m`(SQLite)만 세는데, 롤업은
   이틀이 지나면 Parquet 으로 옮겨가며 SQLite 에서 지워진다. 그 테이블이 담을 수 있는
   날짜는 구조적으로 최대 이틀이라 "3일 이상 관측된 프로세스"는 **영원히 0종**이었다.
   Phase 6 과 4-B 의 착수 조건이 도달 불가능한 상태로 며칠 대기하고 있었다.
   → `argus/storage/history.py` 를 두고 핫+웜을 합쳐 읽는다. 대시보드 타임라인도 같은
   이유로 07-28 이 비어 있었고 함께 고쳤다.

2. **날짜를 세면 2시간짜리 날도 하루다.** 실측 3일이 10.4h / 11.3h / **2.1h** 였다.
   Argus 는 PC 가 켜져 있을 때만 도니 관측 시간은 곧 사용 시간이고, 이걸 무시하면
   "3일 관측"이 실질 하루치가 된다.
   → `MIN_DAY_HOURS = 6.0` 이상인 날만 하루로 센다. 프로세스 지문은 날짜에 더해
   **누적 버킷 100개**(p99 를 세울 최소 표본)도 함께 본다.

**교훈**: 착수 조건이 오래 [대기]에 머물면 데이터가 모자란 것인지 **판정이 틀린 것인지**를
먼저 의심한다. 여기서는 "며칠이면 되나"를 실제로 세어 보다가 드러났다.
| 4 | ~~**Phase 6-A** 프로세스 누수~~ ✅ | 지문 없이 되는 절반을 먼저 했다. `rules` 0.000 → 0.400 | `handle_leak` 미탐 ✅, 6-B 의 이길 상대 |
| 4b | ~~**Phase 6-B** 프로세스 지문~~ ✅ | 6-A 의 오탐 절반을 가렸다 (0.24 → 0.12/시간) | Phase 8 지문 가중, Phase 13 |
| 4c | ~~**Phase 8 재개봉** — 귀인의 신규 프로세스 과대 계상~~ ✅ | 08-02 완료. 경위는 `docs/DONE.md` | 귀인 정확도 |
| 5 | **Phase 4-B** 레짐 추론 | 데이터 축적 필요 | 레짐 조건부 베이스라인, 대시보드 Regimes |
| 6 | **Phase 11** 피드백 | 2 이후, 피드백이 모인 뒤 | 지속 학습 |
| 7 | **Phase 13** 보안 라이트 | ⏸️ **보류.** 데이터 의존이 없다고 봤으나 틀렸다 — 판정 기준을 세울 근거가 없다(위 표). 데이터 계층(`net_activity_5m`)만 먼저 넣었다 | — |
| 8 | **Phase 14** 패키징 — **착수함 (2026-08-03)** | 마지막이 아니게 됐다. 남은 계획이 전부 정확도인데 **막힌 곳은 전달·배포**였다(아래 재검토). 빌드는 되는 것으로 확인 — 남은 것은 트레이·알림·대시보드 | 배포 |
| — | Phase 5 · 7 | **보류** — 이길 상대를 먼저 만든다. 60분 램프는 08-16 에 탈락(`procleak` 이 8.9분에 잡는다). 남은 후보는 분산·비단조·프로세스 밖 누수 | — |
| — | Phase 12 (ETW) | 선택. 판단할 근거가 아직 없다 | — |

### 일정 추정을 걷어낸 이유

원안은 Phase 당 2~7일이었다. **2026-07-27 하루에 4-A·10(부분)·8 을 했다.** 반대로
Phase 1 은 소크 테스트 하나로 하루를 썼다. 추정이 실제와 양방향으로 어긋나므로,
남겨 두면 계획서를 읽는 사람을 오해시킨다. 순서만 남기고 기간은 적지 않는다.

### 도달 지점 (수정)

- ~~최소 유용 지점: Phase 4 완료~~ → **이미 지났다.** 4-B 없이 수집 + 평가 + 룰 탐지
  + 귀인 + 대시보드로 이미 쓸 수 있다. 레짐은 정확도를 올리는 것이지 유용성의 문턱이 아니었다.
- **제품이라 부를 수 있는 지점**: 위 순서의 2번(Phase 9) 완료 시점 — 귀인 리포트가
  저장되고 대시보드에서 "어제 왜 느렸는지"를 볼 수 있게 된다. 그때 Phase 10 DoD 도 채워진다.
