"""네이티브 데스크톱 앱 (PySide6).

**상주 프로세스와 별개다.** 여기에 Qt 이벤트 루프가 있고, 상주(`argus/__main__.py`)는
자기 수퍼바이저 스레드 모델을 그대로 쓴다. 둘을 한 프로세스에 넣으면 메인 스레드를
두고 다투게 되고, 무엇보다 **창이 죽을 때 수집까지 끌고 내려간다** — 관측자가 병목이
되면 안 된다는 설계 규칙 1 이 여기에도 적용된다.

두 프로세스는 SQLite(WAL)를 통해서만 만난다. 창은 읽기 전용으로만 연다.
"""
