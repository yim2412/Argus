"""조회 계층. **UI 가 아니다** — 이름만 대시보드로 남아 있다.

Streamlit 판을 2026-08-09 에 지웠고, `data.py`(조회)와 `theme.py`(색)만 남겼다.
둘 다 UI 프레임워크를 모르며 네이티브 창(`argus/desktop/`)이 그대로 쓴다.

**이름을 안 바꾼 이유**는 `desktop/` 전체가 여기서 import 하기 때문이다. 삭제와
이름 변경을 한 커밋에 섞으면 되돌릴 때 둘을 함께 되돌리게 된다.
"""
