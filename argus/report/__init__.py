"""일일 생산성 리포트 — "어제 무엇을 했나".

**이상탐지와 별개 트랙이다.** 수집 기반만 재활용하고 `detection/`·`decide/` 는 읽지도
쓰지도 않는다. "어제 무엇을 했나"는 "무엇이 이상한가"와 다른 질문이고, 둘을 한 모듈에
넣으면 탐지 문턱을 고칠 때 리포트가 함께 흔들린다.

`builder` 가 하루를 접고, `data` 가 조회한다. `data` 는 `dashboard/data.py` 처럼
**UI 를 모른다** — 창이 그대로 쓰고, 나중에 다른 표현 계층이 붙어도 같다.
"""
