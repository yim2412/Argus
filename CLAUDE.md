# Argus — 프로젝트 규칙

> PC 성능 이상 탐지 상주 프로그램. 스펙은 `Argus.md`, 개발 계획은 `PLAN.md` 참조.
> 이 규칙은 옆 프로젝트 `RL3D` 의 공통 규칙에서 **이 앱에 맞는 것만** 추리고, 배포 전제에 맞춰 보강한 것이다.

## 프로젝트 개요

Windows 상주형 모니터링 exe (Python 3.11 + PyInstaller). psutil/pynvml/PDH 로 리소스를 수집해
SQLite 에 쌓고, 통계 + ML 로 "평소와 다른" 성능 이상을 탐지한 뒤 원인 프로세스를 지목한다.
UI 는 시스템 트레이 + 네이티브 창(PySide6).

**전제: 남에게 배포한다.** 개발 환경(RTX 3080 / RAM 64GB)은 기준이 아니라 한 사례일 뿐이다.

---

## 구조 (2026-07-30 기준)

데이터는 한 방향으로 흐른다. **수집 → 저장 → 탐지 → 판정 → 설명 → UI.**
각 단계는 앞 단계의 산출물만 읽는다. 탐지가 수집기를 직접 부르거나 설명이 DB 를
직접 헤집는 경로는 만들지 않는다 — 그러면 리플레이로 재현할 수 없다.

| 패키지 | 역할 | 알아 둘 것 |
|--------|------|-----------|
| `collector/` | psutil·PDH·NVML 로 리소스를 읽는다 | 수집기 하나가 죽어도 나머지는 계속돼야 한다. `procsource` 가 전 프로세스를 한 번에 가져온다 |
| `storage/` | 핫(SQLite) + 웜(Parquet/DuckDB) | **장기 데이터는 `history` 를 거친다.** SQLite 만 읽으면 이틀치뿐이다 |
| `detection/` | "평소와 다름"을 신호로 | `baseline`(중앙값/MAD) 위에 `rules`(전역)·`procleak`(프로세스별). 룰 표현식은 `expr` 이 `eval()` 없이 평가한다 |
| `decide/` | 신호 → 사건, 무엇을 알릴지 | `fusion` 이 신호를 사건 하나로 접고, `budget`·`suppression` 이 알림 양을 막는다. `severity` 가 등급을 두 축(현재 손실·위험)으로 매긴다 |
| `explain/` | "왜 느렸는지" | `bottleneck`(무엇에 막혔나) → `attribution`(누가 가져갔나) → `report`(문장). `changepoint` 가 시작 시각을 찾는다 |
| `eval/` | 리플레이 + 채점 | `python -m argus.eval`. 탐지기를 건드렸으면 여기를 돌린다 |
| `runtime/` | 스레드·자기예산·자기계측 | `budget` 이 예산 초과 시 스스로 샘플링을 낮춘다 |
| `machine/` | 이 PC 의 능력·성능 기준 | 절대 임계값 대신 여기 값을 기준으로 상대화한다 |
| `ui/` | 트레이 아이콘 + 풍선 알림 | `Shell_NotifyIcon` 직접(의존성 0). 알림 전달자 규약은 `notify(title, message, severity, incident_id=None) -> bool` 하나뿐. `incident_id` 는 풍선을 눌렀을 때 열 사건이다 |
| `desktop/` | 네이티브 창 (PySide6) | `python -m argus.desktop.app`. **상주와 별도 프로세스** — 창이 죽어도 수집은 계속된다. 검증은 `--seconds N` 이 그린 표본 수로 |
| `dashboard/` | **조회 계층** (이름만 대시보드로 남았다) | `data.py`(조회)·`theme.py`(색)뿐. UI 독립(`ttl_cache`)이라 창이 그대로 쓴다. Streamlit 판은 2026-08-09 에 삭제 |
| `config/` | `defaults.yaml` + `rules.yaml` | 임계값이 코드에 박혀 있으면 규칙 3 위반이다 |
| `tools/` | 단독 실행 도구 | `fault_injector`(결함 주입) · `inject_progress`(주입 진행·판정) · `eval_snapshot`(평가 입력 고정) · `rescore_incidents`(사건 재분석) · `readiness`(착수 판정) · `pyc_audit`(캐시 검사) · `mutation_sweep`(규칙 무력화 측정) |

**실시간과 리플레이는 같은 경로를 쓴다**(`detection/live.py`, `detection/replay_source.py`).
탐지기가 실시간에서만 되는 일을 하면 채점이 성립하지 않는다.

---

## 설계 규칙 (가장 중요)

1. **관측자는 가벼워야 한다.** Argus 자신의 CPU/RAM/큐 깊이를 항상 계측하고, 예산(CPU 2% / RSS 300MB)
   초과가 지속되면 스스로 샘플링을 낮춘다. **모니터가 병목이 되는 순간 제품은 실패다.**

2. **하드웨어를 가정하지 않는다.** GPU 유무·벤더, 디스크 종류(NVMe/SATA/HDD), 코어 수, RAM 용량이
   전부 다르다. 절대값 임계값을 코드에 박지 말고 **첫 실행 캘리브레이션(`machine_profile`) 기준의
   상대값**으로 표현한다. "디스크 응답 50ms 이상은 병목"은 HDD 사용자에겐 오탐이다.

3. **임계값·튜닝 상수는 config 한 곳에만.** 매직 넘버를 탐지기 코드 안에 흩뿌리지 않는다.
   튜닝은 코드 수정 없이 YAML 만 고쳐서 되어야 한다. (RL3D 의 "URL 상수는 한 곳에" 규칙의 확장)

4. **관리자 권한을 요구하지 않는다.** ETW·서명검증 등은 권한이 있을 때만 켜지는 **부가 기능**.
   시작 시 사용 가능한 계측 소스를 탐지해 `capabilities` 에 기록하고, 없으면 대시보드에
   "이 기능은 관리자 권한이 필요합니다"로 **드러내 놓고** 비활성화한다. 조용히 실패하지 않는다.

5. **개인정보는 로컬을 벗어나지 않는다.** 프로세스 경로·네트워크 목적지·창 제목은 민감 정보다.
   기본은 로컬 저장만, 외부 전송 없음. 리포트 내보내기에는 익명화 옵션을 둔다.

---

## 수집·저장 규칙

1. **개별 실패가 전체를 죽이지 않는다.** psutil 의 `NoSuchProcess`·`AccessDenied` 는 예외가 아니라
   **정상 상황**이다(프로세스는 수시로 사라지고, 시스템 프로세스는 원래 안 보인다).
   프로세스 1개 조회 실패가 스냅샷 전체를 날리면 안 되고, 수집기 1개가 죽어도 나머지는 계속돼야 한다.

2. **수집 경로는 절대 블로킹하지 않는다.** 큐가 가득 차면 **오래된 것부터 버리고 `drop_count` 를 올린다.**
   수집 스레드가 DB 쓰기를 기다리는 구조는 만들지 않는다.

3. **저장 위치는 `%APPDATA%\Argus\`.** exe 실행 폴더는 임시일 수 있으므로 DB·설정·모델·로그 모두
   영구 위치에 둔다. (RL3D 에서 확인된 함정)

4. **스키마·설정에는 버전 필드를 둔다.** 배포 후 업데이트 시 사용자 DB 를 마이그레이션해야 한다.
   나중에 넣으면 남의 PC 에 있는 DB 를 고치는 일이 된다. **Phase 1 스키마 설계 시점에 반영.**

5. **PDH 성능 카운터는 인덱스로 조회한다.** 카운터명은 로케일별로 번역되어 있어
   영문 문자열 매칭은 한글 Windows 에서 전부 실패한다. 본인 PC 에서만 테스트하면 배포 직전까지 못 잡는다.

6. **자식 프로세스의 출력은 `encoding="utf-8"` 을 명시해 읽는다.** 인코딩 일반 규칙은
   전역 `~/.claude/CLAUDE.md` 에 있고, 여기 적는 것은 **이 앱 고유의 배선**이다:
   `logging_setup.setup()` 이 stdout/stderr 를 **UTF-8 로 `reconfigure` 한다**(한글 로그 때문에).
   그래서 웜 내보내기 자식(`--export-warm`)은 UTF-8 로 쓴다 — 부모가 `text=True` 만 쓰면
   **실행 PC 의 ACP 로 디코딩해서 어긋난다.** 개발 PC 는 2026-08-15 부터 UTF-8 로캘이라
   **여기서는 재현되지 않고, 배포 대상(CP949)에서만 깨진다.** 규칙 2("하드웨어를 가정하지
   않는다")의 인코딩판이다. 해당 자리: `storage/warm.py`(자식=우리 자신), `machine/calibration.py`(자식=powershell).

   **그리고 이 실패는 요란하지 않고 조용하다.** 디코딩은 `subprocess` 의 reader 스레드에서
   일어나 예외가 부모까지 오지 않는다 — `run()` 은 정상 반환하고 **깨진 스트림만 `None`** 이 된다.
   `(done.stderr or "")` 가 그 `None` 을 빈 문자열로 바꿔 **자식이 왜 실패했는지가 사라진다.**
   예외도 백오프도 없으니 신호가 0 이다. 이건 규칙 4("조용히 실패하지 않는다") 위반이다.
   `capture_output=True` 를 새로 쓸 때마다 `encoding=` 이 붙어 있는지 확인한다.

---

## 탐지 규칙

1. **오탐은 미탐보다 비싸다.** 오탐 3번이면 사용자는 알림을 끄고, 그 순간 제품은 죽는다.
   모든 룰에 지속 조건(`for`)과 재발화 억제(`cooldown`)를 붙인다. 순간 스파이크는 이상이 아니다.

2. **탐지가 아니라 설명이 산출물이다.** "이상 감지" 알림은 가치가 낮다.
   "디스크 병목, 기여도 1위 Chrome.exe 68%, 40초 선행"이 제품이다.

3. **수치 없이 모델을 추가하지 않는다.** 새 탐지기는 리플레이 + 결함주입 스코어보드에서
   기존 대비 개선을 **입증한 뒤에만** 채택한다. 개선이 없으면 복잡도 비용만 남으므로 넣지 않는다.

4. **부트스트랩 기간에는 탐지하지 않는다.** 베이스라인이 설 때까지(초기 2시간)는 수집만 한다.

5. **사용자 룰 파일의 표현식은 `eval()` 금지.** AST 화이트리스트 파서로 평가한다.

---

## 작업 방식

> 난이도별 작업 방식(낮음=바로 / 중간 이상=설계 먼저)·TaskList·이견 제기·Git 기본·
> README·주석 규칙은 전역 `~/.claude/CLAUDE.md`. 여기에는 **이 프로젝트 고유분**만 있다.
> 이 앱에서 "중간 이상"은 **새 수집기·탐지기·스키마 변경**이다.

### 검증
- **고쳤으면 실제로 돌려본다.** 각 수집기는 `python -m argus.collector.<name>` 으로 단독 실행되어
  수집 결과를 `[OK]/[FAIL]` 로 찍도록 유지한다. 전체 앱을 띄우기 전 스모크.
- **탐지기를 건드렸으면 리플레이 평가를 돌린다.** `python -m argus.eval` — 저장된 데이터 + 결함주입
  시나리오로 정밀도·재현율·탐지지연·오탐률을 뽑는다. 네트워크·실시간 대기 불필요.
- **"며칠 돌려봐야 아는" 것을 실제로 며칠 기다리지 않는다.** 리플레이 하네스를 배속으로 돌린다.
  이게 Phase 2 를 앞당긴 이유다.
- **테스트는 코드·로직·프로세스 레벨로만.** GUI 검증도 마우스 포인터를 실제로 움직이는 자동화를
  쓰지 않는다. 창이 떴는지·오류가 없는지·데이터가 맞는지는 프로세스/로그/반환값으로 확인한다.
- **새 테스트는 일부러 깨뜨려 실패하는지 확인한다.** `.venv\Scripts\python.exe
  tools\mutation_sweep.py --only <키>` 가 무력화·캐시 삭제·복원을 대신 한다(손으로 하면
  아래 07-29 사고가 다시 난다). 통과는 증거가 아니다 — **아무것도 검증하지 않는 테스트도
  통과한다.**
  **로직과 배선을 따로 재야 한다** — 문턱을 테스트 안에서 직접 만들면 판정 로직은 보지만
  `config` 배선은 보지 않는다. 2026-08-03 에 `procleak` 단조성이 그 상태였다: YAML 을
  고쳐도 판정이 안 바뀌는데 241개가 전부 통과했다.
  **그리고 배선 테스트는 기본값이 아닌 값으로 재야 한다.** `assert engine.window_s ==
  cfg.window_s` 는 코드 기본값과 YAML 기본값이 같으면 **배선이 끊겨도 참이다.**
  2026-08-04 에 레지스트리 배선 구멍(`RuleEngine` 클래스를 등록해 `detection.*` 가
  통째로 무시되던 것)을 찾아 고치고 테스트까지 붙였는데, 그 테스트가 무력화 mutation 에
  330개 전부 통과했다. 같은 날 같은 유형이 네 번 나왔다 — 전부 두 기본값이 우연히
  같아서 아무 신호가 없던 경우다.
  2026-07-29 에 하루 두 번 겪었다. 웜/핫 병합 테스트는 양쪽 버킷 수를 똑같이 만들어 둬서
  규칙을 지워도 10개 전부 통과했고, `python -m argus.storage.history` 스모크는 실제 DB 에
  의존해 중복이 우연히 없는 날에는 통과하면서 아무것도 보지 않았다.
  특히 **조용히 깨지는 규칙**(예외가 아니라 값만 틀어지는 것)에 반드시 적용한다.
- **상수를 무력화했다 되돌린 뒤에는 `__pycache__` 를 지운다.** Python 은 `.pyc` 유효성을
  **(소스 mtime 초 단위, 소스 크기)** 로만 판단한다. `8.0` → `1.0` 은 글자 수가 같아 크기가
  안 변하고, 되돌림이 같은 초 안에 일어나면 mtime 도 그대로여서 **캐시가 무효화되지 않는다.**
  그 순간부터 테스트도 상주 프로세스도 무력화된 값으로 돈다. 2026-07-30 에 `_SCORE_SATURATION_Z`
  가 8.0 대신 1.0 으로 돌아 모든 룰 점수가 1.0 이었고, 그날의 mutation 측정 자체가 오염됐다.
  `.venv\Scripts\python.exe tools\pyc_audit.py` 로 확인하고, 같은 검사가 테스트에도 있다.
- **인코딩은 이 PC 에서 통과해도 증거가 아니다.** 개발 PC 는 UTF-8 로캘(ACP 65001)이고
  배포 대상은 CP949 다. 자식 프로세스 출력을 다루는 코드를 고쳤으면
  `$env:PYTHONIOENCODING="cp949"` 로 자식을 띄워 한 번 더 돌린다. `build/*.log` 중
  2026-08-09 이전 것은 CP949 로 굳어 있다 — 빌드 로그를 파싱하는 도구를 만들면 그때 걸린다.
- **테스트는 코드·로직·프로세스 레벨로만.** GUI 검증도 마우스 포인터를 실제로 움직이는
  자동화를 쓰지 않는다. 창이 떴는지·오류가 없는지·데이터가 맞는지는 프로세스/로그/반환값으로
  확인한다. **(전역으로 올리지 않았다 — 국방시스템공학은 반대 방침이다. 전역 10번 참조.)**
- **GUI(트레이·창)는 2번(보조) 모니터에만 띄워 테스트한다.** 개발 실행 시 환경변수로
  위치를 지정하고, 배포 exe 에는 영향이 없게 한다.

### Git (이 프로젝트 고유분)
- `.gitignore` 대상 중 이 앱 고유: **`data/`, `*.db`, 모델 아티팩트.**
- **수집한 실제 메트릭 데이터를 커밋하지 않는다** — 프로세스명·네트워크 목적지가 들어있다.
  테스트 픽스처는 익명화한 것만. (설계 규칙 5 의 연장이다.)

### 배포 (혼자 쓰는 게 아니므로 처음부터)
- **버전 체계 + `CHANGELOG.md` 를 처음부터 유지한다.** 전역 규칙 9번(YAGNI)의 예외 조건이
  이미 충족된 경우다 — **배포가 전제**라 "남에게 줄 때 도입한다"의 그 시점이 지금이다.
- **크래시는 구조화해 로컬 파일에 남긴다.** 남의 PC 에서 나는 버그는 재현이 불가능하므로
  이게 없으면 디버깅이 성립하지 않는다.

### 문서 (이 프로젝트 고유분)
- **`PLAN.md` 의 Phase 완료 기준(DoD)을 실제로 확인하고 넘어간다.** 못 채웠으면 못 채웠다고 적는다.
- **`PLAN.md` 에는 남은 일만 두고, 끝난 작업은 `docs/DONE.md` 로 옮긴다.**
- **`PLAN.md` 의 Phase 구성·기술 선택은 확정이 아니다.** 데이터를 보고 틀렸다고 판단되면
  계획서를 고치자고 말한다(전역 2번 "이견은 즉시"의 이 앱 적용 지점).
  특히 Phase 5·7·12 는 애초에 "채택 여부 재평가" 조건이 붙어 있다.

---

## 나중에 (필요가 생기면 도입 — 지금은 과함)

| 항목 | 도입 시점 |
|------|-----------|
| 자동 업데이트 채널 | 실제 배포판이 나가고 두 번째 릴리스가 필요해질 때 |
| 익명 텔레메트리 수집(동의 기반) | 사용자가 여럿 생겨 크래시 로그를 수동으로 못 받을 때 |
| Windows 서비스 모드 | 관리자 권한 ETW 수집이 실제로 채택될 때 (Phase 12 통과 후) |
| 설치 마법사 / Inno Setup | Phase 14. 그전까지는 폴더 복사로 충분 |
| CI (빌드·테스트 자동화) | 커밋이 잦아지고 빌드가 깨지는 일이 실제로 생길 때 |
